# ZK-IPD 5 项裁决登记（2026-09-07）

> **登记人**：EvoX 会话 `plan-fab94db1` revision 3
> **登记依据**：`/Users/mac/Documents/ZK-IPD/产品流程细化管理工具 2/AGENTS.md`（59 行）+ `IPD产品经理管理系统·最终完整版AI开发Prompt（全规则闭环无遗留疑问）.md`（182 行）+ `/Users/mac/Documents/ruoyi-ai/README-IPD-OVERRIDE.md` + `docs/开发说明/zk-ipd-override.md`（本仓索引）
> **裁决范围**：本仓后端 `ruoyi-modules/ruoyi-ipd/` + 前端 `apps/web-antd/src/views/ipd/` + 看板 `开发计划-看板镜像.md` + 验收 `docs/ipd-系统说明/验收/`

---

## 裁决 #1：SSOT 状态——未切换，ZK-IPD 是新设计稿对照源

| 字段 | 内容 |
|---|---|
| 状态 | ✅ 裁决已登记 |
| 裁决 | **SSOT 不切换**。当前 SSOT 仍是 `README-IPD-OVERRIDE.md`（自称"优先级高于根 README"，23 KB，2026-09-07 更新，未退场）。ZK-IPD（`/Users/mac/Documents/ZK-IPD/产品流程细化管理工具 2/`）是**新设计稿对照源**，不是 SSOT 替代 |
| 证据 | 本仓 `README-IPD-OVERRIDE.md` 第 3 行原文："本文优先级高于根目录 `README.md`"；ZK 仓 `AGENTS.md` 59 行是"durable workflow decisions"，与 `Prompt` 182 行是设计稿，未声称覆盖本仓 SSOT |
| 影响端点 | 无（不动 SSOT） |
| 影响文件 | `docs/开发说明/zk-ipd-override.md`（已建立 A-1 索引） |
| 关闭条件 | owner 明确要求切换 SSOT 时再议 |
| 风险 | ZK 与 README-IPD-OVERRIDE 内容冲突时已记入 `zk-ipd-override.md` §一，由各裁决项处理 |

---

## 裁决 #2：术语"阶段确认" vs "Gate/Reviewer"——改文案层，Java 包名保留

| 字段 | 内容 |
|---|---|
| 状态 | ✅ 裁决已登记 |
| 裁决 | **UI 层强制统一为"阶段确认"，Java 包名保留 `gate/` 不重命名** |
| 证据 | ZK `AGENTS.md` L28 原文："「阶段确认」replaces ambiguous gate/reviewer terminology in the UI" |
| 影响端点 | 前端文案层：`apps/web-antd/src/views/ipd/review/index.vue` + 5 个 api 模块 `gate-review.ts` / `gate-element.ts` / `gate-element-result.ts` / `gate-material.ts` + 对应 `.test.ts` |
| 影响文件 | 仅前端，**不动 Java 包名 / 类名 / URL 路径** |
| 关闭条件 | C-1 卡全部落地 + vitest 全绿 |
| 风险 | 文件名 `gate-*.ts` 保留但文案全改，可能误导新人；登记到 `zk-ipd-override.md` GAP-1 |
| 执行卡 | C-1 |

---

## 裁决 #3：项目圈 + 治理中心——全部删除

| 字段 | 内容 |
|---|---|
| 状态 | ✅ 裁决已登记 |
| 裁决 | **项目圈 + 治理中心全部删除** |
| 证据 | ZK `AGENTS.md` L54 "Project circle is an in-context collaboration thread" + **L57 "Project Circle is removed from the product"** + **L58 "Governance Center is removed as a duplicate primary destination. Deletion approvals and substantive-output reminders are embedded in 我的工作台"** |
| 影响端点 | 后端：`controller/ProjectCircleController.java` 7 端点；前端：`apps/web-antd/src/views/ipd/project/detail/circle.vue` + `apps/web-antd/src/api/ipd/project-circle.ts` + `project-circle.test.ts` + 路由 `apps/web-antd/src/router/routes/modules/ipd.ts`；前端 `apps/web-antd/src/views/ipd/workbench/index.vue` 中"治理中心"字样 + 链接 |
| 影响文件 | 上述全删 + 删除审批 / 无产出提醒**内嵌**到工作台主体 |
| 关闭条件 | B-1 + C-2 + C-3 全部落地，zk-frontend-drift-guard 0 命中，vitest 全绿 |
| 风险 | 若未来 ZK 反悔恢复项目圈 / 治理中心，需重新建仓——已登记到 `zk-ipd-override.md` GAP-5/6/7 |
| 执行卡 | B-1 + C-2 + C-3 + C-6（无产出提醒） |

---

## 裁决 #4：SOP 动作数 37 vs 69——先 69 兼容，ZK V2 重写后切换

| 字段 | 内容 |
|---|---|
| 状态 | 🟡 暂裁决（待 owner 二次确认） |
| 裁决 | **当前 69 动作保留**，ZK V2 = 37 是新版设计，本仓后端 `ActionCatalog.java` 按基线 RuoYi-AI 保留 69 不强制切到 37 |
| 证据 | ZK `AGENTS.md` L44 原文："SOP V2 is a fixed 37-action catalog"；本仓 `README-IPD-OVERRIDE.md` 第二节"改造后"列 "69 动作 IPD 流程"——两者都是事实表述，但 ZK 是新设计稿，README-IPD-OVERRIDE 是基线说明 |
| 影响端点 | 后端 `service/ActionCatalog.java` + `test/.../ActionCatalogTest.java` 69 项；前端 `apps/web-antd/src/views/ipd/stage/*.vue` 配套 UI |
| 影响文件 | 无（暂不动） |
| 关闭条件 | 即刻关闭：本裁决=保留 69；终态关闭：ZK V2 后端完整重写为 37 后切换 + 全量回归 |
| 风险 | 前端按 69 跑、后端按 69 跑，但 ZK 设计稿是 37——三方漂移；登记 GAP-2 |
| 执行卡 | 暂不执行；D-5 全量回归仅在 69 范围内验证 |

---

## 裁决 #5：不可豁免三关 + 超管放行——保留"未配置从严"机制，不删超管放行

| 字段 | 内容 |
|---|---|
| 状态 | ✅ 裁决已登记 |
| 裁决 | **保留超管放行分支**，因未配置环境下"超管放行"是合法降级路径；ZK "non-waivable"是业务规则，超管放行是机制，两者不冲突 |
| 证据 | ZK `AGENTS.md` L36 原文："Concept Go/No-Go、requirement change and the 90-day review are non-waivable; ordinary waivers require both leads and the super administrator"；本仓 `service/GateEngine.java` 现有"未配置从严"机制 |
| 影响端点 | 后端 `service/GateEngine.java` 三个 Gate（Concept Go/No-Go、需求变更、90 天复盘）的"超管放行"分支 |
| 影响文件 | 无（暂不动） |
| 关闭条件 | 当前关闭：本裁决=保留；如 owner 要求严格不可豁免，再加 `GateImmutableAcceptanceTest` |
| 风险 | 若 owner 要求严格不可豁免，D-3 范围需新增测试覆盖"三关不被任何角色单独放行" |
| 执行卡 | D-3（仅在 owner 裁决"严格不可豁免"时执行） |

---

## 跨裁决：真库 3 名超管违例存量

| 字段 | 内容 |
|---|---|
| 状态 | 🟡 待 owner 拍板 |
| 背景 | 之前盘点发现真库 `127.0.0.1:13306/ipd_dev` 有 3 名超管（违例：ZK L24 "must never have two active super administrators"） |
| 待选项 | (a) 杀一保留 (b) 合并到唯一 (c) 全停重建 |
| 影响 | D-2 P2-7 移交真链路验证 |
| 关闭条件 | owner 拍板 + 真库执行 + 审计链 verify chain=OK |

---

## 跨裁决：HrSyncController 处理

| 字段 | 内容 |
|---|---|
| 状态 | ✅ 裁决已登记 |
| 裁决 | **保留 Mock 占位，不改源码**，仅登记 |
| 证据 | ZK `AGENTS.md` L25 / L40 描述 PM/组长主数据为第三方 REST API 同步；本仓 `controller/HrSyncController.java` 是 Mock 占位，二期接入真实 REST |
| 影响文件 | `controller/HrSyncController.java`（不动） |
| 执行卡 | B-6（仅文档登记） |

---

## 裁决执行依赖图

```
裁决 #1（SSOT 不切换） → A-1 已落地（zk-ipd-override.md）
裁决 #2（术语） → C-1
裁决 #3（项目圈 + 治理中心删） → B-1 + C-2 + C-3 + C-6
裁决 #4（SOP 37 vs 69） → 暂不动
裁决 #5（不可豁免三关） → D-3 仅在 owner 严判时执行
跨裁决（超管存量）→ D-2 前置
跨裁决（HrSyncController）→ B-6 仅文档登记
```

---

## 关闭状态登记

| 裁决 | 当前状态 | 关闭卡 | 关闭时间 |
|---|---|---|---|
| #1 | ✅ 已登记（无后续动作） | — | 2026-09-07 |
| #2 | ✅ 已登记 | C-1 | 待 C-1 完成 |
| #3 | ✅ 已登记 | B-1 + C-2 + C-3 + C-6 | 待上述卡完成 |
| #4 | 🟡 暂裁决 | D-5（终态切换） | 待 ZK V2 重写 |
| #5 | ✅ 已登记 | D-3（条件触发） | 待 owner 严判 |
| 跨裁决 超管存量 | 🟡 待拍板 | D-2 前置 | 待 owner |
| 跨裁决 HrSync | ✅ 已登记 | B-6 | 2026-09-07 |

---

## 登记时间

- 本登记文件写入：2026-09-07
- 配套索引：`docs/开发说明/zk-ipd-override.md`
- 看板镜像：待 P0-9 复核佐证（D-1）
