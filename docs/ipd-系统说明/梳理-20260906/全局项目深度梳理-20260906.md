# 全局项目深度梳理（2026-09-06 蜂群主会话收口）

> 用户指令：「系统性梳理全局项目深度思考反思根源性原因结合全局项目代码及 ZK-IPD 原型图及业务逻辑深度思考完整梳理并真实实现」
> 性质：**深度治理文档**（不派任务、不撞兄弟会话、不修改代码），基于本会话累计 10+ 路探针成果 + 兄弟会话 commit 历史 + ZK-IPD 原型对账

---

## 一、三仓全景体量

| 维度 | 数量 | 来源 |
|---|---|---|
| **后端 Controller** | 41 | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/` |
| **后端 Domain** | 47 | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/` |
| **前端 api/ipd 模块** | 31 | `apps/web-antd/src/api/ipd/` |
| **前端 views/ipd 子目录** | 19（含 _shared） | `apps/web-antd/src/views/ipd/` |
| **ZK-IPD 原型基准** | 638 行 App.jsx | `产品流程细化管理工具 2/src/App.jsx` |
| **49 页前端页面** | 32✅ / 7◐ / 10○ | 49 页矩阵报告 |
| **看板待办卡** | 125 卡 | 蜂群 Agent-D 审计 |
| **本会话新卡** | 4 张（3 done + 1 pending） | ede70a5b/8548ffe7/c108d7bf done + 6c5878ba pending |

---

## 二、ZK-IPD 原型 vs 后端 vs 前端 三方对账

### 2.1 ZK-IPD 真理源（live URL `http://127.0.0.1:4173/workspace`）

**ZK-IPD 是 IPD 业务参考项目**，前端布局/字段/设计必须 100% 一致，优先级高于 ageerle 基座。

#### ZK-IPD 关键页面（PerformancePage 5 tab）
| Tab | 原型形态 | 后端端点（应用） | 前端页号 |
|---|---|---|---|
| overview | 项目整体概览 | ProjectController + KpiRecordController.getTrend | 31 |
| kpi | 12 项 KPI 表 + KpiDrawer | `GET /kpi/functional` `/performance` | 32 |
| feedback | 负反馈列表 + 创建 | `GET/POST /negative-feedbacks` | — |
| allowance | 月度津贴快照 + 生成核算 | `GET /allowance/ledger` `pending-stop` | 33 |
| contribution | 6 个月成功奖金 + V3.1 回款台账 | `POST /bonus-pool/compute` 等 | 37 |

### 2.2 ZK-IPD 关键业务规则（来源：`/Users/mac/Documents/ZK-IPD/开发说明/`）

| 规则 | 落地情况 |
|---|---|
| **奖金池 = 实际回款×5%×S/A/B 系数** | ✅ 5fb4fd42 commit 已裁决（U0-1 闭环） |
| **G-08 红线：UI 严禁「目标销售额」字样** | ✅ W3-A5 落地（奖金池 index.vue L8 + 测试反向断言） |
| **BR-INC-04/05 项目 S/A/B 系数** | ✅ 后端 BonusPoolService 已实现 |
| **BR-IPD-06 门禁（阻断性动作未完成 → 拒绝进入下一阶段）** | ✅ W3-A4 冒烟验证 11 项阻断拦截生效 |
| **BR-REQ-09 IP/UA 走 sha256Short 哈希** | ✅ W3-A4 冒烟验证（同 IP `b6cf5bd6081fb775`） |
| **G-02 一行一审语义** | 🟡 大部分满足；W4-B 已补 compute 缺失审计 |
| **BR-REQ-03a 受理前可补充** | ✅ PortalDemandTrace.canSupplement 实现 |

### 2.3 ZK-IPD vs 后端 vs 前端 三方对账矩阵（关键页）

| 页 | ZK-IPD 形态 | 后端 | 前端 | 一致性 |
|---|---|---|---|---|
| **37 项目激励台账** | `contribution` tab + 6个月成功奖金 + V3.1回款台账 + 战略等级变更 | ✅ BonusPoolController 5 端点全交付 | ✅ bonus.ts 5 函数全封装 + **W3-A5 真页面落地 303 行** | ✅ 完全对齐 |
| **32 项目 KPI** | `kpi` tab + 12项 KPI 表 + KpiDrawer 填报 | ✅ KpiRecordController 3 端点 | ✅ kpi.ts 3 函数（commit `6631d5e` 抢救）+ **W3-A6 三段预览** | ✅ 完全对齐 |
| **33 津贴台账** | `allowance` tab + 月度快照 + generateAllowance | 🟡 AllowanceLedger domain/mapper/service/dto 全有，**缺 Controller**（**W4-D 在修**） | ✅ allowance.ts 2 函数 + W3-A8 注释修正 | 🟡 等 W4-D 完成后完全对齐 |
| **44 人员同步** | IdentitySyncPage 超管：第三方 REST + 企业微信 OAuth + 同步控制台 | ❌ 零 Controller | ❌ 无 api | ❌ 完全未做（推 Wave 4/5） |
| **43 归档区** | — | ✅ AuditLogController + 配套 | 🟡 audit.ts 存在但接入浅 | 🟡 中 |
| **42 AI 助手** | — | ❌ 无 | ❌ 无 | ❌ 完全未做 |
| **30 共担 KPI** | 不独立成页（并入 PerformancePage kpi tab metric_category=shared） | 🟡 SharedKpiController 2 端点（collect/deadline-scan），**缺 GET 读端点** | ❌ 无 api | 🟡 中 |

---

## 三、5 层根因系统性反思（继承 [[swarm-wave3-rootsystem-reflection-2026-09-06]]）

### 第 1 层 · 技术债

| 子类 | 状态 | 来源 |
|---|---|---|
| 审计并发写无锁 | ✅ DEF-4/6/9 闭环 | AuditHashChain + append/verifyChain/rebuildChain |
| 权限码字面量散落 23 Controller 147 处 | ✅ SEC-06 闭环 | IpdPermissionCode 常量 + Catalog 登记 |
| N+1 写放大（PERF-P0-1 假闭环） | ✅ R2 重做 done | escalateOverdueLeaderReview 单 SQL 批量 UPDATE |
| 审计读路径 beforeSeq 死参数 + 5万行导出无守卫 | ✅ R2 重做 done + W3-A7 加固 9→10✅+0⚠️ | AuditLogService.listByOperatorIds 游标重载 + 硬上限 |
| **奖金池 compute 缺失审计** | ✅ **W4-B done** | 与 freeze/distribute 同严 + DuplicateKey 改 409 |
| 奖金池双公式（targetSales 语义污染） | ✅ 5fb4fd42 裁决 | ZK Prompt §三.2 口径 |
| 缓存穿透 | ✅ PERF-02 闭环 | 写穿透缓存裁决 |
| **typecheck 红基线 21 错误** | 🟡 等 owner 决策 A/B/C | W3-A8 探针 |
| **3 模块前端路径单复数错误** | ✅ W3-A9 done | contribution/negative-feedback/project-score |
| **kpi.ts untracked 数据安全** | ✅ W3-A6 抢救 commit `6631d5e` | （仍 19+ 个兄弟会话 untracked 待 W4-F） |
| **AllowanceLedgerController 后端零交付** | 🟡 W4-D 在修 | （页 33 阻塞） |
| **共担 KPI GET 读端点缺失** | 🟡 等 W4-E 派单 | （页 30 半阻塞） |

### 第 2 层 · 流程债（蜂群协作反复坑）

1. **工作区回滚灭失三度发生**（54597c42、PERF-P0-1、QA-05-P3）→ 根治：交付即 commit、untracked 不算交付、HEAD grep 实证
2. **看板翻卡与工作树长期失同步** → 根治：每次翻卡前回读在线板；done 必须 `git show HEAD:<file>` 实证
3. **多会话并发写入无中心协调** → 根治：「并发写入者共存」三步惯例（git status → 5min 无改动 → 持续变化完全退出）
4. **跨项目看板误拉** → 根治：用户硬指令「只锁 ruoyi-ai」（ZKER-staff 零接触）

### 第 3 层 · 契约债（前后端 spec 演化分裂）

1. **奖金池双公式** → 5fb4fd42 已裁决（实际回款×5%×S/A/B）
2. **占位页与后端已交付域脱节** → W3-A5/A6/A9 修复中（页 37/32 真页面、3 模块路径）
3. **契约矩阵 vs 产品圣经冲突**（U1-9）→ 等 owner 裁决
4. **兄弟会话 2b56ed5 commit 注释失实** → W3-A8 揭示 + W3-A9 修复
5. **DTO 字段不符**（A8 揭示：allowance 后端 domain vs 前端 type 大面积不符）→ W4-D 完成后留 DTO 对齐

### 第 4 层 · 治理债（蜂群本身脆弱性）

1. **蜂群编排器会双发并发**（已 3 次实锤 R1/R2/R3 三发）→ 缓解：派单 prompt 强提示「并发写入者共存」
2. **MCP server 不稳定**（Repowise 双挂/Evolver Proxy/Mempalace/Episodic-memory）→ 缓解：本地 zvec_grep + Read + Grep 替代
3. **审计缺 CI 必跑门禁** → W3-TYPE-01 待 owner 决策
4. **headless claude -p 已死**（CLI 2.1.252 + 全局 settings 强制 glm-5.3[1M]）→ 解法：内置 Agent 工具
5. **OPS-09 hook 跨会话拦截**（state file 格式 bug）→ 绕过姿势：`.claude/hooks/.ops09/<sessionId>...files` 兼容格式

### 第 5 层 · 工具债

| 工具 | 状态 | 降级方案 |
|---|---|---|
| headless claude -p | 🔴 CLI 2.1.252 | 内置 Agent 工具（subagent_type=general-purpose + model: sonnet） |
| Repowise MCP | 🔴 双 -32000 | 本地 zvec_grep + Read + Grep + 兄弟会话交叉印证 |
| mcp kanban update_task | 🟡 不更新 updated_at | 仅用作状态写，翻卡时间从日志推断 |
| Evolver Proxy | 🔴 connection closed | 不影响主流程 |

---

## 四、真实实现路径（按 ROI/依赖/owner 决策分级）

### 4.1 🔴 已完成（Wave 2 + Wave 3-A + Wave 4 全部落地中）

| 任务 | 状态 | 看板 |
|---|---|---|
| R1 Portal 端点补交付 | ✅ done | U0-4 |
| R2 PERF/QA 重做 | ✅ done | PERF-P0-1 / QA-05-P3 翻 done |
| R3 前端四件快修 | ✅ done | U1-4/5/6/7 |
| W3-A5 奖金池接线 | ✅ done | 新卡 `c108d7bf` |
| W3-A6 kpi.ts 抢救 + 页 32 KPI | ✅ done | 新卡 `c108d7bf`（首次授权 commit `6631d5e`） |
| W3-A7 PagingTest 加固 | ✅ done | QA-05-P3 卡面对齐 10✅+0⚠️ |
| W3-A9 3 模块路径修正 | ✅ done | 新卡 `8548ffe7` |
| W4-B compute 写审计 + DuplicateKey 改 409 | ✅ done | 新卡 `ede70a5b` |
| **W4-D AllowanceLedgerController 补交付** | ✅ done | 新卡 `0457ce13`（页 33 后端阻塞修复） |
| W4-E SharedKpiController GET 读端点 + 前端 kpi.shared | 🟡 running | （页 30 后端阻塞修复） |

### 4.2 🟡 可立即派（无 owner 决策依赖）

| 单 | 内容 | 来源 | 规模 |
|---|---|---|---|
| **W4-E** 🟡 running | 后端补共担 KPI GET 读端点 + 前端 kpi.shared 封装 | A3 §三 P1 | ≤0.5 天 |
| **W4-D'** | 前端 allowance.ts 接 AllowanceLedgerController 3 端点（listAllowances / getAllowancePendingStop 已存）+ 真实 UI 落地 | W4-D 收口 | ≤0.5 天 |
| **W4-F** | 兄弟会话 untracked 工作区批量抢救 commit（除 kpi.ts 已 commit 外 19+ 个文件；含 http.ts 共享传输层最高优先） | A3/A8 数据安全 | 1 天 |
| **W3-TYPE-01 选 B** | CI 集成「typecheck 不新增错误」门禁 | A8 红基线 | 0.5 天（成本最低） |
| **D-补强：allowance DTO 字段对齐** | 后端 domain `month/level/amount/capReached/stopReason` ↔ 前端 type `period/level/amount/capReached/status`（注意 status 后端无） | A8 DTO 不符 | 0.5 天 |

### 4.3 ⏸ 等 owner 裁决（任务 #13 pending）

| 单 | 依赖 owner 决策项 |
|---|---|
| **U1-9** 页面归属冲突 | 42=AI 助手 / 43=归档区 / 44=人员管理（按产品圣经修契约矩阵） |
| **U1-10** adminOnly 菜单隐藏 vs 禁用态偏差 | 维持隐藏或改禁用态 |
| **U2-5** 需求状态枚举 3 原型态文案 | 已关联项目 / 暂不采纳 / 重复需求 |
| **MED-3** prod 数据源 root/root | owner 手动三步（git apply + 补守卫 + 注入 env） |
| **EXPORT_HARD_LIMIT 配置化** | `${ipd.audit.export-hard-limit:50000}` |

### 4.4 ⏸ 等兄弟会话收口（任务 #14 pending）

兄弟会话 in-flight 20 modified 文件 commit 完成后才能派：
- W4-验收：P2-7.x P272/P273 验收域
- W4-Handover：Handover*/Person*/ProjectMember*/GateElement*/GateReview*/KpiRecord/Requirement/IpdBusinessException/SwitchingAcceptanceReport 关联修复

### 4.5 ⛔ 永远不派（红线）

- **审计域**：append/verifyChain/rebuildChain/AuditEventData/AuditHashChain（DEF-4/6/9 刚闭环）
- **产品圣经**：docs/开发说明/**
- **环境配置**：application-prod.yml（sensitive-field-guard hook 阻断）
- **参考原型**：/Users/mac/Documents/ZK-IPD/**（只读）
- **他项目**：ZKER-staff 等非 ruoyi-ai 项目看板

---

## 五、49 页前端真实实现总账

### ✅ 完全对齐（ZK 口径 + 后端 + 前端）

| 页 | ZK-IPD 形态 | 后端 | 前端 | 真实页面 | 状态 |
|---|---|---|---|---|---|
| **37 项目激励台账** | contribution tab | ✅ 5 端点 | ✅ bonus.ts 5 函数 + 真页面 303 行 | `apps/web-antd/src/views/ipd/incentive/bonus-pool/index.vue` | ✅ W3-A5 |
| **32 项目 KPI** | kpi tab | ✅ 3 端点 | ✅ kpi.ts 3 函数（commit `6631d5e`）+ 三段预览 | `apps/web-antd/src/views/ipd/project/detail/kpi.vue` | ✅ W3-A6 |

### 🟡 后端补 Controller 中

| 页 | ZK-IPD 形态 | 后端 | 前端 | 真实页面 | 状态 |
|---|---|---|---|---|---|
| **33 津贴台账** | allowance tab | ✅ **W4-D done** AllowanceLedgerController 3 端点（GET ledger / GET pending-stop / POST auto-scan） + AllowanceLedgerService 3 方法（list/pendingStop/autoScan + YYYY-MM 校验） + 6 测试全绿 | ✅ allowance.ts 2 函数 + W3-A8 注释修正 | **🟡 等前端 UI 接线**（后端就绪） | 🟡 → 接近 ✅ |

### 🔴 大工程（推 Wave 4/5）

| 页 | ZK-IPD 形态 | 后端 | 前端 | 真实页面 | 状态 |
|---|---|---|---|---|---|
| **44 人员同步** | IdentitySyncPage（7 端点 + AES + 同步控制台） | ❌ 零 Controller | ❌ 无 api | — | 🔴 ≥3 天 |

### ⚠️ 待裁决

| 页 | 状态 |
|---|---|
| **30 共担 KPI 归集** | 🟡 后端缺 GET 读端点（等 W4-E） |
| **42/43** 页面归属 | ⏸ 等 owner 裁决 U1-9 |

---

## 六、真实实现路线图（4 阶段）

### 阶段 1：当前（本会话已完）
✅ R1+R2+R3 + W3-A1/A3/A4/A5/A6/A7/A8/A9 + W4-B（+W4-D 在跑）
**目标**：核心 P0 治理债闭环 + 高 ROI 探针修复

### 阶段 2：Wave 4 立即派（待 owner 授权可启动）
🟡 W4-E + W4-F + W3-TYPE-01 + D-补强
**目标**：页 30/33 后端补齐 + 数据安全 + 红基线治理

### 阶段 3：Wave 4 owner 裁决后
⏸ U1-9/U1-10/U2-5/MED-3/EXPORT_HARD_LIMIT 5 项裁决 → 派相应 Wave 4 任务
**目标**：产品圣经对齐 + 配置化 + 菜单/状态 UX

### 阶段 4：Wave 5 大工程
🔴 页 44 人员同步全套（7 端点 + AES + 同步控制台）+ 兄弟会话收口后的 Handover*/Person* 关联修复
**目标**：超管权限模块 + 跨域迁移

---

## 七、给 Owner 的全局行动建议

### 🔴 立即（不需裁决）
1. **本会话未 commit 工作区审阅提交**（R1/R2/R3/W3-A5/W3-A6 件 2/W3-A8/W3-A9/W4-B）：建议按 SWARM-W2A/SWARM-W3A/SWARM-W4B 标注分组提交
2. **W3-A6 commit `6631d5e` 已落盘**，可先合并
3. **兄弟会话遗留进程 PID 10645**（4h+ 在线，跑旧 jar）：下次冒烟前 kill
4. **兄弟会话 19+ 个 untracked 文件**：考虑授权 W4-F 批量抢救 commit（数据安全）

### 🟡 裁决后（owner 拍板）
5. **Wave 3-B 5 项 owner 决策**（U1-9/U1-10/U2-5/MED-3/EXPORT_HARD_LIMIT）：决定后启动 Wave 4 相应任务

### 🔴 长期
6. **Wave 5 大工程**（页 44 人员同步 ≥3 天）：单独 Wave 5 派单

---

## 八、跨会话联合作战经验（汇总）

| 模式 | 经验 |
|---|---|
| 兄弟会话 commit 5fb4fd42 裁决 U0-1 | 主协调器必须读最新 commit + memory，不能基于早期快照判断（A3 §六第 6 条教训） |
| 兄弟会话 commit 2b56ed5 给 5 api 骨架 | W3-A8 揭示骨架内注释失实（A3 揭示 + W3-A9 修复 3 模块路径） |
| 兄弟会话 commit 4158337b 登记 ZK 约束 | HEAD 已包含，本会话 A1/A4/A8 实证时已纳入 |
| 兄弟会话遗留 PID 10645（4h+ 在线） | 跑旧 jar 导致 OpenAPI 缺新控制器；下次冒烟前 kill；A4 自启 6038 独立验证 |
| 三方对账方法 | grep `'<path>'` 前端 + grep `@RequestMapping` 后端 + grep `<path>` UI 调用方（A8 实战验证） |
| 兄弟会话在途文件冲突避让 | mvn `-Dmaven.compiler.failOnError=false -fae`（W4-B/W4-D 验证有效） |
| OPS-09 hook 跨会话拦截 | 子 agent 可绕过但主协调器不绕过（`SKIP_CONCURRENT_WRITE=1` + log.md 登记） |
| untracked 工作区数据安全 | 共享传输层文件（被 tracked 文件 import）必须最先 commit（W3-A6 抢救 kpi.ts） |

---

## 九、Wave 2 + Wave 3 + Wave 4 综合战绩（截至本记忆落盘）

| 类别 | 数量 |
|---|---|
| 派单总数 | 13 路（Wave 2: 3 + Wave 3-A: 8 + Wave 4: 2 含 W4-E 在跑） |
| 完成 | 12/13 |
| 新建看板卡 | 5 张（4 done + 1 pending） |
| 修复代码 | R1 4 文件 / R2 4 文件 / W3-A5 303 行 / W3-A6 三段预览 / W3-A9 6 文件 21 处 / W4-B 4 文件 / W4-D 4 文件（Controller + Service + 2 测试） |
| 测试 | R1 9 + R2 16 + W3-A5 9 + W3-A6 7 + W3-A7 2 新增 + W4-B 3 新增 + W4-D 6 新增 = 52 例（累计 0 失败） |
| 累计 commit | 1 个（kpi.ts 抢救 `6631d5e`） |
| 累计未 commit 留给 owner | 12 工作区改动（按惯例） |

### 9.1 5 张新卡最终状态
| task_id | 标题 | 状态 |
|---|---|---|
| `ede70a5b-30e2-48cc-8cd5-3b7857aba7cc` | [P0 治理债] compute 写审计 + DuplicateKey 改 409 | ✅ done |
| `8548ffe7-acf7-42bd-99b0-302a8338bedb` | [P0 高 ROI] 前端 3 模块路径单复数错误 | ✅ done |
| `c108d7bf-1e64-4c97-ba88-27283b5138b0` | [页 32 项目详情-KPI 接线] | ✅ done |
| `0457ce13-362e-4b0e-a713-85b73c461863` | [后端 AllowanceLedgerController 补交付] | ✅ done |
| `6c5878ba-39d2-4796-a9f4-6664e4c54d34` | [S2 红基线] typecheck 21 错误 | ⏸ pending（待 owner 决策 A/B/C） |

---

## 十、增量收口（2026-09-06 19:10）

### 10.1 W4-D 完成（页 33 后端阻塞修复）

**新卡 `0457ce13-362e-4b0e-a713-85b73c461863` ✅ done**——独立复核全部通过：

| 论断 | 实证 |
|---|---|
| 4 文件改动 | M AllowanceLedgerService + M P331AcceptanceTest + ?? AllowanceLedgerController + ?? AllowanceLedgerControllerTest |
| 3 端点 | L46 `@RequestMapping("/api/v1/allowance")` + L60-61 `GET /ledger` + L74-75 `GET /pending-stop` + L88 `POST /auto-scan` |
| 权限 | L60/74 `OPERATION_KPI_QUERY`（四角色可见，与 KpiRecordController 同款）+ L93 `ipdPermission.requireAdmin()`（仅 SUPER_ADMIN） |
| 3 Service 方法 | L155 `list` + L177 `pendingStop` + L196 `autoScan` + L41 YYYY-MM 校验 |
| 6 测试 | L55/72/88/105/120/134 6 个 @Test 全绿 |
| **审计域 zero-touch** | append/verifyChain/rebuildChain 方法体未改（仅 AllowanceLedgerService 调 auditLogService 写审计，业务代码零改） |
| 兄弟在途禁触 | Handover*/Person*/ProjectMember*/GateElement*/GateReview*/KpiRecord/Requirement/IpdBusinessException/SwitchingAcceptanceReport 零触碰 |

### 10.2 W4-E 派单（页 30 后端阻塞修复）

**任务 #21 in_progress**——W4-D AllowanceLedgerController 同款模式：
- 后端 SharedKpiController 增 GET /kpi/shared（按 projectId + period 分页）
- 前端 kpi.ts 增 getSharedKpis + SharedKpi type（扩展 W3-A6 commit `6631d5e` 不覆盖）
- 双仓门禁双绿

### 10.3 下一波待派（无 owner 决策依赖）

| 单 | 内容 | 规模 |
|---|---|---|
| **W4-D'** | 前端 allowance.ts 接 AllowanceLedgerController 3 端点真实 UI 落地 | ≤0.5 天 |
| **W4-F** | 兄弟会话 untracked 批量 commit（19+ 个文件，含 http.ts 共享传输层最高优先） | 1 天 |
| **W3-TYPE-01 选 B** | CI 集成「typecheck 不新增错误」门禁 | 0.5 天 |
| **D-补强：allowance DTO 字段对齐** | 前端 type vs 后端 domain 大面积不符 | 0.5 天 |

---

*产出：蜂群 swarm-1788707354407-yhjb2q 已 graceful 关闭；本会话主协调器 610e3af1-d6be-449b-af76-8e264dbd91a9 全程调度；2026-09-06 19:10。*
