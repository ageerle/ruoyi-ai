# U-决策包 2026-09-06（owner 拍板入口）

> **本文件性质**：Wave14 W14-S3 subagent 产出的"最小可行决策包"。只整理可拍板提案，不替 owner 拍板。
> **对应主会话**：a05ccff9-0e1e-4744-a4b4-2403d15413b0（Wave14）。
> **配套 SSOT**：`Wave14-闭环对账与U-决策包索引-20260906.md` §四（索引）+ 本文件（详细提案）。
> **只读边界**：本提案包不入 git commit、不动任何源码与既有文档；仅本文件新建。
> **拍板机制**：owner 在本文件 §0 勾选/填写选项 → 主协调会话按 §末"拍板后下一步动作"派生卡 + 镜像同步。

---

## 0. 决策项总览（拍板快速入口）

| # | 决策项 | 类别 | 阻塞级 | 推荐选项 | 拍板 |
|---|---|---|---|---|---|
| D1 | U0-1 奖金池公式口径（参考） | U0 已决 | — | ✅ A（实际回款×5%×levelCoef） | ✅ **已决**（5fb4fd42） |
| D2 | U1-9 页 42/43/44 归属冲突 | 契约 | U1 | A 按产品圣经修契约矩阵 | ☐ 待拍板 |
| D3 | U1-10 adminOnly 菜单隐藏 vs BR-ORG-06 | 契约 | U1 | B 改为禁用态不隐藏 | ☐ 待拍板 |
| D4 | U2-5 需求状态枚举 3 原型态文案 | 文案 | U2 | A 映射现有后端 8 态 + 补 3 标签 | ☐ 待拍板 |
| D5 | MED-3 prod 守卫第 5/6 条是否补 | 安全 | U0 | B 补双态规矩（合规性） | ☐ 待拍板 |
| D6 | kpi.ts 抢救 commit 授权 | 数据安全 | U0 | A 授权 W14-S3 顺手 commit | ☐ 待拍板 |
| D7 | EXPORT_HARD_LIMIT 配置化 | 运维 | U2 | A 抽 yml 占位 + 默认 50000 | ☐ 待拍板 |
| D8 | 存量 3 超管收敛（数据治理） | 运维 | U1 | B 建独立数据治理卡 + owner 选保留 | ☐ 待拍板 |

---

## D1. U0-1 奖金池公式口径（✅ 已决，仅参考）

**决策项**：奖金池基数（targetSales vs actualReceipts）× 5% × 项目等级系数（S/A/B）的双公式并存收口。

**当前态势**：
1. BonusPoolService 此前存在两套计算路径（`calculateBasePool`/`calculateFinalPool` vs `calculateBonusPoolByZkFormula`），targetSales 字段语义污染 5% 基数应用。兄弟会话 commit `5fb4fd42` 已统一到唯一公式 `actualReceipts × 5% × levelCoefficient`，4 旧 `targetSales` 方法标 `@Deprecated`。HEAD `a65b2527` 实证完整，BonusPoolZkFormulaTest 14/14 绿。参见 `梳理-20260906/Wave3-行动纲领-20260906.md:89, 157`。
2. **新增发现**（`BonusPool反向验证收口-20260906.md`）：ZK-IPD §三.2.5「修正因子」要求叠加 `tierCoefficient`（销售达成率阶梯）× `personalCoefficient`（个人绩效），commit `311007a5` 已扩展 4 因子公式并补 10 测全绿（完整公式：`actualReceipts × 5% × levelCoef × tierCoef × personalCoef`，中性缺省 1.0）。

**选项**：
- ✅ A（已选）：唯一公式 `actualReceipts × 5% × levelCoefficient` + 修正因子可选叠加；ZK-IPD §三.2.1+§三.2.5 100% 一致。

**推荐选项**：✅ A（已由兄弟会话 5fb4fd42 落地；本次新增 §三.2.5 由 311007a5 补充）。

**拍板后下一步动作**：
- 前端奖金池接线按本口径走（W3-A5 已 running）
- AC-INC-16 用例 owner 必按新口径重写（已登记 P3-7.1 待审）

**引用源**：
- `梳理-20260906/Wave3-行动纲领-20260906.md:89, 157`
- `梳理-20260906/前后端一致性缺口总清单-20260906.md:30`
- `BonusPool反向验证收口-20260906.md:230`（commit hash 311007a5）
- `log.md:2389`（口径裁决 2026-09-06 owner 拍板 [CONSISTENCY-1]）

---

## D2. U1-9 页 42/43/44 归属冲突（待 owner 拍板）

**决策项**：契约矩阵把页 42/43/44 标「项目圈-帖子流/候选人/成员」，产品圣经（`_导航地图.md`）定义为 42=AI 文档助手、43=删除审核-归档区、44=人员管理，**49 页清单中不存在「项目圈」页**。哪方为最终真理？

**当前态势**：
1. **契约矩阵错位实锤**（`梳理-20260906/Agent-B-页面矩阵-20260906.md:70`）：`后端需求/前端-后端契约现状矩阵-20260906.md` L69-71 把 42/43/44 标为「项目圈-帖子流/候选人/成员」（ProjectCircleController，P2-9.x）。**产品圣经导航地图定义 42=AI 文档助手/43=删除审核-归档区/44=人员管理**（`_导航地图.md:112-114`）。
2. 当前前端实装（`apps/web-antd/src/router/routes/modules/ipd.ts`）已按 ZK-IPD 原型 15 项菜单对齐；其中 43 归档区路由 meta.authority=['SUPER_ADMIN'] ✅，44 人员管理对应 identity-source Controller 后端从零建（BackendPending）。
3. 项目根因 = 契约矩阵与产品圣经未做强制 diff；真理源单方说了算。

**选项**：
- **A. 以产品圣经为准，修正契约矩阵**：契约矩阵 § 42/43/44 三行从「项目圈」改回「AI 文档助手 / 删除审核-归档区 / 人员管理」；项目圈页若产品真要，单列在 § 50+（不在 49 页内）。
- **B. 以契约矩阵为准，扩展产品圣经**：49 页 → 52 页（+3 项目圈）；产品圣经需 owner 拍板同意打破"49 页"硬约束。
- **C. 双轨并存登记豁免**：保留两套口径 + 在契约矩阵加注「项目圈页尚未纳规」+ 圣经不动（折中，等真正派单时再裁决）。

**推荐选项**：**A**。理由：① 产品圣经是 owner 2026-09-05 拍板的"49 页硬约束"（`Wave14-闭环对账与U-决策包索引-20260906.md` §六 owner 偏好 5★），B 选项打破硬约束代价过大；② 契约矩阵本身是派生文档，应随圣经走；③ 当前前端实装已按圣经落地，逆改前端成本高于改文档。

**拍板后下一步动作**：
- 契约矩阵文档 L69-71 三行替换为「页42 AI 文档助手 / 页43 删除审核-归档区 / 页44 人员管理」
- 在矩阵底部加注「项目圈页若产品要，单列在 §50+，须 owner 重拍 49 页硬约束」
- 看板卡 `W3-C` 同步翻 done（已在 Wave3-行动纲领 B 节挂起）

**引用源**：
- `梳理-20260906/前后端一致性缺口总清单-20260906.md:49`
- `梳理-20260906/Wave3-行动纲领-20260906.md:90`（W3-C）
- `梳理-20260906/Agent-B-页面矩阵-20260906.md:70`
- `docs/开发说明/spec/_导航地图.md:112-114`（产品圣经权威）
- `Wave14-闭环对账与U-决策包索引-20260906.md:69`

---

## D3. U1-10 adminOnly 菜单隐藏 vs BR-ORG-06「禁用态不隐藏」（待 owner 拍板）

**决策项**：当前前端对 adminOnly 3 项（产品目录⑬ / 人员同步⑭ / 超级管理⑭）实现为「菜单隐藏式」（仅 SUPER_ADMIN 可见），但 BR-ORG-06 权限矩阵规定「禁用态不隐藏」（其他角色可见但点入 403）。如何裁决？

**当前态势**：
1. **前端实装**（`Agent-B-页面矩阵-20260906.md:98`）：`ipd.vue` 的 `navItems` 15 项硬编码 + adminOnly 3 项经 `visibleNav` 仅 SUPER_ADMIN 可见，与 BR-ORG-06「禁用态不隐藏」存在偏差。
2. **BR-ORG-06 规则上下文**（`IPD系统_AI开发主Prompt_v3.md:408`）：三层权限矩阵（含禁用态呈现规则）属于 owner 拍板的硬约束。
3. 当前 4 角色矩阵由 Sec01/Sec02/P073 三套测试守护（`QA-07-49页验收矩阵-20260906.md:668`），但**未覆盖菜单可见性维度**。

**选项**：
- **A. 维持隐藏式**：当前实现 + 登记豁免（"adminOnly = 隐藏式，BR-ORG-06 豁免条款"），理由：adminOnly 涉及系统底层运维，泄漏给普通 PM 反而误操作风险高。
- **B. 改为禁用态不隐藏**：按 BR-ORG-06 严格对齐，adminOnly 菜单全员可见，非超管角色点击触发"403 / 无权限"提示；改动小（约 3 行 ipd.vue）。
- **C. 混合式**：adminOnly 菜单仍隐藏（避免泄漏），但在帮助文档 / 工作台公告区显式说明哪些菜单对哪些角色开放（折中方案）。

**推荐选项**：**B**（按 BR-ORG-06 对齐）。理由：① 业主权威规则高于前端实现惯例（CLAUDE.md G-04 硬约束）；② BR-ORG-06 之所以写「禁用态不隐藏」是因"看到才知道有这回事"，避免用户问"我的菜单怎么少了几项"——这是 UX 层面的用户体验设计；③ 改动量极小（`<v-if>` 改 `:disabled` + 加 403 提示），可立即派单（≤2h）。

**拍板后下一步动作**：
- `apps/web-antd/src/layouts/ipd.vue` 的 `visibleNav` 函数：adminOnly 3 项改为返回 `true`（全员可见）
- 给 adminOnly 3 项菜单项加 `:disabled` 标记 + tooltip「仅超管可用」
- 路由守卫层（`meta.authority`）已存在，无需改动
- Sec01/Sec02/P073 测试守护扩展加 1 例：非超管角色点击 adminOnly 菜单 → 403 提示
- 看板卡 `W3-D` 翻 done

**引用源**：
- `梳理-20260906/前后端一致性缺口总清单-20260906.md:50`
- `梳理-20260906/Wave3-行动纲领-20260906.md:91`（W3-D）
- `梳理-20260906/Agent-B-页面矩阵-20260906.md:75, 98`
- `docs/开发说明/外部资源/IPD系统_AI开发主Prompt_v3.md:408`（BR-ORG-06）
- `验收/QA-07-49页验收矩阵-20260906.md:668`

---

## D4. U2-5 需求状态枚举 3 原型态文案（待 owner 拍板）

**决策项**：需求状态枚举原型 7 态（new 新提交 / triaging 分析中 / planned 已规划 / linked 已关联项目 / rejected 暂不采纳 / duplicate 重复需求 / project_requirement 项目需求），后端 v3 八态 SUBMITTED…ARCHIVED，前端已映射 4 态（新增 4 个后端词）。**「已关联项目」「暂不采纳」「重复需求」3 个原型态无前端文案**，如何补？

**当前态势**：
1. 前端实装（`demand/index.vue:31-38`）：状态映射为 新提交 / 分析中 / 已规划 + 新增 已受理 / 处理中 / 已关闭 / 已归档，仅 4 态沿用原型词，3 态已登记差异未做文案。
2. **业务影响**：PM 在需求池看需求状态时，原型「已关联项目 linked / 暂不采纳 rejected / 重复需求 duplicate」三个语义边界对业务决策关键（如「已关联项目」= PM 可继续推进；「暂不采纳」= 留档备查；「重复需求」= 与某已有需求合并），缺少文案会让 PM 误判。
3. 后端 v3 八态：`SUBMITTED / TRIAGING / PLANNED / ACCEPTED / PROCESSING / CLOSED / ARCHIVED / PROJECT_REQUIREMENT`，前 7 态与后端 1:1；缺「LINKED / REJECTED / DUPLICATE」3 态。

**选项**：
- **A. 映射现有后端 8 态 + 补 3 标签**：保留后端 8 态语义不动，前端在「已规划」后加「已关联项目」标签 = 跳转到关联项目动作；「处理中」后加「暂不采纳」「重复需求」标签（允许状态语义扩展）。零代码改动 + 前端 3 行标签补齐。
- **B. 后端新增 3 态字段**：在 `requirement.status` 枚举补 LINKED / REJECTED / DUPLICATE，需 schema migration + 5 个 Controller / Service / Test 适配 + AC-INC 重写。约 2-3 天。
- **C. 业务语义合并**：保留后端 8 态，前端文案映射如下——「已规划」= 含「已关联项目」+「待规划」，「处理中」= 含「暂不采纳」+「重复需求」（语义合并，UI 不区分）。

**推荐选项**：**A**。理由：① 业务语义不需要后端字段细分——前端文案补即可解决 PM 误判；② 后端 8 态已映射前端 7 态工作流（SUBMITTED→TRIAGING→PLANNED→ACCEPTED→PROCESSING→CLOSED→ARCHIVED），「已关联项目」「暂不采纳」「重复需求」是 UI 文案补充而非状态机扩展；③ 改动最小、风险最低、可立即派单（≤0.5h）。

**拍板后下一步动作**：
- 前端 `demand/index.vue` 在状态枚举数组追加 3 个标签项
- 文案沿用 ZK-IPD 原型词（已关联项目 linked / 暂不采纳 rejected / 重复需求 duplicate）
- 登记 U2-5 = 豁免（业务语义合并已映射，不视为后端枚举扩展）
- 看板卡 `W3-G` 翻 done

**引用源**：
- `梳理-20260906/前后端一致性缺口总清单-20260906.md:58`（U2-5）
- `梳理-20260906/Wave3-行动纲领-20260906.md:93`（W3-G）
- `梳理-20260906/Agent-C-ZK一致性-20260906.md:41, 75, 118`
- `docs/开发说明/外部资源/IPD系统_AI开发主Prompt_v3.md`（业务规则上下文）

---

## D5. MED-3 (SEC-NEW-MED-3) prod 数据源守卫第 5/6 条是否补（待 owner 拍板）

**决策项**：`application-prod.yml` 数据源凭证 env 注入改动（commit `3cef25d1`）已落地 `${SPRING_DATASOURCE_USERNAME:}/${SPRING_DATASOURCE_PASSWORD:}` 空默认（fail-fast），但 ProdConfigDeltaGuardTest 第 5 条（双态规矩：已 env 注入 → patch 必须归档 / 仍 root/root → patch 必须待命）+ 第 6 条（patch 头部三件套自校验）由 hook 阻断未直写主树。是否补这两条守卫？

**当前态势**：
1. **env 占位已落地**（`application-prod.yml:66-67` 实证）：commit `3cef25d1` 已用 env 占位，未注入即启动失败，不再裸奔 root/root。Patch `application-prod-med3-delta.patch.applied-20260906` 已归档命名，证明守卫第 5 条「已 env 注入 → patch 必须归档」当前满足。
2. **守卫第 5/6 条现状**（`Wave14-闭环对账与U-决策包索引-20260906.md:54-60`）：本会话复核 ProdConfigDeltaGuardTest 第 5/6 条为「双态规矩」+「patch 头部自校验」，当前未确认是否随 patch 一并落主树。
3. **敏感字段防护**：`.claude/helpers/sensitive-field-guard.cjs` 按设计阻断智能体写 `application-prod.yml`，与本次 MED-3 处置同口径。

**选项**：
- **A. 不补**：env 占位（commit `3cef25d1`）已落地 + patch 已归档命名，已满足"部署门禁"要求；守卫第 5/6 条作为"额外防线"非必需，避免触动 sensitive-field-guard hook 边界。
- **B. 补双态规矩（推荐）**：在主树直接添加 `ProdConfigDeltaGuardTest` 第 5/6 条（不改 prod yml 本身，只补测试）。理由：① 与 batch3 先例一致——`application-prod-batch3.patch` 也有同型守卫；② 守卫是"防回滚"，未来若有会话无意把 env 占位改回 root/root，守卫立即转红；③ 测试是 Java 文件，不触发 sensitive-field-guard hook。
- **C. 整体跳过**：把 MED-3 整张卡标记"已结"，仅在文档登记 env 占位已落地，不再补守卫。

**推荐选项**：**B**。理由：① 与 MED-3 卡片本身的"双态规矩"语义一致（部署态 vs 开发态守护），是原始设计的本意；② Java 测试文件不触发 sensitive-field-guard hook，落地无障碍；③ 与 SEC-NEW-MED-1（Hikari 池）/ SEC-NEW-MED-2（actuator 暴露）/ DEF-9（审计 hash 多实例）四张"部署门禁"对称；④ 风险最低（一段 Java 代码），收益最高（防回滚）。

**拍板后下一步动作**：
- 在 `ruoyi-modules/ruoyi-ipd/src/test/java/.../ProdConfigDeltaGuardTest.java` 追加第 5/6 条（mock @TestEnvironment 或 @TestPropertySource 验证 prod yml 不含 root/root 字面量）
- `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=ProdConfigDeltaGuardTest test` 验证 6/6 绿
- 看板卡 `defd696c` 翻 done

**引用源**：
- `Wave14-闭环对账与U-决策包索引-20260906.md:54-60, 100`
- `梳理-20260906/Wave3-行动纲领-20260906.md:92`（W3-F）
- `验收/application-prod-med3-交付说明-20260906.md:5, 39-44`
- `验收/U0审计卡蜂群核验-20260905.md:16, 37, 59`
- `治理/AUD-GOV-收口-20260906.md:135`（4 张部署门禁）

---

## D6. kpi.ts 抢救 commit 授权（待 owner 授权）

**决策项**：`apps/web-antd/src/api/ipd/kpi.ts` 当前 `git status: ?? untracked`，不在 commit 历史，存在工作区切换/兄弟会话回滚可能灭失风险（与 PERF-P0-1/QA-05-P3 同型事故同根）。是否授权 Wave14-S3 / W3-A5/A6 顺手 commit 该文件防回滚灭失？

**当前态势**：
1. **数据安全事件登记**（`Wave3-行动纲领-20260906.md:167-175` §5.6）：kpi.ts untracked = 数据安全事件，与「54597c42 工作区回滚灭失」同根；建议"Wave 3-A5 或 A6 顺手 `git add + commit kpi.ts`"（加 SWARM-W3A5/A6 标注，与奖金池/项目 KPI 提交绑定）。
2. **当前 status 实证**：`git status: ?? apps/web-antd/src/api/ipd/kpi.ts`（独立前端仓 `/Users/mac/Documents/ruoyi-ipd-web`，不在本仓 `ruoyi-ai`）。
3. **W3-A6 卡面依赖**（`Wave3-行动纲领-20260906.md:140`）：页 32 项目 KPI Vue SFC 接线（接 kpi.ts 已封装 3 函数）+ kpi.ts 抢救 commit。前置条件=kpi.ts 抢救授权（owner 决策）。

**选项**：
- **A. 授权 W3-A5/A6 顺手 commit**（推荐）：与奖金池接线绑定为同一 commit（语义关联：奖金池计算依赖 KPI 数据），加 SWARM-W3A5 注释标注「数据安全事件抢救 commit，非新功能」，避免误读。
- **B. 暂不抢救，等 W3-A6 派单时一并 commit**：保逻辑清晰（kpi.ts 抢救与奖金池接线分离），但工作区切换风险持续存在。
- **C. 移交给前端会话单独处置**：本 Wave14-S3 只做登记 + 备注 + 卡面标注，不做任何前端仓 commit；交由前端会话在下一轮认领。

**推荐选项**：**A**。理由：① 数据安全事件优先级高于功能 commit 边界——灭失风险是已经发生过的真事故；② 与奖金池计算存在语义关联（奖金池 = f(项目 KPI)），合并 commit 可读性反而更高；④ 仅 1 个 untracked 文件 commit，git 历史 +1 条记录，风险可接受。

**拍板后下一步动作**：
- 授权后，W14-S3 / W3-A5 在前端仓执行：`git add apps/web-antd/src/api/ipd/kpi.ts && git commit -m "fix(ipd,SWARM-W3A5): kpi.ts 数据安全事件抢救 commit——与奖金池接线绑定"`
- 卡面 W3-A5 close 段加注「kpi.ts 抢救 commit hash = XXXXXXX」
- 卡面 W3-A6 close 段引用本抢救 commit 作为前置

**引用源**：
- `梳理-20260906/Wave3-行动纲领-20260906.md:131, 140, 167-175, 182`
- `Wave14-闭环对账与U-决策包索引-20260906.md:131`（相关背景）
- `/Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/api/ipd/kpi.ts`（独立前端仓）

---

## D7. EXPORT_HARD_LIMIT 配置化（待 owner 拍板）

**决策项**：`AuditLogService.java:180` 当前硬编码 `EXPORT_HARD_LIMIT = 50_000L`（审计导出硬上限），未抽 `application.yml` / nacos。如需灵活调档（按部署环境差异化）建议改为 `${ipd.audit.export-hard-limit:50000}` 形式，调档免重部署。

**当前态势**：
1. **硬编码现状**（`ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AuditLogService.java:180, 227-228`）：常量 `public static final long EXPORT_HARD_LIMIT = 50_000L;` 在代码层定义，运行期不可调。
2. **W3-A1 派生**（`Wave3-行动纲领-20260906.md:151` §5.3）：A1 子任务实测发现 #4 项「EXPORT_HARD_LIMIT 未配置化」标记为 owner 决策项。
3. **业务影响**：不同部署环境（dev / staging / prod）对导出上限需求不同——dev 可放大便于调试，prod 应收紧防 OOM；当前需重部署才能调整。

**选项**：
- **A. 抽 yml 占位 + 默认 50000**（推荐）：改为 `@Value("${ipd.audit.export-hard-limit:50000}")` 注入或构造器注入；application.yml 加 `ipd.audit.export-hard-limit: 50000` 默认值；环境差异化时只改 yml 不重打包。
- **B. 维持硬编码 + 文档登记**：不动代码，仅在 `AuditLogService` javadoc 标注「如需调整，改本常量后重打包」，理由：极少改动场景，配置化反而增加复杂度。
- **C. 抽 nacos 配置中心**：与现有配置分离走配置中心，运维侧更灵活，但引入 nacos 依赖，本项目当前未启用 nacos（仅 yml）。

**推荐选项**：**A**。理由：① 与项目内已有的 `${sa-token.jwt-secret-key:}` 等占位风格一致（CLAUDE.md L76-77 已登记 yml 占位惯例）；② 改动极小（约 5 行 Java + 2 行 yml）；③ 不引入新依赖（nacos 当前未启用）；④ 运维侧调档免重部署是 win。

**拍板后下一步动作**：
- `AuditLogService.java:180` 删除 `public static final long EXPORT_HARD_LIMIT = 50_000L;`
- 改为 `@Value("${ipd.audit.export-hard-limit:50000}") private long exportHardLimit;`（构造器注入或字段注入）
- `application.yml` / `application-dev.yml` / `application-prod.yml` 各加 `ipd.audit.export-hard-limit: 50000`
- `AuditLogService.listByOperatorIds` 调档上限验证测试 1 例（用 `@TestPropertySource` 覆盖 yml 验证）
- 看板派生卡 P3-X 创建并翻 done

**引用源**：
- `梳理-20260906/Wave3-行动纲领-20260906.md:151`（§5.3 W3-A1 项 4）
- `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AuditLogService.java:180, 218, 227-228`
- `CLAUDE.md` L76-77（yml 占位惯例）

---

## D8. 存量 3 超管收敛（数据治理，待 owner 拍板）

**决策项**：P2-7.3 超管移交验收实锤真库 `persons` 表存在 3 名在任 SUPER_ADMIN（900101 ipd-admin / 9110001 傅志谦 / 2096266884100935682 系统管理员），违反页 49「始终只有一名活动超管」不变式。本卡已加防御（多名在任超管拒绝移交），但存量收敛本身是运维/数据治理动作——保留哪一名作为唯一超管？

**当前态势**：
1. **真库实证**（`验收/P2-7.3-超管移交-验收-20260906.md:69-71`）：真库 13306/ipd_dev `persons` 表 25 人全 ACTIVE，在任 SUPER_ADMIN 有 3 名（900101 ipd-admin / 9110001 傅志谦 / 2096266884100935682 系统管理员）。
2. **业务影响**：3 名超管并存导致 ① 权限边界模糊（3 人均可发起移交，谁是"现任"不清）；② 审计责任分散（任何超管动作都写 SUPER_ADMIN_TRANSFER 审计，但发起人不唯一）；③ 未来页面「超级管理员身份展示」会有歧义。
3. **已加防御**（同文件 L71）：原兄弟实现用 `LIMIT 1` 静默选一做移交——其余超管不受影响，移交后不变式仍破。本轮已加防御：多名在任超管时拒绝移交（错误信息明示「须先收敛至一名再移交」+ 新测试 `multipleAdmins_rejected`）。

**选项**：
- **A. 立即 owner 决策保留 1 名**：从 3 名中确认保留（疑似 900101 ipd-admin 为种子账号），其余 2 名人工降级为普通角色（如 SYSTEM_ADMIN），治理动作 ≤0.5h。
- **B. 建独立数据治理卡 + owner 后续拍板**（推荐）：本 Wave14-S3 / P2-7.3 卡只交付代码 + 防御；存量治理动作建独立卡 P2-7.4「超管存量收敛」，挂 owner 拍板，defer 至下一会话。
- **C. 维持现状不变式破**：登记为「已知存量缺陷」，不动真库（prod 部署后由运维侧自行收敛）。

**推荐选项**：**B**。理由：① P2-7.3 卡交付边界不含数据治理（验收记录已明确「防御已加，存量治理非本卡代码范围」）；② owner 拍板保留哪一名需结合实际业务上下文（谁是种子、谁是测试账号、谁是真实运维），AI 不替 owner 拍人事决策；③ 建独立卡让数据治理有明确路径可追溯，符合"可独立验收"卡片粒度原则。

**拍板后下一步动作**：
- 建独立卡 P2-7.4「超管存量收敛」+ 关联 P2-7.3 卡面「PARTIAL 边界声明 §6」
- 卡面描述：3 名在任超管 → 由 owner 决策保留 1 名 → 其余降级为普通角色
- 验收标准：真库 `SELECT COUNT(*) FROM persons WHERE person_type='SUPER_ADMIN' AND account_status='ACTIVE'` = 1
- 看板卡 W3-X（继承 W3-D 之后）翻 todo → 认领 → done

**引用源**：
- `验收/P2-7.3-超管移交-验收-20260906.md:69-71, 87-88`（§6 PARTIAL 边界声明）
- `梳理-20260906/Wave3-行动纲领-20260906.md:24`（§三.D 兄弟在途文件 + 本卡 P2-7.3）
- `log.md:2376`（存量收敛待 owner 裁决建数据治理卡）

---

## 末. 拍板流程

1. **owner 在本文件 §0 勾选/填写每个 D 项的「拍板」列**（A/B/C 或备注）
2. **主协调会话收到拍板信号** → 派生对应卡面 → 看板镜像同步（`开发计划-看板镜像.md` 追加段）
3. **本文件 §0 拍板列更新为「✅ 已决 YYYY-MM-DD」** + commit hash 回填（如有）
4. **本决策包只读**：拍板后归档到 `docs/ipd-系统说明/治理/决策记录/` 子目录（主协调会话执行）

## 引用源完整性自检

| D 项 | 引用源数 | 最小 1 个 file:line | 通过 |
|---|---|---|---|
| D1 | 4 | ✅ Wave3-行动纲领:89 | ✅ |
| D2 | 5 | ✅ 缺口清单:49 | ✅ |
| D3 | 5 | ✅ 缺口清单:50 | ✅ |
| D4 | 4 | ✅ 缺口清单:58 | ✅ |
| D5 | 5 | ✅ Wave14:54-60 | ✅ |
| D6 | 3 | ✅ Wave3-行动纲领:167-175 | ✅ |
| D7 | 3 | ✅ Wave3-行动纲领:151 | ✅ |
| D8 | 3 | ✅ P2-7.3 验收:69-71 | ✅ |

**8/8 通过**——每条决策项至少有 1 个 file:line 引用源（实际平均 4 个），无悬空决策。

---

*产出：蜂群 W14-S3 subagent af5fb4893c98fd1c7（Wave14 主会话 a05ccff9）。2026-09-06。*