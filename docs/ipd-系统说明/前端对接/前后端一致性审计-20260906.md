# 前后端一致性审计 + 看板全量对账（2026-09-06 蜂群四路审计）

> 执行模式：蜂群 4 路并行只读审计（A 前向契约 / B 反向缺口 / C 看板核验 / D ZK-IPD 原型抽查），主线程汇总收口。
> 审计对象：后端 `/Users/mac/Documents/ruoyi-ai`（40 Controller + ruoyi-admin IpdPlatformAuthController = 41 个，182 方法级端点）；前端 `/Users/mac/Documents/ruoyi-ipd-web`（21 个 api/ipd 模块，95+ 调用点）；原型 `/Users/mac/Documents/ZK-IPD`。
> 与《前端49页收口报告-20260906.md》（07:11 快照）的关系：本文是其时间线后置的对账与终审，收口报告 §四 债务清单中多项已被后续 commit 清偿（见 §三）。

---

## 一、四路审计总结论

| 路 | 范围 | 核心结论 |
|---|---|---|
| A 前向契约 | 前端 21 api 模块 → 后端 41 Controller | **一致度 ~97%**：95+ 调用点路径/方法 100% 对上（零 404），body 字段零错位；**4 个新发现**（§二.1），16 模块完全干净 |
| B 反向缺口 | 后端页面型模块 → 前端页面/49 页地图 | **15 个后端模块前端零调用**；实锤反向 404（GET /public/demands/{code}）；双向缺口（SharedKpi/津贴无 GET 读端点）；前端完成度 ~58%（26 实做 + 5 半接 / 49） |
| C 看板核验 | 27 张 inreview + 92 张 todo 静态对 commit | 20 张 inreview 实际已收口、~16 张 todo 已被 commit 推翻；SEC-NEW-MED-3 修复从未进主树；2 个测试文件丢失于 .codex 备份区；P1-4.2 是最大关键路径 |
| D ZK-IPD 抽查 | 5 条业务线三方比对（原型↔后端↔前端） | 招标线一致、KPI 后端一致前端缺、移交/变更实现自洽；**奖金池双公式并存=口径错误级阻塞**（§二.5）；GateElementResult 判定表前端零调用 |

**总评**：后端是与权威规格对齐度最高的一侧（招标/KPI/移交/变更近乎逐条可溯源 BR/AC 编号）；前端已交付板块质量高、差异登记诚实，欠账集中在 P3 激励板块（29-37 页）与判定表；系统性风险集中在激励线「两套真理打架、后端代码单方面裁决」。

---

## 二、问题清单（按严重度）

### 2.1 前向契约新发现（Agent A，全部前端侧可修）

| # | 位置 | 问题 | 修复方向 |
|---|---|---|---|
| A-1 | `api/ipd/product.ts:122-124,139-143` | `changeProductStatus` / `bindProductProject` 对后端 `Void` 响应 `normalize(null)` 出**空壳 Product 对象**，调用方拿到 `{id:undefined,...}` 静默数据失真 | 前端改返回 `Promise<void>`，删 normalize 链 |
| A-2 | `api/ipd/audit.ts:44-46` | 前端发 `beforeSeq` 参数，后端 `/audit-logs/scope` 不收——**静默丢参数** | 前端删参数，或后端补收（留缺口登记） |
| A-3 | `api/ipd/stage-action.ts:167` | 注释引用不存在的 `/api/v1/attachments` 端点 | 改注释登记真实缺口（附件端点后端未交付） |
| A-4 | `api/ipd/*.ts` ossId | 前端 `string` 拼串 vs 后端 `Long`——潜在类型雷 | 留待附件端点落地时一并定契约 |

### 2.2 前后端双向缺口（Agent B + 收口报告时间线对账）

| # | 缺口 | 方向 | 现态 |
|---|---|---|---|
| B-1 | `GET /public/demands/{code}` 门户查询进度 | 前端已调（portal.ts:216）后端无 | **实锤 404**，页39 靠五态兜底；后端补 U1 |
| B-2 | SharedKpi / 津贴（Allowance）读端点 | 双向都缺 | 后端无 GET、前端无模块——双向缺口 |
| B-3 | P3 激励板块 9 页（29-37） | 后端全交付、**前端整板块缺失** | kpi/bonus/allowance/contribution/negative-feedback 前端零模块 |
| B-4 | GateElementResult 判定表 | 后端已交付、前端零调用 | 33 要素逐项判定+否决硬阻断的 UI 链路断在最后一环（Agent D 证实） |
| B-5 | 15 个页面型后端模块零前端调用 | 反向 | 完整清单见 Agent B 报告；其中 RequirementChange 6 端点、GateReview 写入、KPI/Handover Controller 均已在近期 commit 交付，等前端承接 |

### 2.3 看板与账实不符（Agent C，已处置）

- 20 张 inreview 卡卡面已有收口证据 → 翻 done（本会话 §四台账）。
- ~16 张 todo 已被 commit 推翻 → 翻 done（同上）。
- **SEC-NEW-MED-3 卡面声明失实**：卡面 00:00「修复落地」块声称 patch 已 apply（prod 数据源改 `${SPRING_DATASOURCE_USERNAME:}`/`${SPRING_DATASOURCE_PASSWORD:}`），但主工作树 `ruoyi-admin/src/main/resources/application-prod.yml:64-65` 现态仍是 `root`/`root` 字面量；`application-prod-med3-delta.patch.applied-20260905` 档案文件存在而 yml 未变——典型 worktree 工作丢失模式（与后端 HEAD 不自洁同源）。已退回 todo。
- 2 个测试文件丢失于 `.codex` 备份区，需幂等重放回主仓。
- P1-4.2 为最大关键路径（阻塞 P1-11.1 / QA-03 / SEC-04）。

### 2.4 时间线对账（收口报告 07:11 §四 vs 现 commit）

| 收口报告债务 | 当时状态 | 现态（本审计核验） |
|---|---|---|
| #6 RequirementChange 无 Controller | P0 债务 | ✅ 已清偿（6 端点交付，前端 changes.vue 已接） |
| #7 GateReview 写入端点 | P0 债务 | ✅ 已清偿（双签/盲签/仲裁/终裁全实现） |
| #8 KPI/Incentive/Handover 无 Controller | P0 债务 | ✅ 后端已清偿（前端仍缺 → B-3） |
| #5 Portal 查询/撤回/附件端点 | P0 债务 | ❌ 仍未交付（B-1 仍是实锤 404） |
| #9 Person 管理 Controller | P0 债务 | ❌ 仍未交付（页44 占位） |
| #3 Change 域 GET 列表/详情 | P0 债务 | ❌ 仍未交付（页25 列表占位/页26 整页占位） |

### 2.5 ZK-IPD 原型抽查（Agent D，5 线矩阵）

**阻塞级**：
1. **奖金池双公式并存**：`BonusPoolService` 旧 Q1 路径（目标销售额×poolRate×tier）与 ZK 路径（实际回款×5%×S/A/B×阶梯×绩效）并存，`compute()` 主入口已走 ZK 口径落库，且 `targetSales` 字段被写入 actualReceipts 值（**字段语义污染**）。根因是真理源体系内部打架：ZK 完整版 Prompt=实际回款×5% vs 主Prompt Q1+验收清单 AC-INC-16+mock-data=目标销售额×5%。**必须 owner 裁决唯一口径**：删另一条路径 + 修订外部资源文档，否则 AC-INC-16 用例一跑即红。
2. **GateElementResult 判定表前端零调用**（B-4）。

**高优**：
3. Gate 提交前材料强校验（会议纪要+评审材料）：原型有门禁、规格#5 有要求、后端前端双缺。
4. mock-data.js 的 33/14 要素清单与权威要素 v1 三重不一致（名称/标准/否决位），gateReviews 还让 G2/G3/G4 也双签——建议作废或重生成 mock 的 gateElements 段。

**低危/已登记**：S/A/B 区间制 vs E21 固定值；BR-TEAM-05 落选通知暂由审计行承载；admin 页「14 否决位」注释 vs seed 15；原型顺序五节点签署链 vs 并行盲签（实现取规格侧，正确）；变更单「组长+超管」vs 双 PM 双签（前端已知情登记，建议升格 owner 裁决项）。

**权威文档自相矛盾清单**（owner 裁决材料）：要素 v1 汇总 14 否决 vs 逐项表 15；ZK 完整版回款口径 vs 主Prompt 目标销售额口径；mock-data 要素全套 vs 要素 v1 全套；ZK「连续 2 月无产出仅提醒」vs 主Prompt「制度停发」；变更五节点行政链 vs BR-GATE-07 双签否决。

---

## 三、看板对账台账（本会话 45 张翻转，全部有卡面证据）

| 批次 | 翻转 | 卡 | 证据要点 |
|---|---|---|---|
| 1 | inreview→done ×10 | SEC-NEW-MED-1/2、DEF-9、PERF-P0-1/2、AUD-GOV-PERF-AUD/SEC-AUD、QA-05-P1/P2、AUD-GOV-01 | 卡面均有 ruoyi-close 收口块 |
| 2 | inreview→done ×10 | P4-2.1、P4-1.1、P3-4.1、P2-7.2、P2-7.1、P2-5.4、P2-5.3、P2-3.2、P2-3.1、P1-6.1 | P411 13/13 绿、P271/P272 17/17、P254 16/16+真库 probe 64/64、P232 真库 HTTP 32/32、P161 62/62 等 |
| 2 | inreview→todo ×1 | SEC-NEW-MED-3 | 主树 yml 仍 root/root（§2.3） |
| 3 | todo→done ×21 | WAVE3、AC-CHECK、QA-04-D2、P2-3.3、P2-6.1、P2-6.2、P2-7.3、P3-1.1/1.2/1.3、P3-2.1、P3-3.1/3.2/3.3、P3-4.2/4.5/4.4、P3-6.2、P3-7.1、P3-8.2、P0-10.21 | commit 推翻（P3 系列对应 fb2b1627/08761b8b 等）；P0-10.21 应标页四页实存由 Agent D 交叉证实 |
| 4 | →inreview ×3 | DB-02（卡面自相矛盾待对账）、P4-3.1（13 测全绿待 QA）、P0-7.3（行为测试 16/16 绿、Vue 实联未完） | 按 QA 复核口径保留待审 |

**保持不动**（6 张 inreview）：QA-05-P3、QA-03、P1-11.1、P1-4.4、P1-3.3、P0-3.3——QA 终审口径，不抢推。

---

## 四、遗留行动项（下批承接）

| # | 行动 | 仓 | 优先级 |
|---|---|---|---|
| 1 | 奖金池口径 owner 裁决 → 删冗余路径 + targetSales 字段语义修复 + 外部资源文档同步 | 后端 | **U0 阻塞** |
| 2 | Portal 查询进度端点 `GET /public/demands/{code}` 补交付 | 后端 | U1 |
| 3 | SharedKpi/津贴 GET 读端点 + P3 激励 9 页前端承接 | 双端 | U1（最大板块缺口） |
| 4 | GateElementResult 判定表前端接入（逐项打勾+否决硬阻断） | 前端 | U1 |
| 5 | Gate 材料强校验（会议纪要+评审材料前置） | 双端 | U1 |
| 6 | product.ts 两函数 Promise<void> / audit.ts beforeSeq / stage-action.ts 注释 | 前端 | U2（本会话修复，见 log） |
| 7 | SEC-NEW-MED-3 patch 重放（主树 yml 仍 root/root）+ 部署须注入 SPRING_DATASOURCE_* | 后端 | U0 部署门禁 |
| 8 | 2 个丢失测试幂等重放（.codex 备份区） | 后端 | U2 |
| 9 | mock-data.js gateElements 段作废/重生成 | 文档 | U2 |
| 10 | P1-4.2 关键路径推进（阻塞 P1-11.1/QA-03/SEC-04） | 后端 | U1 |

> 注：后端兄弟会话仍在主树活跃（P272/P273/IpdReportService/Gate.java 等），前端兄弟流活跃（report 页/router/access.ts）；本会话所有写入均避开上述在途文件。
