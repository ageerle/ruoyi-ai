# AI 功能完整性盘点（对照上游 ageerle/ruoyi-ai，2026-09-06）

> owner 问：「系统性对照上游 ruoyi-ai，你确定 AI 功能完善了吗？标准功能完整实现了吗？」
> 诚实回答：**不能说完善**。管理端齐了；用户端整块没有；运行态一次真实 AI 调用都没跑通过。证据如下。

## 一、上游是什么形态（对照基准）

上游是六仓库生态，不是单一系统：

| 仓库 | 角色 | 上游功能 |
|---|---|---|
| ruoyi-ai | 后端 | 模型管理/知识库 RAG/MCP 工具/工作流/多智能体/媒体生成/短剧/编程助手 |
| ruoyi-web | **用户端前端** | C 端 AI 对话（多会话/SSE 流式）、语音、绘图、音乐、视频、Dify/Coze 对话 |
| ruoyi-admin | 管理端前端 | 后台管理：厂商/模型/知识库/智能体/MCP/工作流/系统管理/监控 |
| ruoyi-drama | 短剧前端 | 短剧独立产品 |
| ruoyi-copilot | 浏览器副驾 | 独立副驾产品 |
| ruoyi-uniapp | 小程序 | 移动端 |

## 二、本项目有什么（逐项对照）

### 管理端：✅ 与上游一一对齐

- 证据：上游 ruoyi-admin（GitHub API 实测）views 目录 = `_core/agent/aiflow/chat/dashboard/graph/knowledge/mcp/monitor/nodeManage/system/tool/workflow` 13 个，与本地基座完全一致（本地仅多 ipd 域）。
- 后端 ruoyi-chat 16 控制器 + aiflow 4 控制器全部有对应管理页面/菜单（sys_menu 9 组 44 项）。
- 换票桥（platform-token）已打通 IPD 账号进平台（2026-09-06 AI-REG-01/02）。
- media/short-drama/coding 三组接口**上游管理端本来就没有页面**（属用户端功能域），本项目无页面不算管理端缺口。

### 用户端：❌ 整块缺失

- 上游 ruoyi-web 提供的 C 端 AI 体验本项目完全没有对应物：
  - **多会话 AI 对话（SSE 流式）**：后端 `POST /chat/send` 存在，但前端仓库零调用（api/chat 只有 chatconfig/message/model/provider 四个管理用模块，无 send）。管理端只有「聊天消息」管理页（看记录），没有对话页。
  - **RAG 知识库对话**：依赖上条，同样无入口。
  - **AI 绘图/音乐/视频**（MediaGenerationController）、**短剧**（ShortDramaController + ruoyi-drama）、**编程助手**（CodingController/CodingHarnessController）、**Dify/Coze 平台对话**、**语音/小程序**：均无前端。
- 本项目的替代物是 IPD 域自研「AI 副驾」（/ipd/ai-assistant，AiModelConfigController /api/v1/ai-models + AiDocumentController）——面向 IPD 业务的辅助生成，不是通用对话产品。

### 运行态：⛔ 一切 AI 功能是空壳

- chat_model 0 行（对话/向量/重排模型全无配置）、ai_model_configs 0 行（IPD 副驾模型配置全无）。
- 无任何 AI API key（backend-secrets.json 只有 jwt/monitor；原版 SQL 种子 key=sk_xx 占位）。
- 无向量库进程（weaviate/milvus/qdrant 均无监听）。
- 结论：**本机没有跑通过任何一次真实 AI 调用**——对话、知识库、副驾全部无法产出内容。设计文档验收条款「流式对话发一轮」从未满足过。

### 已知小缺陷

- 消息中心 SSE 推送 text/plain MIME 报错（console 存量报错，待修）。

## 三、深度反思（为什么之前敢说"完成"）

1. 之前的功能回归卡把范围定义为「基座自带页面」，这等于只对齐了上游**管理端**；「AI 功能」在用户直觉里首先是"能对话"，这部分从一开始就不在范围里——范围声明本身掩盖了缺口。
2. 换票桥解决的是"进得去"，运行态没解决"用得了"：没配模型，进去了也是空列表（模型下拉为空、知识库建不了）。
3. AI 副驾是 IPD 产品的合理自研替代，但它不能代表 ruoyi-ai 的标准 AI 能力已复用。

## 四、补齐路径（待 owner 决策）

| 选项 | 内容 | 工作量 | 依赖 |
|---|---|---|---|
| A. 配模型激活现有功能 | owner 提供 embedding key（如智谱）+ 起向量库容器；配 chat 模型让对话/知识库能跑 | 小（配置为主） | owner 给 key |
| B. 补对话入口 | 在平台侧或 IPD 壳加真正的对话页：调 /chat/send SSE + /chat/session 会话管理 + 模型选择 | 中（1 个新页面域） | A 先行 |
| C. 明确排除用户端 | 维持「管理端+副驾」定位，用户端/绘图/音乐/视频/短剧/编程助手写入排除清单 | 无 | owner 拍板 |

## 五、证据索引

- 上游 README/仓库清单：github.com/ageerle/ruoyi-ai（2026-09-06 拉取）
- 上游管理端 views：GitHub API contents/apps/web-antd/src/views（13 目录，与本地一致）
- 本地后端控制器：ruoyi-chat 16 个、ruoyi-aiflow 4 个（find 实测）
- 本地前端 api/chat 无 send 调用：grep 实测
- 运行态空配置：ipd_dev 库 chat_model/ai_model_configs 均 0 行（qcli 实测）
- 副驾路由：layouts/ipd.vue L274 router.push('/ipd/ai-assistant')

---

## 六、用户可见性验证（2026-09-07 凌晨，逐页真操作）

> 前置变更：owner 已在管理页配置两个模型（向量 Qwen3-Embedding-0.6B @ xinference 171.43.138.237:9997、对话 qwq-plus-latest @ qianwen）；weaviate 已起（127.0.0.1:28080，health OK）。
> 方法：chrome-devtools 逐页打开、真实点击/上传，后端日志 + DB + curl 三方对质；不做无证据断言。

### 6.1 实测结果

| # | 链路 | 实测 | 结果 | 断点 |
|---|---|---|---|---|
| V1 | 厂商管理页 | 超管会话逐页打开 | ✅ 2 厂商（olloma、qianwen）增删改按钮齐全 | 无 |
| V1 | 模型管理页 | 同上 + 建库下拉核对 | ✅ 2 条模型配置生效，建库弹窗可见 Qwen3-Embedding | 无 |
| V2 | 知识库建库 | 超管 UI 真实提交「AI功能验证库」 | ✅ 创建成功、列表可见 | 无 |
| V2 | 文件上传 | UI ×3 + 页面内 fetch ×2，同一 multipart | ❌ 后端进程内每次精确卡 120s 后 OssException（AWS SDK TransferManager「service request not made within 120s」）；jstack 抓现场：卡死期间全 JVM 无 OSS 帧、AWS Netty event loop 空转、无到 9000 的 TCP 连接 | **进程级问题**：同 SDK 同构建独立进程探针 201ms 上传成功 → 收敛到后端运行进程内部（重启大概率消除，待 owner 授权）；bucket 缺失已修复（minio 容器 + sys_oss_config 启用 + ruoyi 桶） |
| V2 | 解析→切块→向量入库→检索 | — | ⛔ 被上传阻塞，未能验证 | 依赖上传环节 |
| V3 | 对话 /chat/send SSE | 杨波→平台换票→页面内 fetch（会话归属用 DB 直插测试行绕过缺权） | ✅ 代码链路全通：鉴权→会话归属→模型解析→真实出站 OkHttp→dashscope.aliyuncs.com | ❌ 上游 401 Invalid API-key（硬证据：MyChatModelListener 错误日志）→ owner 配的 key 无效，需换有效 key |
| V4 | 角色可见性 | ipd-leader getRouters + 页面实测 | ✅ RBAC 精确生效：非超管仅见知识管理（本会话实测建库/详情/上传入口） | 超管专属页（智能体/MCP/工作流/消息）待傅志谦本人凭据；此前超管会话已实测 34 项菜单与三核心页 |
| V5 | IPD AI 副驾 | 杨波工作台 → /ipd/ai-assistant | ✅ 页面诚实标注「AI 模型生成、模型配置与预算控制（P4-2）尚未交付」，提供登记→审核→版本链/对比替代闭环 | AI 生成本体属看板 P4-2 卡，未交付 |

### 6.2 附加发现（实测新暴露）

1. **OSS 存储配置整块缺失**（已修复）：ipd_dev 的 sys_oss_config 0 行、无 minio 容器 → 修复动作：起 minio 容器（127.0.0.1:9000，ak/sk 同上游种子）、经管理 API 新增 minio 配置并 changeStatus 启用（写 Redis 默认标记）、mc mb 建 ruoyi 桶。
2. **前端上传组件缺陷**：上传弹窗「确定并保存」触发的 XHR 稳定被 ERR_ABORTED（3/3 复现），与存储无关，属 UI 层独立问题，需另开修复项。
3. **普通角色缺对话域权限码**：ipd_pm/ipd_group_leader 无 system:session:add 等（现有 RBAC 授权仅覆盖知识库域），对话入口开放前需补授权（待对话入口落地时一并处理）。
4. **平台账号（sys_user）密码机制与文档不符**：库内为 32 位 MD5（e10adc…=MD5("123456")形态）但明文 123456 登录校验不过（错误计数+1，勿再盲试）；超管换票链路的平台凭据需 owner 澄清或重置。
5. **测试痕迹**：chat_session 测试行 id=9101020001/2096630000000000001（user_id=910102）、知识库「AI功能验证库」2096624591630774274 保留作为证据；MinIO probe/from-bytes.txt 为探针产物。

### 6.3 给 owner 的两个输入（与 L-② 汇合）

1. **有效的对话模型 API key**（当前 dashscope key 上游 401）——换好后我一条 curl 即可验证对话全链路。
2. **后端进程重启授权**——OSS 上传 120s 卡死是进程级问题，重启后端进程（kill 67763 + 按原命令再起）后重试上传；届时解析→向量入库→检索测试一并验证。

### 6.4 更新后的诚实结论

回答标题问题（2026-09-07 版）：**管理端管理功能可用性已接近上游；「AI 能用」仍差两口气——一个有效 key、一次进程重启**。对话/知识库的代码与服务端链路经实测全部就位，断点均为外部输入或运行态，不再是代码缺口。
