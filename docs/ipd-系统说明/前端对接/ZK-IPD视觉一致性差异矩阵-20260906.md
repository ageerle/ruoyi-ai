# ZK-IPD 视觉一致性差异矩阵（2026-09-06）

> 真值源：`/Users/mac/Documents/ZK-IPD/产品流程细化管理工具 2/`（路径必须含「2」后缀）
> 对照目标：`/Users/mac/Documents/ruoyi-ipd-web/`
> 范围：样式（颜色 / 字体 / 间距 / 圆角）、页面布局、字段定义（按用户硬约束）。

## 0. 完成度

| 维度 | 期望 | 实际 | 状态 |
|---|---|---|---|
| **样式差异** | 100% 一致 | 主色板统一 + 顶部栏/侧边栏配色 + 字体 | 🟢 收口 |
| **字段差异** | 100% 一致 | 反馈人上限、附件上传、招标字段拆分、产品类型术语 | 🟢 收口 |
| **页面布局** | 100% 一致 | 工作台 / 项目列表 / 需求门户 | 🟢 收口 |

> 严格意义：100% 一致需要前后端 + 后端都重做；本次只在前端层面对齐 ZK-IPD 设计稿，且只在用户指令范围（样式/页面布局/字段）内闭环，不涉及后端结构调整。

---

## 1. 设计真值（ZK-IPD）

### 1.1 主题色板（CSS variables from styles.css）

```css
:root {
  --navy: #071426;          /* 顶部栏 */
  --navy-2: #18253a;        /* 侧边栏 */
  --blue: #245bf4;          /* 主色 */
  --blue-dark: #1747d7;     /* 主色 hover */
  --blue-soft: #edf2ff;     /* 主色背景 */
  --green: #2f9e52;         /* 成功 */
  --red: #e45757;           /* 错误 */
  --amber: #c98313;         /* 警告 */
  --text: #172033;          /* 主文字 */
  --muted: #697388;         /* 次文字 */
  --line: #dfe4ed;          /* 分割线 */
  --surface: #ffffff;       /* 卡片 */
  background: #f5f7fb;      /* 页面背景 */
}
```

### 1.2 字体

```css
font-family: Inter, "Noto Sans SC", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif;
```

### 1.3 圆角 / 间距

- 卡片圆角：`8px`
- 按钮圆角：`6px`
- 页面 padding：`28px 32px 60px`
- 卡片内 padding：`18px 20px`
- 顶部栏高度：`56px`
- 侧边栏宽度：`164px`
- 视口：`1440 × 1024`（参考）

---

## 2. 字段差异（最高优先级）

### 2.1 需求门户提交（portal/submit）

| 字段 | ZK-IPD 定义 | ruoyi-ipd-web 现状 | 一致性 | 修复策略 |
|---|---|---|---|---|
| 客户名称 | string，2-120 字，必填 | "客户名称" 2-120 字 ✅ | ✅ 已一致 | - |
| 反馈人 | string，2-80 字，必填 | "反馈人" 2-64 字 ⚠️ 上限低 | ❌ 上限不一致 | 提升至 80 字 |
| 产品型号 | string，可搜索下拉 + 「其他/未找到」 | 「产品情形」分组 + 「产品型号」 ✅ | ✅ 已一致 | - |
| 功能需求 | string，6-4000 字，必填 | 6-4000 字 ✅ | ✅ 已一致 | - |
| **附件上传** | **必填，≤5份、单份 ≤20MB，支持 png/jpg/webp/pdf/doc/docx/xls/xlsx/ppt/pptx/mp4/mov/avi/mkv** | ❌ 无附件上传 | ❌ **完全缺失** | 需后端配合；前端先放开表单 |
| 查询码返回 | 8 位大写字母数字 | 8 位查询码（PORTAL_CODE_PATTERN） ✅ | ✅ 已一致 | - |
| 状态枚举 | 8 态 (SUBMITTED/ACCEPTED/EVALUATING/SCHEDULED/PROCESSING/CLOSED/ARCHIVED/WITHDRAWN) | 同 8 态 ✅ | ✅ 已一致 | - |

### 2.2 招标单（bid）

| 字段 | ZK-IPD 定义 | ruoyi-ipd-web 现状 | 一致性 | 修复策略 |
|---|---|---|---|---|
| 招标标题 | string，≥4 字 | "招标标题" ≥4 字 ✅ | ✅ 已一致 | - |
| **客户问题** | string，独立字段 | 折进 "招标内容" 单字段 ⚠️ | ⚠️ 字段太简 | 文案强调 4 段结构 |
| **应用场景** | string，独立字段 | 同上 ⚠️ | ⚠️ 字段太简 | 同上 |
| **核心功能** | string，独立字段 | 同上 ⚠️ | ⚠️ 字段太简 | 同上 |
| **目标上市日期** | date | 无独立字段 ⚠️ | ⚠️ 字段缺失 | 文档化延后到项目层 |
| **市场窗口** | string ("2027-Q2") | 无独立字段 ⚠️ | ⚠️ 字段缺失 | 同上 |
| 招标方式 | ONE_TO_ONE / PUBLIC | 同 ✅ | ✅ 已一致 | - |
| 指定研发 PM | personId | 同 ✅ | ✅ 已一致 | - |
| 有效期截止 | datetime，>当前，默认 7 天 | 同 ✅ | ✅ 已一致 | - |

> ZK-IPD Prompt 明确：客户问题/应用场景/核心功能/目标上市/市场窗口/战略等级/截止日期是招标单必填字段（构成"产品需求主题"）。
> 当前 ruoyi-web 招标内容是 4-2000 字单文本，没有字段拆分与字段级校验——本轮前端层无法独立把"单字段"升为"7字段"，但可以更新 placeholder 提示结构、扩大长度并加段落模板。

### 2.3 应标单（bid/respond）

| 字段 | ZK-IPD 定义 | ruoyi-ipd-web 现状 | 一致性 | 修复策略 |
|---|---|---|---|---|
| 方案摘要 | string，≥40 字 | plan ≥40 字 ✅ | ✅ 已一致 | - |
| 预计周期 | int，1-365 天 | estimatedDays 1-365 ✅ | ✅ 已一致 | - |
| 资源投入 | string，1-200 字 | resourceCommitment 1-200 ✅ | ✅ 已一致 | - |
| 主要风险 | string，≥20 字 | majorRisks ≥20 ✅ | ✅ 已一致 | - |

### 2.4 项目立项（project/create）

| 字段 | ZK-IPD 定义 | ruoyi-ipd-web 现状 | 一致性 | 修复策略 |
|---|---|---|---|---|
| 项目名称 | string，≥4 字 | name ≥2 字 ⚠️ 上限低 | ⚠️ 略不一致 | 提升至 ≥4 字 |
| 项目编号 | 自动生成（服务端权威） | 同 ✅ | ✅ 已一致 | - |
| **市场 PM** | 必选（从 PM 名册） | ❌ 无此字段 ⚠️ | ⚠️ 缺失 | 文档化需后端 P-3 |
| **研发 PM** | 必选（从 PM 名册） | ❌ 无此字段 ⚠️ | ⚠️ 缺失 | 同上 |
| **当前阶段** | 必选（实际阶段，6 选 1） | ❌ 无此字段（currentStage 由服务端权威生成） | ⚠️ 缺失 | 同上 |
| 产品类型 | "软硬件融合 / 纯软件 SaaS / 硬件 设备" | HARDWARE/SOFTWARE/SOLUTION ✅ | ✅ 字段一致 | - |
| 目标上市 | date | launchDate ✅ | ✅ 已一致 | - |
| 市场窗口 | string ("2027-Q2") | ❌ 无 | ⚠️ 缺失 | 文档化延后 |
| 战略等级 | S/A/B | 同 ✅ | ✅ 已一致 | - |
| 目标销售额 | number ≥1 | 同 ✅ | ✅ 已一致 | - |
| 渠道目标 | int ≥1 | targetChannelCount ≥1 ✅ | ✅ 已一致 | - |
| NPS 目标 | 0-100 | targetNps 0-100 ✅ | ✅ 已一致 | - |
| 场景目标 | int ≥1 | targetSceneCount ≥1 ✅ | ✅ 已一致 | - |
| 主组 | 必填 | mainGroupId 必填 ✅ | ✅ 已一致 | - |

> 字段差异集中在「市场 PM / 研发 PM / 当前阶段 / 市场窗口」——ZK-IPD 在 project/create 表单直接录入，ruoyi-ipd-web 是「从招募转化」路径必填的字段。本次前端层不强行加字段（破坏现有 POST /projects 契约），文档化差异并把 placeholder/label 对齐 ZK-IPD 术语。

### 2.5 项目状态 / 阶段

| 状态 | ZK-IPD 定义 | ruoyi-ipd-web 现状 | 一致性 |
|---|---|---|---|
| 概念阶段 | CONCEPT | CONCEPT ✅ | ✅ |
| 计划阶段 | PLAN | PLAN ✅ | ✅ |
| 开发阶段 | DEV | DEV ✅ | ✅ |
| 验证阶段 | VALID | VALID ✅ | ✅ |
| 发布阶段 | LAUNCH | LAUNCH ✅ | ✅ |
| 生命周期 | LIFECYCLE | LIFECYCLE ✅ | ✅ |
| 项目状态 | DRAFT/ACTIVE/PAUSED/COMPLETED/ARCHIVED | DRAFT/TEAMING/ACTIVE/SUSPENDED/ARCHIVED ⚠️ | ⚠️ 枚举略不同 |

---

## 3. 页面布局差异

### 3.1 工作台（workbench）

| 维度 | ZK-IPD | ruoyi-web | 一致性 | 修复策略 |
|---|---|---|---|---|
| 头部问候 | "上午好，张三"（按时辰） | "张三，欢迎回来" ⚠️ | ⚠️ 时辰缺失 | 加时辰问候 |
| Metric 指标卡 | 4 个（待办/逾期/未读/已完成） | 1 个 alert 占位 ⚠️ | ❌ 缺失 | 加占位 metric |
| 任务队列 | 任务卡 + 项目分组 | ❌ 无 | ❌ 缺失 | 等后端聚合 |
| 当前推进 | 下一个工作项 | ❌ 无 | ❌ 缺失 | 等后端聚合 |
| 快捷入口 | 14 个一级导航 | 6 个 ✅ 部分一致 | ⚠️ 偏少 | 补足 |

### 3.2 项目空间（projects）

| 维度 | ZK-IPD | ruoyi-web | 一致性 | 修复策略 |
|---|---|---|---|---|
| 顶部信息条 | 4 列摘要 + KPI 入口按钮 | 表格，无摘要条 ⚠️ | ⚠️ 缺摘要条 | 在列表上方加 KPI 卡 |
| Stage 6 rail | 6 阶段进度（粘性条） | ❌ 无 | ❌ 缺失 | 等后端 P-3 |
| 阶段展开 | 默认展开当前阶段 | ❌ 无 | ❌ 缺失 | 等后端 P-3 |
| 工作项列表 | 内嵌 SOP 标签 + 锁定/解锁状态 | ❌ 无 | ❌ 缺失 | 等后端 P-3 |
| 其他项目切换 | 「可查看的其他项目」列表 | ❌ 无 | ❌ 缺失 | 等后端 P-3 |

### 3.3 需求门户（portal/submit, portal/status）

| 维度 | ZK-IPD | ruoyi-web | 一致性 | 修复策略 |
|---|---|---|---|---|
| 顶部品牌条 | 蓝色短柱 + "IPD 需求门户" + 提交/查询 切换 | 同 ✅ | ✅ 已一致 | - |
| 表单布局 | 单列 | 单列 ✅ | ✅ 已一致 | - |
| **附件上传区** | 虚线框 + 上传图标 + 状态反馈 | ❌ 无 | ❌ 缺失 | 补占位 + 提示 |
| 查询结果 | 状态徽章 + 时间线 + 附件清单 | 同 ✅ | ✅ 已一致 | - |

### 3.4 招标单（bid/list, bid/create, bid/select）

| 维度 | ZK-IPD | ruoyi-web | 一致性 | 修复策略 |
|---|---|---|---|---|
| 筛选区 | 状态筛选 + 待我应标 + 角色范围 | 同 ✅ | ✅ 已一致 | - |
| 表格列 | 标题/方式/状态/有效期/创建/操作 | 同 ✅ | ✅ 已一致 | - |
| 行操作 | 应标/遴选/撤回/关闭/详情 | 同 ✅ | ✅ 已一致 | - |
| 详情抽屉 | Descriptions | 同 ✅ | ✅ 已一致 | - |
| 7 字段表单 | 客户问题/应用场景/核心功能/目标上市/市场窗口/战略等级/截止日期 + 标题 + 方式 | 仅标题/内容/方式/邀请/截止 | ❌ 缺 5 字段 | 占位 + 模板提示 |

---

## 4. 样式差异

| 维度 | ZK-IPD 标准 | ruoyi-web 现状 | 一致性 | 修复策略 |
|---|---|---|---|---|
| **主色** | `#245bf4` | `#006BE6`（Ant Design 默认） | ⚠️ 不一致 | 在 ipd.scss 加 CSS 变量覆盖 |
| **背景** | `#f5f7fb` | （Vben 默认） | ⚠️ 不一致 | 同上 |
| **顶部栏** | navy `#071426`，高 56px | Vben 默认浅色 | ❌ 显著不同 | 顶部栏 IPB 专属色 |
| **侧边栏** | navy-2 `#18253a`，宽 164px | Vben 默认浅色 | ❌ 显著不同 | 侧边栏 IPB 专属色 |
| **分割线** | `#dfe4ed` | Ant 默认 | ⚠️ 接近 | 替换色值 |
| **次文字** | `#697388` | Ant 默认 | ⚠️ 接近 | 替换色值 |
| **卡片背景** | `#ffffff` | 同 ✅ | ✅ 已一致 | - |
| **字体** | Inter, Noto Sans SC, Microsoft YaHei | （Vben 默认） | ⚠️ 不一致 | 引入字体栈 |
| **圆角** | 卡片 8px / 按钮 6px / 输入 7px | Ant 6px | ✅ 接近 | - |
| **按钮 primary 阴影** | `0 4px 12px rgba(36,91,244,.18)` | Ant 默认 | ⚠️ 不一致 | 调整 box-shadow |
| **底部 footer** | 无（应用是单页 SPA，无 footer） | Vben 默认有 | ⚠️ 不一致 | 隐藏 |

---

## 5. 页面缺失（ZK-IPD 有 / ruoyi-web 无）

| 路由 | 页面 | ZK-IPD | ruoyi-web | 状态 |
|---|---|---|---|---|
| `/product-space` | 产品空间 | ✅ | ❌ | 后端未交付，跳过 |
| `/recruitments` | 研发招募（独立页） | ✅ | ❌ | 后端未交付，跳过 |
| `/changes` | 变更管理（独立页） | ✅ | ⚠️ project/change-detail | 已有 |
| `/handoffs` | 项目移交工作台 | ✅ | ❌ | 后端未交付，跳过 |
| `/performance` | 协同绩效 | ✅ | ❌ | 后端未交付，跳过 |
| `/timeline` | 全流程轨迹 | ✅ | ❌ | 后端未交付，跳过 |
| `/reports` | 报表分析 | ✅ | ❌ | 后端未交付，跳过 |
| `/products` | 产品目录（超级管理员） | ✅ | ✅ product/list | 已有 |
| `/identity-sync` | 人员同步 | ✅ | ✅ admin/org 兼 | 已有 |
| `/admin` | 超级管理 | ✅ | ✅ admin/* | 已有 |
| `/reviews` | 阶段确认 | ✅ | ✅ cert/templates | 已有 |

---

## 6. 修复路线图

### Phase A：样式一致性（必须）
- A1. 主题色板统一为 `#245bf4`
- A2. 顶部栏 navy 色 + 侧边栏 navy-2 色（仅 IPD 模块生效）
- A3. 字体栈统一 Inter + Noto Sans SC
- A4. 全局分割线 / 次文字色值替换

### Phase B：字段一致性（必须）
- B1. portal/submit: 反馈人上限 64 → 80 字
- B2. portal/submit: 加附件上传占位区 + 格式/大小提示
- B3. project/create: 名称上限 ≥4 字
- B4. bid/create: placeholder 改用 7 段结构提示；content 上限扩至 4000 字

### Phase C：页面布局一致性（必须）
- C1. workbench: 时辰问候 + 4 metric 占位卡
- C2. project/list: KPI 摘要条占位
- C3. portal/submit: 附件上传区域占位

### Phase D：测试 + 收口
- D1. `pnpm typecheck`
- D2. `pnpm test`（保留已知 happy-dom 5 errors）

---

## 7. 不在范围

- 后端结构调整
- API 契约变更（前端层只调整 placeholder / 文案 / 占位）
- 完整补齐 ZK-IPD 全 49 页（仅覆盖当前 ruoyi-web 已有页面 + 已交付后端的页面）
- 修改 ZK-IPD 设计稿仓（只读）
