# 验证复核报告（2026-09-07 · 验证收口线）

> 角色分工（owner 裁决）：另一 Ruflo 会话主刀执行治理波；本线为**独立验证收口线**——只验证、抓假红假绿、落证据，不改执行会话的在途代码。
> 基线：ruoyi-ai `bafbce03`（快照后执行会话继续推进至 `f1fff833` WAVE22-B）。
> 原始证据：`.codex/ipd-dev/gov6/`（be-test.log / fe-vitest-*.log / fe-checktype.log / fe-build.log / qa08-rerun/ / baseline-*.txt）。

## 1. 前端：真绿（四关全过）

| 关卡 | 结果 |
| --- | --- |
| `cd apps/web-antd && npx vitest run --dom src/api/ipd` | 16 文件 / 71 测全过 |
| `cd apps/web-antd && npx vitest run --dom src/views/ipd` | 45 文件 / 394 测过 + 1 skip |
| `pnpm run check:type`（根目录） | 退出 0 |
| `pnpm run build:antd`（根目录） | 退出 0，日志 **0 条** TS 诊断（TS4058 陷阱未复发） |

采用 WAVE22-B 定论的"三要素"跑法（app 子目录 + `--dom` + 显式路径）；根目录 `vitest.ipd.config.mts` 姿势已证实会丢 plugin-vue，本线未采用。
**结论：执行会话"前端 0 bug"定论，独立复核成立。**

## 2. 后端：1474 测，8 个非绿（6 Failures + 2 Errors），逐个定性

| # | 测试 | 症状 | 定性 |
| --- | --- | --- | --- |
| 1 | WebSocketOriginGuardTest.parentAllowedOriginsNotWildcard | application.yml 断言内容缺失 | **在途中间态**——yml 正被执行会话编辑 |
| 2 | TenantExcludesConsistencyTest.allDdlTablesExcluded | 4 张新表未登记 tenant.excludes：`ipd_business_config` / `ipd_business_config_versions` / `switching_acceptance` / `gate_review_observers` | **真实欠账**——本仓铁律：不登记会被自动追加租户过滤，表现为"数据查不到" |
| 3 | AcceptanceMatrixValidationTest.coveredRowsHaveEvidence | AC-INC-02 的 `P034AcceptanceTest#testAC02`（# 号方法引用）被当文件路径校验 | 验收矩阵**数据格式 bug**（unitTestClass 字段格式不兼容） |
| 4 | Qa04EntityContractTest.softDeletableEntitiesCarryDelFlag:106 | 软删标记与 del_flag 不一致 | **真实契约漂移**（entity/DDL 层） |
| 5-6 | KpiRecordServiceTest.businessConfigService_*（2 ERROR） | UnnecessaryStubbingException @321/@347 | **测试漂移**——BusinessConfig 改造后实现不再走该 stub 缝，测试未跟上 |
| 7 | DeletionRequestServiceTest.submitGoesToLeaderReviewWithWorkdayDeadline | expected: 8（工作日截止日） | **在途中间态**——DeletionRequest 三件套（Controller/Service/Test）均未提交 |
| 8 | LaunchDateDualSignGuardAcceptanceTest.launchDateWriteSitesAreEnumerated | ProjectController.java 出现新 launch_date 写入点未登记双签设计 | 守卫**按设计抓到**真实新增写入点，需补登记 |

**判定**：无 NoClassDefFoundError / target 目录互踩症状，**不是并发假红**。其中 #2、#4、#5-6、#8 是真问题（配置债 / 契约漂移 / 测试漂移 / 未登记写入点），#1、#7 属执行会话在途，#3 是矩阵数据 bug。修复归属：执行会话（相关文件全在其在途域，本线不越界）。

## 3. qa08 复跑（独立 out-dir，未覆盖执行会话产物）

- **PASS 20.9% → 22.1%**（52→55），fail 7→1，blocked 48→44（产物：`.codex/ipd-dev/gov6/qa08-rerun/`）
- 本线动作：`receipt_ledger` 表已落库（幂等 DDL `2026-09-06-ipd-receipt-ledger-table.sql`，走 `mysql-app.cnf`）——此前"SQL 已 commit 但表未建"
- 剩余 1 fail：AC-GLB-12（Gate 33 要素 / 15 否决项覆盖检查）
- 脚本 env 注记"audit_log_chain_heads 缺表（影响 16039 登录）"**已过期**（该表 2026-09-07 13:25 已建），待更新注记
- blocked 主因：前端仓库未拉入 ruoyi-ai（UI 验证缺位）、外部依赖（企微扫码登录）等

## 4. 环境发现

- `mysql-migrator.cnf` 凭据**已失效**（Access denied for `ipd_migrator`@`192.168.65.1`）；`mysql-app.cnf` 实际**可执行 DDL**（与本机旧认知"app 无 DDL 权限"相反，直灌前先试 app 通道）
- `receipt_ledger` 表注释在客户端显示为乱码（charset 层面），仅注释，不影响功能

## 5. 看板与 plan 镜像

- ruoyi-ai 项目 375 卡在线；本报告证据与 **QA-08 卡**（`3ba1f028`，todo，"249AC全量执行与交付结论签注"）直接相关
- 本线**未翻任何卡状态**：看板状态机归执行会话单写（OPS-09 / 单一写入者）；本报告即 QA-08 的签注证据，待执行会话收掉 §2 真问题后翻卡

## 6. 复跑方式（留痕）

```bash
# 后端（错峰：执行会话落盘后再跑）
cd /Users/mac/Documents/ruoyi-ai && mvn -o -pl ruoyi-modules/ruoyi-ipd test
# 前端（三要素姿势）
cd apps/web-antd && npx vitest run --dom src/api/ipd src/views/ipd
cd .. && pnpm run check:type && pnpm run build:antd
# qa08（独立 out-dir）
python3 docs/ipd-系统说明/验收/qa08-ac-acceptance.py --out-dir <任意独立目录>
```

---

## 7. 第二轮复跑（2026-09-07 · owner「立即执行」指令）

### 7.1 后端：1515 测（+41 新测），8 个非绿**完全稳定复现**

同一清单逐项不变（#1~#8 同 §2）。两轮运行间完全一致 → 排除偶发/并发假红，均为确定性问题。执行会话本时段推进的是 WAVE23~25 新波次（a11y 清零 / 3200 请求压测 / 移动走查 / qa08 登录链解锁），这 8 个尚未收。

双源印证：执行会话 commit `106de362` 自报 qa08 PASS 22.1%（52→55、FAIL 2→1），与本线 §3 独立复跑数字完全一致。

### 7.2 前端：3 个新 commit 后 vitest 仍全绿，但 check:type 新增退出 2 —— 已由本线闭

- `api/ipd` 16 文件 / 71 测全过；`views/ipd` 46 文件 / 399 测过 + 1 skip（新焦点陷阱 +5 测）
- 唯一根因：`use-focus-trap.test.ts:17` 未用导入 `beforeEach`（TS6133），由 commit `eba1fa7` 引入——vitest 不查未用导入所以它绿，typecheck 一查即红（AGENTS.md「测试绿 ≠ 类型门绿」陷阱实例）
- **本线已修**：commit `78bf6b5`（单文件，未触碰在途 92 文件）；复跑 check:type exit 0 + 焦点陷阱 5 测仍绿

### 7.3 待 owner 决策

- `653eb311`（WAVE25-PUSH）：本地已攒 **146 commit** 待同步远端；push 被钩子拦截属预期，需 owner 明确授权
- §2 中 #2 / #4 / #5-6 / #8 四个真问题仍在执行会话域内待收，收后本线再做第三轮复跑签注

---

## 8. 真环境 HTTP E2E（2026-09-07 · owner「真环境验证」指令）

> 脚本：`.codex/ipd-dev/gov6/e2e-http.py`（gitignored）；结果快照 `e2e-run-ab2/ab3.log`。不再停留在单测/Mock 层。

### 8.1 Phase A 读腿（双轮 PASS）

admin 真 Bearer 登录后 GET：products / projects / product-groups / audit-logs / notifications 全部 200 code=0 返真数据；persons / kpi / workbench 根路径 404 属探针姿势（需子路径），非缺陷。

### 8.2 Phase B 写腿·真落库（双轮 PASS，可复现）

`POST /api/v1/ai-documents` → 服务端校验/摘要 → 真库 `ai_documents` 落盘 → HTTP 回读 + 直查库 + 本地重算 sha256 **三方一致**：

| 轮次 | doc_id | version | status | sha_match |
|---|---|---|---|---|
| 1 | 2096895627102298113 | v1 | GENERATED | ✅ |
| 2 | 2096896580987682818 | v1 | GENERATED | ✅ |

### 8.3 Phase C 真模型生成：两重阻塞已定性

1. **无可用 LLM key（硬阻塞）**：credentials/backend-secrets/主 yml（Qdrant 空 key、zhipu web-search 段）/框架 chat_model 表（0 行）/环境变量/本地推理（11434/1234 不通）全查空。AiChatClient 为 OpenAI 兼容 `/chat/completions`，提供任一兼容 key 即可走 `e2e-http.py --api-key` 闭环。
2. **`/ai-models` 管理面对 SUPER_ADMIN 403 code=30001（待收真问题）**：源码三查全对（`IpdRolePermissionCatalog` READ_SET 含 `ipd:ai-model:list`、ADMIN_WRITE 含 edit、桥接实时查 personType=SUPER_ADMIN、persons 表 900101 全对），audit-logs 同型注解同账号能过——疑似运行 jar 落后源码或存在未知判定分支，待执行会话部署循环结束后复测定论。

### 8.4 环境发现（增值）

- 首登强制改密墙真生效：leader（must_change_pwd=1）全业务口 20003，AC-AUTH-02 真实可用
- 多会话竞争同一 admin 账号：本线 02:40 重置后，执行会话 17:45:53（CST）再次改写 hash；随后 bootstrap 全账号重置中。**账号域此刻归执行会话，本线停止碰账号**
- 后端处于部署循环（01:58 / 02:36 / 02:38 / 之后多轮重启），502 均为重启窗口，非缺陷
