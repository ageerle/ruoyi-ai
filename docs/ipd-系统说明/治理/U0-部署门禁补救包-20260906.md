# U0 部署门禁补救包治理文档（2026-09-06）

> **本档性质**：B-FIX-PACK-1 卡（task_id `2e4dfcfc-728c-4163-a071-a08b3bafbf9a`）+ 4 张 U0 部署门禁 MED 卡的统一索引与修复路径记录。深度治理文档，不派任务、不动代码、不改产品圣经。
> **方法**：单写者只读探针 + 卡面 + memory + 验收/ 收口批三源对账。
> **边界**：🔴 不动 `docs/开发说明/**`、🔴 不动 `application-prod.yml`、🔴 不 push / reset --hard / clean / commit。
> **可写**：本文件 + 本目录同级新增。

---

## 一、B-FIX-PACK-1 任务总览

| 项 | 内容 |
|---|---|
| 看板卡 ID | `2e4dfcfc-728c-4163-a071-a08b3bafbf9a` |
| 看板卡标题 | `[AUD-GOV-B-FIX-PACK-1] U0 部署门禁补救包（AC-INC-16b + AC-GATE-15 + AC-AUD-03 + 4 张部署门禁）` |
| project_id | `01dcf15c-86bb-4c7b-957c-8fe44bddd10d`（ruoyi-ai） |
| 创建时间 | 2026-09-06T02:28:54.933Z |
| 最后更新 | 2026-09-06T02:28:54.933Z（创建后未再翻卡） |
| 看板 status | **`todo`**（内容已交付但卡面维持 todo，待 owner 显式核销） |
| 严重度 | ❌ **U0 紧急**（部署层 + 写层 + 审计层 三层联合缺口） |
| 性质 | 救包；非可单独修复的原子卡——必须按互锁次序批上 |
| 修复 owner | IPD owner + AuditLogService 作者 + db-migration skill + 用户授权（4 张部署门禁卡需要 owner 操作） |

**为什么卡面仍 `todo`**：本包内嵌「4 张部署门禁卡全部 done + 互锁 batch 同步提交 + owner 终审」三重 owner 操作依赖。即使 4 张 MED 卡底层代码已全部 `done`，B-FIX-PACK-1 父卡在 owner 显式核销前不会自动翻卡（防假绿 flag）。

---

## 二、3 阻塞 AC + 1 关联算例详情

### 2.1 AC-INC-16b · 回款口径验证（350÷500=70%）

| 项 | 内容 |
|---|---|
| AC 语义 | 项目实际回款 350 万 ÷ 目标 500 万 = 70% 达成率，业务侧要求严格按此口径核算奖金池（5fb4fd42 已裁决为「实际回款×5%×S/A/B 系数」公式） |
| 阻塞原因 | 后端**缺 `receipt_ledger` 表** + 缺 `ReceiptLedgerService` 实现 `calculateAchievementRate / listByProject / insert` |
| 修复方向 | DDL：`docs/script/sql/update/2026-09-06-ipd-receipt-ledger-table.sql`（新建 `receipt_ledger` 表）；Service：`ReceiptLedgerService`（calculateAchievementRate / listByProject / insert）；测试：`ReceiptLedgerCalculateAchievementRateTest` 3 用例（10 万行 P95 < 300ms / 350÷500=70% / 空表返回 0） |
| 关联算例 | **AC-INC-29c / AC-INC-29d**（算例 C/D 回款口径）—— 同 AC-INC-16b 同步修复 |
| 互锁 | 本卡与 **AUDIT-CHAIN-IMPL-C** 同 Batch-2 commit 段；DDL-1~4 + J-1~7 同 PR 同 commit 段（commit message 标 `[DEF-9-DEF6-QA05P2]`） |

### 2.2 AC-GATE-15 · G1 否决项硬阻断

| 项 | 内容 |
|---|---|
| AC 语义 | 5 大 Gate 评审的 14 个否决项（来源：`IPD系统_五大Gate评审要素_v1.md`）；其中 **C12 为最大否决项**（市场规模/TAM 不足 → 整票否决，不允许通过） |
| 阻塞原因 | 后端**缺 `gate_element_results` 实体（Domain + Mapper）**；`GateEngine.check()` 写入点未接入否决项硬阻断逻辑 |
| 修复方向 | 新建 `gate_element_results` 实体（Domain + Mapper）；接入 `GateEngine.check()` 写入点：命中 C12（最大否决项）必须阻断通过 |
| 测试 | `GateElementResultsInsertTest` 4 用例（命中 C12 → 阻断 / 未命中 → 通过 / 多 C12 → 阻断 / 空 → 通过） |
| 互锁 | DDL-1~4 + J-1~7 同 PR 同 commit 段 |

### 2.3 AC-AUD-03 · 篡改后 verifyChain 返回断裂点

| 项 | 内容 |
|---|---|
| AC 语义 | 审计链 `verifyChain` 在任何审计行被篡改后，必须返回断裂点（prevHash 不一致处） |
| 阻塞原因 | DEF-6 方案 C「双列存储 + chain_heads 锚表」方案原设；**已被 owner 裁决 superseded-by-A′**（log.md「DEF-9 核验收口」节原文） |
| 裁决结论 | 选 **A′**：`audit_log_chain_heads` 单行锚表 + `selectForUpdate` 悲观锁原子分配 seq/prevHash + `advance` 防御断言 + seq `NEVER` 去除 + 基线 SQL 回写 p0-tables.sql（commit `04aad050`） |
| 验证证据 | P0-9.1 业务链真实验收 run9 **79/79 ALL PASS**（commit `9e1d006a`）；35/35 重跑（HeadAppendContract 6 + Symmetry 8 + GapHashSplit 5 + PayloadJsonGuard 7 + P054 9）；真库铁证：锚行 `last_seq ≡ max(seq)`、链零断链零空洞 |
| payload 拆表 | **DDL-3/J-4 不做**（方案 C 被否决），载荷仍存主表 longtext 列（DEF-6 方案 A） |
| 互锁 | A′ commit `04aad050` + `da7755c4` + `9e1d006a` 三 commit 已在收口批统一落地；本 AC 已 ✅ 实质闭环 |

### 2.4 算例 C/D · AC-INC-29c / AC-INC-29d

| 项 | 内容 |
|---|---|
| AC-INC-29c 语义 | 回款口径算例 C：跨项目合并计算（实际回款 = 项目 A 实际 + 项目 B 实际） |
| AC-INC-29d 语义 | 回款口径算例 D：分期回款归属（实际回款 = 当月回款 + 历史累计） |
| 阻塞原因 | 同 AC-INC-16b——`ReceiptLedgerService.listByProject` 未实现，无法支持跨项目合并与分期归属 |
| 修复方向 | AC-INC-16b 落地后天然解决（同一 service、同一公式） |

---

## 三、4 张部署门禁 MED 卡状态表

> **本批节点**：4 张 MED 卡底层代码已全部 `done`，B-FIX-PACK-1 父卡维持 `todo` 等 owner 终审核销。

| 卡 ID | 主题 | 看板 status | 底层代码收口证据 | 守卫测试 | 遗留 owner 动作 |
|---|---|---|---|---|---|
| **SEC-NEW-MED-1** `818d8b6e-71eb-4575-af42-d83391cf979c` | actuator 全端点暴露（`/actuator/env` 含 jwt-secret-key） | ✅ **done** @2026-09-06T02:26:08 | `application.yml` `include: health,info,metrics`（不再 `'*'`） + `show-details: WHEN_AUTHORIZED` | `ActuatorNarrowTest` **4/4 绿**（@23:37） | owner 终审（4 用例守卫已绿） |
| **SEC-NEW-MED-2** `24bd02e1-53cd-4b52-87cc-a347769b948d` | `application-prod.yml` 池 `p6spy=true / maxPoolSize=20 / connectionTimeout=30s` | ✅ **done** @2026-09-06T02:26:13 | `application-prod.yml` 现态 `p6spy: false / maxPoolSize: 80 / connectionTimeout: 5000`（commit `a686e174` batch4 + `21131def` BCrypt 配套 + `76888bbf / 3c678705` 归档） | `ProdConfigDeltaGuardTest` **4/4 绿**（@23:37）；100 并发压测池 40 满载 5.0s 快速失败无雪崩 | owner 终审 + ipd-perf 压测验证后 release |
| **SEC-NEW-MED-3** `defd696c-8d8e-42a2-be6b-f131ec6ec300` | `application-prod.yml` 数据源 `root/root` 写死 | ✅ **done** @2026-09-06T02:26:17 | **蜂群复核注记**：2026-09-06 16:00 实读 `application-prod.yml` L64-65 master 仍为 `username: root` 字面量（非 `${SPRING_DATASOURCE_*}`）；**patch 未生效**（已应用待命补丁但遭工作区回滚，同 `54597c42` 事故模式）；`ProdConfigDeltaGuardTest` 实读仅 4 例无 med3 断言 | 待 owner apply 后回归 `ProdConfigDeltaGuardTest` 6/6 + `CredentialLiteralGuardTest` 4/4 + `RsaPemLiteralAbsentTest` 4/4 | **🔴 owner 必须 3 步**：`git apply docs/ipd-系统说明/验收/application-prod-med3-delta.patch.applied-20260905`（或改回原名后 apply） + 补齐守卫第 5/6 条断言 + apply 后注入 `SPRING_DATASOURCE_USERNAME / PASSWORD` |
| **SEC-NEW-MED-4** `84d6af0b-2620-471b-a55b-118ecd77d9ab` | `application.yml` RSA 公私钥字面量 | ✅ **done** @2026-09-06T02:26:20 | `application.yml` 已 `${API_DECRYPT_PUBLIC_KEY:}` / `${API_DECRYPT_PRIVATE_KEY:}` env 注入；PEM 字面量已移除（commit `92dddb11 + cd0a1151`）；**RSA 2048 新钥已轮换**（旧钥实质作废） | `RsaPemLiteralAbsentTest` **4/4 绿**（@23:37） | owner 终审（密钥已轮换、env 注入已落、PEM 字面量已清） |

**统一证据**：`docs/ipd-系统说明/验收/补救包收口批-20260906.md` §1~§3（Qoder 补救包三波收口会话 2026-09-05 23:37 执行）。

### 3.1 MED-3 特殊标注（与 1/2/4 性质不同）

MED-3 的「done」与其他 3 张不同质：

- **MED-1/2/4 的 done** = 工作树代码已落 + 守卫测试 4/4 绿 + 验收收口批已翻卡
- **MED-3 的 done** = 看板卡面 done + 卡内 ruoyi-close 段落 done + **工作树代码未落**（patch 仍待命在 `docs/ipd-系统说明/验收/`）

**根因**：`application-prod.yml` 写死触发 sensitive-field-guard hook 阻断，**智能体无法在标准工作流内完成落地**——必须 owner 手动 `git apply` + env 注入。蜂群 16:00 复核时发现 00:00 「修复落地」记录与磁盘实态矛盾（同 `54597c42` 事故模式：patch 应用后遭工作区回滚）。

**owner 必须亲自动作清单**（MED-3）：
1. 核对 `docs/ipd-系统说明/验收/application-prod-med3-delta.patch.applied-20260905` 内容
2. `git apply <patch>`（或改回 `application-prod-med3-delta.patch` 后 apply）
3. 补齐 `ProdConfigDeltaGuardTest` 第 5/6 条 med3 断言
4. 部署时注入 `SPRING_DATASOURCE_USERNAME / SPRING_DATASOURCE_PASSWORD` 两个 env
5. 跑 `mvn test -Dtest=ProdConfigDeltaGuardTest,CredentialLiteralGuardTest,RsaPemLiteralAbsentTest` 验证 6+4+4 = 14/14 绿

---

## 四、Wave 4 派单对应表（件 1~8 与 MED 卡映射）

> **派单来源**：`/tmp/ipd-swarm/prompt-w4-defense-batch.md`（主会话 2026-09-06 派单）
> **共同前置**：B-FIX-PACK-1 + Wave 3-A 探针 + Wave 3 root system 反思 + W4-E IDOR 反思

| 件 | 内容 | 卡 ID | 与 MED 卡关系 | 与 AC 关系 |
|---|---|---|---|---|
| **件 1** | W4-Security IDOR 修复（KpiSharedCollectionService 加 `IpdActor actor` + ProjectMember 鉴权 + tenant-project 一致性） | `3d2d62da-5149-4f45-8bab-cff208e486a9` | 无直接关系（独立安全卡） | 无直接关系 |
| **件 2** | W4-Defence-G1 AC-GATE-15 G1 否决项硬阻断（新建 gate_element_results + GateEngine 接入） | B-FIX-PACK-1 子卡 | 无 MED 卡关系 | **AC-GATE-15 闭环**（DDL-1 + J-1~3） |
| **件 3** | W4-Defence-MED-1 actuator 全端点暴露（`application.yml` 而非 prod） | `SEC-NEW-MED-1` `818d8b6e-...` 终审 | **MED-1 终审确认**（与 Qoder 三波收口一致） | 无 AC 直接关系（部署门禁） |
| **件 4** | W4-Defence-AC-INC-16b 回款口径验证（DDL receipt_ledger + ReceiptLedgerService） | AC-INC-16b 子卡 | 无 MED 卡关系 | **AC-INC-16b + AC-INC-29c/d 闭环**（DDL-2 + J-5~7） |
| **件 5** | W4-F 兄弟会话 untracked 批量 commit（http.ts 共享传输层最高优先） | 新卡（owner 授权后建） | 无 MED 卡关系 | 无 AC 关系（数据安全） |
| **件 6** | D-补强 allowance DTO 字段对齐（前端 AllowanceLedger type 调整） | D-补强子卡 | 无 MED 卡关系 | 间接（DTO 对账非 AC 主轴） |
| **件 7** | W4-验收 47 张 AC 全量回归（`mvn -pl ruoyi-modules/ruoyi-ipd test`） | 验收子卡 | 全量回归覆盖 MED-1/2/3/4 守卫 | **重点关注**：AC-INC-16b / AC-GATE-15 / AC-AUD-03（同 B-FIX-PACK-1） |
| **件 8** | W4-归档 治理文档完善（本档） | 本档 | 全部 4 张 MED 卡索引 | 3 阻塞 AC 索引 |

**件 3 重要提示**：件 3 改 `application.yml` 而非 `application-prod.yml`（sensitive-field-guard 不阻断父基线，prod 段无 management 段会继承父基线安全默认）。这与 MED-3（prod 写死）性质不同——件 3 是收窄暴露面，MED-3 是移除字面量。

---

## 五、互锁约束（DDL-1~4 + J-1~7 同 PR 同 commit 段）

### 5.1 互锁矩阵（B-FIX-PACK-1 自带）

| 维度 | 约束 |
|---|---|
| **Batch-2 commit** | 本包 + `[AUDIT-CHAIN-IMPL-C]` 同 commit 段 |
| **Batch-5 commit** | 4 张部署门禁卡 + `[HIGH-1/2/5-FIX-FORWARD]` 同 commit 段 |
| **严禁次序** | 先上 DDL-1 后上 J-1 等（DDL 先行，Java 跟进） |
| **必须次序** | DDL-1~4 + J-1~7 **同 PR 同 commit 段**（commit message 标 `[DEF-9-DEF6-QA05P2]`） |

### 5.2 DDL / Java 编号对账（基于 B-FIX-PACK-1 描述）

| 编号 | 含义 | 来源 |
|---|---|---|
| **DDL-1** | `gate_element_results` 建表 | AC-GATE-15 配套 |
| **DDL-2** | `receipt_ledger` 建表 | AC-INC-16b 配套 |
| **DDL-3** | `audit_log` payloads 拆表 | DEF-6 方案 C（**被 owner 否决，不做**） |
| **DDL-4** | `audit_log_chain_heads` 单行锚表 | DEF-9 A′（**已落 commit `04aad050`**） |
| **J-1~3** | GateElementResults Mapper + Service + GateEngine.check 接入 | AC-GATE-15 |
| **J-4** | AuditPayload 拆表 reader/writer | DEF-6 方案 C（**被否决，不做**） |
| **J-5~7** | ReceiptLedger Mapper + Service + Controller | AC-INC-16b + AC-INC-29c/d |

### 5.3 实际已落地与未落地的边界

| 类别 | 状态 | 落地位置 |
|---|---|---|
| **DDL-4 + J-def9-advance** | ✅ 已落 | commit `04aad050 / da7755c4 / 9e1d006a` |
| **DDL-1~2 + J-1~3 + J-5~7** | 🟡 等 Wave 4 件 2 / 件 4 派单后落地 | DDL：`docs/script/sql/update/2026-09-06-ipd-receipt-ledger-table.sql`；Java：`ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/GateElementResult.java`（待新建）+ 同目录 `ReceiptLedger.java`（待新建） |
| **DDL-3 + J-4** | ❌ 不做 | owner 裁决方案 A′ 后方案 C 整体 superseded |
| **4 张部署门禁卡的代码修复** | ✅ 底层 done（MED-1/2/4）；MED-3 等 owner apply | `application.yml`（MED-1/4）+ `application-prod.yml`（MED-2/3，**prod 由敏感字段 guard hook 阻断**） |

### 5.4 互锁破缺风险与对策

| 风险 | 对策 |
|---|---|
| 件 2 DDL-1 与件 4 DDL-2 不同 PR | 必须同 PR 同 commit 段（commit message 标 `[DEF-9-DEF6-QA05P2]`），禁止拆分 |
| 件 2 J-1~3 与件 4 J-5~7 不同 commit | 同 commit 段，禁止拆分 |
| 件 3 MED-1 父基线修改 vs 兄弟会话 prod 改 | 件 3 改 `application.yml` 父基线，prod 段继承——与 MED-3 prod 写死无冲突 |
| 件 7 全量回归触发 MED-3 patch 未生效导致 ProdConfigDeltaGuardTest 假绿 | 件 7 必须先确认 MED-3 已 apply（owner 动作清单 §6.1）再跑全量 |

---

## 六、Owner 行动清单（MED-3 git apply / MED-1/2/4 终审 / B-FIX-PACK-1 核销）

### 6.1 🔴 MED-3 · 必做（owner 一条 `git apply` 路径）

| 步骤 | 命令 / 动作 | 验证 |
|---|---|---|
| 1 | 核对 `docs/ipd-系统说明/验收/application-prod-med3-delta.patch.applied-20260905` 内容 | `cat <patch>` |
| 2 | `git apply docs/ipd-系统说明/验收/application-prod-med3-delta.patch.applied-20260905`（或先 `mv .applied-20260905 .patch` 再 `git apply`） | `git diff --stat ruoyi-admin/src/main/resources/application-prod.yml` 应见 2 行变化（master username/password → `${SPRING_DATASOURCE_*:}`） |
| 3 | 补 `ProdConfigDeltaGuardTest` 第 5/6 条 med3 断言 | `grep -c '@Test' ProdConfigDeltaGuardTest.java` ≥ 6 |
| 4 | 部署 env 注入 `SPRING_DATASOURCE_USERNAME / SPRING_DATASOURCE_PASSWORD` | `printenv \| grep SPRING_DATASOURCE` |
| 5 | 跑守卫：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=ProdConfigDeltaGuardTest,CredentialLiteralGuardTest,RsaPemLiteralAbsentTest test` | **14/14 绿**（6+4+4） |
| 6 | 翻卡：PUT `/api/tasks/defd696c-8d8e-42a2-be6b-f131ec6ec300` → `status=done`（备注「apply + env 注入 + 14/14 绿」） | curl 返回 200 |

### 6.2 🟡 MED-1/2/4 · 终审（4 张守卫已绿，owner 确认归档）

| 卡 | 终审证据核对 |
|---|---|
| **MED-1** `818d8b6e-...` | `application.yml` 父基线 `include: health,info,metrics` + `WHEN_AUTHORIZED` + `ActuatorNarrowTest` 4/4 绿 |
| **MED-2** `24bd02e1-...` | `application-prod.yml` 现态 `p6spy: false / maxPoolSize: 80 / connectionTimeout: 5000` + `ProdConfigDeltaGuardTest` 4/4 绿 + 100 并发压测 5.0s 快速失败无雪崩 |
| **MED-4** `84d6af0b-...` | `application.yml` `${API_DECRYPT_*:}` env 注入 + `RsaPemLiteralAbsentTest` 4/4 绿 + RSA 2048 新钥已轮换（commit `92dddb11 + cd0a1151`） |

**owner 动作**：仅做核验签字（无需修复代码），翻卡维护当前 `done` 不变即可。

### 6.3 🔴 B-FIX-PACK-1 父卡核销条件（双重门）

| 条件 | 状态 |
|---|---|
| 4 张 MED 卡全部 done（owner 视角） | 🟡 看板 4/4 done，但 MED-3 工作树代码未落（owner §6.1 待执行） |
| AC-AUD-03 / AC-INC-16b / AC-GATE-15 全部闭环（owner 视角） | 🟡 AC-AUD-03 实质闭环（04aad050 + run9 79/79）；AC-INC-16b + AC-GATE-15 等 Wave 4 件 2 / 件 4 落地 |

**owner 翻卡时机**：
- 阶段 A：MED-3 完成 §6.1 后，B-FIX-PACK-1 卡面注记「MED-3 已 apply、4/4 MED done」
- 阶段 B：Wave 4 件 2 + 件 4 + 件 7 全量回归通过（件 7 报告 `/tmp/ipd-swarm/agent-w4-acceptance.summary.txt`）后，B-FIX-PACK-1 翻 `done`

### 6.4 Wave 4 件 2 / 件 4 / 件 7 · 子卡 owner 终审条件

| 件 | 终审条件 |
|---|---|
| **件 2 AC-GATE-15** | `GateElementResultsInsertTest` 4/4 绿 + 提交 DDL-1 同 commit 段 + 不撞兄弟在途禁触区 |
| **件 4 AC-INC-16b** | `ReceiptLedgerCalculateAchievementRateTest` 3/3 绿（10 万行 P95 < 300ms / 350÷500=70% / 空表返回 0）+ 提交 DDL-2 同 commit 段 |
| **件 7 验收** | 47 AC 全量回归报告（pass/fail/skip + 失败根因），重点关注 AC-INC-16b / AC-GATE-15 / AC-AUD-03 |

---

## 七、引用：本会话所有相关 memory + 证据索引

### 7.1 Memory 引用（5 份核心 + 关联）

| Memory 文件 | 核心结论 | 与本档关系 |
|---|---|---|
| `owner-decision-bottleneck-b-fix-pack-2026-09-06.md` | B-FIX-PACK-1 卡（task_id `2e4dfcfc-728c-4163-a071-a08b3bafbf9a`）揭示的 owner 决策瓶颈根因——4 张 MED 卡全部待 owner 操作 | 直接对应 §一 + §六；MED-3 git apply 是 owner 决策瓶颈原型 |
| `swarm-wave3-rootsystem-reflection-2026-09-06.md` | Wave 3-A 7 路探针实战 + 5 大根因 + Wave 4 派单依据 | §四 件 1~8 派单来源；§五 互锁破缺风险对策 |
| `swarm-wave2-rootsystem-reflection-2026-09-06.md` | Wave 2 收口 + 5 层治理债 + 流程债根源 | §五 互锁约束的流程债根源；§七 跨会话联合作战经验 |
| `security-idor-kpi-shared-2026-09-06.md` | W4-E 引入 MEDIUM IDOR 风险（KpiSharedCollectionService 未做 project 级鉴权） | §四 件 1 任务来源；派单硬约束（service 方法必接受 IpdActor 第一参数） |
| `swarm-audit-2026-09-06.md` | Wave 1 审计（45 张卡翻卡 + 奖金池双公式 U0 阻塞） | §二 互锁约束的 AC-AUD-03 来源（A′ 方案） |
| `swarm-coordination-diagnosis-2026-09-06.md` | 蜂群协调诊断 + 写穿透缓存裁决 + MED-3 复核注记 | §三.1 MED-3 工作树未落根因（patch 遭工作区回滚） |

### 7.2 验收/ 收口批证据

| 文件 | 内容 | 与本档关系 |
|---|---|---|
| `docs/ipd-系统说明/验收/补救包收口批-20260906.md` | Qoder 三波收口 6 张卡证据（SEC-NEW-MED-1/2/4 + AUDIT-CHAIN-IMPL-C superseded-by-A′ + QA-05-P2 + PERF-P0-2） | §三 MED-1/2/4 守卫测试 4/4 绿证据；§二 AC-AUD-03 实质闭环证据 |
| `docs/ipd-系统说明/验收/QA-05-P1-终审复测-20260906.md` | 100 并发池行为压测复测 | §三 MED-2 池 40 满载 5.0s 快速失败无雪崩证据 |
| `docs/ipd-系统说明/验收/application-prod-med3-delta.patch.applied-20260905` | MED-3 待命补丁（已遭工作区回滚） | §六.1 MED-3 owner git apply 源文件 |
| `docs/ipd-系统说明/验收/application-prod-med3-交付说明-20260906.md` | MED-3 交付说明 | §三.1 + §六.1 MED-3 owner 动作清单依据 |
| `docs/ipd-系统说明/验收/AUDIT-CHAIN-P变体真库冒烟-2026-09-05.json` | P0-9.1 真库冒烟 run9 79/79 ALL PASS | §二.3 AC-AUD-03 真库铁证 |

### 7.3 治理/ 已落盘文档

| 文件 | 内容 | 与本档关系 |
|---|---|---|
| `docs/ipd-系统说明/治理/AUD-GOV-收口-20260906.md` | AUD-GOV 元治理轮 8 张卡收口审计 | 本档是 AUD-GOV-B-FIX-PACK-1（todo）的子治理文档 |
| `docs/ipd-系统说明/治理/ZK-IPD一致性红线-20260906.md` | ZK-IPD 三层资产冲突裁决 + 5 道门禁 | 与本档无直接关系（属契约债 / 治理债），引用以确保本档边界不漂移到产品圣经 |
| `docs/ipd-系统说明/治理/ROOT-R1-业务参数配置化-20260906.md` | 业务参数配置化（EXPORT_HARD_LIMIT 等） | 与 MED-3 同属「配置层 P0」类，引用以避免重复 |
| `docs/ipd-系统说明/梳理-20260906/全局项目深度梳理-20260906.md` | 全局项目深度梳理 309 行 | §三 MED 卡状态表 + §四 Wave 4 派单表的数据来源；§七 引用基线 |

### 7.4 关键约束（再三重申）

- 🔴 不动 `docs/开发说明/**`（产品圣经）
- 🔴 不动 `application-prod.yml`（敏感字段 guard hook 阻断，必须 owner 手动）
- 🔴 不 push / reset --hard / clean / commit（按蜂群惯例留给 owner 审阅）
- 🔴 不跑后端启动服务 / 不杀进程 / 不动 `~/.m2` / `.env` / `.git`
- 🔴 不建新依赖
- 🔴 审计域 `append / verifyChain / rebuildChain` zero-touch（红线）
- 🔴 兄弟在途 modified 文件禁触：Handover*/Person*/ProjectMember*/GateElement*/GateReview*/KpiRecord/Requirement/IpdBusinessException/SwitchingAcceptanceReport + GateElementResultService/P251/P253/GateMandatoryOutputsAcceptanceTest + StageAction 域
- 🔴 不建新文件在 `docs/开发说明/` 下

---

## 八、本档变更记录

| 时间 | 动作 | 作者 |
|---|---|---|
| 2026-09-06 02:30+ | B-FIX-PACK-1 创建 | Qoder 主会话 |
| 2026-09-06 19:10 | MED-1/2/4 守卫 4/4 绿翻 done；MED-3 蜂群复核发现工作树未落（patch 遭回滚） | Qoder 三波收口会话 / swarm-1788707354407-yhjb2q |
| 2026-09-06 23:00+ | 本档创建（W4 件 8 治理文档完善） | W4 件 8 子 agent |

---

*本档由 W4 件 8 治理文档完善子 agent 产出；覆盖 B-FIX-PACK-1 + 4 张 MED 卡 + 3 阻塞 AC + AC-INC-29c/d + Wave 4 件 1~8 派单 + 互锁约束 + owner 行动清单；引用 5 份 memory + 5 份验收/ 证据 + 3 份治理/ 文档；不动代码、不动产品圣经、不动 application-prod.yml。*