# OPS-06 业务健康监测初版（2026-09-07）

## 卡面状态
- **task_id**: `6f725e41-a73e-4ded-b8c5-da906b247705`
- **卡面状态**: todo → inreview（owner 复核）
- **优先级**: U2 中
- **依赖**: OPS-01, OPS-04

## 1. 业务健康监测三件套
1. **DB 健康**：MySQL `processlist` 实时查询 ipd_app 连接数（应 < 80 池水位）
2. **Redis 健康**：INFO command 监控 `connected_clients` / `used_memory_human`
3. **OSS 健康**：MinIO bucket 列表 / `mc admin info` 实时延迟

## 2. 审计失败可定位 trace
- 每条审计事件携带 traceId（MDC 注入）
- audit_log_chain_heads 表关联 trace
- 链路追踪链路：HTTP → Controller → Service → AuditLogService.append

## 3. 调度重试耗尽可见
- 调度任务失败第 N 次告警（默认 3 次耗尽）
- 告警通过 NotificationService（已就位）+ AuditLogService（已就位）双通道
- 需 `@EnableScheduling` 启用（当前未启用，等 OPS-04 scheduler 合入）

## 4. 日志凭证脱敏
- application.yml 已配：`LOGGING_LEVEL_ORG_RUOYI=info`
- `sensitive-field-guard.cjs` PreToolUse hook 拦截 Write/Edit（已就位）
- `PlusWebInvokeTimeInterceptor` 已识别出日志泄露登录参数（需后续修复）
- scripts/start.sh 加固（commit `9228ecae`）：env -i 最小化 + prod profile 禁用 OOM dump

## 5. 报警路径演练
- 本机：spring-boot-actuator `/actuator/health`（基础）
- dev：SnailJob 调度 + NotificationService
- prod：OPS-06 监控依赖完整：
  - spring-boot-actuator（健康）
  - SnailJob（调度）
  - NotificationService（告警）
  - AuditLogService（审计）
- 仅缺 3 行注解（@EnableScheduling + @Scheduled）+ 1 个完整 trace 接线

## 6. 测试
- 监测脚本：22 测试 0 失败 0 skip（独立验收已闭环）
- 真实 DB app 只读 / Redis / MinIO 探测通过
- 调度可见 0/3（@EnableScheduling 未启用，阻塞本卡 done 翻牌）
- 审计 trace 未接线（commit `f8c439a7` 后部分恢复）

## 7. 残留风险
- **@EnableScheduling 未启用**：依赖 OPS-04 调度框架合入主树
- **trace 接线不完整**：仅部分 controller 注入 MDC，全链路 trace 需补
- **PlusWebInvokeTimeInterceptor 凭证泄露**：登录参数写入 sys-console.log，框架层漏洞需修
- **卡面描述「真实故障/耗尽报警演练」未做**：等真实环境 + owner 授权后由人工补跑

## 8. 推荐下一步
- 等 OPS-04 调度合入主树后启动 `@EnableScheduling` 注解
- 补全 audit trace 接线（MDC + filter + interceptor 串联）
- 修复 PlusWebInvokeTimeInterceptor 凭证泄露（框架层 issue）
- owner 真实环境演练后由人工补跑报警测试

## 9. 结论
- 监测工具切片已独立验收 22 测试 0 失败 0 skip
- 真实 DB app 只读 / Redis / MinIO 探测通过
- 调度可见 0/3（@EnableScheduling 未启用，阻塞整体 exit 0 但 PARTIAL）
- 整体**exit 3 PARTIAL**，不等于业务健康
- 本卡**不翻 done**，待 OPS-04 调度合入主树后重新走 inreview 流程
- 已翻 inreview 等待 owner 复核
