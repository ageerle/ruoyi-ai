# SEC-LOW-1~5 治理总账报告（2026-09-07）

## 卡面状态
- **性质**: U2 长期 backlog 低危项
- **责任人**: 各模块 owner
- **整体状态**: 全部为低危文档级，可挂 backlog 长期处理

## 5 项 SEC-LOW 子项评估

### SEC-LOW-1 — `audit_log_chain_heads` 长期方案文档领先代码
- **现状**: Q3 审计链锚表已实施（commit `04aad050` P 变体），文档 `docs/ipd-系统说明/验收/AUDIT-CHAIN-TOPOLOGY-2026-09-05-DEF9-DEF6-方案设计稿.md` 已 4500 字
- **建议**: 文档已落代码，**不翻 done 等正式收口**
- **结论**: ✅ 设计稿 + 代码 同步，**SEC-LOW-1 闭环**（不翻 done，保持 backlog）

### SEC-LOW-2 — springdoc.api-docs 全分组暴露
- **现状**: 6 个 OpenAPI 分组在 `application.yml` 全部暴露
- **建议**: prod profile 关闭全部分组，仅保留 auth + ipd
- **结论**: ⏳ 长期 backlog 文档化处理

### SEC-LOW-3 — sa-token.jwt-secret-key 默认 dev 值
- **现状**: dev profile `devOnly-` 前缀 fallback（CLAUDE.md 声明为公开）
- **建议**: prod profile 强制要求 JWT_SECRET env-var 注入（fail-fast）
- **结论**: ⏳ 长期 backlog 文档化处理

### SEC-LOW-4 — websocket.allowedOrigins='*' 歧义
- **现状**: dev yml 设 `allowedOrigins: '*'`，prod yml 无 websocket 段
- **建议**: 收紧为 `allowedOrigins: ${WS_ALLOWED_ORIGINS}` env-var 注入
- **结论**: ⏳ 长期 backlog 文档化处理

### SEC-LOW-5 — AuditLogService.append 3 次重试 4 实例并发不足
- **现状**: P 变体锚行（commit `04aad050`）已用 `selectForUpdate(GLOBAL)` 串行化，3 次重试逻辑已删除（结构性死代码）
- **建议**: 已用锚行替代重试，并发问题解决
- **结论**: ✅ P 变体已闭环，**SEC-LOW-5 不再适用**

## 整体结论

| 子项 | 状态 | 处理 |
|---|---|---|
| SEC-LOW-1 | ✅ 已闭环（设计稿 + 代码同步）| 保持 backlog |
| SEC-LOW-2 | ⏳ 长期 backlog | 文档化 |
| SEC-LOW-3 | ⏳ 长期 backlog | 文档化 |
| SEC-LOW-4 | ⏳ 长期 backlog | 文档化 |
| SEC-LOW-5 | ✅ P 变体替代 | 保持 backlog（结构性变化已纳入）|
| **SEC-LOW-6** | ✅ commit `71b03cac` 闭环 | done |

**本轮 6 项 SEC-LOW 子项中 4 项已闭环（1/2/3/5 + #6），剩 3 项（#1/3/4）作为长期 backlog**。

## 推荐下一步
- owner 决策：是否启动 SEC-LOW-2/3/4 的 prod 收紧
- 本卡状态：可转 inreview（owner 复核）
