# P2-5.1 合规卡落地收口（2026-09-06）

## 5 步落地

1. **docs**：`docs/ipd-系统说明/后端需求/卡片-P2-5.1-合规卡-20260906.md`（设计稿：4 端点 + 字段定义 + AC/BR 引用 + 复用清单，76 行）
2. **code**：新增 8 个文件（精确 add）
   - `controller/ComplianceController.java`（4 个端点：retention-rules / data-deletion-request / audit-trail / permission-separation）
   - `service/ComplianceService.java`（4 个公开方法 + 私有 toAuditEntryVO / escapeJson / iso）
   - `dto/DataRetentionRuleVO.java`
   - `dto/DataDeletionRequestDTO.java`（@Valid 校验）
   - `dto/DataDeletionRequestVO.java`
   - `dto/PermissionSeparationVO.java`
   - `dto/AuditEntryVO.java`
3. **DDL**：无（合规卡不新增物理表，「合规请求 = 审计事件」轻量模型；审计走 AuditLog 单点收口，避免再建 data_deletion_requests 表带来的双写一致性问题）
4. **看板**：本会话无 MCP 可用；P2-5.1 状态在本落地文档登记（详见 5 步模板 4）
5. **commit**：本次 commit 落地 P2-5.1（详见下方 commit hash）

## 测试

- **JUnit 5 Platform Launcher 直接运行**（避免并发会话的 AiProviderTesterTest.java 阻断 mvn test）
- 测例数：**11/11 PASS**（11 个测试，包含 1 个 JSON 配置解析用例、1 个 30 天 deadline 用例、2 个审计覆盖用例、2 个 R/W 分离判定用例、2 个 audit-trail 用例、3 个边界用例）
- 命令：
  ```
  javac -d target/my-test-classes -cp "$(cat /tmp/ipd-cp.txt)" src/test/java/org/ruoyi/ipd/service/ComplianceServiceTest.java
  java -cp "/tmp:target/classes:target/my-test-classes:$(cat /tmp/ipd-cp.txt):<junit-launcher>" \
       RunJUnit org.ruoyi.ipd.service.ComplianceServiceTest
  ```
- 关键输出：
  ```
  [        11 tests found           ]
  [         0 tests skipped         ]
  [        11 tests started         ]
  [         0 tests aborted         ]
  [        11 tests successful      ]
  [         0 tests failed          ]
  ```

## 合规要点验证

- **个保法 30 天 deadline**：✅ `DELETION_DEADLINE_DAYS=30` 常量 + `createDeletionRequest` 内 deadlineAt=now+30d，`createDeletionRequest_sets30DayDeadline` 测例断言 ±5s 误差通过
- **审计链不可绕过**：✅ `ComplianceService.createDeletionRequest` 唯一写路径 = `auditLogService.append`，`createDeletionRequest_auditsAction_andBlocksBypass` 用 ArgumentCaptor 验证 action="COMPLIANCE_DELETION_REQUEST" + entityType/entityId/operatorId 全部就位
- **R/W 权限分离**：✅ `permissionSeparation_detectsReadWriteConflict_forAdmin`（SUPER_ADMIN → conflict=true）+ `permissionSeparation_returnsClean_forMarketPm`（MARKET_PM → conflict=false）双测通过

## 已知限制 / 后续可补

- 数据保留规则配置目前默认 7 年（2557 天）/ SOFT_DELETE / DSL-内部留存，运营侧可在 sys_config 配 `compliance.retention.<resourceType>` JSON 覆盖
- 数据删除请求的真实删除流程仍走 `DeletionRequestController`（避免越权直删）；P2-5.1 仅创建合规请求事件
- R/W 分离当前以 person_type 唯一来源为口径；后续若引入 sys_role 多角色支持，需升级为「角色-权限码多对多」映射
