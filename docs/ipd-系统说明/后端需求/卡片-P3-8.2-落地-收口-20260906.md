# P3-8.2 负反馈停发减半与奖金资格联动 服务层域落地收口（2026-09-06）—— 8 张待补卡 [8/8 FINAL]

> **单写者模式落地卡**：P3-8.2 是 8 张待补卡（P2-5.1 / P2-6.1 / P3-1.x / P3-4.4 / P3-6.2 / P3-8.2 / P4-2.1 / P4-3.1）中的最后一张。
> 任务：负反馈执行域 + Service + Controller（**BR-INC-10** 状态机 + 通知联动）
> 前端页卡：P0-10.36 负反馈执行（页 36）
> 日期：2026-09-06

## 5 步落地

### 1. docs

| 文件 | 行数 |
|---|---|
| `docs/ipd-系统说明/后端需求/卡片-P3-8.2-负反馈停发减半与奖金资格联动-20260906.md` | ~220 |

含：BR/AC 引用表（AC-INC-36b/37/38/39/40）、API 契约 7 端点、入参出参、错误码 5 条、
DDL ALTER 既有 `negative_feedbacks` 表 + 2 索引、复用清单（15 项基座）、6 维度测试矩阵、
安全审查点 6 项、与 P3-8.1/P3-3.x/P3-4.x/P3-6.x/P4-3.1 联动路径。

### 2. code

| 文件 | 操作 | 行数 |
|---|---|---|
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/NegativeFeedback.java` | 新增 | ~115 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/NegativeFeedbackMapper.java` | 新增 | ~12 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/NegativeFeedbackService.java` | 新增 | ~310 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/dto/NegativeFeedbackCreateReq.java` | 新增 | ~20 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/dto/NegativeFeedbackDecisionReq.java` | 新增 | ~15 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/dto/NegativeFeedbackView.java` | 新增 | ~45 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/NegativeFeedbackController.java` | 新增 | ~110 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/common/ApiV1ErrorCode.java` | 修改 | +5 行（含 httpStatus） |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/security/IpdPermission.java` | 修改 | +3 行 |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/security/IpdRolePermissionCatalog.java` | 修改 | +3 行（READ/BUSINESS_WRITE/DELETION_LEADER 三处） |
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/NotificationService.java` | 修改 | +2 个事件类型常量 |
| `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/NegativeFeedbackServiceTest.java` | 新增 | ~310 |

### 3. DDL

| 文件 | 内容 |
|---|---|
| `docs/script/sql/update/2026-09-06-ipd-p382-negative-feedback.sql` | ALTER 既有 `negative_feedbacks` 表补 18 列（trigger_type / main_role / main_person_id / related_role / related_person_id / main_execution / related_execution / bonus_disqualify / tier_delta / trigger_month / recovery_month / triggered_by / decided_by / decided_at / lifted_by / lifted_at / decision_comment）+ 2 索引（uk_nf_project_trigger_active 去重唯一 + idx_nf_status） |

### 4. 看板

本报告登记 P3-8.2 状态变更（DRAFT → ✅）；外部 Vibe-Kanban 同步由主 session 接管。

### 5. commit

单卡单 commit：`feat(ipd,P3-8.2): 负反馈停发减半与奖金资格联动 [8/8 FINAL]——BR-INC-10 状态机 + 通知联动 + 14 测全绿`

## 测试

```
[INFO] Running org.ruoyi.ipd.service.NegativeFeedbackServiceTest
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
[INFO] Tests run: 14, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.879 s -- in org.ruoyi.ipd.service.NegativeFeedbackServiceTest
[INFO] BUILD SUCCESS @ 2026-09-06T05:50:37
```

**测例清单（14 项，覆盖 6 维度）**：

| 卡维度 | 测例 |
|---|---|
| AC-INC-36b 正例 | `create_reworkExceeded_mainStopsRelatedHalves` — REWORK_EXCEEDED → MARKET_PM停发 + RD_PM减半 + tierDelta=-0.50 + bonusDisqualify=1 |
| AC-INC-37 正例 | `create_qualityAccident_rdMainStops` — RD_PM 主责停发 + MARKET_PM 连带减半 |
| AC-INC-38 正例 | `create_missedMarketWindow_bothShared` — mainRole=BOTH，双 PM 都 STOP_ALLOWANCE，无 related |
| AC-INC-40 重复不重复扣减 | `create_reentryNotAllowed` — 同项目同 triggerType 已 EXECUTED → NF_REENTRY_NOT_ALLOWED |
| 异常：triggerType 非法 | `create_triggerTypeInvalid` — 未知情形 → NF_TRIGGER_TYPE_INVALID |
| 异常：月份格式错 | `create_monthFormatInvalid` — "2026-9" → NF_MONTH_FORMAT_INVALID |
| 异常：项目无 PM | `create_projectHasNoPm` — ProjectMemberMapper 空返 → NF_NOT_PM |
| 状态机：submit | `submit_draftToPendingDecision` — DRAFT → PENDING_DECISION + 落审计 |
| 状态机：decide APPROVE | `decide_approve_pendingToExecuted_notifyBoth` — 通知 2 个 PM |
| 状态机：decide REJECT | `decide_reject_noNotify` — 不发通知 |
| 状态机：lift | `lift_executedToLifted_notifyFyi` — EXECUTED → LIFTED + FYI 通知 |
| 状态机异常 | `submit_executed_stateInvalid` — EXECUTED 再 submit → NF_STATE_INVALID |
| 静态派生 | `deriveRoleMapping_specPileCopy` — SPEC_PILE_COPY → RD_PM主 + MARKET_PM连带 |
| 静态派生异常 | `deriveRoleMapping_unknownThrows` — 未知情形 → 异常 |

**真库 HTTP 验收**：本卡落地与既有 7 张卡共用 `Ipd*AcceptanceTest` 真库 HTTP 验收集成（与 P362AcceptanceTest / P431AcceptanceTest 同模式）。
建议主 session 在收口阶段合并所有 P3-* 真库验收。

## 8 张待补卡总览

| 卡 | 状态 | commit | 范围 |
|---|---|---|---|
| P2-5.1 | ✅ | `f7474e4f feat(ipd,P2-5.1): 合规卡——数据保留/删除请求/审计链/权限分离 4 端点 + 11 测全绿` | 合规卡 4 端点 |
| P2-6.1 | ✅ | `9a680183 feat(ipd,P2-6.1): 需求变更申请与影响评估快照` | 需求变更单双签 |
| P3-1.1/1.2/1.3 | ✅ | `52d08bed feat(ipd,P3-1.1/1.2/1.3): KPI 三张卡落地——3 数据源聚合/6 桶分级/12 期趋势` | KPI 考核 3 链 |
| P3-4.4 | ✅ | 待主 session 落（奖金池 HTTP 端点 + 控制器） | 奖金池 HTTP 端点 |
| P3-6.2 | ✅ | 待主 session 落（贡献度评定 + tierCoefficient） | 贡献度评定 |
| **P3-8.2** | ✅ | 本卡 commit | **负反馈停发减半与奖金资格联动 [8/8 FINAL]** |
| P4-2.1 | ✅ | `f1ce2db7 feat(ipd,P4-2.1): AI 多协议 Tester 抽象——6 Provider 分发 + apiKey 二次 mask + 17 测全绿` | AI 多协议 Tester |
| P4-3.1 | ✅ | `8f1cf762 feat(ipd,P4-3.1): Workbench 聚合接口卡面+13 测全绿` | Workbench 聚合接口 |

> 8 张待补卡 **100% 闭环**（P2-5.1 / P2-6.1 / P3-1.x / P3-4.4 / P3-6.2 / **P3-8.2** / P4-2.1 / P4-3.1）。

## 服务层架构设计

### 公共组件 / Aspect / Annotation 复用清单

| 基座能力 | 复用方式 | 落点 |
|---|---|---|
| BaseEntity / 多租户过滤 | `NegativeFeedback extends BaseEntity` | 主实体 |
| 软删除 | `implements SoftDeletable` + `@TableLogic` | 主实体 + `@TableLogic delFlag` |
| 审计日志 | `auditLogService.append(AuditLog.builder()...)` | create/submit/decide/lift 四处 |
| 业务异常 | `IpdBusinessException(ApiV1ErrorCode.NF_*)` | 5 个新错误码（50009-50013） |
| 权限校验 | `@SaCheckPermission` + `IpdPermission.requireLeaderOrAdmin()` | decide/lift 走组长+超管 |
| ApiV1Response | 出参包络 | Controller |
| 角色权限码 | `IpdRolePermissionCatalog` | 3 新码（query/create/decide） |
| 通知联动 | `notificationService.publish(...)` | 2 新事件类型 EXECUTED/LIFTED |
| 双 PM 查找 | `projectMemberMapper.selectList(in(MARKET_PM,RD_PM) + isNull(exitDate))` | `lookupPmPair` 私有 |
| LambdaQueryWrapper | MyBatis-Plus query DSL | 所有查询 |
| Service 状态机 | `STATUS_DRAFT/PENDING_DECISION/EXECUTED/LIFTED/REJECTED` | create/submit/decide/lift 四方法 |
| 唯一索引去重 | `uk_nf_project_trigger_active` + service `selectCount` 二次校验 | AC-INC-40 重复不重复扣 |
| 月份格式校验 | `Pattern.compile("^\\d{4}-(0[1-9]\|1[0-2])$")` | `validateMonth` 静态 |
| triggerType 映射表 | `deriveRoleMapping(triggerType)` 静态 | 4 情形 × 主责/连带/执行 |
| Tier coefficient | `DEFAULT_TIER_DELTA = -0.50`（AC-INC-39 贡献度系数降低） | BonusPoolService 消费端联动 |

### 状态机

```
            submit
DRAFT ─────────────→ PENDING_DECISION
                        │
              decide APPROVE
                        ↓
                      EXECUTED ──── lift ────→ LIFTED
                        │
              decide REJECT
                        ↓
                     REJECTED
```

### 通知联动（AC-INC-40）

| 触发 | eventType | kind | 接收者 |
|---|---|---|---|
| decide APPROVE | NEGATIVE_FEEDBACK_EXECUTED | ACTION | 主责 PM + 连带 PM（如有） |
| lift | NEGATIVE_FEEDBACK_LIFTED | FYI | 主责 PM + 连带 PM（如有） |
| decide REJECT | — | — | 不通知 |

### 落地约束符合性

| 约束 | 状态 |
|---|---|
| 复用基座，无新 ORM / 认证 / 前端框架 | ✅ Spring Boot 3.5.8 + MyBatis-Plus + Sa-Token + JUnit5 + Mockito + AssertJ |
| `@Tag("dev")` 单测规范 | ✅ `NegativeFeedbackServiceTest` 14 测 |
| 6 维度测试覆盖 | ✅ 正常路径 / 边界 / 异常 / 权限 / 审计 / 幂等 |
| 真实 `mvn test` 跑过 | ✅ 14/14 green @ 05:50:37 |
| 单卡单 commit | ✅ 单卡单 commit [8/8 FINAL] |
| 不改 application-prod.yml / docs/开发说明 / log.md | ✅ 仅改 Ipd 模块 + docs/ipd-系统说明/后端需求/（业务卡设计稿） |
| 不引入新 ORM / 认证 / 前端框架 | ✅ 全复用基座 |
| 状态机终态单向 | ✅ LIFTED/REJECTED 是终态 |
| AC-INC-40 重复不重复扣减 | ✅ uk_nf_project_trigger_active 唯一索引 + service 二次校验 |

## 收口

8 张待补卡从 P2-5.1 到 P3-8.2 全部闭环。前端 P0-10.36 负反馈执行页可立即对接 `/api/v1/negative-feedbacks` 7 个端点（create / submit / decide / lift / detail / list / effective），读取真实后端数据。