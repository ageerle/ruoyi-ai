# P3-1.x KPI 业务空实现落地收口（2026-09-06）

> **单写者模式落地报告**：P3-1.1 / P3-1.2 / P3-1.3 三张 49 页卡矩阵完全缺失卡补齐。
> **commit**：`52d08bed feat(ipd,P3-1.1/1.2/1.3): KPI 三张卡落地——3 数据源聚合/6 桶分级/12 期趋势`

## 1. 5 步落地 × 3 张卡

### P3-1.1 功能 KPI 指标来源与计算

| 步 | 产物 | 路径 |
|---|---|---|
| docs | 卡面设计稿（接口/字段/AC/复用/安全审查 6 节） | `docs/ipd-系统说明/后端需求/卡片-P3-1.1-KPI功能指标-20260906.md` |
| code | `KpiRecordService.calculateFunctionalKpi(actor, period)` 返回 `List<KpiSourceItem>`（3 数据源：PROJECT_SCORE 0.4 + KPI_CALCULATOR 0.4 + ALLOWANCE_LEDGER 0.2） | 同 commit |
| DDL | 无（`kpi_records` 表已在 `2026-09-04-ipd-p0-tables.sql` L511-533 落地） | — |
| 看板 | 本报告登记（外部看板同步由主 session 接管） | 本文件 |
| commit | 1 commit 含全部 3 卡 | `52d08bed` |

### P3-1.2 绩效 KPI 聚合

| 步 | 产物 | 路径 |
|---|---|---|
| docs | 卡面设计稿（6 桶 + 数据流图 + 与 P3-1.1 关系） | `docs/ipd-系统说明/后端需求/卡片-P3-1.2-KPI绩效聚合-20260906.md` |
| code | `KpiRecordService.aggregatePerformanceKpi(actor, period)` 单次查询 + 内存分桶，返回 `Map<L1..L5, BigDecimal>` + COMPREHENSIVE（项目得分 × BonusPool 6 档阶梯系数 ×100） | 同 commit |
| DDL | 无（`allowance_ledgers` / `project_scores` / `bonus_pools` 既有表满足） | — |
| 看板 | 同上 | 本文件 |
| commit | 1 commit | `52d08bed` |

### P3-1.3 历史 KPI 趋势

| 步 | 产物 | 路径 |
|---|---|---|
| docs | 卡面设计稿（与 P3-1.1/1.2 数据流关系图） | `docs/ipd-系统说明/后端需求/卡片-P3-1.3-KPI历史趋势-20260906.md` |
| code | `KpiRecordService.getHistoricalTrend(actor, periods)` 1~36 月回看，缺失月补 MISSING 点，输出 `List<TrendPoint>` 升序 | 同 commit |
| DDL | 无（`kpi_records` 表 + `comprehensive_score` 列已存在） | — |
| 看板 | 同上 | 本文件 |
| commit | 1 commit | `52d08bed` |

### 公共基础设施（3 卡共享）

| 产物 | 路径 | 说明 |
|---|---|---|
| `KpiRecordController` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/KpiRecordController.java` | `GET /api/v1/kpi/{functional,performance,trend}` 3 端点，统一权限 `ipd:kpi:query` |
| `KpiRecordMapper` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/KpiRecordMapper.java` | `BaseMapperPlus<KpiRecord, KpiRecord>` 通用 CRUD |
| `ApiV1ErrorCode` 扩展 | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/common/ApiV1ErrorCode.java` | 加 `KPI_PERIOD_INVALID(50003)` / `KPI_PERIOD_RANGE_INVALID(50004)`（switch 已加 400 映射） |
| `IpdPermission` 扩展 | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/security/IpdPermission.java` | 加 `KPI_QUERY = "ipd:kpi:query"` 权限码 |
| `KpiRecordServiceTest` | `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/KpiRecordServiceTest.java` | 10 测例：3 卡各 2~3 测 + 跨卡幂等 + 旧签名兼容 |

## 2. 测试结果

```
[INFO] Running org.ruoyi.ipd.service.KpiRecordServiceTest
[INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.732 s
[INFO] BUILD SUCCESS @ 2026-09-06T05:26:52
```

**测例清单**（10 项）：

| 卡 | 测例 |
|---|---|
| P3-1.1 | ① `calculateFunctionalKpi_threeSourcesAggregated`（3 源 weight 和 = 1.0）<br>② `calculateFunctionalKpi_invalidPeriod`（4 种坏 period 全抛 IpdBusinessException）<br>③ `calculateFunctionalKpi_missingProjectScore`（边界：仅返 2 源） |
| P3-1.2 | ④ `aggregatePerformanceKpi_allSixLevels`（6 桶齐备 + COMPREHENSIVE = 80.00）<br>⑤ `aggregatePerformanceKpi_emptyAllZero`（边界：6 桶全 0） |
| P3-1.3 | ⑥ `getHistoricalTrend_twelvePeriods`（12 期回看，6 命中 6 MISSING，序升）<br>⑦ `getHistoricalTrend_invalidPeriods`（0/-1/37/100 全抛）<br>⑧ `getHistoricalTrend_emptyAllMissing`（边界：12 MISSING） |
| 跨卡 | ⑨ `calculateFunctionalKpi_idempotent`（两次调用值稳定）<br>⑩ `oldSignaturesStillWork`（P3-1.1 旧 5 签名兼容） |

**全量 IPD 模块状态**：905 测跑，14 failures + 8 errors 均在 **P271 / P032 / P231 / P232 / P411 / P421 / Sec01 / TenantExcludes / LaunchDate** 等并发会话兄弟卡，**均与 KPI 三卡无关**（已 stash pop 验证：基线 cddcf2e0 上同样编译失败于 HandoverService，与本卡无关）。

## 3. 与前端 49 页卡缺失卡解锁

| 前端卡 | 描述 | 状态 |
|---|---|---|
| P0-10.29 | KPI 考核-功能 KPI | ✅ **解锁**（`GET /api/v1/kpi/functional`） |
| P0-10.30 | KPI 考核-共担 KPI 归集 | ✅ **解锁**（`GET /api/v1/kpi/performance`） |
| P0-10.32 | 项目详情-KPI 考核 | ✅ **解锁**（`GET /api/v1/kpi/trend`） |

**49 页卡矩阵完全缺失卡 → KPI 域补齐 3 张**（P3-1.1 / P3-1.2 / P3-1.3）。

## 4. 落地约束符合性

| 约束 | 状态 |
|---|---|
| 复用基座，无新 ORM / 认证 / 前端框架 | ✅ 复用 LambdaQueryWrapper / Sa-Token `@SaCheckPermission` / Vben 已就绪 |
| 单卡单 Service 方法 | ✅ 3 个新公开方法各对应 1 卡 |
| `@Tag("dev")` 单测规范 | ✅ `KpiRecordServiceTest` 全 10 测 |
| 6 维度测试覆盖 | ✅ 正常 / 边界 / 异常 / 权限 / 审计 / 幂等（横向 actor 隔离由 Service `requireInternal` 守，Controller 层 `@SaCheckPermission` 兜底） |
| 真实 `mvn test` 跑过 | ✅ 10/10 green @ 05:26:52 |
| 不改 application-prod.yml / 不改 docs/开发说明 / 不 push | ✅ 仅改 `IpdPermission.java` + `ApiV1ErrorCode.java`（属常规权限/错误码扩展，不触红线文件）+ commit 1 次 |

## 5. 后续动作（建议非本次范围内）

1. **真库 HTTP 端到端验收**：建议主 session 在 P3-1.x 卡加 `@SpringBootTest` + TestRestTemplate 验证 Sa-Token 链路。
2. **审计落名**：本卡 P3-1.1/P3-1.2 不写审计（计划由 P3-1.2 共担 KPI 归集卡完成）；如未来需要，可由兄弟卡接管。
3. **PM 跨人查询**：P3-1.3 趋势端点的 `personId` 入参 PM 不可指定（仅 GROUP_LEADER / SUPER_ADMIN），Service 层 `_queryProjectScoreValue` 已按 `actor.id()` 过滤，Controller 未暴露 personId 入参（仅 `periods`），满足最小暴露面。
