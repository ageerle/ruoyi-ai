# P4-3.1 聚合接口落地收口（2026-09-06）

## 5 步落地

| 步 | 产物 | 行数 |
|---|---|---|
| 1. docs | `docs/ipd-系统说明/后端需求/卡片-P4-3.1-聚合接口-20260906.md` | 137 |
| 2. code | `WorkbenchController.java` + `WorkbenchService.java`（**已落地**，本卡前已存在）+ `WorkbenchServiceTest.java`（265 行，本卡新增）+ `P431AcceptanceTest.java`（201 行，本卡新增） | 466（新测） |
| 3. DDL | 无（纯聚合查询） | 0 |
| 4. 看板 | `docs/ipd-系统说明/开发计划-看板镜像.md` P4-3.1 行：⬜ 待认领 → ▶ 本会话（2026-09-06）后端落地 13 测全绿 | +12 -6 |
| 5. commit | `8f1cf762` — feat(ipd,P4-3.1): Workbench 聚合接口卡面+13 测全绿 | 4 files, +609 -6 |

## 测试

- 命令：`JAVA_HOME=/Users/mac/tools/jdk-17/Contents/Home /Users/mac/tools/maven/bin/mvn -pl ruoyi-modules/ruoyi-ipd -Dtest='WorkbenchServiceTest,P431AcceptanceTest' -Dsurefire.failIfNoSpecifiedTests=false test`
- 关键输出：
  - `[INFO] Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`  ← WorkbenchServiceTest
  - `[INFO] Tests run: 5, Failures: 0, Errors: 0, Skipped: 0`  ← P431AcceptanceTest
  - `[INFO] Tests run: 13, Failures: 0, Errors: 0, Skipped: 0`  ← 合计
  - `[INFO] BUILD SUCCESS`
- **测例数：13/13 全绿**

## 6 维度覆盖矩阵

| 维度 | WorkbenchServiceTest 用例 | P431AcceptanceTest 用例 |
|---|---|---|
| 正常路径 | `summary_returnsStatsAndTasksForCurrentActor` | `summary_returnsAllFields` |
| 边界 | `summary_returnsEmptyWhenActorHasNoVisibleProjects` | `summary_emptyScope` |
| 异常 | — | `summary_permissionDenied_returnsForbidden`（403 + FORBIDDEN 码） |
| 并发 | 不适用（纯读 + 无副作用） | — |
| 权限 | `summary_filtersTasksByOwnerRole_whenMarketPm` / `summary_adminSeesAllTasks` | `summary_filtersByOwnerRole` |
| 责任队列分组 | `summary_groupsTasksByProject` | — |
| 当前推进 | `summary_currentAdvance_picksFirstOpenAction` | （已合并到 `summary_returnsAllFields`） |
| 删除审批分流 | `summary_deletionPending_roleMapping` | `summary_deletionPending_forGroupLeader` |
| 逾期 | `summary_marksOverdueActionsAsHighPriority` | — |

## 与前端 Track 13 解锁

- 前端页 03 工作台（`/Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/views/ipd/workbench/index.vue`）已通过 `fetchWorkbenchSummary` 调用真实后端 `/api/v1/workbench/summary`，**主要字段全部填实数据**：
  - 4 metric 卡（待我处理 / 临期超期 / 未读通知 / 已完成）→ 100% 真实
  - 责任任务队列 → 100% 真实（按项目分组）
  - 当前推进 → 100% 真实
  - 删除审批数 → 100% 真实（按角色分流）
- 仍占位（15%）：`queueTabs.initiated`（我发起的）+ `queueTabs.followed`（我的关注）两个 tab count 恒 0——后端未返回对应计数。
  - 这两块**非 P4-3.1 聚合接口职责**（属 P0-10.3 单卡追踪范围），本卡不修。
  - 前端 UI 已有兜底"count=0 → 显示空态"，不阻塞页 03 验收。

## 5 步落地完整性自检

- [x] docs 设计稿：1 张卡面 / 7 节 / 7 条 BR / 8 测试 / 6 安全审查
- [x] code 落地：Controller + Service 已就位（前置）+ 单测 + 验收测试 = 466 行新代码
- [x] DDL：无（聚合查询零写）
- [x] 看板：镜像行更新，状态 ▶ 本会话实施，待 QA 复核
- [x] commit：8f1cf762 单卡单 commit，精确 add 4 文件

## 后续待 QA 独立复核

- 真库（ipd_dev）HTTP 端到端验证：mvn test + SpringBootTest + 真 H2/MySQL
- 浏览器侧（前端仓库 session）：page 03 实际渲染、网络抓包、断网空态
- OPS-05 通知 / SEC-02 越权回归
