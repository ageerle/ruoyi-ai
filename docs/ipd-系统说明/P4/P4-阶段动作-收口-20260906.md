# P4 阶段动作收口（2026-09-06）

> **执行者**：P4 阶段动作实做 agent（MiniMax-M3）
> **执行环境**：单 agent / 单项目 / 串行 TDD
> **commit**：无（mvn test 未达全绿，按纪律不提交）

---

## 重要前置说明

P4 任务清单声明"10 张卡全 TDD 闭环"。在**单 agent 单次会话**的工程纪律下，**实际完成 0 张卡全绿**。**本会话工作树已被多张并发卡片预占**，具体情况如下：

- **工作树状态**：仓库 working tree 包含 35+ 个未跟踪文件（来自其他并行 agent 的进行中卡片）：
  - `AiModelConfigService.java` / `AiModelConfigController.java` / `AiModelConfig.java` 域
  - `GuestDemandService.java`（P4-1.1 的演进版）
  - `GateElementResultService.java` / `GateElementResultController.java`（P1-6.2 相关）
  - `HandoverService.java` / `HandoverController.java`（P2-7.x）
  - `LegacyScanController.java`（P1-9 相关）
  - `ProjectMemberController.java`（P2-4.2）
  - `SopTemplateController.java`（P1-3.3）
  - 各类 DTO（`AiModelSaveReq`, `AiModelView`, `GuestDemandSubmitReq`, `GuestDemandSubmittedView`）
  - 多份 SQL 迁移脚本
  - 多份验收/治理/前端交接 markdown

- **build 状态**：`mvn compile` 在 6+ 处失败，因为：
  - 域类（Person/Requirement/ProjectMember/HandoverRecord/Gate/ApiV1ErrorCode）的字段/方法缺失
  - 等等
  - **P4-1.1 GuestDemandService 域（Requirement）已部分扩展**（本次会话补登 `customerName` + `rawModel`）
  - **P2-4.2 ProjectMember 域已部分扩展**（本次会话补登 `memberType` + `approvalRef`）
  - **P4-1.1 ApiV1ErrorCode 已补登**（本次会话加 `PRODUCT_INACTIVE` 并补 switch 表达式）
  - **AiModelConfigService encrypt 依赖已补**（本次会话加 `ruoyi-common-encrypt` 到 IPD pom）
  - **但仍剩余 5+ 处域类缺字段**（Person.getId/getName/getAccountStatus、HandoverRecord.getHandoverRole/getNote、Gate.getElementSnapshot/setElementSnapshot、RequirementMapper 是否需要 selectCount 等）

- **结论**：本会话**未完成任何 P4 卡片全绿**。仅完成 **P4-1.4 的 TDD 测试文件骨架**（10 个测试用例，0 个绿），为后续 agent 接力奠基。

---

## 卡清单（P4 阶段所有子卡）

| 卡 ID | 标题 | AC 编号 | 状态 | Commit | 测例数 | 备注 |
|---|---|---|---|---|---|---|
| **P4-1.2** | 查询码隔离与受理前撤回补充 | AC-REQ-04/04b | todo（未承接） | — | — | 依赖 P4-1.1（working tree 中 GuestDemandService 未完成域补全） |
| **P4-1.3** | 需求路由双PM、待指派与5工作日提醒 | AC-PROD-09/AC-REQ-03 | todo（未承接） | — | — | 依赖 P4-1.1 + P2-4.1 + OPS-05 |
| **P4-1.4** | 需求七态处理、采纳与变更关联 | AC-REQ-06/07 | **本会话 TDD RED** | — | 10 个用例/0 绿 | 测试文件已写，build 未绿 |
| **P4-2.2** | AI 生成适配、超时与 token 预算 | AC-AI-02/08/09/10 | todo（未承接） | — | — | 依赖 P4-2.1（AiModelConfigService 在 working tree 中但 untracked 状态） |
| **P4-2.3** | AI 助手业务串联与风险提示 | AC-AI-07 | todo（未承接） | — | — | 依赖 P4-2.2 + P1-10.2 |
| **P4-3.1** | 四角色工作台聚合与深链接 | — | todo（未承接） | — | — | 需新建 WorkbenchService（无既有服务） |
| **P4-4.1** | 项目组织绩效汇总与导出契约 | AC-INC-34 | todo（未承接） | — | — | 依赖 P3-2.2/3.3/4.4（**P3 子卡本身多张依赖未闭环**） |
| **P4-5.1** | P4 需求到 AI 归档完整业务验收 | — | todo（端到端） | — | — | 依赖 P4-1.4 + P4-2.3 + P2-6.2 |
| P4-1 / P4-2 / P4-3 / P4-4 / P4-5 | [汇总] | — | todo | — | — | 不可执行，需子卡先完成 |

---

## 唯一动笔的卡：P4-1.4 需求七态处理、采纳与变更关联

### TDD 进度

**Step A: AC → Given/When/Then**（已沉淀在测试类 javadoc + 方法命名）

- **AC-REQ-06 七态全可达**（SUBMITTED → ACCEPTED → EVALUATING → SCHEDULED → PROCESSING → CLOSED → ARCHIVED）
- **AC-REQ-07 关联 IPD 节点**（写 projectId + 审计 + 状态守卫）
- **BR-REQ-05 状态机合法性**

**Step B: 测例骨架（10 个）**——文件已落 `src/test/java/org/ruoyi/ipd/service/P414AcceptanceTest.java`

| # | 方法 | 维度 | 期望 |
|---|---|---|---|
| 1 | `acceptFromSubmitted` | 正向 SUBMITTED→ACCEPTED | 条件 UPDATE 1 次 |
| 2 | `fullLifecycleReachable` | 正向全链 6 跳 | 条件 UPDATE 6 次 |
| 3 | `rejectIllegalSkipState` | 反向：跳级 SUBMITTED→CLOSED | STATE_CONFLICT |
| 4 | `rejectTransitionFromTerminal` | 反向：终态再迁 | STATE_CONFLICT |
| 5 | `rejectBackwardSkip` | 反向：EVALUATING→ARCHIVED | STATE_CONFLICT |
| 6 | `rejectUnknownTarget` | 边界：未知状态 | STATE_CONFLICT |
| 7 | `rejectNotFound` | 异常：需求不存在 | NOT_FOUND |
| 8 | `acceptNotifiesDualPm` | 业务：采纳通知双 PM | notification × 2 |
| 9 | `rejectNotifiesSubmitter` | 业务：拒绝通知 | audit × 1 |
| 10 | `associateWithProject` / `cannotAssociateArchivedRequirement` | AC-REQ-07 关联 IPD 节点 | projectId 写 + 终态拒 |

**Step C: 实现**——`RequirementLifecycleService` 类**未创建**（build 阻断，无法验证 Service 编译）

**Step D: mvn test 全绿**——**未达**

```
mvn -pl ruoyi-modules/ruoyi-ipd -am -Dtest=P414AcceptanceTest test
→ BUILD FAILURE（多文件编译错误，非 P414 引发）
   - Person.getId/getName/getAccountStatus 缺失
   - HandoverRecord.getHandoverRole/getNote 缺失
   - Gate.getElementSnapshot/setElementSnapshot 缺失
```

**Step E: git commit**——**未执行**（按 TDD 纪律，无绿不提交）

---

## 本会话 build hygiene 补登

为尝试打通 build 链路，**最小代价**补登 4 项（每项均为**纯字段加法**，无业务逻辑）：

| 文件 | 改动 | 用途 |
|---|---|---|
| `pom.xml` | + `<artifactId>ruoyi-common-encrypt</artifactId>` | 让 `AiModelConfigService`（working tree untracked）能 import EncryptUtils |
| `Requirement.java` | + `customerName` + `rawModel` 字段 | 让 `GuestDemandService`（working tree untracked）的 setCustomerName/setRawModel/getRawModel 调用可达 |
| `ProjectMember.java` | + `memberType` + `approvalRef` 字段 | 让 `ProjectMemberController`（working tree untracked）的 m.getMemberType()/m.getApprovalRef() 可达 |
| `ApiV1ErrorCode.java` | + `PRODUCT_INACTIVE` 枚举 + switch 补全 | 让 `GuestDemandService` 的 PRODUCT_INACTIVE 调用可达；HTTP=404 |

**剩余 build 阻断**（其他 session 卡片在途，本会话不抢）：

| 缺失符号 | 所在域 | 推测所属卡 |
|---|---|---|
| Person.getId/getName/getAccountStatus/setAccountStatus | `Person.java` | P0-7 认证状态机 |
| HandoverRecord.getHandoverRole/getNote/.handoverRole() | `HandoverRecord.java` | P2-7 移交 |
| Gate.getElementSnapshot/setElementSnapshot | `Gate.java` | P1-6.2 / P1-6 Gate 要素定义 |
| RequirementMapper.selectCount（重载）| `RequirementMapper.java` | P4-1.1（已用 selectCount 检查查询码重复） |

---

## 自检报告

- **已读文档**：
  - `docs/ipd-系统说明/P2/P2-阶段动作-收口-20260906.md`（模板基线）
  - `docs/ipd-系统说明/外部资源/IPD系统_六阶段标准动作清单_v3.md`（P4=D 阶段，D01-D11 11 个动作，主要为"轻管，仅登记完成"）
  - `docs/ipd-系统说明/开发计划-看板镜像.md`（P4-1.x～P4-5.x 卡定义）

- **已调 MCP**：
  - `list_projects` → ruoyi-ai 项目 ID `01dcf15c-86bb-4c7b-957c-8fe44bddd10d`
  - `list_tasks` → 筛选 todo 状态 91 张；P4 前缀 8 张子卡 + 5 张汇总
  - `get_task` × 8（P4-1.2 / 1.3 / 1.4 / 2.2 / 2.3 / 3.1 / 4.1 / 5.1）

- **mvn test 结果**：
  ```
  P414AcceptanceTest: 0/N（编译失败，无法运行测试）
  ```

- **git diff stat**（5 文件 +76 行，未提交）：
  ```
   docs/.../log.md                                   | 50 +++++  (其他 session 改动)
   ruoyi-modules/ruoyi-ipd/pom.xml                   |  5 ++   (本会话 +encrypt 依赖)
   .../ApiV1ErrorCode.java                           |  1 +    (本会话 +PRODUCT_INACTIVE)
   .../ProjectMember.java                            | 10 ++   (本会话 +memberType/approvalRef)
   .../Requirement.java                              | 10 ++   (本会话 +customerName/rawModel)
   .../P414AcceptanceTest.java                       | 新增    (本会话 TDD 测试骨架)
  ```

- **未提交原因**：单测 0 绿（TDD 纪律第一条：no green = no commit），build hygiene 改动属于其他卡片的 in-progress 域补全，本会话无法独立 commit 闭环

---

## 风险与后续

1. **工作树污染严重**：35+ untracked 文件横跨 10+ 张卡，多 agent 写而不提交导致 build 长期红
2. **建议治理路径**：
   - **立即**：派一个 build-hygiene 专项 agent 把所有域类缺失字段补齐（清 working tree untracked 文件的依赖），不写业务逻辑，只加字段/方法签名
   - **随后**：每张卡 owner 重新认领、commit，或放弃并 revert
   - **本会话 P4-1.4 TDD 骨架** 留在 working tree 待 build 通后即可执行 Step C/D/E
3. **P4 阶段本会话收口 0/10**——按 Track 3 P2-3.3 模板口径，"诚实收口"优先于"伪全绿"

---

## 一句话总结

**P4 阶段本会话收口：0/10 张全绿，1 张（P4-1.4）TDD RED 测试骨架已落（10 个用例）待 build 解锁后闭环；4 项 build hygiene 域补登已做（pom +encrypt、Requirement 2 字段、ProjectMember 2 字段、ApiV1ErrorCode +1 枚举）。工作树 35+ untracked 文件横跨多卡，build 长期红，建议派 build-hygiene 专项 agent 解锁后再续 P4。**
