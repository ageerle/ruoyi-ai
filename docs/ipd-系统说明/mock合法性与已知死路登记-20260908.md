# Mock 合法性规约与已知死路登记（D 批次机制 3，2026-09-08 立）

> 触发：WB-17-1 Gate 仲裁真活验证发现 mock 造了真库不可能的数据组合（`decision=null` 的 NOT NULL 列 + PENDING 状态的 REJECTED gate），单测全绿业务死路。
> 本文是病根三（mock 与真库约束脱钩）的规约本体 + 存量违法清单 + 由此牵出的生产死路登记。
> 证据：2026-09-08 四路只读蜂群盘点（198 个测试文件 + 9 张表 DDL 抽查 + 真库数据探针）。

---

## 一、Mock 合法性规约（三条硬规则）

**写测试前先读写入路径**：聚合器/待办类测试 mock 的**投递锚字段**（confirmerId / leaderId / reviewerId / decision / arbitratorId 等）取值，必须能由真实写入路径产生。写用例前先 grep 对应 Service 的 insert/update 链，确认「什么状态下哪些字段是什么值」。反例见下表 A1-A3：mock 给 PENDING 态配了 confirm 时才回填的确认人 ID。

**状态组合必须满足状态机**：外层状态与子行状态的组合必须真实可达。仲裁行只挂 REJECTED gate（`requireArbitratable` 语义）；签署占位行只挂 PENDING gate；「确认人 ID 回填」只发生在终态迁移时（回填与离开待办态是同一事务的两面）。复制粘贴相邻聚合器测试时，强制 diff 检查状态过滤条件（WB-17-1 缺口③的直接教训）。

**NOT NULL 列必须显式赋值**：mock 依赖表的 NOT NULL 列在 builder 中必须给值——与被测路径无关也要补，防「真库不可能行」潜伏（反例见 B 类清单）。

配套要求：「未办态」用例优先走真实写入路径构造数据（P254AcceptanceTest 范式：`service.sign()` 真实触发 settle→openArbitration 预落行，而非 builder 直造）；纯 mock 用例须在 DisplayName 标注「未覆盖 DDL 合法性」。

规约挂载点：`.claude/skills/gen-test/SKILL.md`（生成时生效）+ `AGENTS.md`「构建/测试」节（假绿陷阱新形态）。

---

## 二、存量违法清单（蜂群盘点，修复 A1-A4 时参照 P254 范式）

### A 类：真库不可能的违法组合，且对应聚合器真活投卡恒空（Critical）

| # | 测试 | mock 组合 | 违反的约束（源码/DDL 实证） | 真活后果 |
|---|------|----------|------------------------------|----------|
| A1 | StrategicChangeAggregatorTest L69-72/119-121/135-137 | `status="PENDING_SECOND" + confirmerId(2L)` | LaunchDateChangeService.propose() L76-86 落 PENDING_SECOND 时**不写 confirmerId**；setConfirmerId 仅在 secondDecision() L146 且同时离态。DDL confirmer_id nullable | `StrategicChangeAggregator.java:56` `equals(null)` 恒 false → **LD- 卡真活永不投递** |
| A2 | StrategicChangeAggregatorTest L96-98/122-124/138-140 | `status="PENDING_LEADER" + leaderId(...)` | CoefficientChangeService.propose() L71-79 不写 leaderId；decide（L108）才回填且同时转终态 | **CC- 卡真活永不投递** |
| A3 | ContributionConfirmAggregatorTest L69-70/96-97/108-109 | `status="SUBMITTED" + leaderId(...)` | ContributionService L275 双 PM 齐评仅 setStatus(SUBMITTED)；setLeaderId 在 confirm（L367/373）时写且同时转 CONFIRMED | **CT- 卡真活永不投递** |
| A4 | KeyGateAggregatorTest L83-85/140-143/170-171 | `gate PENDING + review(decision=null)` | gate_reviews 全部 3 条 Java 写入路径不产生 NULL decision（sign 落带值行 / insertAbstain 落 ABSTAIN / GateCreationService 落 "PENDING" 字符串哨兵且缺 gateId）| `KeyGateAggregator.java:57` `isNull(decision)` 真库恒空 → **GR- 签署卡真活永不投递**（WB-17-1 仲裁卡恒空的镜像）|

**真库探针佐证（2026-09-08 ipd_dev 实查）**：四类违法组合均 0 命中（mock 独有）；但 launch_date_change_requests 现存 **1 条在途 PENDING_SECOND 行 confirmer_id=NULL**——该真待办当前投不出卡，死路有真实业务影响。

**次级**：KeyGateAggregatorTest L141-143 断言旧轮残留行（round=1 vs gate currentRound=2）**投卡**，而 KeyGateArbitrationAggregator 有 round 防御——同构缺口。

**主代码连带疑点**（非测试问题，单独排查）：GateCreationService.autoCreateGate 写 decision="PENDING" 不在 DDL 值域（APPROVE|REJECT|ABSTAIN）、builder 缺 reviewer_id（DDL NOT NULL，真库 insert 会失败）、实体字段与 gate_reviews DDL 列不齐——疑为旧范式残留。

### B 类：NOT NULL 列 mock 为 null（真库不可能的行，Warning，无直接死路）

| 测试 | 缺的 NOT NULL 列 |
|------|------------------|
| DeletionReviewAggregatorTest L54-59/82-84 | reason、requester_id |
| StageSignAggregatorTest L52-58 | stage_id、depth、is_bio_feature |
| StrategicChangeAggregatorTest | LD 缺 reason/proposer_id/proposer_role；CC 缺 reason/market_pm_id/rd_pm_id/proposer_id |

### 对照自洽（无需整改）

KeyGateArbitrationAggregatorTest（2026-09-08 已修，PENDING 用例是显式防御测试）、KpiFillAggregatorTest、CloseoutAggregatorTest、HandoverAggregatorTest、P251/P252/P254（P254 是规约推广范式）。

---

## 三、已知死路登记（生产代码，待 owner 拍板修复方向）

A1-A4 对应四张真活恒空的卡。修复方向二选一（与 mock 规约联动）：

| 方向 | 做法 | 适用 |
|------|------|------|
| 甲：对齐 gate_reviews/仲裁范式 | 写入路径**预落确认人**（propose 时即写 confirmer_id/leader_id，decide 只 UPDATE 决策），聚合器不动 | 待办语义=「指派给具体人」，与 2026-09-08 仲裁修复同构（最佳实践先例） |
| 乙：改聚合器锚点 | 聚合器改「项目 scope + 角色匹配」投递（不依赖个人 ID 回填） | 待办语义=「某角色待办」，如组长确认可按组匹配 |

判定线索：LD 的第二签署人（confirmerRole 已随行落库）语义是**指派**（提案时已知谁签）→ 方向甲；CT 的确认组长是「项目所在组组长」→ 两个方向皆可。**建议 owner 逐卡拍板**；修复时同步按第一节规约改造对应测试。

---

## 四、与契约登记的联动

本文件的 known_deadlock 与 `workbench-tasktype-契约登记.yaml` 各条目的 known_deadlock 字段一一对应（key_gate / strategic_change / contribution_confirm 三类）；修复死路时须同步更新契约登记与 WorkbenchTaskContractDriftTest 覆盖面。
