# P1-11.1 新硬件项目到阶段推进真实验收（2026-09-05）

## 结论

**后端业务链 VERIFIED**（产品→项目→SA 认证→门禁拒→补齐 C11/C12+交付物登记→门禁过 CONCEPT→PLAN；D11 BioCV 失败恢复→DONE）。  
**附件字节 PARTIAL**：交付物走 `ossId=1` 登记（BR-IPD-03 行级满足），真实 MinIO 上传属 **P1-4.2**（进行中，勿抢）。

## 环境

- HTTP：`127.0.0.1:16039`（jar `ruoyi-admin-p1111.jar`）
- MySQL：`ipd_dev@13306`
- 单测：`P1111AcceptanceTest` **6/6** GREEN（`@Tag("dev")`）

## 真库剧本（摘要）

| 步骤 | 结果 |
|------|------|
| POST product `PM_NEW` + project B + `["沙特"]` | 200 |
| GET cert-items | `SABER/SASO` 落库 |
| GET gate-checklist CONCEPT | C11/C12 `ok=false` |
| POST advance-stage | **400** BR-IPD-06（门禁拒绝） |
| C11/C12：IN_PROGRESS→deliverable→DONE | 200 |
| D11：无完成日 DONE→400；fields FAR/FRR+DONE | 恢复 200 |
| POST advance-stage | **200** `currentStage=PLAN` |
| DB | cert + C11/C12 DONE + deliverables≥2 |

## 证据

- `.codex/ipd-dev/runtime/evidence-p1111.json`
- 同轮 **DEF-1** HTTP 复验：POST `/gate-elements`→200，`audit_logs.after_data`=`{"detail":"G1/D7155"}`（JSON_VALID=1）

## 残留

1. 附件真实对象存储：等 P1-4.2  
2. QA 独立复核：本卡 → ◇ inreview
