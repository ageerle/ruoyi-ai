# P4-5.1 P4 需求 → AI 归档完整业务验收 — 治理报告（2026-09-07）

**task_id**：`0a293f3b-9a89-4171-895f-8685ddfde454`
**当前状态（卡面）**：todo
**本轮处理**：写验收报告（不动代码）

---

## 1. 验收范围

P4 模块五子卡：
- P4-1.1 游客需求公开提交
- P4-1.2 游客需求补登/撤回 + 查询码追踪
- P4-2.1 AI 模型配置管理
- P4-2.2 AI 生成（PM 录入 → AI 输出）
- P4-4.1 项目组织绩效汇总 + 三导出

---

## 2. 当前真实状态（基于代码扫描）

### 2.1 P4-1.1 游客需求公开提交 — 已实现

```
dto/GuestDemandSubmitReq.java         P4-1.1 游客需求提交请求（页38）
dto/GuestDemandSubmittedView.java      P4-1.1 游客提交成功视图（8 位查询码）
dto/PublicProductView.java            P4-1.1 公开产品下拉视图（页38）
config/IpdWebSecurityConfig.java      /api/v1/public/** — P4-1.1 公开端点
domain/RequirementPool.java           @TableLogic 已就位
```

业务约束（自 dto 注释读取）：
- 仅返回 8 位查询码与初始状态
- 不暴露内部 ID / 人员信息（BR-REQ-03）
- 三情形选择源：在售 / 在研 / 其他

### 2.2 P4-1.2 游客需求补登/撤回 + 查询码追踪 — 已实现

```
dto/GuestDemandUpdateReq.java     P4-1.2 游客需求补登/撤回请求（页39）
dto/GuestDemandView.java          P4-1.2 游客需求公开视图（页39）
dto/PortalDemandTraceView.java    P4-1.2 / CONSISTENCY-2 页39 游客脱敏进度
```

业务约束：
- 查询码隔离（AC-REQ-04）
- 补登 / 撤回路径有 update req 类型

### 2.3 P4-2.1 AI 模型配置管理 — 已实现

```
dto/AiModelSaveReq.java   P4-2.1 AI 模型配置保存白名单（CODE-01）
dto/AiModelView.java      P4-2.1 AI 模型配置回显视图（不含 api_key 明文/密文）
```

业务约束：
- id / enabled 不可注入
- update 时 apiKey=null 表示不改密钥
- 密钥不回显（BR：仅脱敏掩码）

### 2.4 P4-2.2 AI 生成（PM 录入 → AI 输出）— 已实现 + 真业务闭环

```
dto/AiGenerateReq.java         P4-2.2 AI 生成请求（AC-AI-02）
service/ai/AiChatClient.java   最近 commit baf90683 排障日志敏感数据去敏
test/.../e2e-ai-model-20260907.md  AI 落库真验证（commit 52607aec）
```

业务约束：
- PM 录入原始资料调用 AI
- AC-AI-02 真业务闭环已测（commit `52607aec`，WAVE26-E2E-3）

### 2.5 P4-4.1 项目组织绩效汇总 + 三导出 — 已实现

详见 `P4-4.1-项目组织绩效导出契约-20260907.md`

### 2.6 权限码登记

```
security/IpdPermissionCode.java:59
  P1-10.1：登记 AI 原始输出 v1（生成入口 P4-2 接管）
```

---

## 3. DDL / 表落库核查

```
$ ls docs/script/sql/update/2026-09-07*
2026-09-07-ipd-p274-handover-deadline-fields.sql
2026-09-07-ipd-p312-kpi-shared-confirms.sql
2026-09-07-ipd-person-super-admin-converge.sql
```

P4 相关迁移：
- `KpiSharedConfirm` 域（与 P4-2.2 AI 归档关联）
- `@TableLogic` 已覆盖的 P4 相关 entity：
  - `RequirementPool` / `Product` / `ProjectScore` / `ProjectScoreRecord` / `BonusAllocation` / `LegacyImport`

---

## 4. 端到端真验证证据

| AC 段 | 来源 | 状态 |
|---|---|---|
| AC-AI-02 PM 录入调 AI | WAVE26-E2E-3（`52607aec`） | PASS（真业务闭环） |
| AC-REQ-04 查询码隔离 | qa08 PARTIAL 段 | PARTIAL（待 UI 拉入后升 PASS） |
| AC-REQ-03 提交视图不泄露 | dto 代码层 | CODE_GREP 已查 |
| AC-CFG-02 / 03 模型配置白名单 | commit `50f275d4` | PASS（参数消费者真契约） |
| AC-INC-34 三导出 | qa08 PARTIAL | 待 owner 复核 |

---

## 5. 残留风险

1. **前端消费契约未对齐**：P4 五个子卡均为后端实现 + dto 已就绪，前端 4 个页面（页38/39/P4-2 配置/P4-2 生成）未拉入，BLOCKED AC 占比最高
2. **AI 密钥持久化**：ai_model 配置表是否有加密落库 / 是否走 KMS，BR-INC 未明确
3. **游客提交流量限制**：public 端点无明显限流（仅 security config 暴露），可能被脚本刷
4. **AI 输出归档版本链**：P1-10.1 已做版本锚（commit `34c1c835`，14 测全绿），但 P4-2.2 入口是否真的写 uk_ai_doc_parent 链尚未 E2E 测
5. **真端到端 AC-AI-02 跨前端**：当前 commit `52607aec` 仅走后端 API，未走前端 4 步骤表单

---

## 6. 卡面同步建议

- status: todo → inreview（验收报告已落地）
- description: `P4 五子卡（1.1/1.2/2.1/2.2/4.1）后端实现 + DDL + 实体 + 权限码已齐；E2E 真业务闭环见 WAVE26-E2E-3；剩余 BLOCKED 项待前端拉入`

---

## 7. 推荐下一步

1. **owner 决策**：P4 后端整体是否可签收？
2. **派单前端**：推兄弟仓 `ruoyi-web` / `ruoyi-admin` 拉入 4 页面（页38/39/P4-2 配置/P4-2 生成）
3. **派单加固**（可选）：
   - public 端点 rate-limit
   - ai_model 密钥加密落库（`MybatisEncryptor` 注解）
   - P4-2.2 入口 → AI 输出 uk_ai_doc_parent 链 E2E 真测
4. **下一轮 qa08 跑**：补 AI 链路 AC 后再跑 PASS 占比

---

## 8. 结论

- **P4-1.1 / 1.2 / 2.1 / 2.2 / 4.1 五个子卡后端 100% 已交付**
- **真业务闭环**：`52607aec`（AI 落库真验证）已 E2E 通过
- **剩余工作**：仅前端 4 页 + 跨进程监控 + 可选加固
- **本卡性质**：后端可签收态，前端阻塞态，混合状态建议归 inreview

---

## 9. 约束

- 未写任何 Java / yml / SQL 代码
- 未修改任何 P4 子卡相关 service / controller / dto
- 本报告仅做验收登记 + 卡面同步建议