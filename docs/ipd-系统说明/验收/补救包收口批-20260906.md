# 补救包收口批——6 张卡证据归档（2026-09-06）

- 执行会话：Qoder 补救包三波收口会话（owner 点名 6+19 张卡 + 两项 AskUserQuestion 授权后执行）
- 执行时间：2026-09-05 23:37 PDT 起（= 2026-09-06 CST）
- 性质：**零新代码改动**的收口批——代码修复此前已由兄弟会话落地并提交，本批只做：证据核对、测试重跑、翻卡、登记
- 测试重跑：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='ActuatorNarrowTest,ProdConfigDeltaGuardTest,AuditChainHeadAppendContractTest,AuditChainSymmetryTest,AuditChainGapHashSplitTest,AuditPayloadJsonGuardTest,P054AcceptanceTest,P231AcceptanceTest,P341AcceptanceTest,DeletionRequestServiceTest' test`
  → **69/69 全绿，BUILD SUCCESS，Finished at 2026-09-05T23:37:46-07:00**（仓库根、单模块、无 -am 无 clean、错峰）
- 对账说明：DEF-6 板上已是 done、DEF-9 板上已是 inreview（与 log.md 23:25 收口一致），本批无需动作，仅在此登记核对结论

## 1. SEC-NEW-MED-1 — actuator 端点暴露收窄 ✅

| 项 | 证据 |
|---|---|
| 父基线收窄 | `application.yml` 键 `management.endpoints.web.exposure.include: health,info,metrics`（不再 `'*'`）＋ `management.endpoint.health.show-details: WHEN_AUTHORIZED`（不再 ALWAYS） |
| prod 行为 | `application-prod.yml` 无 management 段 → 继承父基线安全默认；另有 R9c 显式 pin 段（demo.enabled/springdoc/sa-token，commit 3c678705）防父基线翻转 |
| dev 行为 | `application-dev.yml` 显式覆盖 `'*'` + ALWAYS（本地调试便利，注释已说明） |
| 守卫 | `ActuatorNarrowTest` 4/4 绿（@23:37）——include 收窄 / WHEN_AUTHORIZED / dev 覆盖 / dev ALWAYS 四维断言 |
| 卡内建议未采纳项 | ①IP 白名单：当前部署形态为本机/内网 + WHEN_AUTHORIZED 已挡未授权详情，公网部署时再评估（登记为部署前置检查项）；②metrics 相对建议的 health,info 多一枚：监控自用、无敏感键，有意保留 |
| CI 门禁 | 本仓 CI 仅 wiki-lint/publish-images；静态守卫测试即门禁载体（任何环境 mvn test 可跑） |

## 2. SEC-NEW-MED-2 — prod 连接池 + p6spy ✅（压测数值见 QA-05-P1 终审报告）

| 项 | 证据 |
|---|---|
| 池容量 | `application-prod.yml` `spring.datasource.dynamic.hikari.maxPoolSize: 80`（a686e174 batch4 20→80；21131def SEC-HIGH-1 BCrypt cost 配套） |
| 快速失败 | `connectionTimeout: 5000`（batch3 patch 经用户授权路径消费，76888bbf/3c678705 归档） |
| p6spy | `p6spy: false`（同上，对齐父基线安全默认） |
| 守卫 | `ProdConfigDeltaGuardTest` 4/4 绿（@23:37） |
| 压测复证 | 100 并发数值由《QA-05-P1-终审复测-20260906.md》（第三波）提供 |

## 3. SEC-NEW-MED-4 — RSA 公私钥字面量 + 轮换确认 ✅

| 项 | 证据 |
|---|---|
| env 注入 | `application.yml` `api-decrypt.publicKey: ${API_DECRYPT_PUBLIC_KEY:}`、`privateKey: ${API_DECRYPT_PRIVATE_KEY:}`（prod 段同），PEM 字面量已从仓库移除（R8-P0-4 92dddb11 + cd0a1151） |
| **密钥已轮换（卡的待确认项→答案：已轮换）** | RSA 2048 新钥生成并启用（92dddb11+cd0a1151，log.md R8-P0 系列记录）；新私钥经 gitignored credentials.json + env 注入（R9c R3） |
| 历史字面量处置 | 旧钥因轮换**实质作废**；git 历史清除（rewrite + force push）未做也不必做——默认不做破坏性历史重写，如 owner 要求彻底清除另行立项 |
| fail-fast | 留空时 enabled=true 启动即失败（注释明示语义） |

## 4. AUDIT-CHAIN-IMPL-C — 方案 C 实施入口 → superseded by A′ ✅

- **owner 已裁决**：2026-09-05 Q2 拍板**否决设计稿方案 C**、选 A′（log.md「DEF-9 核验收口」节原文）
- **A′ 已落地**：commit 04aad050（`audit_log_chain_heads` 单行锚表 + `selectForUpdate` 悲观锁原子分配 seq/prevHash + `advance` 防御断言 + seq `NEVER` 去除 + 基线 SQL 回写 p0-tables.sql）+ da7755c4（P054 构造器补参，修复 HEAD 编译断链）
- **验证**：P0-9.1 业务链真实验收 run9 **79/79 ALL PASS**（9e1d006a，结果 JSON 已归档 `验收/P0-9.1-业务链真实验收结果-run9-chainheads-live-ALLPASS-20260906.json`）；本会话重跑 **35/35 绿**（HeadAppendContract 6 + Symmetry 8 + GapHashSplit 5 + PayloadJsonGuard 7 + P054 9）@23:37
- **真库铁证**：锚行 `last_seq ≡ max(seq)`、链零断链零空洞（log.md 23:25 PyMySQL 探针）
- **payloads 拆表（DDL-3/J-4）不做**：方案 C 被否决，载荷仍存主表 longtext 列（DEF-6 方案 A）
- 收口判定：按 owner 裁决 superseded，非「照原方案实施完成」

## 5. QA-05-P2 — 审计 hash 链写串行化 ✅（压测数值并入 P1 终审）

- 卡面「长期方案：chain_heads 锚表原子分配 seq」即 A′，已落地（同上节证据）
- 验收项 1「审计链校验 PASS」：run9 79/79 + 真库探针零断链 ✅
- 验收项 2「ipd_perf 100 并发 transit 写 P95<3s」：数值由《QA-05-P1-终审复测-20260906.md》提供（第三波执行，届时若超线则本卡重开）
- 写路径语义变化：append 由「每写 ORDER BY seq DESC 全表取尾」变为「锁锚行 O(1) 取 seq/prevHash」，持池时间不再随表增长放大

## 6. PERF-P0-2 — expireOverdue N+1 ✅

- 现状：`BidInvitationService.expireOverdue()` 单条条件 UPDATE（`SET status='EXPIRED' WHERE status='OPEN' AND expire_at<now`），javadoc 明确标注「PERF-P0-2：单 SQL 条件 UPDATE，消除 N+1 selectCount 与恒等三元冗余」
- 守卫：`P231AcceptanceTest` 10/10 绿（@23:37，含过期语义用例）

## 看板操作记录

- 翻卡 6 张 → done：SEC-NEW-MED-1 / SEC-NEW-MED-2 / SEC-NEW-MED-4 / AUDIT-CHAIN-IMPL-C（注记 superseded-by-A′）/ QA-05-P2 / PERF-P0-2（经 PUT /api/tasks/{id}，project=ruoyi-ai）
- 本批 6 张均为兄弟直建的板上卡（非镜像 managed 行），镜像无对应行可回写；漂移计数不因收口变化，维持既有登记口径
