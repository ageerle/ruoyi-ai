# P1-6.1 缺口补齐实现与验收（Gate 要素定义管理、校验与版本生命周期）

- 执行者：reviewer-qoder（Qoder 复核会话转实现）
- 时间：2026-09-06 00:40–01:10 PDT
- 授权：用户拍板「清理垃圾数据、补 P1-6.1 缺口」（认领备注见看板前态链）
- 关联：`验收/P074-P241-P251-三卡复核-reviewer-qoder-20260906.md` §5.2（本报告的缺口来源）

## 1. 交付范围：QA 7 项缺口逐项对账

| # | QA 缺口 | 状态 | 交付 |
|---|---|---|---|
| 1 | 版本生命周期 draft/publish/archive | ✅ | status 列 + `publish()/archive()` + 状态机守卫（create 即 draft/version=0/enabled='0'，publish 转正并 version+1，archive 终态置停用） |
| 2 | 复制/revert 历史恢复 | ✅ | `copy(id,newCode)` 克隆定义为新草稿（归档复活路径）；`revert(id,auditLogId)` 从审计 before_data 快照恢复草稿定义（服务审计全面升级为字段级 before/after 快照） |
| 3 | 已发布编辑 409 | ✅ | published/archived 携定义字段（名称/标准/否决/排序/双否决/阈值）更新一律 STATE_CONFLICT 409；**仅 enabled 启停是管理操作不受限**（保住「停用列表可管理」与现有前端启停流程） |
| 4 | vetoDualRequired 校验 | ✅ | 列 + 枚举 '0'/'1' 校验 + `'1' 仅允许 isVeto='1'` 配对校验（评审侧消费归 P2-5.2） |
| 5 | thresholdJson 校验 | ✅ | 列 + 校验：≤512 字符、必须 JSON 对象、键非空、值均为整数；空白归一化为 null |
| 6 | DB element_code 唯一索引 | ✅ | `uk_gate_element_code` 已 apply ipd_dev（原卡「执行另需授权」由本轮用户指令覆盖，迁移文件含回滚参考） |
| 7 | 种子真库入库验证 | ✅ | 上一轮复核探针已实证（33 条 7/6/5/8/7 与 AC-GATE-14 一致），本轮未动 |
| 8 | P161AcceptanceTest 回主仓 | ✅ | 主仓重写 46 项（.codex 集成树旧候选 44 项契约过时：旧版管理员列表含停用/双参构造缺失，不直接复用） |

## 2. 变更清单

- `domain/GateElement.java`：+status/version/vetoDualRequired/thresholdJson
- `dto/GateElementCreateReq.java`、`dto/GateElementUpdateReq.java`：+双否决/阈值字段（create 请求 enabled 仅兼容不采纳——草稿一律不可见，javadoc 已注明）
- `dto/GateElementLifecycleReq.java`：新增（copy 用 elementCode / revert 用 auditLogId）
- `service/GateElementService.java`：重写（62 项测试背书）。要点：
  - 错误面全部迁 `IpdBusinessException/ApiV1ErrorCode`（PARAM_INVALID 400/10001、NOT_FOUND 404/50001、STATE_CONFLICT 409/50002、INTERNAL 500/90001），弃 ServiceException
  - 身份审计升级：operatorId/Name/Role 取 IpdActor 会话投影（SEC-01 契约对齐），弃 controller 传 `String.valueOf(actor.id())`
  - update 在副本上合并+校验，校验失败零污染读出定义；生命周期守卫（409）先于值校验（400），刻意设计
  - 新增动作审计：CREATE/UPDATE/PUBLISH/ARCHIVE/COPY/DISABLE/REVERT，快照含 gateCode/elementCode/elementName/passStandard/isVeto/sortOrder/enabled/status/version/vetoDualRequired/thresholdJson（AuditEventData.json 契约，DEF-1/DEF-6 不破）
- `controller/GateElementController.java`：+`/{id}/publish`、`/{id}/archive`、`/{id}/copy`、`/{id}/revert`（均 ipd:gate-element:edit + requireAdmin）；write 端点全部改传 IpdActor
- `common/IpdBusinessException.java`：+`(ApiV1ErrorCode, String)` 双参构造（增量，兼容既有调用）
- 测试：`P161AcceptanceTest`（46 项，@Tag dev，全新）、`GateElementServiceTest`（12 项，重写适配）、`GateElementAuditJsonTest`（4 项，重写适配——DEF-1 回归语义保留，detail 断言升级为快照断言）

## 3. DDL 与数据卫生（ipd_dev，迁移文件 `docs/script/sql/update/2026-09-06-ipd-p161-gate-element-lifecycle.sql`）

1. gate_review_elements +4 列：status(varchar16 default 'published')/version(int default 1)/veto_dual_required(char1 default '0')/threshold_json(varchar512 null)——存量 43 行全部落 published/version=1
2. +唯一索引 `uk_gate_element_code(element_code)`（前置探针：43 行 element_code 零重复）
3. **G2-6 种子语义翻正**：is_veto 0→1（DOC-05《五大Gate评审要素_v1》L103 明确 ❌ 强制认证缺失或周期冲突；翻正后全库否决位 14→15，与规格逐元素分布 G1=5/G2=5/G3=0/G4=3/G5=2 完全对齐）。gate_element_results 无任何 G2 判定行引用，零连带
4. 数据卫生：上轮发现的 10 条兄弟测试残留（QA03-*/DEF1/D7155/Q*）已软删 del_flag='2' 且 enabled='0'（应用层不看 del_flag，enabled 才是可见开关）；应用可见分布恢复 7/6/5/8/7=33

## 4. 测试与探针证据（时间戳 2026-09-06 01:00–01:06 PDT）

- `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='GateElementServiceTest,GateElementAuditJsonTest,P161AcceptanceTest' ... test` → **62/62 绿**（12+4+46，Tests run: 62, Failures: 0, Errors: 0, BUILD SUCCESS）
- surefire XML：P161 `tests="46" failures="0" errors="0"` @01:05（tests>0 达标）；回归 GateEngineTest 12 + P251AcceptanceTest 14 + P241AcceptanceTest 9 = **35/35 绿**（GateElementResult 域/P251/P241 零破坏）
- 真库探针（PyMySQL socket 只读+DDL）：4 列在表、唯一索引在表、43 行 status='published'、G2-6 is_veto='1'、否决位总数 15
- 覆盖正反例：46 项含生命周期状态机全迁移（draft→publish→archive→copy 复活）、已发布定义编辑 409 × 6 字段、双否决配对、thresholdJson 7 类非法值 + 越界、revert 快照恢复/非草稿 409/外来审计与 legacy 无快照行拒绝、controller 信封（400/404/409/500）、三内部角色对 7 个写端点 403 零写

## 5. 假红插曲（并发定责，非交付缺陷）

00:55–01:00 三轮编译失败均为兄弟会话在途窗口：encrypt jar 半写入（AiModelConfigService 引用）、Handover/ProjectMember/GuestDemand/RequirementChange 中间态、ProjectScore 域字段未落。错峰重试后全部自愈；期间我新增的 `IpdBusinessException(code,message)` 构造器曾被并发写覆盖一次，已重放（兄弟 RequirementChangeService 同样依赖该构造器，属公共增量，归并一致）。

## 6. 边界与遗留（不阻断本卡）

1. **AC-GLB-12 口径**：原文「14 项否决项全部生效」需 P1-6.2 按 DOC-05 复核为 **15**（G2-6 翻正后）；33 项可判定不变
2. **前端页 47**：现组件无生命周期 UI（draft/publish/archive/copy/revert 按钮与列表状态列），API 注释「仅含启用要素」仍成立（草稿天然不可见）；需前端另起任务对齐
3. GET 列表的超管限定由生产侧 Sa-Token 注解（ipd:gate-element:list）实现，standalone 测试不解析注解，测试只覆盖 requireAdmin 写端点 403
4. 唯一索引为全局唯一：软删残留码不可复用（当前 10 条残留码已被占用）——如需「停用码可复用」另议（G-02 禁删前提下不建议）
5. 真实在途评审的版本冻结效果归 P1-6.2（本卡不含）；401/403 口径遗留仍归 SEC-02
