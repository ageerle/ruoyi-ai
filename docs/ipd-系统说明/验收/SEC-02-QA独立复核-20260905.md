# SEC-02 QA 独立复核报告

> 复核时间: 2026-09-05
> 复核员: QA 独立复核子代理 + root 复核
> 复核范围: 只读，禁改生产代码、禁跑 mvn、禁 git 操作
> 工作区: /Users/mac/Documents/ruoyi-ai
> 审计基线: HEAD 34e62d9d（继承自 cccbd86c）

---

## 一、看板卡定义（来源：开发计划-看板镜像.md）

| 列 | 内容 |
|---|---|
| 卡KEY | SEC-02 |
| 标题 | 基础对象授权与全部现存 IPD 入口隔离 |
| 验收要点 | 六身份×五控制器 ACL 矩阵；未登录拒绝；越权拒绝；跨组拒绝；状态机越权拒绝 |
| 源状态证据 | ◐ 部分就位：IpdPermission/IpdWebSecurityConfig/6处@SaCheckPermission；六身份矩阵运行时验证缺；AC-AUTH-09/10/11 覆盖未验 |
| 优先级 | U0 |
| 依赖 | API-01, SEC-01 |
| 指派 | 后端开发；QA 独立复核（待认领） |
| allowedPaths | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/{controller,security,common} |
| 验证命令 | 新增 SEC02AcceptanceTest（@Tag dev）；mvn test -Dtest=SEC02AcceptanceTest |

---

## 二、本仓实现现状核实

### 2.1 安全框架就位

| 组件 | 路径 | 状态 |
|---|---|---|
| IpdPermission 授权中心 | security/IpdPermission.java | 存在（130+ 行），含 requireInternal/Admin/ProjectCreator/ProductCreator/ProductWriter/ActionWriter/bindCreateAudit |
| IpdPermissionCode 权限码枚举 | security/IpdPermissionCode.java | 存在（41 行） |
| IpdPermissionExceptionHandler | security/IpdPermissionExceptionHandler.java | 存在 |
| IpdWebSecurityConfig | security/IpdWebSecurityConfig.java | 存在（commit 095bb35b 落地） |
| PlusDataPermissionInterceptor | ruoyi-common-mybatis 拦截器 | 存在（172 行） |
| 6 处 @SaCheckPermission 注解 | controller/{Project,Product,CertTemplate,StageAction,GateElement,DeletionRequest}Controller.java | 全部就位 |

### 2.2 实施验证（commit 095bb35b）

commit 095bb35b 已落地："fix(ipd): /api/v1/** 从基线 Sa-Token 放行 + IPD 统一认证拦截器兜底（SEC 防匿名访问业务接口）"，确证 SecurityConfig 在主仓已就位。

---

## 三、判定

| 维度 | 状态 |
|---|---|
| 框架就位 | 就位（IpdPermission + IpdWebSecurityConfig + PlusDataPermissionInterceptor） |
| 注解挂载 | 6 处 @SaCheckPermission 已挂（Project/Product/CertTemplate/StageAction/GateElement/DeletionRequest） |
| SEC-02 专属测试 | 不存在（src/test/ 仅有 P062/P064/P111/P131/P143） |
| 六身份矩阵运行时验证 | 本仓不可核验（需真实 HTTP+MySQL+Sa-Token 环境） |
| AC-AUTH-09/10/11 覆盖 | 本仓不可核验（运行时验证缺失） |
| StatusTransition 越权防护 | 本仓不可核验 |
| 跨组/游客/未登录拒绝规则 | 框架在位，无实测证据 |

---

## 四、结论

判定: KEEP_PARTIAL

理由:
1. 安全框架层已就位（IpdPermission + IpdWebSecurityConfig + 6 处 @SaCheckPermission + commit 095bb35b）
2. SEC-02 专属验收测试类缺失，无法本地核验六身份矩阵
3. 跨组/游客/未登录拒绝规则仅框架在位，无实测证据
4. 卡片自注记亦承认「运行时验证缺」，当前 inreview 状态与实现进度相符

建议后续动作:
1. 在 ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/security/ 新增 SEC02AcceptanceTest（含 @Tag dev），覆盖 6 身份 × 5 控制器 ACL 矩阵
2. 加入 StatusTransition 越权防护、跨组隔离、游客/未登录拒绝的集成测试
3. 执行验证命令确认全量 PASS 后关闭此卡

---

## 五、证据索引

| 证据 | 路径 | 来源 |
|------|------|------|
| IpdPermission | security/IpdPermission.java | 本仓源码 |
| IpdWebSecurityConfig | security/IpdWebSecurityConfig.java | 本仓源码 |
| PlusDataPermissionInterceptor | ruoyi-common-mybatis | 本仓源码 |
| commit 095bb35b | git show 095bb35b | 本仓 git log |
| 6 处 @SaCheckPermission | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/ | 本仓源码 |
| 看板卡原文 | docs/ipd-系统说明/开发计划-看板镜像.md | 本仓 |

---

纪律声明:
- 本报告为只读复核，未修改任何生产代码或文档
- 未执行 mvn 编译或测试
- 未执行 git commit / git push
- 未修改看板状态
- 凡标注「本仓不可核验」之声明，均因证据文件位于其他工作树或动态环境，本复核员仅作提示性记录

---

## JSON 结果块

```json
{"card":"SEC-02","testClasses":["P062AcceptanceTest","P064AcceptanceTest","P131AcceptanceTest","P143AcceptanceTest","IpdAuthServiceTest","ProjectServiceTest"],"verdict":"KEEP_PARTIAL","staticFindings":"安全框架基本就位：IpdPermission/IpdPermissionCode枚举完整(41行)、IpdWebSecurityConfig拦截器已就位(commit 095bb35b)、PlusDataPermissionInterceptor已实现(172行)、ProjectController已挂6处@SaCheckPermission。但本仓无SEC-02专属验收测试类，六身份矩阵运行时验证缺失，AC-AUTH-09/10/11覆盖无法本地核验","residualGaps":["SEC-02专属验收测试类缺失","六身份矩阵HTTP测试无法本地核验","StatusTransition越权防护运行时验证缺失","拒绝不泄露超权限可见数据的数据隔离测试缺失","跨组/游客/未登录拒绝规则仅框架在位无实测证据"],"reportPath":"docs/ipd-系统说明/验收/SEC-02-QA独立复核-20260905.md","kanbanNote":"SEC-02本仓KEEP_PARTIAL：安全框架就位(IpdPermission+IpdWebSecurityConfig+6处@SaCheckPermission+commit 095bb35b)，但无SEC-02专属测试类，六身份矩阵与AC-AUTH-09/10/11运行时验证均本仓不可核验，需协调员mvn补测"}
```
