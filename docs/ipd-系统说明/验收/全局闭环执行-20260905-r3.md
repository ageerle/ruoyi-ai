# 全局闭环执行报告（第三轮 r3）— API-03 收口 + inreview 回归

- **会话角色**：第二方 QA/独立复核泳道 → 本轮遵用户「立即执行剩余计划」指令转**执行**
- **执行时间**：2026-09-05 12:33–12:41 PDT
- **基线 HEAD**：起始 056640ca（兄弟蜂群归仓，252 测试全绿）→ 本会话提交 30a98c39
- **边界**：仅新增/提交本会话测试文件；不推送 origin（遵用户前轮裁决）；共享文档（SSOT 镜像 / log）写而不提交，留协调器归仓

## 一、头号 U0 API-03 已收口（done）

**缺口**（r2 复核判定）：advice 专用 handler 已由兄弟提交，但卡片要求的 `Api03AcceptanceTest`（MockMvc 真实 HTTP 反例）始终缺失——"未达验收，禁仅凭服务层绿关闭"。

**本轮交付**：新增 `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/advice/Api03AcceptanceTest.java`（141 行，@Tag dev），经 `MockMvcBuilders.standaloneSetup` 真实 HTTP dispatch 穿过**真实生产 `IpdServiceExceptionAdvice`**（非本地替身），5 用例：

| 用例 | 触发路径 | 断言 |
|---|---|---|
| 反例① 原密码错 | service 抛 CURRENT_PASSWORD_INCORRECT | HTTP 400 + code 10001 + message "原密码错误" |
| 反例② 新旧相同 | service 抛 PASSWORD_UNCHANGED | HTTP 400 + code 10001 + message "新密码不能与当前密码相同" |
| 反例③ 服务层 PASSWORD_LENGTH | 30 汉字过 @Size(max=72) 但 UTF-8 超 72B，service 抛 | HTTP 400 + code 10001 + 枚举消息 |
| 反例④ 新密码不足 8 位 | @Valid @Size(min=8) 拦截（不进 service） | HTTP 400 + code 10001 |
| 正例 合法改密 | service void 成功 | HTTP 200 + code 0 + verify session.revokeAll |

修复前该异常落 `Exception` 兜底 → HTTP 500 + code 90001（INTERNAL_ERROR）且丢真实消息；四条反例断言 400/10001 即回归护栏。

**验证证据**（防假绿）：
- `mvn -o -pl ruoyi-ipd test -Dtest=Api03AcceptanceTest` → **Tests run: 5, Failures 0, Errors 0, Skipped 0**，BUILD SUCCESS @ 12:33:32
- 验收集 `-Dtest=Api03AcceptanceTest,IpdAuthServiceTest,IpdAuthChangePasswordExceptionTest,Api01AcceptanceTest` → **25/25 GREEN**（5+4+6+10），Skipped 0，@ 12:34:18，满足卡片"既有 IpdAuthServiceTest 与 IpdAuthChangePasswordExceptionTest 保持全绿"
- 未加 `failIfNoTests`；surefire `Tests run=5>0` 且 `Skipped=0` → 证明 @Tag dev 真实执行而非静默跳过
- commit **30a98c39**（1 file changed，141 insertions，仅含该测试文件，未扫兄弟 WIP；父 056640ca；未推送）
- 看板 `set API-03 done`：update:1 / unchanged:239，写后 drift false

## 二、inreview 回归（27/27 GREEN @ 12:38:04）

| 测试 | 结果 | 对应卡 |
|---|---|---|
| Sec02AcceptanceTest | 7/7 | SEC-02（六身份矩阵单测） |
| SecApi01OperatorIdContractTest | 1/1 | SEC-01（operatorId 残留**已随归仓修复**） |
| IpdAuthSessionStpTypeTest | 2/2 | P0-7.3 相关 |
| GateEngineTest | 12/12 | P1-6.1 相关 |
| GateElementServiceTest | 5/5 | P1-6.1 相关 |

合计 27/27，Failures 0，Errors 0，Skipped 0，active builds 0（错峰干净）。

## 三、inreview 未收口的诚实判定（防假绿级联）

**无一可诚实关**，原因各异：

- **SEC-01**（枢纽，无依赖）：命名 `Sec01AcceptanceTest` **缺失**；现存 `SecApi01OperatorIdContractTest` 仅反射扫 5 控制器证明无 `@RequestParam operatorId`（覆盖验收一项），未覆盖 AC-AUTH-08（普通 PM 调超管接口 403 且零写入正反例）/ 角色来自会话 / AC-HR-07/08（角色不可跨、无代理组长）/ AC-GLB-11（四角色体系）。**StageActionController 当前 dirty（兄弟在途）**——写命名验收测试须待其提交后错峰进行。已刷新看板 note（第九轮"禁抢改"残留 note 已过时）。
- **SEC-02**：`unresolved_deps=['SEC-01']` **依赖阻塞**；单测 7/7 绿但 Javadoc 自陈"不覆盖真实 HTTP"，卡片要"全部现存 Project/Product/StageAction 及管理入口 + 写事务内重验对象权限/状态"，比单测广；`验收/SEC-02-real-http-probe.sh` 现已存在（2224B @ 08:55）。
- **P0-7.3**：命名 `P073AcceptanceTest` 缺失；验收涉 Sa-Token token 轮换 / 重放拒绝 / 过期，**需真实 Redis 会话存储**，standalone 难以忠实伪造 → 需集成窗口。
- **P1-6.1**：`unresolved_deps=['SEC-01']` 依赖阻塞；命名 `P161AcceptanceTest` 缺失；AC-GATE-14（G1-G5 共 33 项 seed 入库）**需真实 DB** 核对。

**关键依赖链**：SEC-01 是枢纽，收口它方可解锁 SEC-02 + P1-6.1。

## 四、看板与治理状态

- 起始 drift false 240；`set API-03 done`（update:1 / unchanged:239）；`set SEC-01 inreview` note 刷新（写前 drift 一度 true，`reconcile(apply)` 顺带 SSOT→board 同步 update:3，写后 drift false）
- 终态：**drift false / total 240 / unmanaged 0**；statuses done 57（较 r2 的 52，+5，含本会话 API-03）/ todo 172 / inreview 5 / inprogress 6
- OPS-09 遵循：全程错峰（active builds 0）、单模块、无 -am 无 clean、证据带时间戳、写前重读、共享文档不提交留归仓

## 五、后续计划（交协调器 / 兄弟 owner 泳道）

1. **SEC-01 收口**（最高杠杆，解锁 SEC-02 + P1-6.1）：待 StageActionController 提交后，写 `Sec01AcceptanceTest`（standalone MockMvc + mock IpdPermission 抛 IpdPermissionException(403)，复用 P064 permission 模式）覆盖 AC-AUTH-08 403 零写入正反例 + 角色来自会话；AC-HR-07/08 + AC-GLB-11 角色目录部分另测。
2. **P0-7.3**：需 live Sa-Token/Redis 集成窗口验证 token 轮换 / 重放 / 过期。
3. **SEC-02 / P1-6.1**：SEC-01 done 后解锁；P1-6.1 AC-GATE-14 需真实 DB seed 核对。
4. **归仓**：SSOT 镜像（API-03 done + SEC-01 note）+ log 第十一轮 + 本报告由协调器下次归仓提交。

## 六、防假绿纪律声明

本轮**只关能忠实证明的卡**（API-03：修复已提交 + 缺口单一可忠实补齐 + 真实 HTTP 层 5 反例 Skipped:0）；对 4 张安全 / Gate 卡**拒绝以相关测试绿或部分覆盖假关**（依赖阻塞 + 命名验收测试缺失 + 需 live 基础设施），避免 SEC-01→SEC-02/P1-6.1 的假绿级联。这是"完整闭环"与"假绿防护"之间的正确取舍：闭环了真正可闭环的，诚实标注了尚不可闭环的。
