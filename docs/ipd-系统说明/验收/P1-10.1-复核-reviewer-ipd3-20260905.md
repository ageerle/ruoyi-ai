# P1-10.1 AI 文档原始输出与不可丢失版本链 — 独立复核报告（reviewer-ipd3，2026-09-05）

## 判定：✅ 复核通过

交付 commit `34c1c835`。AC-AI-04 / AC-AI-06 / BR-AI-03 逐条代码对照全过，测试独立复现，真库列与唯一索引在位，无致命缺陷。

## 1. 测试独立重跑

```bash
export PATH="$HOME/tools/maven/bin:$PATH"; export JAVA_HOME="$HOME/tools/jdk-17/Contents/Home"
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=OPS05AcceptanceTest,P033AcceptanceTest,P1101AcceptanceTest \
    -Dsurefire.failIfNoSpecifiedTests=false test
# 仓库根；不带 -am 不带 clean；启动 23:09:17 PDT
```

- **BUILD SUCCESS（exit 0）**，Finished 2026-09-05T23:09:32-07:00
- 本卡 XML（mtime 23:09:32 = 本次自跑产物）ElementTree 独立解析：
  **tests=14 failures=0 errors=0 skipped=0**（无假绿形态）

## 2. 真库探针（只读 socket，pymysql）

- `ai_documents`：**22 列**；`content_sha256`（CHAR(64)）/ `reviewed_by` / `reviewed_at` / `parent_version_id` / `version_no` / `status` 全在
- `uk_ai_doc_parent(parent_version_id, UNIQUE)` 在位——DB 层防分叉防线成立
- 迁移文件含回滚段（`docs/script/sql/update/2026-09-05-ipd-p1-10-1-ai-document-version-chain.sql`）
- `tenant.excludes` 含 `ai_documents`（此前已登记，键名核对 ✅）

## 3. 卡面验收标准逐条代码对照（commit 34c1c835 态）

| 验收标准 | 对照结论 |
|---|---|
| v1 保留 | ✅ `createGenerated`：versionNo=1、parent=NULL、status=GENERATED、sha256 落锚；`revise` 纯 insert 追加——服务全程零 updateById/零 content 更新通道，v1 物理不可触碰 |
| 父版本链 | ✅ 新行 `parent=HEAD.id`、`versionNo=HEAD+1`；`history` 自起点上溯至根（根必须 v1）再按父指针逐环下探，校验版本号连续 |
| 并发版本冲突明确报错 | ✅ 双防线：服务层 `baseVersionId≠HEAD → STATE_CONFLICT`（409 类语义，零插入）+ DB 层 `uk_ai_doc_parent` 撞键 `DuplicateKeyException → STATE_CONFLICT`，绝不静默覆盖 |
| 历史版本不可覆盖 | ✅ 通道收敛：全服务仅 insert + review 的条件 UPDATE（且仅 `status/reviewed_by/reviewed_at` 三列，UPDATE 集不含 content/title/sha256） |
| sha256 摘要 | ✅ `sha256Hex`：SHA-256 UTF-8 → 64 位 hex；create 与 revise 均落 `content_sha256` |
| 审核落名落时（BR-AI-03） | ✅ `review`：条件 UPDATE（eq status=GENERATED）落 `reviewed_by/reviewed_at`；幂等（已 REVIEWED 返回既有行，**不覆盖首位审核人**）；ARCHIVED 拒绝 |
| AC-AI-06 反例覆盖 | ✅ 断链（父版本软删）/ 跳号 / 截头（根非 v1）/ 起点不在链上 → 全部 STATE_CONFLICT（`history_*_rejected` 三反例 + `onChain` 校验） |

## 4. 范围外核对（不扣分项确认）

- AI 生成 / 模型配置 / 预算 → **P4-2**：本卡 create 端点仅登记版本链首环，controller 注释已标注接管关系。✅ 未越界也不缺位
- ARCHIVED 归档流转 → **P0-6.2**：本卡仅保留状态常量与拒绝语义。✅
- HTTP 实例端到端：本仓口径为 Mockito 单测 + 真库结构探针；16045 活体系 22:04 旧 jar（新端点 `No endpoint`，老端点 401 对照），不可作证据但本卡未宣称 HTTP 验收。✅ 不构成缺口

## 5. LOW 观察（不阻塞，记录供后续卡参考）

- `review()` 并发竞态路径：条件 UPDATE 0 行且终态非 REVIEWED（如他方并发 ARCHIVED）时静默返回终态行而不报冲突——幂等语义取舍，主契约（不覆盖首位审核人）有测试锁定。若 P0-6.2 归档接入后需要「审核撞归档必须报错」，届时加判终态即可。
- `history` 按父指针逐环 selectOne（链长 N 次）；`uk_ai_doc_parent` 保证每父至多一子，selectOne 无歧义。链天然短，性能可接受（交付者已在证据中自报，认可）。

## 6. 结论

版本链「不可丢失」核心契约（v1 锚点 + append-only + 双防线防分叉 + 完整性校验）全部落地且测试锁定。**✅ 复核通过**。
