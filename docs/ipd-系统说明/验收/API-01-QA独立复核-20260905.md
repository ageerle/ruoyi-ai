# API-01 QA独立复核报告

**复核日期**: 2026-09-05  
**复核员**: QA独立复核员  
**工作区**: /Users/mac/Documents/ruoyi-ai  
**审计基线**: HEAD 71c24095  

---

## 一、看板卡信息提取

### 卡原文（开发计划-看板镜像.md Line 68）

| 字段 | 内容 |
|------|------|
| # | API-01 |
| 卡名 | IPD统一失败包络与错误码 |
| 验收要点 | 成功/参数错/未登录/权限拒绝/资源缺失/状态冲突/内部错误形状一致；traceId可关联日志；不给旧R包络 |
| 状态 | 执行者Codex任务01a07119（PARTIAL，多轮迭代） |
| 优先级 | U1 |
| 依赖 | SEC-01 |
| allowedPaths | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/**、common/**、security/**、dto/**及对应测试 |
| 验证命令 | 新增 Api01AcceptanceTest（@Tag dev）；mvn -pl ruoyi-modules/ruoyi-ipd -am -Dtest=Api01AcceptanceTest test |
| 依据 | 全局实现审计-F10；TS-09 |

---

## 二、本仓实现核实

### 2.1 核心类文件存在性

| 文件 | 存在 | 备注 |
|------|------|------|
| ApiV1Response.java | 存在 | code=0成功，五字段{code,message,data,timestamp,traceId} |
| ApiV1ErrorCode.java | 存在 | 七码（OK+6段错误），HTTP映射完整 |
| IpdPermissionExceptionHandler.java | 存在 | 限5 controller+1 auth，Order=HIGHEST_PRECEDENCE |
| IpdServiceExceptionAdvice.java | 不存在 | 本仓缺失 |

### 2.2 ApiV1Response 形状核查（TS-09）

成功: code=0 | 失败: code=错误码 | traceId三级兜底(MDC.traceId > MDC.X-Trace-Id)

### 2.3 ApiV1ErrorCode 六段位核查

| 错误码 | code | HTTP | 场景 |
|--------|------|------|------|
| OK | 0 | 200 | 成功 |
| PARAM_INVALID | 10001 | 400 | 参数错 |
| UNAUTHORIZED | 20001 | 401 | 未登录 |
| FORBIDDEN | 30001 | 403 | 权限拒绝 |
| NOT_FOUND | 50001 | 404 | 资源缺失 |
| STATE_CONFLICT | 50002 | 409 | 状态冲突 |
| INTERNAL_ERROR | 90001 | 500 | 内部错误 |

七码覆盖验收要点所列六类加成功

### 2.4 控制器挂载核查

| 控制器 | 路由 | 使用ApiV1Response |
|--------|------|-------------------|
| ProductController | /api/v1/products | 是 |
| ProjectController | /api/v1/projects | 是 |
| GateElementController | /api/v1/gate-elements | 是 |
| CertTemplateController | /api/v1/cert-templates | 是 |
| StageActionController | /api/v1/stage-actions | 是 |
| IpdAuthController | /api/v1/auth | 是 |
| DeletionRequestController | /api/v1/deletion-requests | 是 |

### 2.5 TraceId 注入机制核查

⚠️ **缺失 TraceFilter/TraceInterceptor**

grep 结果仅显示 ApiV1Response 内部读取 MDC，但无过滤器写入 MDC.traceId 或 X-Trace-Id。
这意味着 traceId 字段在实际 HTTP 请求中恒为 null。

---

## 三、验收要点逐条判定

| # | 验收要点 | 判定 | 证据 |
|---|----------|------|------|
| 1 | 成功包络形状一致（code=0） | 满足 | ApiV1Response.ok() 所有7控制器已用 |
| 2 | 六段位错误码 | 满足 | ApiV1ErrorCode 七码覆盖 |
| 3 | 失败统一走ApiV1Response（不给旧R） | 部分满足 | IpdPermissionExceptionHandler 已接；但 IpdServiceExceptionAdvice 缺失 |
| 4 | traceId可关联日志 | 不满足 | 无TraceFilter写入MDC，traceId恒null |
| 5 | Api01AcceptanceTest存在并PASS | 不满足 | 本仓无此测试类 |

---

## 四、既有声明核对

### 4.1 commit 6c626221 ✅ 真实
feat(ipd): P0-3/P0-4 参数种子54键 + 统一响应 ApiV1Response

### 4.2 commit ebf6026a ✅ 真实
fix(ipd): 认证链路依赖补齐 + ApiV1 包络完整版

### 4.3 "Api01AcceptanceTest 7项全PASS" | 本仓不可核验
本仓不存在 Api01AcceptanceTest.java，无法核验测试计数与PASS状态。

---

## 五、残留缺口

| # | 缺口 | 严重度 | 建议 |
|---|------|--------|------|
| 1 | IpdServiceExceptionAdvice 缺失 | 高 | 实现统一Service层异常兜底 |
| 2 | TraceFilter/TraceInterceptor 缺失 | 中 | 添加过滤器写入MDC.traceId |
| 3 | Api01AcceptanceTest 缺失 | 高 | 按卡验证命令新增测试类(@Tag dev) |

---

## 六、复核结论

**判定**: FAIL - 未满足验收条件

**理由**:
1. 成功包络 ✅ 已实现
2. 错误码枚举 ✅ 已实现
3. 权限异常转换 ✅ 已实现（IpdPermissionExceptionHandler）
4. Service层统一异常兜底 ❌ 缺失（IpdServiceExceptionAdvice 不存在）
5. traceId 注入 ❌ 缺失（无过滤器写入MDC）
6. 验收测试 ❌ 缺失（Api01AcceptanceTest 不存在）

**卡注记印证**: "成功包络已有；失败仍走旧R，API-01/P0-4.1待办" 与本仓核实一致。

---

**复核人**: QA独立复核员  
**复核时间**: 2026-09-05  
**报告路径**: docs/ipd-系统说明/验收/API-01-QA独立复核-20260905.md
