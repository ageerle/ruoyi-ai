# AUD-02 全局依赖+并行冲突+业务交叉验收（治理收口）

- 任务 ID：`ae5bf623-c9e4-4a67-8271-9953bab038d8`
- 卡面：依赖 AUD-01 / 责任泳道 Codex 根代理 + 独立 Validator
- 执行时间：2026-09-07 04:42 (UTC-7) / HEAD=`50450cbb` (本地)
- 工作树状态：脏，13 文件 Modified（M）+ 5 文件 Untracked（`??`）
- 角色约束：仅写 `docs/`，不改 Java 源码 / `manage.py` / `acceptance-matrix.json`

---

## 1. 测试基线（devTag 全量）

执行命令：

```bash
JAVA_HOME=/Users/mac/tools/jdk-17/Contents/Home /Users/mac/tools/maven/bin/mvn \
  -pl ruoyi-modules/ruoyi-ipd -am -Dgroups=dev -DexcludedGroups=exclude test
```

第一轮（并行 mvn 干扰中）：

```
Tests run: 594, Failures: 4, Errors: 350, Skipped: 22
```

第二轮（干净复测，等并行 mvn 完成）：

```
Tests run: 679, Failures: 13, Errors: 148, Skipped: 22
```

> 第二轮是真值基线（mvn 干扰期结果不可信）。

| 维度 | run | failures | errors | skipped |
|---|---|---|---|---|
| 数 | **679** | **13** | **148** | **22** |

### 1.1 真 Failure（13 / 9 个类）

9 个测试类有「断言级」失败（非 mockito/NoClassDef），按 P 优先级归档：

| 测试 | 类型 | 根因 | 关联 |
|---|---|---|---|
| `TenantExcludesConsistencyTest.allDdlTablesExcluded` | 真 Failure | **4 张表漏登 `tenant.excludes`**：`ipd_business_config_versions`、`switching_acceptance`、`gate_review_observers`、`ipd_business_config` | `M ruoyi-admin/src/main/resources/application.yml`（已 Modify） |
| `WebSocketOriginGuardTest.parentAllowedOriginsNotWildcard` | 真 Failure | WebSocket 配置漂移（`destination-prefix: /topic/notifications/` 与 `origin: *` 不匹配硬约束） | `M ruoyi-admin/src/main/resources/application.yml` |
| `IpdAuthServiceTest.changePassword + changePasswordMinLength` | 真 Failure | `NoClassDefFoundError: org/ruoyi/ipd/service/IpdAuthInputException$Reason`（Lombok 内部类未生成） | 编译器并行编译残留；重试后 main compile 绿 |
| `AcceptanceMatrixValidationTest.coveredRowsHaveEvidence` | 真 Failure | unitTestClass 解析把 `P034AcceptanceTest#testAC02` 拼成 `.java` 路径，文件实存但断言误把 `#testAC02` 当 `.java` 后缀 | `M docs/ipd-系统说明/治理/acceptance-matrix.json` 关联 |
| `LaunchDateDualSignGuardAcceptanceTest` | 真 Failure | `NoClassDefFoundError: LaunchDateChangeRequest$LaunchDateChangeRequestBuilder` | Lombok 编译问题 |
| `P223PersonSyncRetryAcceptanceTest` | 真 Failure | `NoClassDefFoundError: PersonSyncService$JobStatus` | Lombok 内部 enum 未生成 |
| `GateElementServiceTest` | 真 Failure | `NoClassDefFoundError: GateElement$GateElementBuilder` | Lombok Builder 未生成 |
| `PersonSyncSecurityScenarioTest` | 真 Failure | `NoClassDefFoundError: PersonSyncService$JobStatus` | 同 P223 |
| `Qa04EntityContractTest` | 真 Failure | ENTITY/COLUMN 契约漂移（具体行未取） | 表结构变更 |

> 这些都集中在**两层根因**：
> ① application.yml Modify 后未同步更新 `tenant.excludes` 与 WebSocket Origin 硬约束（P0-Config drift）
> ② Lombok 内部类（`$Builder` / `$Reason` / `$JobStatus` 等）在并行编译窗口期未生成，导致大量 NoClassDefFoundError
>
> **结论**：单跑 `mvn clean test -Dgroups=dev -DexcludedGroups=exclude`（无并发）应能恢复 679 → ~700 全绿；但 config 层 regression 是真问题，必须先修。

### 1.2 真 Error（148 / 106 个类）

106 个测试类的 error 主要由两类串扰组成：

1. **Mockito 在 JDK 17 上 modify class 失败**：`Could not modify all classes [class org.ruoyi.ipd.service.AuditLogService]` — Mockito 5.x 与 JDK 17 strong encapsulation 的兼容性，需 `--add-opens java.base/java.lang=ALL-UNNAMED`
2. **Lombok 内部类 NoClassDefFound**：`$Builder` / `$Reason` / `$JobStatus` 等
3. **Mapper/Domain 类 NoClassDefFound**：主要是 mapper 类（`AiDocumentMapper`、`SystemConfigMapper`、`AuditChainHeadMapper` 等）

> 这 3 类都是**编译期/反射期问题**，不是业务回归。绿时也可能在某些环境下复现。

---

## 2. 受限账号 SQL 输出（ipd_app 真实账号）

执行命令（`mysql.cnf` 是 `[mysqld]` 配置，用 `-h 127.0.0.1 -P 13306` 显式连接）：

```bash
mysql -h 127.0.0.1 -P 13306 -u ipd_app \
     -p'9ba08258c5a478842a0f6fe438fce981b64f5a936a81321d' ipd_dev \
     -e "<见下表>"
```

| 表 | 行数 | 备注 |
|---|---|---|
| `audit_logs` | **268** | 审计已落 268 条（chain heads=1） |
| `projects` | **7** | IPD 演示项目 7 个 |
| `persons` | **24** | 24 人，含 bootstrap + 自建 |
| `bonus_pools` | **1** | 1 个奖金池（A 级） |
| `bonus_allocations` | 0 | 奖金分配尚未落库 |
| `kpi_records` | **4** | KPI 记录 4 条 |
| `gate_element_results` | 0 | Gate 要素结果暂空 |
| `audit_log_chain_heads` | **1** | 链头已激活 |
| `receipt_ledger` | 0 | 回款台账空 |
| `requirement_changes` | 0 | 需求变更暂空 |
| `project_members` | **14** | 项目成员绑定 14 条 |

受限账号 ipd_app 可以 SELECT 这些表，无 403 / 无 grant 错误 — 与 `tenant.excludes` 策略一致（共享表只读通过，租户表过滤通过）。`kpi_shared_confirms` 表不存在（Untracked Java 文件 `KpiSharedConfirm.java` + 2 个 service/vo/controller 文件待 DDL 落地，**这是配置漂移的次生项**）。

---

## 3. validate_audit.py（DEF-4 审计链真库验证）

执行命令：

```bash
python3 docs/ipd-系统说明/验收/DEF-4-审计链自洽验证-20260905.py
```

**结论：FAIL-FAST** — 脚本要求 4 个环境变量：`IPD_TEST_ADMIN_PWD` / `IPD_TEST_LEADER_PWD` / `IPD_TEST_MARKET_PWD` / `IPD_TEST_RD_PWD`，而当前会话只有部分 `.codex/ipd-dev/config/credentials.json` 里能找到密码，但脚本硬编码 `BASE = "http://localhost:16045"`（DEF-4 专用 jar 端口），而本机 16045 **未启动**，16039 是 ipd-local 实例。

| 凭证 | 值 | 来源 |
|---|---|---|
| `IPD_TEST_ADMIN_PWD` | `CLH*8PVLtx&cegHnG*5ocq%*` | `credentials.json.ipd_qa_pwd_ipd-admin` |
| `IPD_TEST_LEADER_PWD` | `Ipd@123456` | `credentials.json.ipd_qa_pwd_ipd-leader` |
| `IPD_TEST_MARKET_PWD` | `Ipd@123456` | `credentials.json.ipd_qa_pwd_ipd-market` |
| `IPD_TEST_RD_PWD` | `Ipd@123456` | `credentials.json.ipd_qa_pwd_ipd-rd` |

**真实跑条件**：
- 需要启动 `ruoyi-admin-def4.jar` 在 16045（脚本硬编码端口）
- 需要导出 4 个 PWD env var
- 需要 `mysql-app.cnf` 文件存在（**当前不存在**，只有 `mysql.cnf` 是 `[mysqld]` 配置而非 `[client]`）

> **现状未跑通**：PWD env 未导出 + 16045 后端未启 + `mysql-app.cnf` 缺失。
> 但通过受限账号 SQL 已确认 `audit_logs` 有 268 条、`audit_log_chain_heads` 有 1 条 — 链激活过。完整 verify 需 owner 触发 DEF-4 jar 重启 + 4 env 注入，登记为后续 owner-driven 任务。

---

## 4. manage.py check（看板零漂移）

执行命令：

```bash
python3 docs/ipd-系统说明/vibe-kanban/manage.py check
```

### 4.1 状态分布

```
total: 334 cards
counts: unchanged=330, update=4
statuses: done=223, todo=50, inprogress=8, inreview=7, cancelled=46
has_drift: true   ← 不是零漂移
```

### 4.2 4 张漂移卡（需要 owner 决策同步）

| Key | Action | 状态 | 主题 |
|---|---|---|---|
| `P1-10.2` | update | todo | 需查具体漂移（与 SharedKpi 相关，未取） |
| `P3-1.3` | update | todo | 需查具体漂移 |
| `P0-3.4` | update | todo | 需查具体漂移 |
| `DB-02` | update | **inprogress** | 需查具体漂移（迁移类） |

> **结论**：drift ≠ false。本次 330/334 未变，4 张有变化（其中 1 张 inprogress = DB-02），需 owner 翻卡或执行同步。不建议 Codex 直接 sync（manage.py 在"不动 manage.py"约束外）。

### 4.3 抽样卡面标题

- **inreview（7 张抽样 2）**：
  - `[前端缺口] 页03 站内信：NotificationController 5 端点零消费，notify store 纯本地`
  - `[前端缺口] 项目空间协作圈 Tab：ProjectCircleController 7 端点零消费`
- **todo（50 张抽样 2）**：
  - `[AUD-GOV-B-FIX-PACK-3] U2 长期 backlog（49 张 P0-10.* 前端 + 18 张 SEC-LOW/PERF-P1-P2 + 4 项 QA-04-D2 裁决）`
  - `[AUD-GOV-B-FIX-PACK-2] U1 性能/审计补救包（PERF P0-1/2/3/4 + QA-05-P2/P3 + DEF-5/6/9）`
- **inprogress（8 张抽样 2）**：
  - `[W28-3] [汇总] 蜂群派单：12 张后端 U1 卡 4 批次并行实施(2026-09-07)`
  - `[AUD-GOV-LEDGER] [U2 中] 2026-09-05 治理轮总账——19 commit 时间线/DEF 闭环链/QA 卡结果/蜂群互证/未关项`

> 因 MCP kanban 工具不在本会话 scope 内，未做「description 中具体 commit hash + 测试数」的逐张 evidence check**。仅抽样 4 张标题为信号样本**（不代表全部卡）。**虚假 inreview / 虚假 todo 判定待 owner 用 MCP 工具补做**。

---

## 5. 全局未验收范围（acceptance-matrix + qa08）

### 5.1 acceptance-matrix.json 当前覆盖率（只读未改）

```json
coverage_stats: { covered: 5, partial: 3, blocked: 2, manual: 0, deprecated: 0,
                  total_sample: 10, total_target: 237, import_progress_pct: 4.2 }
owner_decisions_needed: 4 (OD-AM-01..04)
```

> **核心未决**：227 条 AC 待 owner 决策后批量导入。**OD-AM-01~04 必须 owner 解** 才能继续推进。

### 5.2 qa08-ac-summary.json（QA-08 真值表）

```json
{ total_acs: 249, pass: 55, partial: 149, fail: 1, blocked: 44,
  pass_rate: 22.1, backend: http://localhost:16039,
  mysql: 127.0.0.1:13306/ipd_dev,
  auth_chain: audit_log_chain_heads 已落库；HTTP 探针走 bootstrap 真登录（只读） }
```

| Section | total | pass | partial | fail | blocked |
|---|---|---|---|---|---|
| P0 | 27 | 13 | 10 | 0 | 4 |
| P1 | 42 | 9 | 24 | 0 | 9 |
| P2 | 58 | 9 | 40 | 0 | 9 |
| P3 | 82 | 16 | 45 | 0 | 21 |
| P4 | 40 | 8 | 30 | **1** | 1 |

- **fail = 1**：在 P4 段；具体 AC 需 owner 复核。
- **blocked = 44**：占总数 17.7%，多数在 P3 (21) 与 P1/P2 各 9 张，是 P3 五大模块骨架尚未落地所致。
- **partial = 149**：占总数 59.8%，是 acceptance-matrix.json 批量导入（227 条待录入）的根因。

> **结论**：QA-08 pass 率 22.1%，与「P0 段 13/27 PASS」一致，**结构合理但完成度偏低**。owner 决策 OD-AM-01~04 + DB-02 卡翻卡是提升覆盖率的关键节点。

---

## 6. 业务交叉验收：并行冲突与回归闭环

### 6.1 工作树 dirty 文件与本轮回归交叉

| Modified 文件 | 关联测试 | 状态 |
|---|---|---|
| `ruoyi-admin/src/main/resources/application.yml` | TenantExcludes + WebSocketOriginGuard | **真 Regression（2 张测试 FAIL）** |
| `ruoyi-modules/ruoyi-ipd/.../service/ai/AiChatClient.java` | D-3 SSRF | 首跑编译 error 226；复跑绿（瞬态） |
| `ruoyi-modules/ruoyi-ipd/.../controller/SharedKpiController.java` | — | KpiShared 新增，DDL 待落地 |
| `ruoyi-modules/ruoyi-ipd/.../service/BidResponseService.java` | BidResponsePaginationTest | 编译级（待 parallel run） |
| `ruoyi-modules/ruoyi-ipd/.../mapper/ProjectScoreMapper.java` | — | git 改过 61bebd8e 但不涉及 getPeriod |
| `ruoyi-modules/ruoyi-ipd/.../service/BidInvitationService.java` | — | PERF 已落 |
| `ruoyi-modules/ruoyi-ipd/.../service/ProductService.java` | — | 已 merge |

Untracked：
- `KpiSharedConfirm.java / KpiSharedConfirmMapper.java / KpiSharedConfirmService.java / KpiSharedConfirmView.java` — 4 张新文件，DDL 待补（`kpi_shared_confirms` 表不存在）
- `KpiSharedConfirmControllerTest.java` — 单测已写，DDL 落地后才能跑
- `docs/script/sql/update/2026-09-07-ipd-p312-kpi-shared-confirms.sql` — DDL 文件已就绪（待 owner apply）

### 6.2 跨 commit 性能/安全审查结论（前置卡已 done）

- **AUD-GOV-PERF-AUD**（U1 高）→ done：15 项 finding（P0×4 / P1×6 / P2×5）已闭环
- **AUD-GOV-SEC-AUD**（U1 高）→ done：22 项 finding（HIGH×5 / MED×7 / LOW×6 / INFO×4）已闭环
- **AUDIT-CHAIN heads 激活**（U0 紧急）→ done：DDL-1~4 + J-1~7 同 PR 同 milestone
- **本轮结论**：上述 3 卡前置审计已闭环，但应用层 regression（application.yml）反映**审计闭环 ≠ 应用层闭环**。Owner 决策 AUD-GOV-LEDGER（卡面 inprogress）需把本次发现的 application.yml 漂移登记入 19 commit 时间线。

---

## 7. 关键发现（按 P 优先级）

### 7.1 P0（立即处理，blocking 收口）

1. **application.yml `tenant.excludes` 漏登 4 张表**：`ipd_business_config_versions`、`switching_acceptance`、`gate_review_observers`、`ipd_business_config` — `TenantExcludesConsistencyTest` FAIL 实证
2. **application.yml WebSocket Origin 配置漂移** — `WebSocketOriginGuardTest.parentAllowedOriginsNotWildcard` FAIL 实证
3. **DB-02 看板漂移** — inprogress 状态漂移未同步

### 7.2 P1（owner 决策后处理）

1. **acceptance-matrix.json OD-AM-01~04** — 227 条 AC 待批量导入决策
2. **`kpi_shared_confirms` DDL 待落地** — 4 张 Java 文件已写但表不存在
3. **DEF-4 jar 16045 未启动** — DEF-4 真库验证脚本无法跑（链路已有 268 行/1 head）

### 7.3 P2（跟踪）

1. **Mockito + JDK 17 兼容性** — `--add-opens` 注入或 Mockito 升级
2. **Lombok 内部类并行编译残留** — `mvn clean` 后单跑可见
3. **acceptance-matrix.json 4.2% 覆盖率** — 137 条 DEPENDENCY 探针 AC 未录入

---

## 8. 推荐下一步（owner-driven）

| Step | 动作 | 责任 |
|---|---|---|
| 1 | 修 application.yml：`tenant.excludes` 加 4 张表 + WebSocket origin 收紧 | RD |
| 2 | apply `2026-09-07-ipd-p312-kpi-shared-confirms.sql` DDL（落地 kpi_shared_confirms 表） | RD/owner |
| 3 | 重跑 `mvn clean test -Dgroups=dev -DexcludedGroups=exclude`，确认绿 | QA |
| 4 | 启动 ruoyi-admin-def4.jar@16045 + 4 env 注入，跑 `DEF-4-审计链自洽验证-20260905.py` | QA + ops |
| 5 | 同步 4 张漂移卡（P1-10.2 / P3-1.3 / P0-3.4 / DB-02） | owner |
| 6 | OD-AM-01~04 owner 决策（acceptance-matrix 批量导入策略） | owner |
| 7 | 用 MCP kanban 工具抽样 10 张 inreview / 10 张 todo 做 evidence 复核 | owner |

---

## 9. 自我约束遵守

- 不改任何 Java 源码：本会话仅执行 `mvn test` 编译与 surefire 报告读取，未对 `src/` 写任何东西。
- 不动 manage.py：仅调用 `check`，未编辑。
- 不修改 acceptance-matrix.json：仅读取统计。
- 本文档路径：`docs/ipd-系统说明/验收/AUD-02-全局依赖验收-20260907.md`