# P2-5.3 Gate 条件遗留项、关闭凭证与阻断 · 验收记录（2026-09-06）

## 1. AC 对照

| AC | 期望 | 实现 | 用例 |
|---|---|---|---|
| AC-GATE-16 | 判定要素为「⚠️ 带条件通过」但不填责任人/关闭期限 ⇒ 拒绝提交 | `GateElementResultService.judge`：CONDITIONAL 判定强制三件套（conditionNote/responsiblePersonId/leftoverDueAt），缺任一抛 10001；改判 PASS ⇒ 遗留四件套经 LambdaUpdateWrapper 显式清空（不走 updateById 忽略 null） | `conditionNeedsResponsible`、`conditionNeedsDueAt`、`conditionLegacyPersisted`、`rejudgeToPassClearsLegacy` |
| 关闭需证据 | 遗留关闭必须附凭证 | `close(gateId, resultId, evidence, actor)`：evidence 空拒绝（HTTP 层 @NotBlank + service isBlank 双防线）；仅责任人本人或超管可关；关闭落 closed_evidence + leftover_status=CLOSED + LEGACY_CLOSE 审计；已关闭不可重复关 | `close_evidenceRequired`、`close_onlyResponsibleOrAdmin`、`close_twiceRejected` |
| AC-GATE-17 前半 | 期限前未关闭 ⇒ 逾期提醒 | `scanOverdue`（超管触发）：leftover_due_at 已过且 OPEN ⇒ 标记逾期 + GATE_CONDITION_OVERDUE 通知责任人（publishDaily 每日去重）+ LEGACY_SCAN_OVERDUE 审计；未到期不通知 | `overdueScanNotifiesAndDedups` |
| AC-GATE-17 后半 | 逾期遗留未关闭 ⇒ 不允许进入再下一个 Gate | Gate 提交（`submit`）前查同项目前序 Gate 的 OPEN 遗留：逾期未关 ⇒ 阻断提交；已关闭或期限内 ⇒ 放行 | `overdueLegacyBlocksNextGate`、`closedOrInTermLegacyAllowsSubmit` |
| 不能删要素消除遗留 | 要素停用/删除后遗留仍在 | 遗留查询只依赖 gate_element_results 本表（leftover_* 列），不 join 要素主档；要素停用/删除不影响逾期标记与阻断 | `legacySurvivesElementRemoval` |

## 2. 测试证据

`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='P251AcceptanceTest,P252AcceptanceTest,P253AcceptanceTest,P254AcceptanceTest' test`
收口复跑（01:4x，双签批四卡同批）：**P253 Tests run: 11, Failures: 0, Errors: 0**；
四卡合计 **56/56 全绿**，BUILD SUCCESS exit 0（/tmp/p25x-final-close.log）。

## 3. 真库证据（ipd_dev @ 127.0.0.1:13306，实例 16040 ruoyi-admin-p25x.jar）

- DDL `docs/script/sql/update/2026-09-06-ipd-p253-legacy-responsible.sql` 已 apply：gate_element_results 增
  `responsible_person_id bigint`、`closed_evidence varchar(500)`（information_schema 回读在位）；
  历史 CONDITIONAL 行保持 NULL 不回填，阻断只拦逾期未关行，口径不冲突。
- 探针 `.codex/ipd-dev/runtime/evidence-p253-http/probe-p253.sh`：五账号登录 + A~F 六段
  （判定校验/提交随行/逾期阻断/扫描通知/关闭凭证与权限/要素停用防线）+ 库内复核，
  终局 **PASS=34 FAIL=0**（probe-p253-out.txt）。
- 三步断言口径修正后复验（非实现缺陷，均脚本侧）：
  - C1 非超管扫描被拒：requireAdmin 层先拦，实返 30001「权限不足」（非 403 文案）；
  - D1 关闭缺凭证：@NotBlank 层拦截返回 10001「不能为空」（service isBlank 防线由单测 close_evidenceRequired 覆盖）；
  - F4 逾期通知落库：notification_events count=1 复核通过。
- 造数：project 9100000000000000071 / gates 63(G1)+64(G2) / members 900103+900104（复用 P2-5.2 双签批造数）。

## 4. 边界与遗留

- 遗留责任人取值域为项目成员 Person；「组长兜底责任人」未在规格内，本卡未实现（规格 AC-GATE-16 只要求必填）。
- 逾期提醒的每日去重依赖 publishDaily 的 (type, receiver, date) 口径，与 P2-5.4 GATE_SIGN_SOON 同机制。
- P2-5.4 已在本卡基础上续作（submit() 内 signDueAt 初始化、GateReview 仲裁链），见同日 P2-5.4 验收记录。
