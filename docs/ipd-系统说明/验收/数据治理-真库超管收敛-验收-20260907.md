# 数据治理：真库在任超管收敛 — 验收记录

- 日期：2026-09-07
- 卡片：`[数据治理] 真库 3 名在任超管收敛——owner 裁决保留哪一名`（看板 key `288e0d6b-3ad6-45a1-bac6-6ae904ca0a4c`，外部直投）
- 来源：P2-7.3 验收 §4-③（`docs/ipd-系统说明/验收/P2-7.3-超管移交-验收-20260906.md`，2026-09-06）
- 不变式源：ZK-IPD §九 / AC-HAND-07 / BR-ADM-02/03
- 结论：**已收敛（2026-09-07 19:16 owner 授权本会话执行 SQL 并复验通过）**——在任超管唯一：900101

## 1. Owner 裁决（2026-09-07）

- 保留 **900101 ipd-admin**（日常联调主账号，2026-09-07 18:49 仍有真实登录）。
- 另两名**均置 `account_status='DISABLED'`**（不改 person_type）：
  - `9110001` 傅志谦（GMP-001，ZK-IPD 场景种子——原稿正牌超管，从未登录）
  - `2096897116407382018` 系统管理员（EMP0001，create_by=-1 脚本创建，从未登录）

## 2. 真库探针证据（只读，13306/ipd_dev，2026-09-07）

| id | name | employee_no | 来源证据 | 活动痕迹 |
|---|---|---|---|---|
| 900101 | ipd-admin | LOCAL-900101 | remark 自认「LOCAL_FIXTURE：手工验收种子，非HR同步」 | 2026-09-07 18:49 登录（联调主账号） |
| 9110001 | 傅志谦 | GMP-001 | remark「ZK-IPD 场景：超级管理员」；原稿 `scenario.mjs` role=super_admin、README「超级管理员｜傅志谦」 | 2026-09-07 13:13 建，last_login NULL，audit_logs 0 行 |
| 2096897116407382018 | 系统管理员 | EMP0001 | create_by=-1 脚本创建 | 2026-09-07 17:43 建，last_login NULL，audit_logs 0 行 |

**卡面 ID 漂移勘误**：卡面与 P2-7.3 验收文档记录的第三名 `2096266884100935682` 在真库已物理消失（含软删行均无），现为上述替身 `2096897116407382018`。以本记录为最新口径。

**处置安全性**：后两名从未登录、audit_logs 零活动，处置不会打断任何在途联测；两人 group_id 均为 NULL、非任何组组长，无引用断链风险。

## 3. 交付物

- SQL：`docs/script/sql/update/2026-09-07-ipd-person-super-admin-converge.sql`
  - 幂等（仅命中 `account_status='ACTIVE'` 行，重放零副作用）；
  - 内置前置快照 + 两段回读校验（在任超管应恰好 1 行且 id=900101）；
  - remark 登记处置原因（审计口径：SQL 通道无应用层审计，以 remark + 本记录留痕）。
- 不动 Java：HandoverService「多名在任拒绝移交」防御按 `person_type + account_status` 双口径判定，置 DISABLED 即满足，无需代码变更。

## 4. 生效机制（复用 P2-7.3 验收 §3-④ 结论，零 security 改动）

`IpdAuthSession.currentPerson()` 每请求实查 `persons`；`scopeOf` 对 `DISABLED` 返回 `Scope.NONE` → `requireInternal` 401。被处置两名若有历史 token，**下一个请求即失效**，无需清会话。

## 5. Apply 后复验探针（人工 apply 后执行）

```bash
/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/software/mysql-8.0.46-macos15-arm64/bin/qcli \
  --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-app.cnf \
  --default-character-set=utf8mb4 ipd_dev \
  -e "SELECT id,name,account_status FROM persons WHERE person_type='SUPER_ADMIN' AND account_status='ACTIVE';"
# 期望：恰好 1 行，id=900101
```

**Apply 实录（2026-09-07 19:16）**：owner 明确授权本会话执行交付 SQL。前置快照核对通过（3 行 ID 与裁决时一致）→ 整文件执行 EXIT=0 → 独立回读核验：①在任超管（type+status 双口径）恰 1 行 id=900101；②9110001 / 2096897116407382018 均 DISABLED（update_time 19:16:34，remark 已登记处置原因）；③person_type 未改（全表 SUPER_ADMIN 仍 3 行，符合裁决）。

**探针口径提示**：P2-7.3 验收文档 §7 的复验 SQL 仅按 `person_type='SUPER_ADMIN'` 过滤——收敛后它会返回 3 行（其中 2 行 DISABLED），须按本记录的「类型+状态」双口径读数。

## 6. 边界与遗留

| 项 | 状态 | 说明 |
|---|---|---|
| 真库实际收敛 | ✅ 已完成 | 2026-09-07 19:16 owner 授权执行 SQL（EXIT=0），复验探针通过：在任超管唯一（900101） |
| ZK-IPD 场景联测 | ℹ️ 提示 | 原稿超管是傅志谦；收敛后场景联测以 900101 扮演超管（LOCAL_FIXTURE 种子本为本地联调替代） |
| 9110001 name 库内坏字节 | ℹ️ 不处理 | 同一结果集中「系统管理员」显示正常而该行乱码，证明是灌种子时字符集错误写入的坏字节（非客户端读取问题）；本卡按 id 处置不受影响；若需修复 name 值须另立卡走 SQL 通道 |
