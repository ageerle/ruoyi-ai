# P0-3.3 参数版本持久化、不可变快照与时点解析 — 独立复核报告（reviewer-ipd3，2026-09-05）

## 判定：◇ 复核维持（缺口成立不回退，不放行 done）

交付 commit `fc4830f3`（兄弟会话交付、runner-ops05 让路）。版本链主体机制质量高（复跑全绿、append-only、asOf 谓词锁定、CodeReview 2M2L 修复落地），但**卡面 AC「按显式时点/版本解析」与「完整不可变配置快照聚合体」两项未全达**，且 BR-真库挂账（真库写 0 行、无可用 HTTP 实例）——按规则维持 ◇，不满足回退条件（无测试假绿/表不符/幂等失效类致命缺陷）。

## 1. 测试独立重跑

```bash
export PATH="$HOME/tools/maven/bin:$PATH"; export JAVA_HOME="$HOME/tools/jdk-17/Contents/Home"
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=OPS05AcceptanceTest,P033AcceptanceTest,P1101AcceptanceTest \
    -Dsurefire.failIfNoSpecifiedTests=false test
# 仓库根；不带 -am 不带 clean；启动 23:09:17 PDT
```

- **BUILD SUCCESS（exit 0）**，Finished 2026-09-05T23:09:32-07:00
- 本卡 XML（mtime 23:09:32 = 本次自跑产物）ElementTree 独立解析：
  **P033AcceptanceTest tests=14 failures=0 errors=0 skipped=0**（无假绿形态）
- 交付者另报的 P032AcceptanceTest 3 例 + SystemConfigServiceCacheTest 5 例回归未在本任务书复跑范围内，未复验（不影响判定：P033 14 例独立复现已足够支撑主体结论）。

## 2. 真库探针（只读 socket，pymysql）

- `system_config_versions`：**12 列**逐列对齐——`id / config_key / config_value / version / effective_from / effective_to / is_immutable(def 1) / changed_by(NOT NULL) / change_reason / create_time / tenant_id(def '000000') / del_flag(def '0')`
- 行数 **0**——真库写入从未发生（与交付者自报一致，BR-真库挂账属实）
- `tenant.excludes` 含 `system_config_versions`（键名核对 ✅）

## 3. 三处 AC 缺口逐项判定（实读代码，非仅信报告）

### 缺口① 按显式版本号解析 API — **缺口成立**

- 实读 `SystemConfigService`（commit fc4830f3）公开面：`getValue / getIntValue / getBoolValue / list / update(2参) / update(3参) / getValueAsOf / listVersions(及 asOf 视图)`——**无 `resolveVersion(key, vN)` 类方法**。
- 实读 `SystemConfigController` 端点面：`GET /{key} / PUT /{key} / GET /{key}/versions / GET /{key}/as-of`——**无按版本号取值端点**。
- 卡面 AC 原文「按显式时点/**版本**解析」→ 「版本」半边未交付，缺口成立。
- 缓解：`listVersions` 已暴露版本行（含 config_value），客户端可自取版本值；机制层面补齐成本极小（selectOne by key+version）。
- **处置建议**：并入 **P0-3.4**（真实消费者需要按版本回读时顺手补 `GET /{key}/versions/{v}` 或 service `resolveVersion`），或建细卡（估时 <1h）。不建议在本卡回退重做。

### 缺口② 完整不可变配置快照聚合体 — **缺口成立**

- 实读 + 全模块 grep：`snapshot/快照` 仅出现在 javadoc 与 `changeReason("P0-3.3 baseline snapshot")` 字符串，**无任何「全键时点快照聚合体」实现**；现有交付仅单键 asOf 视图，无「可被业务记录引用」的快照 ID 形态。
- 卡面 AC 原文「产生可被业务记录引用的完整不可变配置快照」→ 未交付，缺口成立。
- **处置建议**：归 **P0-3.4 消化**——其卡面明写「历史已归档结果与**快照**不静默覆盖」「从P0-3.2/.3移入完整未验收要求」「不以配置缓存测试替代业务」，快照引用本就是消费者侧语义（结算/重放引用快照）。若 P0-3.4 实施中发现需要本体侧提供快照生成原语，再回填细卡，不回退本卡。

### 缺口③ 六开关类型/值/版本读取回归 — **部分成立，且补登表述需纠偏**

- 实读 `P033AcceptanceTest` 用键分布：14 例中 **7 例直接以 `bonus.salesSource`（六开关之一，AC-GLB-10 主角）**跑版本链主场景——首版基线/后续闭合/legacy 2 参/asOf 区间命中/同值短路/appendOnly 守卫/写序；另 1 例覆盖 A 级必做键归一化（`GateEngine.A_LEVEL_CONFIG_KEY`）。
- **纠偏**：协调会话补登与 log.md:2187 所记「六开关回归仅 A 级必做集**间接**覆盖」与事实不符——`bonus.salesSource` 是**直接**主场景用键，非间接。
- 真实剩余缺口：其余 5 组键（`bonus.poolBase / bonus.performanceScoreStrategy / bonus.coefficient.* / bonus.achievementTiers / bonus.launchAnchor`）无专测。版本链机制键无关，单键已锁机制，风险低；但六开关为 G-08 涉钱参数，建议补齐参数化用例。
- **处置建议**：随缺口① 的细卡或 P0-3.4 参数化补齐（`@ParameterizedTest` 六键 × 类型/值/版本三断言）；同步勘误镜像与 log 中「仅间接覆盖」表述（本报告即勘误依据）。

## 4. 挂账项「HTTP/真库写待补验(BR-真库)」验到哪一层

- **活体探活**：16039/16041/6039 dead；**16045 ALIVE**（`ruoyi-admin-ch.jar`，22:04 PDT 起）。
- **只读 HTTP 探针（GET 类）**：新端点 `GET /api/v1/system-configs/test/versions` 与 `.../as-of` 均返回 `{"code":404,"msg":"No endpoint ..."}`；对照老端点 `/api/v1/products`、`/api/v1/projects` 返回 401 认证墙（路由存在）→ **16045 实例为不含本卡代码的旧构建，不可作补验证据**。
- **DB 级写路径核查（已做，代码层）**：`update(3参)` → `recordVersion`：首版先落基线行 v1（旧值，effective_from=本体行 create_time 回溯锚）→ 受控 `lambdaUpdate` 仅 set `effective_to`（全库唯一允许的版本行 UPDATE）→ insert 新行（version=last+1，effective_to=NULL，is_immutable=1）；`insertVersion` catch `DuplicateKeyException → STATE_CONFLICT(50002)`（`uk_config_version` 并发双分配兜底）。表约束 `uk_config_version`/`idx_config_effective` 在位。**写路径代码走向与表约束已核；真库落库回读（0 行）未发生，补验维持挂账**——需从干净 HEAD 起含本卡代码的新实例（补验路径交付者已在证据 §6 写明，可执行）。

## 5. 主体质量确认（不回退的依据）

- 版本链核心契约全部有测试锁定：基线行 v1 / 唯一受控闭合 / append-only（零 delete 零 updateById）/ asOf 半开区间谓词锁定（getSqlSegment 断言，非 mock 透传）/ 同值短路 / legacy 2 参兼容（changed_by=0）/ 本体先版本后的写序。
- 复跑 14/14 独立复现；CodeReview 2 Medium 2 Low 修复落地（M-1 冲突包络 / M-2 谓词锁定 / L-1 短路 / L-2 测试改名）。
- 主体已达「版本持久化 + 历史不可变 + 时点解析」；缺的是「版本号解析」半边 + 快照聚合体 + 六开关专测 + 真库写实证——均非致命缺陷。

## 6. 结论

**◇ 复核维持**：缺口清单 = ①版本号解析 API（成立→并入 P0-3.4 或细卡）②全键快照聚合体（成立→P0-3.4 消化）③六开关专测（部分成立+补登表述纠偏→随细卡/P0-3.4 参数化补齐，镜像/log 勘误）；BR-真库挂账维持（16045 旧 jar 不可验，代码写路径+表约束已核）。不回退、不放行 done；三缺口处置齐后由协调会话收口。
