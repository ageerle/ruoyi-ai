# QA-07 49 页中文 / 错误空态 / 无出口流程验收 — 治理报告（2026-09-07）

**task_id**：`655f45e0-971c-4891-8e49-f5bb48b19cad`
**当前状态（卡面）**：todo，标签 `BLOCKED_DEPENDENCY 49 卡`
**本轮处理**：写治理报告 + 卡面失真修正

---

## 1. 卡面失真识别

卡面标签 `BLOCKED_DEPENDENCY 49 卡` 在本会话被误读成"QA-07 卡被 49 张卡阻塞"。
**正确含义**应是：本卡验收对象是 IPD 系统 49 个业务页面（项目立项/产品/需求/激励/AI/模板/Gate 等）的中文文案、错误提示、空状态、流程出口，必须等这 49 张页面业务卡全部收口后才能执行端到端验收。

**修正建议**（写在治理报告里，等卡面同步时一并修正 description）：

- description 改为：`BLOCKED_BY=49张业务页面卡收口（详见 docs/开发说明/spec/_导航地图.md）`
- 标签改为：`依赖=49业务页面`
- 关联清单：`docs/开发说明/spec/_导航地图.md` 中 49 页索引

---

## 2. 当前真实状态（基于代码扫描）

### 2.1 中文字面量分布

```
$ grep -rn "中文\|i18n\|@TableLogic" ruoyi-modules/ruoyi-ipd/src/main/
77 处
```

- 77 处匹配，但其中真正的 i18n 化基础设施**几乎为零**：
  - `service/channel/EmailChannelHandler.java:27`：注释 `支持简单的 i18n 模板渲染：zh-CN / en-US 两套主题/正文模板`（仅文档声明，未实现）
  - `service/channel/EmailChannelHandler.java:135`：注释 `渲染主题：subjectPrefix + 原 title（i18n 化留给后续卡）`（**作者自承认未做**）
- 没有 `messages.properties` / `messages_zh_CN.properties` / `LocaleResolver` / `MessageSource` 等任何 i18n 基础设施
- 没有 `Accept-Language` 解析处理

### 2.2 错误空态 / 无出口流程

未做系统性扫描；本卡待依赖 49 张业务页全部收口后再执行：
- 错误码统一性（前端 message key 与后端 `IpdBusinessException` 业务码映射）
- 空状态提示（"暂无数据" vs "正在加载" vs "暂无权限"区分）
- 无出口场景（用户进入死页面时是否有"返回/取消/联系管理员"）

---

## 3. 真实依赖关系

| 上游依赖 | 当前状态 | 卡面是否声明 |
|---|---|---|
| 49 张业务页后端 controller 收口 | 多数已 inreview/done，少数 WIP | **未声明**（卡面仅写"49 卡"含糊） |
| 前端 49 页 Vben 组件实现 | 由兄弟仓 `ruoyi-web` / `ruoyi-admin` 提供，本仓不可控 | **未声明** |
| 错误码字典统一（`IpdBusinessException` → 前端 message key） | 未做 | **未声明** |
| i18n 基础设施（即便只 zh-CN 单语） | 未做 | **未声明** |

---

## 4. 残留风险

1. **真阻塞未暴露**：卡面"BOCKED_DEPENDENCY 49 卡"无法让 owner 知道具体哪 49 卡 + 哪些状态可验收
2. **验收口径未约定**：未定义"中文验收通过"的判定标准（错别字率 / 术语一致性 / 空态文案完整度）
3. **错误码字典空白**：前端的 `el-message.error` 接收字符串，后端抛业务码，两侧无 mapping，49 页每页可能有 5-15 条错误提示，潜在 200+ 处文案待对齐

---

## 5. 推荐下一步（不写代码，纯治理）

1. **本轮卡面同步**：将 description 替换为 `BLOCKED_BY=49张业务页面卡收口`，标签改为 `依赖=49业务页面`
2. **建验收基线**（独立子任务，可派 owner）：
   - 把 49 页 → 49 行 `page_name | backend_api | frontend_route | current_status | chinese_pass_rate`
   - 抽样 3 页（P0：项目立项 / P0：需求 / P0：奖金）做中文验收试点
3. **建错误码字典骨架**（独立子任务）：
   - 后端 `IpdBusinessException` 业务码 → 前端 message key
   - 走文档契约（不写代码），待两边 owner 各自实现
4. **不写新 i18n 框架**：本卡仅做治理 / 卡面修正，i18n 化推后续卡（按作者注释自承认的范围约束）

---

## 6. 结论

- QA-07 **不应**继续以 todo + 含糊描述停在看板
- 本轮完成：卡面失真识别 + 真实依赖清单 + 推荐行动项
- 下一步 owner 决策：是否将本卡降级为"抽样验收"绕过 49 卡阻塞？或维持 BLOCKED 但 description 修正？

**未修改任何 Java / yml / SQL / 配置文件**。
**未引入新依赖**。
**未做 i18n 代码实现**（明确不属本卡范围）。