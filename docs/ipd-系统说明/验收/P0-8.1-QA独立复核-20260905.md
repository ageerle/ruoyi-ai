# P0-8.1 QA 独立复核报告

> **复核时间**: 2026-09-05  
> **复核员**: QA 独立复核子代理  
> **复核范围**: 只读，禁改生产代码、禁跑 mvn、禁 git 操作  
> **工作区**: /Users/mac/Documents/ruoyi-ai  

---

## 一、看板卡定义（来源：开发计划-看板镜像.md L107）

| 列 | 内容 |
|---|---|
| 卡KEY | P0-8.1 |
| 标题 | 种子数据与Mock初始化幂等验收 |
| 验收要点 | 启动两次无重复人员/组；默认账号仅dev且必须改密；33要素否决数按裁决；21认证按国家逐项核对 |
| 源状态证据 | ◇ PARTIAL：dev且非prod门控、重复不覆盖组长/账号、写入失败检查已修；17隔离JUnit含真实MyBatis/MySQL回滚验收全PASS0skip；33要素14否决21认证逐项核对通过；完整应用两次提交后启动及真实事务代理仍待验证；证据global-20260905/seed/README.md |
| 优先级 | U1 |
| 依赖 | OPS-02, DOC-05 |
| 指派 | 后端开发；QA独立复核（待认领） |
| allowedPaths | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/ 内 IpdMockDataInitializer*.java, ActionCatalog*.java 对应controller/service/domain/mapper及同名单测（含需新增文件） |
| 验证命令 | 新增 P081AcceptanceTest（@Tag dev）；mvn test -Dtest=P081AcceptanceTest |
| 关联 | P0验收:51；F13 |

---

## 二、本仓实现现状核实

### 2.1 实现代码存在性

| 文件 | 路径 | 存在 | 备注 |
|---|---|---|---|
| IpdMockDataInitializer.java | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/config/ | ✅ | @Profile("dev")，90行，幂等ensureGroup/ensurePerson |
| ActionCatalog.java | ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/seed/ | ✅ | 69动作，含P08（项目计划与里程碑排期） |
| P081AcceptanceTest.java | ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/config/ | ❌ **未在主仓** | 仅存在于 .codex/ipd-integration/*/ 副产物目录 |

### 2.2 主仓现有验收测试类

ruoyi-modules/ruoyi-ipd/src/test/ 下共 6 个 AcceptanceTest：
- P062AcceptanceTest.java
- P064AcceptanceTest.java
- P111AcceptanceTest.java
- P131AcceptanceTest.java
- P143AcceptanceTest.java
- P161AcceptanceTest.java

**P081AcceptanceTest 未在主仓 test/ 目录中，本仓不可核验。**

### 2.3 验收目录状态

| 路径 | 状态 |
|---|---|
| docs/ipd-系统说明/验收/ | ✅ 目录存在，**内容为空** |
| .codex/ruflo/global-20260905/seed/ | ✅ 有证据文件（README.md, catalog-snapshot.json, tests.stdout.txt 等） |

---

## 三、验收要点逐条判定

### AC-1: 启动两次无重复人员/组

**判定: ⚠️ 部分满足（代码层面）**

证据：
- IpdMockDataInitializer.java L53-64: ensureGroup() 按 groupName 查重，已存在则返回
- IpdMockDataInitializer.java L66-84: ensurePerson() 按 employeeNo 查重，已存在则返回
- 代码逻辑确认为幂等

**缺口**: 
- 未在主仓发现 P081AcceptanceTest，无法从测试角度验证「两次启动无重复」
- 真实MySQL环境验证缺失（本仓不可核验，需依赖 .codex 副产物中的测试输出）

### AC-2: 默认账号仅dev且必须改密

**判定: ⚠️ 部分满足（代码层面）**

证据：
- IpdMockDataInitializer.java L26: `@Profile("dev")` — 仅 dev profile 生效 ✅
- IpdMockDataInitializer.java L30: `public static final String INITIAL_PWD = "Ipd@123456"` — 默认密码硬编码
- **未在本仓代码中发现「强制改密」逻辑**（must_change_pwd 字段设置或首登拦截）

**缺口**:
- must_change_pwd=1 设置逻辑未在本仓实现代码中定位
- 改密拦截逻辑（如有）未在 allowedPaths 范围内找到

### AC-3: 33要素否决数按裁决

**判定: 📋 本仓不可核验**

证据：
- catalog-snapshot.json 存在于 .codex/ruflo/global-20260905/seed/（非主仓）
- ActionCatalog.java 仅定义69个动作，不包含33要素否决项数据
- 33要素数据可能存储在 DB seed 或外部配置中，本仓代码未定位

### AC-4: 21认证按国家逐项核对

**判定: 📋 本仓不可核验**

证据：
- 21认证列表未在 IpdMockDataInitializer、ActionCatalog 或其他 seed 文件中定位
- 认证数据可能来自外部资源文件或 DB，本仓代码无直接证据

---

## 四、证据链核验

### 4.1 本仓可核验证据

| 证据 | 位置 | 核验结果 |
|---|---|---|
| IpdMockDataInitializer.java | 主仓 ruoyi-modules/ruoyi-ipd/.../config/ | ✅ 存在，@Profile("dev") 确认 |
| ActionCatalog.java | 主仓 ruoyi-modules/ruoyi-ipd/.../seed/ | ✅ 存在，69动作确认 |
| P081AcceptanceTest.java | 主仓 test/ 目录 | ❌ **不存在** |
| docs/ipd-系统说明/验收/ | 主仓 docs/ | ✅ 目录存在但为空 |

### 4.2 其他工作树证据（本仓不可核验）

| 证据 | 位置 | 备注 |
|---|---|---|
| seed/README.md | .codex/ruflo/global-20260905/seed/ | 在其他工作树，本仓不可直接核验 |
| catalog-snapshot.json | .codex/ruflo/global-20260905/seed/ | 非主仓文件 |
| tests.stdout.txt | .codex/ruflo/global-20260905/seed/ | 非主仓文件 |
| P081AcceptanceTest.java | .codex/ipd-integration/*/ (多处副产物) | 未归入主仓 test/ 目录 |

---

## 五、判定结论

### 总体判定: **KEEP_PARTIAL**

| 维度 | 状态 |
|---|---|
| 代码实现 | ⚠️ 部分到位（dev门控✅，幂等逻辑✅，改密逻辑❓） |
| 测试覆盖 | ❌ P081AcceptanceTest 未在主仓 |
| 验收证据 | 📋 本仓验收目录为空，证据在其他工作树 |
| 33要素/21认证 | 📋 本仓代码无法核验 |

### 关键缺口

1. **P081AcceptanceTest 未归入主仓 test/ 目录** — 验证命令要求的测试类不存在于 allowedPaths 范围内
2. **must_change_pwd 逻辑未定位** — AC-2「必须改密」的实现代码在本仓未找到
3. **33要素/21认证数据不在本仓代码中** — 需依赖外部工作树证据

### 本仓不可核验声明

以下声明因证据在其他工作树或需动态环境验证，**本仓不可核验**：
- 「17隔离JUnit含真实MyBatis/MySQL回滚验收全PASS0skip」— 需 mvn test 运行
- 「33要素14否决21认证逐项核对通过」— 需 catalog-snapshot.json 比对
- 「完整应用两次提交后启动及真实事务代理仍待验证」— 需运行时环境

---

## 六、建议

1. **P081AcceptanceTest 需归入主仓** test/java/org/ruoyi/ipd/config/P081AcceptanceTest.java
2. **must_change_pwd 逻辑需明确位置** — 在 IpdMockDataInitializer 或其他安全组件中实现
3. **33要素/21认证数据需在本仓有明确定义** — 或建立可核验的证据链接

---

*本报告为 QA 独立只读复核，未修改任何生产代码或文档。*
