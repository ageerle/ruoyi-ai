# P0-7.4 / P2-4.1 / P2-5.1 三卡独立复核 + P1-6.1 缺口复查（2026-09-06）

- **复核方**：Qoder 复核会话（用户在四选项中拍板「复核待审卡」）
- **时间**：2026-09-06 00:14 – 00:40 PDT
- **方法**：独立重跑单测（错峰 / 单模块 / 不带 `-am` / 不带 `clean`）+ 磁盘现态逐条 AC 读码 + PyMySQL socket 只读探针（凭证不上命令行）
- **结论**：P0-7.4 ✅ / P2-4.1 ✅ / P2-5.1 ✅（均翻 done）；P1-6.1 维持 ◇ inreview 退回（缺口未补）

## 1. 独立测试重跑

| 轮次 | 时间 | 结果 |
|---|---|---|
| 第一轮 | 00:21:04–07 | 45 跑 3 Error（全在 P241）——见 §5.1 定责 |
| 第二轮 | 00:22:06–16 | 仅执行 2 类 9 测（并发构建搅局，作废） |
| 第三轮 | 00:22:49–52 | **45/45 全绿**：P074=14 / P241=9 / P251=13 / GateElementServiceTest=5 / GateElementAuditJsonTest=4，BUILD SUCCESS |

命令：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='P074AcceptanceTest,P241AcceptanceTest,P251AcceptanceTest,GateElementServiceTest,GateElementAuditJsonTest' -Dsurefire.failIfNoSpecifiedTests=false test`

## 2. 逐卡 AC 对照结论

### P0-7.4 企微 Mock 绑定/扫码/离职解绑 —— ✅ 通过
- 四端点齐：`GET /wecom/url`（匿名）、`GET /wecom/callback`、`POST /wecom/bind`、`POST /wecom/unbind`；`IpdWebSecurityConfig` 两组 interceptor exclude 均覆盖 url+callback，bind/unbind 不豁免（须登录）
- `wecomScanLogin` 复用 `admit()` 状态机：RESIGNED 拒 / DISABLED 拒 / FROZEN→HANDOVER_ONLY / ACTIVE→FULL；未绑定 400「账号未绑定」（AC-AUTH-05）
- `bindWecom`：空白拒 / 已绑拒 / 被他人占用拒（BR-ORG-06 全局唯一查询）+ WECOM_BIND 审计
- `unbindWecom`：**LambdaUpdateWrapper 显式 set null**（MP updateById 忽略 null 字段的修复在码，注释在案）+ WECOM_UNBIND 审计含 reason；controller 层 revokeAll 撤全会话（14 测含「解绑后撤销该人全部会话」）
- 14 测试覆盖全部主责 AC（AUTH-04/05/06、BR-ORG-06、代绑 403、超管放行）

### P2-4.1 成员绑定评级与津贴基准快照 —— ✅ 通过
- 顺序与守卫全在：角色枚举 → 项目/人员存在 → RESIGNED/DISABLED 拒 → 角色互斥（AC-TEAM-10）→ 同项目同人同角色幂等拒 → 等级缺失拒 → `allowance.{level}` 缺失拒（-1 兜底拒，不留静默默认）
- 快照原子写入 lockedLevel/lockedAmount + joinDate + bonusEligible；MEMBER_BIND 审计 afterData 含快照值（AC-HR-03 / AC-INC-02 不追溯由快照语义保证，测试 2 直接验证「绑定后升 L3 仍按 L2=1500」）
- 真库证据（承接原验收）：L3 绑定 lockedAmount=2000 为参数表真实取数
- 兄弟叠加说明：P2-4.2 在途改动给 `bindMember` 加了备案门禁（FOR UPDATE 计数 + approvalRef），对 P2-4.1 行为为**加法式**，未削弱任何既有守卫；P241 测试 00:21 被补一处 lenient 桩（`allowance.projectCountThreshold`），**9 个断言原封未动**（已逐条核对，无假绿）

### P2-5.1 Gate 提交与逐项判定附件快照 —— ✅ 通过
- checklist：按 Gate 列启用要素含 isVeto/当前判定/证据引用（要素定义表实为 `gate_review_elements`，@TableName 已核）
- judge：终态守卫、要素归属校验、CONDITIONAL 必填说明、**FAIL 必填证据**（AC-GATE-02）、G1-1 PASS 阈值服务端按 `gate.g1.minCustomerVerifications`（默认 5）或 ≥1 书面意向裁决（AC-GATE-1a~1d；参数即时读取，1d 语义成立）、改判幂等走 update + GATE_ELEMENT_JUDGE 审计
- submit：全要素已判校验、**否决项 FAIL 阻断**（AC-GATE-15/19/20，含 FAIL 缺证据同样阻断）、快照冻结入 `gates.element_snapshot` + startedAt 置位、重复 submit 拒、GATE_SUBMIT 审计
- 低优观察（不阻断本卡）：
  1. `judge()` 提交后仅有 status 守卫、无 startedAt 守卫——提交等待签署期间判定行仍可改，冻结快照不受影响，但库内判定行与快照可能漂移；建议 P2-5.2/P2-5.3 或后续小卡补守卫
  - 2. 现库快照 2795 字节，原验收记录 B8 读数 3405 字节——判定行/审计/快照共存自洽，疑为 probe 两轮间差异，备忘不追
  3. 见 §4 种子污染——影响后续 G1 流程，不影响本卡逻辑（逻辑对要素集透明）

## 3. 真库只读探针（ipd_dev @13306，socket）

| 验点 | 结果 |
|---|---|
| `gates.element_snapshot` | longtext NULL ✓ |
| `gate_element_results.evidence_ref` | varchar(500) NULL ✓ |
| 造数清理 | projects 9100000000000000001 del_flag='2' ✓；gates 9100000000000000002 del_flag='2'、startedAt 非空、快照在 ✓ |
| 判定行 | gate ...002 共 17 行：PASS=16 / FAIL=1（与原验收一致）✓ |
| 审计链 | WECOM_BIND=1 / WECOM_LOGIN=2 / WECOM_UNBIND=2 / MEMBER_BIND=1 / GATE_ELEMENT_JUDGE=20 / GATE_SUBMIT=1 ✓ |
| 正牌种子 | G1-1~G1-7=7 / G2=6 / G3=5 / G4=8 / G5=7，**共 33 条**，与 AC-GATE-14 完全一致 ✓（P1-6.1 缺口⑦「种子真库入库验证」由此实证） |

## 4. 发现：共享种子表被测试残留污染（需清理，非本批缺陷）

`gate_review_elements` 里 G1 实际启用 17 条 = 正牌 7 条 + **残留 10 条**（未软删、enabled='1'）：

`D7155`(DEF1复验)、`DEF1152609`、`QA03-1788638687/…9201/…9259/…9325/…9667`、`QA03-DIAG-1`、`Q1788647239`、`Q1788648469`——为 9 月 5 日下午 QA03 矩阵探针与 DEF1 会话所造，未清理。

**影响**：任何新 G1 Gate 的「全部适用要素已有判定」都会把这 10 条算进去；P1-6.2「33 要素效果验收」也会被污染。原验收「G1 17 启用要素」的读数即含此批残留（判定 17 行同理）。**建议**：软删这 10 条（del_flag='2'）或补清理卡；本复核未动库。

## 5. 过程记录

### 5.1 首轮 3 Error 定责（假红实录，时间戳链）
- 00:17 `ProjectMemberService.java` 被兄弟（P2-4.2 在途）加入 `allowance.projectCountThreshold` 读取；P241 测试（23:46 版）未桩该键 → Mockito 严格桩报错 3 例
- 00:21 兄弟补 lenient 桩（P241 8640→8884 字节）并自行重跑全绿（XML 00:21:32–48，覆盖了我首轮报告）
- 00:22:49 我方第三轮干净复跑 45/45 绿——归因闭环，非交付缺陷

### 5.2 P1-6.1 缺口复查（维持退回）
昨晚 22:36 QA 复核所列 7 项缺口，现状：
1. 版本生命周期 draft/publish/archive——**仍缺**（服务仍仅 listByGate/create/update/disable 四方法，文件 mtime 停在 15:17）
2. 复制 / revert 历史恢复——**仍缺**
3. 已发布编辑 409——**仍缺**
4. vetoDualRequired 校验——**仍缺**
5. thresholdJson 校验——**仍缺**
6. DB element_code 唯一索引——**仍缺**（information_schema 探针：非主键唯一索引数=0）
7. 33 项种子真库入库——**本次实证通过**（见 §3 正牌种子行）
8. P161AcceptanceTest 回主仓——**仍未回**（主仓仅有 GateElementServiceTest/GateElementAuditJsonTest）

### 5.3 解锁影响
- P2-5.1 → done 后，**P1-6.2 的依赖只剩 P1-6.1**（其缺口 1–6 待实施）；P0-3.4 的依赖里 P2-4.1/P2-5.1 已 done，余 P0-3.3（inreview）与 P3-4.4（未实施）
- 401/403 口径遗留维持原议：归 SEC-02 复审统一定调，本复核不动

## 6. 状态操作

- `manage.py set` 三卡 → done（note 指向本报告）
- P1-6.1 保持 inreview（note 追加本次复查结论）
- 本复核零业务代码改动、真库全程只读
