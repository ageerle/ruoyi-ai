# P2-3.2 真库 HTTP 验收（AC-TEAM-03 / AC-TEAM-05 / BR-TEAM-03 / BR-REC-BID-03）

- 日期：2026-09-05 22:41 PDT；执行：Qoder 主协调会话（P2-3.2 实施+验收）
- 实例：`127.0.0.1:16052`，jar `.codex/ipd-dev/runtime/ruoyi-admin-p232.jar`（22:40 工作树原位构建，内嵌件 javap 核验：BidInvitation 含 JsonFormat、LegacyImportService 构造器含 Qualifier、NotificationService 完整）
- 结果：**32/32 PASS**；机器证据同目录 `P2-3.2-真库HTTP验收-20260905.json`
- 账号：ipd-market(900103)/ipd-rd(900104)/ipd-admin(900101)，密码取 `.codex/ipd-dev/config/credentials.json`

## 验收矩阵（T02–T17 摘要）

| 组 | 用例 | 结果 |
|---|---|---|
| 创建 | T02/T02b/T02c PUBLIC 招标单 code=0/OPEN/create_by=900103 落库 | PASS×3 |
| 创建 | T03 ONE_TO_ONE 指定邀标（target=900104） | PASS |
| 应标 | T04/T04b/T04c 首次应标 code=0/PENDING/rdPmId=会话用户 | PASS×3 |
| 幂等 | T05/T05b 重提同 id + 库内仅 1 行（BR-REC-BID-03） | PASS×2 |
| 应标 | T06 admin 第三方应标 code=0 | PASS |
| 前置 | T07 DB 直插 900105 第三人行，三人 PENDING 就绪 | PASS |
| 拒绝不留痕 | T08–T08d 拒绝 code=0 + data=null(204 语义) + 业务表零行 + 审计零 reject 行（AC-TEAM-03/BR-TEAM-03） | PASS×4 |
| 名单 | T09 非目标人应标 ONE_TO_ONE ⇒ 30001 | PASS |
| 校验 | T10 摘要<40 字 ⇒ 10001；T11 非法 decision ⇒ 10001 | PASS×2 |
| 越权 | T12 非本人撤回 ⇒ 30001 | PASS |
| 原子遴选 | T13–T13e 遴选 code=0/SELECTED + 中标 ACCEPTED + 两落选 REJECTED（AC-TEAM-05 三选一） | PASS×5 |
| 状态机 | T14 遴选后再应标 ⇒ 50002 | PASS |
| 隐私 | T15/T15b/T15c rd 视角仅本人 1 行 / market 视角全量 3 行 | PASS×3 |
| 审计 | T16 accept≥2 行；T16b select afterData 含 selectedRdPmId+rejectedRdPmIds 清单 | PASS×2 |
| 撤回 | T17/T17b 本人撤回 code=0/WITHDRAWN | PASS×2 |

## 单测（Mockito 契约层）

- `P232AcceptanceTest` 16/16 + `P231AcceptanceTest` 10/10 全绿（22:30 BUILD SUCCESS，surefire XML 重写核验）
- 蜂群 CodeReview 必修项已闭环：H-1 遴选并发双中标（selectByIdForUpdate 行锁）/ M-1 幂等查后插竞态（锁定读串行化）/ M-2 隐私落选断言假绿（ArgumentCaptor 锁 wrapper 契约）/ L-2 decision 白名单

## 过程缺陷修复（本轮新发现）

1. **expireAt JSON 反序列化失败**（存量缺陷，非本卡引入）：`application.yml` 的 `jackson.date-format` 键顶格缩进被破坏，全局日期格式从未生效，`/api/v1` 走 Spring 默认 ISO，`"2026-09-20 00:00:00"` 报 10001。修复：`BidInvitation.expireAt` 加 `@JsonFormat(pattern="yyyy-MM-dd HH:mm:ss", timezone="GMT+8")`（R8-P0-11 先例：字段级就地标注）。application.yml 的缩进破坏属共享配置，登记待后续勘误卡处理（影响全链路，未在本卡擅改）。
2. **镜像 OPS-05 行结构破坏**：兄弟会话写入单元格内未转义半角 `|`（kind 括号内 FYI 竖线 ACTION）致列数 11，manage.py plan() 解析崩溃、set/check/sync 全链失效。勘误：改全角（语义不变）。
3. **沙盒构建教训**：多会话并发期重打 fat jar 会裹挟兄弟在途半成品（boot2 抓到 22:28 的 NotificationService 中间态）；/tmp 沙盒剔除法又因缺根 `lombok.config`（copyableAnnotations）丢 `@Qualifier` 致 LegacyImportService 注入炸。最终解：等兄弟 commit 完整版（e1cc6ae1）后仓库根原位构建 + javap 三要素核验。

## 遗留与边界

- L-1（存量 create_by=-1 行可见性降级）：非发起人视角按"宁少看不多看"处理，注记不迁移
- 真库验收已过但按纪律**不标 done**：QA 独立复核待认领 → 转 inreview
- 关联：P0-10.21（前端页21）后端契约与实现双就绪（fc4830f3 差异表 D-1~D-7 与本实现一致：40001→50002 映射、提交路径、解决方案摘要≥40 字），前端联调依赖解除
