# P2-5.2 + P0-3.4 双签与参数消费者——实现与真库验收（2026-09-06）

- 批次：三卡起手批续作（承接 P074/P241/P251）
- 实例：127.0.0.1:16040（ruoyi-admin-p252.jar，独立端口，验收后已停）
- 数据库：ipd_dev（MySQL 8.0.46 @13306）；造数 id 段 9100000000000000011/21-24/31-35/41-45/51-54/61-62，验收后已软删
- 证据目录：`.codex/ipd-dev/runtime/evidence-p252-http/`（probe-final-out.txt 28/28、probe-p034-final-out.txt 20/20）

## 1. P2-5.2 Gate 双签（AC-GATE-03/04/05，BR-GATE-03/04/08）

### 交付物
| 文件 | 说明 |
|---|---|
| GateReviewMapper.java | gate_reviews 表 Mapper（表已在库，uk gate+type+round） |
| GateReviewService.java | 签署矩阵：G1/G5 双 PM 盲签；G2/3/4 领域主导方单签（leadSide 按领域，owner 2026-09-05 决策） |
| GateReviewController.java | POST /api/v1/gates/{id}/sign、GET /review |
| P252AcceptanceTest.java | 15 用例（@Tag dev）15/15 绿 |

### 签署矩阵
- G1/G5：市场PM+研发PM 并行盲签——在途互不可见（仅 otherSubmitted 标志）；任一 REJECT ⇒ Gate REJECTED+双方 GATE_REJECTED 通知；双 APPROVE ⇒ APPROVED 同时揭示
- G2 领域签署：市场主导（产品定位 A/R）；G3/G4：研发主导（差异化创新 A/R）；非主导方签署拒绝
- 期限 due_at=startedAt+gate.signDeadlineDays（默认 3，BR-GATE-04）；超时弃权/仲裁归 P2-5.4

### 真库 HTTP 验点（28/28 全绿）
- 盲签：市场签后研发视图只见 otherSubmitted=true，other 无 decision/opinion（A4b）
- 双过：双 APPROVE → APPROVED，视图揭示对方结论（A6/A7）
- 否决：REJECT → REJECTED + notification_events 2 条（收件人 900103/900104）（B2/E5）
- 领域：市场签 G3 拒「研发PM主导签署」；研发单签即 APPROVED（C1/C2）
- 防线：未提交（startedAt NULL）拒、组长拒、ABSTAIN 拒、重复签拒、终态后拒（D1-D3/A5/B4）
- 库内：gate_reviews 5 行 decisions 与 gates 终态一致、GATE_SIGN 审计 5 条、due=+3d（E1-E7）

### 缺陷修复
- 首版 requireNotSigned 用 selectCount 被 mock 恒 0 短路（单测假绿）→ 改用 roundRows 流内判断；真实库并发由 uk_gr_gate_type_round 唯一约束兜底
- **judge() 冻结守卫（复核建议补）**：兄弟复核会话指出 P2-5.1 提交后无 startedAt 守卫，签署期间改判会与 element_snapshot 漂移——本批在 GateElementResultService.judge() 补「已提交并冻结快照，签署期间不可改判」，P251AcceptanceTest 追加 judge_afterSubmitFrozen_rejected（回归 14/14）

## 2. P0-3.4 参数消费者真实版本验收（AC-CFG-02/03、AC-HR-05；AC-GLB-10 部分）

### 结论
- AC-CFG-02 ✓：签署期限参数改 5 → 新 Gate due=+5d；既有已落 due=+3d 不重算（真库 S1-S4）
- AC-CFG-03 ✓：同实例不重启，改参后下一次签署/绑定即新值（S2→S4、S5→S6 均 <1s 生效）
- AC-HR-05 ✓：allowance.L3 改 2200 → 新绑定 lockedAmount=2200；回切 2000 后再绑定按 2000；既有绑定快照不被回切影响（S5-S8b）
- 版本链 ✓：每次 PUT 落 system_config_versions 不可变行，GET versions 可查（S9/S10d，AC-GLB-09 联证）
- AC-GLB-10 **BLOCKED**：bonus.salesSource 参数侧可切 SHIPMENT/RECEIPT 且热读即时可见（S10a-c），但「同项目重算随配置变」的消费者（BonusPool 分配/归档/重算链）不存在——P3-4.4 待认领（源码证据：无 BonusPoolService；ReceiptLedgerService 硬编码 RECEIPT 口径不读参数）。待 P3-4.4 完成后补验。

### 单测
- P034AcceptanceTest.java 4 用例（@Tag dev）：非默认期限、同实例参数 3→5 即时读取、L3 2000→2200 新绑定即时生效、既有快照不追溯

## 3. 环境事件（透明披露）
- **900102 ipd-leader 密码重置**：验收发现组长种子密码 hash 不匹配 Ipd@123456（同前批 900101/900103/900104 漂移形态，判定为某会话直改库无审计）。按前批处置用 hutool BCrypt cost10 重置回文档默认 Ipd@123456（BcryptCheck 校验通过后生效）。audit_logs 有 prev/curr_hash 链式约束，手工补登记行会断链，故以本节披露替代。
- 打包陷阱：admin 模块 classes 未变时 spring-boot repackage 增量跳过，fat jar 不换内嵌依赖——须删旧 ruoyi-admin.jar 再 package（本次以内嵌 ipd jar md5==.m2 md5 复核）。

## 4. 遗留
- 越权返回 401/20001 而非 SEC-02 口径 403（沿用前批口径，建议 SEC-02/QA 复审定调）
- G2~G5 判定链、超时弃权/轮次/仲裁：P2-5.3/P2-5.4 待做
- AC-GLB-10 重算链：待 P3-4.4
- 兄弟域 P233AcceptanceTest 编译错（BidInvitation.createBy 找不到，00:32 在途写入）挡住全模块 testCompile——本卡测试已错峰重跑通过，兄弟文件未动

## 5. 回归
- 五套验收单测 56/56 全绿@2026-09-06 00:36:48（P251=14 含新守卫例 / P252=15 / P034=4 / P241=9 / P074=14，错峰单模块不带 -am）
- 兄弟域 P233 编译错曾在 00:32 挡住 testCompile，00:33 其自行修复，未动其文件
