# P072 / P081 归仓 Red/Green 阶段建议 — 主协调会话执行参考

> **登记时间**: 2026-09-05 08:58 PDT
> **登记会话**: 本会话（治理层）
> **触发**: 系统注入 red/green discipline（"no failing test on record" → 编辑实现前先红测试）
> **基线 HEAD**: dd7d1171
> **OPS-09 登记**: 本会话**仅登记**——不写测试、不改实现

---

## 一、本会话 red/green discipline 适用性

按 red/green 铁律 "Before editing implementation code, write a test that fails for the missing behavior and run it to see it fail — then make the change"：

- 本会话**未编辑**任何 Java 实现代码
- 本会话**未编辑**任何测试文件
- 最近 5 个 commit 全部是 docs/ipd-系统说明/ 下的审计/登记交付物
- 因此本会话**当前不触发 red/green**（无 "failing test on record" 状态）

但**下游修复动作**（补全 IpdAuthExceptionHandler / migrate_native_db.py）**会**触发 red/green——主协调会话在动手前必须先写失败测试。

---

## 二、建议 Red 阶段（主协调会话执行）

修复 §前几轮 阻塞登记**前**，按 red/green 铁律先红后绿：

### 2.1 为 IpdAuthExceptionHandler 缺失写失败测试

**目的**：证伪"测试代码引用未存在类"是真实问题，而非现状即正确。

```java
// 建议放在 ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/contract/AuthExceptionHandlerContractTest.java
// @Tag("dev")
class AuthExceptionHandlerContractTest {
    @Test
    void ipdAuthExceptionHandlerClassMustExist() {
        // red：先证伪类不存在
        assertDoesNotThrow(() ->
            Class.forName("org.ruoyi.ipd.controller.IpdAuthExceptionHandler"),
            "IpdAuthExceptionHandler 必须存在以满足 P072 契约");
    }
}
```

跑测应见 1 failed：ClassNotFoundException。修复后（创建空类或委派到 IpdServiceExceptionAdvice）转绿。

### 2.2 为 migrate_native_db.py 缺失写失败测试

```java
// 建议放在 ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/contract/NativeMigrationScriptContractTest.java
// @Tag("dev")
class NativeMigrationScriptContractTest {
    @Test
    void migrateNativeDbScriptMustExist() {
        Path script = Path.of("docs/ipd-系统说明/运行手册/migrate_native_db.py");
        // red：先证伪脚本不存在
        assertTrue(Files.exists(script), "migrate_native_db.py 必须存在于主仓运行手册目录");
        String content = Files.readString(script);
        assertTrue(content.contains("07-user-g2-planning-decision"));
    }
}
```

跑测应见 1 failed：NoSuchFileException。修复后（从 .codex 副本拷贝该脚本到主仓）转绿。

### 2.3 Red 阶段命令（合规：错峰 + 单模块 + 不带 -am 不带 clean）

```bash
export PATH="$HOME/tools/maven/bin:$PATH"
export JAVA_HOME="$HOME/tools/jdk-17/Contents/Home"
cd /Users/mac/Documents/ruoyi-ai
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='AuthExceptionHandlerContractTest,NativeMigrationScriptContractTest' -Dsurefire.failIfNoSpecifiedTests=false test 2>&1 | tail -40
```

**期望**：2 tests, 2 failed (ClassNotFoundException + NoSuchFileException)
**这正是 red 阶段**——证明 bug 真实存在。

### 2.4 Green 阶段（修复后）

- 创建 `org.ruoyi.ipd.controller.IpdAuthExceptionHandler` 类（最小骨架即可）
- 从 .codex 副本拷贝 `migrate_native_db.py` 到主仓
- 重跑红测试 → 应转绿
- 再跑 P072 + P081 AcceptTest test-compile → 不再缺符号

---

## 三、本会话 OPS-09 合规再声明

- 本会话**未写**任何 Red 测试（属主协调会话写范围）
- 本会话**未改**任何实现代码
- 本会话**未绕过**并发写单一写入者约束
- 本会话**未**触发假绿形态（不会"先把缺符号测试改成现存类"骗转绿）
- 主协调会话拿到本登记 + 上一份 §归仓阻塞登记 后，可按 red/green 顺序闭环
