# P2-4.2 主/附加项目定义与超额备案审核 验收记录（2026-09-06）

执行会话：Qoder 执行会话（按 owner 拍板「◇即预解锁」规则，在 P2-4.1 ◇ 交付基线上叠加）。

## 1. 验收标准逐条对照

| AC | 口径 | 实现 | 证据 |
|---|---|---|---|
| AC-TEAM-11 反例 | 已绑 2 个，第 3 个未录备案 ⇒ 禁止绑定 | bindMember 五参重载：active==threshold-1 且 approvalRef 空白 ⇒ ServiceException | P242 `thirdBindWithoutApprovalRejected` |
| AC-TEAM-11 正例 | 第 3 个携带备案编号 ⇒ 允许，编号 trim 落库+审计 | `.approvalRef(active == threshold-1 ? approvalRef.trim() : null)`，审计 afterData 含 memberType/approvalRef | P242 `thirdBindWithApprovalAllowed` |
| 上限硬顶 | 第 4 个即使带备案也拒绝 | `active >= threshold` 先于备案分支 ⇒ 拒绝 | P242 `fourthBindRejectedEvenWithApproval` |
| 主/附加定义 | 首个活跃绑定=PRIMARY，其余=ADDITIONAL | `memberType = active == 0 ? "PRIMARY" : "ADDITIONAL"` | P242 `firstBindIsPrimary` / `secondBindIsAdditional` |
| 并发不突破 | 计数真实下推+锁 | 计数 wrapper 条件 person_id+exit_date IS NULL，`.last("FOR UPDATE")` 锁 person_id 索引 | P242 `concurrencyLockSegment`（捕获 SQL 段断言三要素）|
| 阈值配置驱动 | allowance.projectCountThreshold 生效 | SystemConfigService.getIntValue，缺省回退 3；threshold<=0 防御回退 3 | P242 `thresholdIsConfigDriven`（threshold=2 场景）|
| P2-4.1 兼容 | 4 参旧签名零回归 | 4 参 = approvalRef=null 的 5 参委托；P241 全量回归绿 | P242 `fourParamLegacyDelegation` + P241 9/9 |

## 2. 测试证据（时间戳为准）

- P242AcceptanceTest：`Tests run: 9, Failures: 0, Errors: 0` @2026-09-06 00:20:46
  （命令：`mvn -o -q -pl ruoyi-modules/ruoyi-ipd -Dtest=P242AcceptanceTest test`）
- P241 回归：`Tests run: 9, Failures: 0, Errors: 0` @00:21:32
- 全模块：TOTAL tests=663 failures=0 errors=2 skipped=22 @00:21:49——**2 错全在 P063，与本卡无关**（定责见 §4）

## 3. 真库证据（ipd_dev @127.0.0.1:13306）

- 迁移：`docs/script/sql/update/2026-09-06-ipd-p242-member-type-approval.sql`（幂等 information_schema 判列）apply @00:23:46；回读 `member_type varchar(16)`、`approval_ref varchar(64)` 两列在位（基线探针先确认两列原不存在，非静默跳过）
- 冒烟事务 @00:24:14：INSERT(id=999901, ADDITIONAL, PC-SMOKE-242)→回读一致→`SELECT COUNT(*) ... FOR UPDATE` 在事务内执行成功(lockcount=1)→DELETE 残留 0→ROLLBACK 后残留 0，零残留
- 配置种子：`system_configs` 中 `allowance.projectCountThreshold=3` 在位（只读探针）

## 4. P063 既有回归定责（非本卡引入）

- 现象：全模块跑时 P063AcceptanceTest 2 Errors（`can not find lambda cache for DeletionRequest`）
- 定责链：git stash 全部在途改动→单跑 P063 **8/8 绿** @00:22:16→stash pop 恢复→单跑 P063 仍 2 错 @00:22:45 ⇒ 由工作树在途状态引入
- 根因归属：`DeletionRequestService.java`（git M，兄弟会话在途修改，配套 `2026-09-06-ipd-perf01-deletion-escalate-index.sql`，主题正是 escalate）与 P063 挂的两个 escalate 用例完全吻合；本卡改动仅 ProjectMember 族文件，与 DeletionRequest 零交集
- 处置：不碰兄弟 WIP 文件（单一写入者原则），留其会话自愈/复核会话跟进

## 5. 叠加改动边界（相对 P2-4.1 交付基线快照）

- 新增：P242AcceptanceTest.java（215行9用例）、2026-09-06-ipd-p242-member-type-approval.sql
- 叠改：ProjectMemberService（javadoc+五参重载+限额/备案/FOR UPDATE 段，4 参签名原样保留）、ProjectMemberController（BindRequest 加可选 approvalRef、MemberView 加 memberType/approvalRef、bind 调五参）、ProjectMember domain（memberType/approvalRef 两字段）
- 越卡适配（登记）：P241AcceptanceTest setUp 增 1 行 lenient stub 喂饱新增的 threshold 查询（Mockito 严格模式对未 stub 调用爆 PotentialStubbingProblem）——P241 断言零改动
