# P1-9.1 存量项目导入与历史缺失标记（2026-09-05）

## 结论

**VERIFIED**：超管 `POST /api/v1/projects/legacy-import` 创建 `source=LEGACY` 项目；申报阶段之前的动作标 `HISTORICAL_MISSING`（**不伪造 DONE**）；门禁视同满足；审计 `PROJECT_LEGACY_IMPORT`；批量错误行隔离。

## 交付

| 项 | 说明 |
|----|------|
| DDL | `docs/script/sql/update/2026-09-05-ipd-legacy-import.sql`（已灌 `ipd_dev`） |
| API | `POST /legacy-import`、`POST /legacy-import/batch`（`requireAdmin`） |
| 服务 | `LegacyImportService`；`GateEngine` 认历史缺失 |
| 单测 | `P191AcceptanceTest` **6/6** |

## 真库证据摘要

- 无 `missingHistoryAck` → 400
- `declaredStage=develop` → `currentStage=DEV`，标记含 C11 不含 D05，status 仍 `NOT_STARTED`
- CONCEPT checklist：C11 `ok=true`，reason 含「历史缺失」
- batch：成功 + 短名失败同行返回
- 证据：`.codex/ipd-dev/runtime/evidence-p191.json`
