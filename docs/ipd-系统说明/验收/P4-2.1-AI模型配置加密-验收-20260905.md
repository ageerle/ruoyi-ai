# P4-2.1 AI 模型配置加密密钥与脱敏返回 — 验收证据（2026-09-05）

## 1. 卡面与决策

- 主责 AC：AC-AI-01（provider/endpoint/apiKey/model/temperature/maxTokens 均可配置；密钥加密存储）
- 关联 BR：BR-AI-01；spec 页48（batch-04-pages-38-49.md:857）
- **加密选型（用户拍板 2026-09-05）**：AES（ruoyi-common-encrypt `EncryptUtils.encryptByAes/decryptByAes`），
  主密钥来自**环境变量 `IPD_AIMODEL_ENCRYPT_KEY`**（`@Value` 直读，不动任何 yml；密钥不进代码仓）。
  卡面 BLOCKED_PERMISSION 前提（「无加密依赖、存储位置待裁决」）至此不成立。

## 2. 交付物（allowedPaths 内）

| 文件 | 说明 |
|---|---|
| domain/AiModelConfig.java | 实体；列对齐 dbc75862 预铺 ai_model_configs（见 §4） |
| mapper/AiModelConfigMapper.java | MP mapper |
| dto/AiModelSaveReq.java / AiModelView.java | 对外契约（View 无 apiKey，仅 maskedKey） |
| service/AiModelConfigService.java | 加密/脱敏/fail-fast/同名 409/enable 互斥/probe 白名单消息 |
| controller/AiModelConfigController.java | 7 端点，@SaCheckPermission + requireAdmin/requireInternal |
| test/.../P421AcceptanceTest.java | 11 用例 @Tag("dev") |

超字面最小必经改动（登记）：
- `ruoyi-modules/ruoyi-ipd/pom.xml`：+ruoyi-common-encrypt 依赖（BOM 已管版本）
- `security/IpdPermissionCode.java`：+`ipd:ai-model:list` / `ipd:ai-model:edit`
- `security/IpdRolePermissionCatalog.java`：list→READ_SET、edit→ADMIN_WRITE
- `test/.../P032HttpAcceptanceTest.java`：**越卡面顺手修复既有回归**（见 §5）

## 3. 安全行为与测试证据

- 密钥加密：明文进/密文出（AES-base64）；主密钥缺失 → 写操作 fail-fast INTERNAL_ERROR，verify insert never（不退化明文）
- 不回显：任何端点只给 maskedKey（前4****后4，基于密文计算）；HTTP $.data.apiKey doesNotExist
- update apiKey=null 不触碰密文列（MP null 跳列）；换钥审计 beforeData=keyRotated，afterData 仅 maskedKey/provider/model/enabled
- 连接测试失败不泄露凭证：127.0.0.1:1 必拒连；消息只含 host+失败类别，断言不含明文/密文
- 参数边界：temperature>2 / ftp 端点 / 短密钥 → 10001；0 与 2 合法
- enable 全局唯一：两次条件 UPDATE（先清位后置位），captor 断 SQL segment 含 is_active

测试证据（surefire 报告）：
- `P421AcceptanceTest`：**Tests run: 11, Failures: 0, Errors: 0, Skipped: 0**（2026-09-05 23:4x，适配后）
- 全模块回归：**total=593 failures=0 errors=0 skipped=22**（22 为非 dev tag 用例，历来如此）

## 4. DDL 冲突适配（重要事件，QA 复核重点）

真库 apply 时发现 `ai_model_configs` 已存在（create if not exists 静默跳过），结构与首发设计不一致：

- 既有表来源：**dbc75862**「Wave2 并行落盘—12表DDL」（2026-09-05 15:56 提交，注释明确「P4-x AI 模型配置」），
  已由兄弟会话 apply 进 ipd_dev；`TenantExcludesConsistencyTest` 亦锁定该表名在租户排除清单
- 处置：**撤回我方 DDL**（docs/script/sql/update/2026-09-05-ipd-p421-ai-model.sql 已删除），
  实体/服务**适配既有列**：model_name / endpoint_url / api_key_encrypted / config_json / is_active；
  temperature/maxTokens 进 config_json（P4-2.2 可扩展 timeoutMs/retryTimes/预算/限流而不再改表）
- 对外 API 契约不变（AiModelSaveReq/AiModelView 形状稳定，P0-10.48 前端依赖不受影响）
- 真库验证：SHOW CREATE TABLE 回读一致；按实体列集冒烟 INSERT→回读（is_active=0、JSON_VALID(config_json)=1）→DELETE，
  零行残留；uk_model_name 预检（同名 409）+ DB 唯一键兜底

## 5. 越卡面修复登记：P032HttpAcceptanceTest（既有回归，非本卡引入）

- 现象：`updateReturnsNewValueAndInvalidated` NPE（actor is null）
- 根因归属：**fc4830f3**（P0-3.3）把 `SystemConfigController.update` 改为 `actor.id()`，
  仅同步了 service 层 P032AcceptanceTest，漏了 HTTP 层 P032HttpAcceptanceTest（其 setUp 无 requireAdmin 返回值 stub）；
  该提交自述仅跑了「P032 3 用例」（service 层），HTTP 层 6 用例未跑
- 基线复核：`git stash` 我方全部改动后单跑 P032Http **同样失败**（1 Error）→ 与本卡无关的铁证；stash pop 恢复后修复
- 修复：setUp 补 `doReturn(new IpdActor(9L,"admin","SUPER_ADMIN",null)).when(ipdPermission).requireAdmin()`
  （403 用例各自的 doThrow 后置覆盖默认，不受影响）；修复后 6/6 绿

## 6. 遗留（不阻塞验收）

- 应用级 HTTP 冒烟（打包起服打真实端点）：与 P4-1.1/P1-3.3 同因——需全仓打包且与兄弟会话 WIP 撞车，登记待打包窗口
- 生产部署须注入 IPD_AIMODEL_ENCRYPT_KEY（32 字节）；缺省时服务拒绝写密钥（fail-fast），不降级

## 7. 结论

AC-AI-01 正反例全部落地，11/11 + 全模块 593/593 绿，真库列对齐冒烟通过；建议 ◇ inreview 转 QA 独立复核。
