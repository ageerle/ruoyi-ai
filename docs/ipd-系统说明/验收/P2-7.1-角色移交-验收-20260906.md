# P2-7.1 单项目角色移交与承接资格 · 验收记录（2026-09-06）

## 1. AC 对照

| AC | 期望 | 实现 | 用例 |
|---|---|---|---|
| AC-HAND-01c | 组长代离职人一键移交 ⇒ 可操作、移交成功 | `HandoverService.initiateOnBehalf`（requireLeaderOrAdmin；反查在任绑定须 RESIGNED/FROZEN_PENDING_HANDOVER；冻结窗口 + 建单 + 直落 COMPLETED） | `onBehalfOneShotCompletes` |
| AC-HAND-01d | 名下项目全清 ⇒ 自动 DISABLED + 企微解绑 + 禁止登录 | `disableIfAllCleared`：活跃绑定计数=0 时 set account_status=DISABLED、wecom_user_id=null、wecom_bound_at=null + ACCOUNT_DISABLED_AFTER_HANDOVER 审计；禁止登录由既有 DISABLED→Scope.NONE→401 兜底（P0-7.3 语义，本卡未重复 HTTP 级验证） | `freezeWindowThenDisableWithWecomUnbind`、`notAllClearedKeepsAccount`（反例） |
| AC-HAND-03 | 移交完成前访问该项目只读，编辑接口返回 3xxxx | 冻结窗口由 `freezeForHandover` 置 FROZEN_PENDING_HANDOVER，IpdAuthService.Scope.HANDOVER_ONLY→20002（P0-7.3 已交付语义）；set 段断言覆盖 | `freezeWindowThenDisableWithWecomUnbind`；**PARTIAL**：编辑接口 3xxxx 的 HTTP 层回归未在本卡 PATHS 内展开（依赖 P2-7.2/P0-7.3 联动补证） |
| AC-HAND-06 | 只移交研发 PM，市场 PM 不变 | `exitForHandover(projectId, personId, role)` 按三元组精确退出；接手绑定仅目标角色 | `onlyTargetRoleMoves` |

承接资格正反例：角色不匹配拒（`toRoleMismatchRejected`）、离职拒（`toResignedRejected`）、重复在途移交拒（`duplicateDraftRejected`）、超项须备案号且落库（`overQuotaRequiresApprovalRef`，复用 P2-4.2 bindMember 五参重载全链校验）、原绑定消失中止（`exitZeroRowsAborts`）、本人 DRAFT→仅接手人 accept（`normalFlowDraftThenAcceptByRecipientOnly`）。

## 2. 测试证据（worktree 隔离跑法）

主工作树持续被兄弟会话在途半成品挡编译（见 §4），改用 `git worktree add /tmp/ipd-p271-wt HEAD` 独立工作树，检出 HEAD（667ca4b3）+ 复制本卡 12 个在途文件后构建——闭包自洽，不触碰主工作树 target/ 与兄弟 WIP：

- 01:12:41 `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='P271AcceptanceTest,P242AcceptanceTest' test` → **P271 Tests run: 10, Failures: 0, Errors: 0**；**P242 回归 Tests run: 9, Failures: 0, Errors: 0**；BUILD SUCCESS exit 0（/tmp/p271-wt-run2.log）
- 01:13:51 全模块 `mvn -o -pl ruoyi-modules/ruoyi-ipd test` → **678 跑，Failures 0，Errors 1，Skipped 22**（exit 1，/tmp/p271-wt-full.log）。唯一错误 `P032HttpAcceptanceTest.updateReturnsNewValueAndInvalidated:80`（IpdActor NPE），定责见 §4，与本卡零交集。22 skipped 为 dev profile 按 @Tag 过滤的既有基线。

## 3. 真库证据（ipd_dev @ 127.0.0.1:13306）

- DDL `docs/script/sql/update/2026-09-06-ipd-p271-handover-role-note.sql` 幂等 apply：01:55:45 前完成（00:55:45）回读 `handover_role varchar(16)`、`note varchar(1000)` 两列在位（information_schema 回读）
- 冒烟事务（00:56:05）：INSERT handover_records(id=999801, 'PROJECT', 101, 201, 'RD_PM', JSON 载荷, 'P2-7.1 冒烟移交', 'DRAFT') → 回读一致 → SELECT ... FOR UPDATE lockcnt=1 → DELETE → ROLLBACK，残留 0 行
- handover_records 表本体为 P0-7.3 既有交付（0 行起步，状态机 DRAFT|CONFIRMED|COMPLETED 既有 CHECK 约束内适配）

## 4. 工地与定责（多会话共享工作树）

1. **主工作树主编译挡板**：兄弟在途 `BidInvitationService.java:49 非法的表达式开始`（M 未提交），maven all-or-nothing 主编译持续失败；与本卡 PATHS 零交集，未触碰。
2. **HEAD 自身缺文件**：已提交 `RequirementChangeService` 引用 `RequirementMapper`，但后者为未跟踪文件（兄弟提交漏 add）→ 纯 HEAD 不可编译；worktree 方案中从主工作树补复制 `RequirementMapper.java` + 工作树版 `Requirement.java` 解决。
3. **P032 基线错误定责**：`git log -L 69,69:SystemConfigController.java` → `fc4830f3 09-05 22:29 feat(ipd,参数版本链): P0-3.3` 将 `update(key, value)` 改 `update(key, value, actor.id())` 并去掉 requireAdmin()，P032 测试未同步 mock actor → HEAD 基线自挂 1 错。非本卡引入，留 P0-3.3 卡主跟进。
4. **P271AcceptanceTest.java 曾被吃**：01:0x 发现磁盘上 318 行测试文件连同 git status ?? 记录消失（兄弟会话 git 操作）；从本会话 transcript 逐条回放恢复（初版 heredoc + 1278/1291 两次修复补丁），补修回放缺口 1 处（156 行 initiateOnBehalf 5→6 参补 approvalRef=null）。现磁盘文件 322 行为本会话恢复+修复后版本。
5. **target/classes 两度被清**：maven-status 增量缓存亦出现「Nothing to compile 但 class 缺失」假象；worktree 方案天然规避。

## 5. 边界与遗留

- AC-HAND-03 编辑接口 3xxxx HTTP 层回归：**PARTIAL**，待 P2-7.2 批量移交或 P0-7.3 联动补 HTTP 级证据。
- AC-HAND-01d 禁止登录：DISABLED→Scope.NONE→401 为 P0-7.3 既有语义，本卡验证到「账号态落 DISABLED + 审计」为止。
- 设计偏差登记：状态机落既有 DDL 的 DRAFT→COMPLETED（CONFIRMED 中间态未用）；handover_role/note 为本卡新增列（表 0 行无包袱）。
- 全模块 678 中 1 错为 P032（fc4830f3 引入），修复责任在 P0-3.3 卡主；本卡交付面 P271 10/10 + P242 回归 9/9 全绿。
