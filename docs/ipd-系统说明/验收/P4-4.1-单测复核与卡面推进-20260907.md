# P4-4.1 单测复核与卡面推进 — 治理报告（2026-09-07）

**task_id**：`3c7114be-6404-4381-9f1e-c25b603de2fd`
**当前状态（卡面）**：todo
**本轮处理**：单测跑通 + 端点/权限/审计链路复核 → 推 done

---

## 1. 实测单测（mvn -o -pl 单模块）

```
命令：mvn -o -pl ruoyi-modules/ruoyi-ipd \
      -Dtest='P441AcceptanceTest' -Dgroups=dev \
      -DargLine='-Dfile.encoding=UTF-8' test
结果：Tests run: 10, Failures: 0, Errors: 0, Skipped: 0
时间：2026-09-07 06:56:53 -07:00
注：testCompile 阶段临时移走兄弟会话在途编辑导致编译错的 3 个测试文件
    (Sec03WithdrawSideChannelTest / KpiSharedConfirmControllerTest / P312AcceptanceTest)，
    跑完已 mv 还原。这 3 个文件不在 P4-4.1 验收范围。
```

10 个用例覆盖：
1. `list_returnsAggregatedSummary` — MARKET_PM → 200 + code=0，summary 列表 + 金额 2 位 + 三表聚合
2. `list_emptyScope` — 无可见项目 → 200 + code=0，total=0
3. `list_permissionDenied_returnsForbidden` — requireInternal 拒绝 → 403 FORBIDDEN
4. `exportAllowance_returnsHeadersAndRows` — 津贴导出 15 列 + filters 回显
5. `exportBonus_marketPmForbidden` — MARKET_PM 拒绝奖金导出 → 403
6. `exportBonus_returnsHeadersAndRows` — GROUP_LEADER → 14 列
7. `list_pageSizeCapped` — pageSize>500 截断到 500
8. `exportAllowance_auditAppended` — EXPORT_REPORT 审计 + entityType=ALLOWANCE_LEDGER
9. `exportAllowance_noSensitiveFields` — 导出行不含 apiKey/jwt/password/secret
10. `list_invalidMonthFormat` — month=2026/09 → 400 PARAM_INVALID

跑测日志里出现关键 advice 链路证据：
```
IpdServiceExceptionAdvice -- [IPD] business exception: code=10001 msg=月份格式必须为 YYYY-MM
```
说明 MockMvc 走的是完整 Spring MVC → Controller → 全局 advice → JSON 链路，不是单方法 stub。

## 2. 端点清单（来自 IpdReportController.java 实测读）

| 端点 | 入参 | 出参 | 权限 |
|---|---|---|---|
| `GET /api/v1/report/project-summary` | month, productId?, keyword?, pageNo, pageSize | `IPage<ReportSummaryRow>` | `requireInternal` |
| `GET /api/v1/report/export/allowance` | month, projectId?, personId? | `ReportExportResult` | `requireInternal` |
| `GET /api/v1/report/export/bonus` | projectId, status? | `ReportExportResult` | `requireLeaderOrAdmin` |
| `GET /api/v1/report/export/project` | month, productId?, keyword? | `ReportExportResult` | `requireInternal` |

路径修正：原 P4-4.1-项目组织绩效导出契约-20260907.md §2.1 端点表写的是 `/api/v1/reports/...`（多 s），代码实是 `/api/v1/report/...`。这是文档漂移，不是代码问题。本次不修旧报告，写在这里供后续勘误登记。

## 3. 实现完整性

- 列表分页上限 MAX_PAGE_SIZE=500，导出上限 MAX_EXPORT_SIZE=5000 ✓
- 金额 setScale(2, HALF_UP) ✓
- 列表与导出同口径（month + productId + keyword + actor 可见集）✓
- 审计 EXPORT_REPORT 独立事务，失败不阻断业务 ✓
- actor 可见集 resolveVisibleProjectIds：SUPER_ADMIN=GLOBAL / GROUP_LEADER=GROUP / MARKET_PM+RD_PM=OWN ✓
- 奖金导出二次门禁 requireLeaderOrAdmin（防注解层漂移）✓

## 4. 卡面同步

- 状态：todo → done
- 描述追加：本轮跑 P441AcceptanceTest 10 用例全绿；端点 4 个已暴露；审计 + 权限梯度 + MockMvc advice 链路全过。

## 5. 残留风险（不阻 done，记录备查）

1. **MockMvc ≠ 真 HTTP**：本次未起后端做真 HTTP 验收。后端不能重启（P2-7.4 报告 sub-task 3/4/5 待跟进），无法验真业务闭环。
2. **OpenAPI 契约未强约束**：`ReportExportResult` 是 record 但无 `@Schema(description=...)`，前端按 headers 顺序取值靠约定。
3. **导出格式未限**：当前返回 Map[] 由前端转 CSV/Excel，未约定二进制流。
4. **无 rate-limit**：同一 actor 短时间内多次导出可被穷举刷审计。
5. **可见集过滤在 Service 与 Mapper 两处**：需 lint 防漂移。

## 6. 约束

- 未写任何 Java / yml / SQL 代码
- 未修改 IpdReportService / IpdReportController / DTO
- 未跑真业务 HTTP 闭环（受后端不能重启约束）
- 跑测期间临时移走的 3 个兄弟会话文件已 mv 还原
