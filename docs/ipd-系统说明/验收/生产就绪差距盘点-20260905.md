# 生产就绪差距盘点（第二方治理轮 · 2026-09-05）

> 执行者：Qoder 第二方治理会话（用户本轮明确授权：系统性梳理生产就绪待办 + **完整更新看板**）
> 时间窗：15:26–15:40（本机）　基线 HEAD：起 `7b13a409`（15:27:42，DEF-1 真库闭环证据）
> 方法：OPS-09 错峰只读验证（单模块 `-o` 无 `-am` 无 `clean`）+ 活体 HTTP 探针 + 看板/镜像/代码三方对账；看板写入集中在 §7 所列动作，前后各跑 `manage.py check` 守卫。

---

## 一、总结论

**距离生产就绪尚远，但阻塞结构已经清晰**：后端 P0/P1 核心 67 卡 done，真正卡住「可上线」的是三层：

1. **U0 安全线未闭环**（SEC-01 / SEC-02 均 inreview，活体实证缺口仍在）；
2. **前端 49 页 47 缺**（QA-07 实测 2 partial/47 missing，跨仓工程 P0-10.x 几乎全部未动）；
3. **全量签注链未启动**（QA-03/04/05/06/07/08 六张独立验收卡 + P0-9.1 真实业务链总验收全为 todo）。

生产就绪判定：**NOT READY**；最短关键路径见 §6。

## 二、U0 安全线（活体实证，本轮亲测）

| 卡 | 状态 | 本轮证据（均带时间戳） | 判定 |
|---|---|---|---|
| SEC-02 基础对象授权与全部现存IPD入口隔离 | inreview | 15:29:57 活体 16039（15:23:55 启动，含 P0-4.1 timestamp 修复，**非 stale**）no-token：`/api/v1/system-configs`、`/api/v1/audit-logs` → **500/90001**；对照 `/api/v1/projects` → 401/20001。工作树核对：兄弟在途 `IpdRolePermissionCatalog.java` dirty diff 仅补 `ipd:system-config:*`（**未提交**），`audit-log:*` 码仍缺；`IpdPermissionExceptionHandler.assignableTypes` 仍只 7 控制器（缺 AuditLog/SystemConfig/CoefficientChange/LaunchDateChange） | **仍开**。缺陷 A-audit + B 未收口；修复半截在脏树，勿以「已在修」当已修 |
| SEC-01 绑定会话操作人与四角色接口权限 | inreview | `Sec01AcceptanceTest` 13/13 绿（12:56 R4 主协调独立复现）；但卡片治理闭环三步（更新cell/推status/全模块回归）owner 未走完；12轮 write-differentiation RBAC 仍 PARTIAL | **维持 inreview**，owner=兄弟安全泳道收口，本轮不抢翻（只登记不代翻） |
| AUD-GOV-01 全局闭环治理盘点与蜂群执行 | inreview | owner=Qoder 主会话（root-94ae），R4 明文「不抢翻 status」 | 本盘点报告作为其收口输入，翻卡归 owner |

DEF-1（U0 阻断，GateElement 审计 JSON 列）：已 FIXED——`b74f46bf` 修复 + `GateElementAuditJsonTest` 第二方复跑 4/4 绿（15:23:57–15:24:09）+ 兄弟真库闭环 v6 矩阵 62/62、活体建要素 200、审计 `JSON_VALID` 3/3（证据 commit `7b13a409`，15:27:42）。**遗留问题=SSOT 未纳管致 drift=True，本轮已纳管收口（§7）**。

## 三、inreview / inprogress 逐卡裁决（5+4）

| 卡 | 裁决 | 依据 |
|---|---|---|
| P1-6.1 Gate要素定义管理校验与版本生命周期 | inreview 维持 | 缺 `P161AcceptanceTest`；生命周期/新包验收未齐（第九轮即判 PARTIAL）；DEF-1 修复后超管建要素通道刚通，版本生命周期主流程尚未真库复验 |
| P0-7.3 Refresh退出与会话即时撤销 | inreview 维持 | 缺 `P073AcceptanceTest`；P0-10.1 注记「正在接P073真正refresh」在途 |
| P0-10.1 / P0-10.2 前端登录/首登改密 | inprogress 维持 | 浏览器正向改密/企微/完整密码策略未验，自注 PARTIAL |
| P1-11.1 新硬件项目到阶段推进真实验收 | inprogress 维持 | 全链剧本真实验收未完 |
| P1-4.2 真实附件上传下载和动作归属鉴权 | inprogress 维持 | OSS 真实读写 + SEC-04 依赖未闭环 |

## 四、测试守护缺口（假绿风险面）

- **done 卡无按卡名守护**：API-02（缺 `Api02AcceptanceTest`）。P1-5.1/P1-8.1 本轮降级处理：语义守护实际存在——复跑 `GateEngineTest` **12/12** + `StageActionServiceTest` **13/13** 绿（15:29:57 起后台批次，BUILD SUCCESS），覆盖 log 所述 P1-5.1/P1-8.1 全部行为断言；缺的只是按卡名可追溯性，风险从「假绿」降为「守护命名缺口」。
- **inreview 卡无守护**：P0-7.3、P1-6.1（见 §三）。
- **真库门控默认跳**：全量 devTag 324 中 18 项被 `@EnabledIfSystemProperty(ipd.scope.mysql.enabled)` 门控跳过（第十三轮 15:14 全量态），CI 默认不跑真库——涉钱/权限/删除类卡必须靠显式真库批次（如 P131 18/18）补盲。
- **Mock 绿 ≠ HTTP 闭环** 已两次实锤（DEF-1 与缺陷 A/B）：新增涉审计/鉴权写路径的卡，验收命令必须含真库 HTTP 反例。

## 五、业务主干与运维部署缺口（165 todo 主题地图）

**分布**：P0 系 63（其中前端 49 页卡 + 汇总卡 7；真实后端缺口见下）、P2 系 30、P3 系 28、P4 系 15、QA 6、OPS 2、SEC 1、DB 1。

1. **参数体系整线未动**：P0-3.2（参数管理API/激活版本）→ P0-3.3（版本持久化/不可变快照）→ P0-3.4（Gate/津贴/奖金真实消费者验收）三连 todo——`system_configs` 只有 seed，AC-CFG-02/03、AC-HR-05、AC-GLB-10 主责验收无实现载体。
2. **删除审核引擎未完**：P0-6.1（分级删除申请/审核/意见持久化）、P0-6.3（撤回并发与2+2工作日升级）todo（P0-6.2/6.4 等兄弟卡进度以板为准）。
3. **企微线**：P0-7.4（Mock绑定/扫码/离职解绑）todo。
4. **P0 总验收**：P0-9.1（真实登录、删除、审计业务链）todo——**P0 阶段的出口卡**。
5. **数据模型缺口**：DB-02（批量创建16个缺失域对象）U1 todo。
6. **通知与运维**：OPS-05（站内通知/可执行待办事件）、OPS-06（业务健康/审计故障/任务失败监测）todo——生产运行必备可观测性。
7. **部署配置雷区（本轮实扫）**：`demo.enabled: true`（application.yml L320-322）**仍开启**，上线前必须关闭或交付明确的环境覆盖方案；tenant excludes 已有 IPD 独立认证注释段（L143），新增租户共享表时需继续登记；prod 密钥/JWT 走敏感字段守卫，无泄露证据。
8. **前端 49 页**：QA-07 实测 2 partial/47 missing（本仓外 `ruoyi-ipd-web`），是交付物形态上最大的可见缺口；P2/P3/P4 共 73 卡为后续阶段业务纵深（绩效/AI/奖金等），可按里程碑分批，不阻塞首版生产就绪。

## 六、关键路径（生产就绪最短序）

1. **SEC-02 收口**（兄弟在途 Catalog diff 提交 + assignableTypes 四控制器补齐 + 重部署活体复验 401/403 矩阵）→ 解锁 SEC-01 治理闭环、QA-03 全矩阵、SEC-04。
2. QA-03/QA-04 独立验收跑通 → P0-9.1 真实业务链总验收。
3. 参数线 P0-3.2→3.3→3.4 + 删除线 P0-6.1→6.3 补齐（含各自 AcceptanceTest）。
4. 前端 49 页按批次推进（P0-10.1/10.2 收口 → 03–12 → 后续批次）→ QA-07 解除 BLOCKED。
5. 运维：OPS-05/OPS-06 上线前必需（监测与通知）；QA-05 性能、QA-06 恢复演练在灰度前完成。
6. **QA-08 249AC 全量执行与交付结论签注**——最终出口，所有阻断卡有负责人和下一步方可宣布生产就绪。
7. 配置收尾：`demo.enabled=false` 交付基线 + 部署文档（禁以 dev profile 上生产）。

## 七、本轮看板动作记录（用户授权「完整更新看板」范围内）

1. **DEF-1 纳管收口**：manage.py KEY 正则扩 `DEF` 族（先例：AUD-GOV 扩容）；SSOT 镜像补 DEF-1 行（✅ done + 全证据链）；看板卡补托管块 → sync → unmanaged 归零、drift 归 False。
2. **AUD-GOV-02 治理卡建档**：本盘点轮按先例登记为镜像/看板卡（done，证据=本报告），归属「Qoder 第二方治理会话」，避免与 AUD-GOV-01（root-94ae owner）重复/抢翻。
3. **SEC-02 证据 note 登记**（`manage.py set SEC-02 inreview --note …`，**不翻状态**）：15:29:57 活体 500 复验 + 修复半截在脏树的事实入卡。
4. **不抢翻清单**（证据不足或归属他人）：SEC-01、AUD-GOV-01、P1-6.1、P0-7.3、P0-10.1/10.2、P1-11.1、P1-4.2 全部维持现状态。
5. 写板全程：前检 drift → 单进程原子操作 → 后检 check；镜像/manage.py 改动留工作树由主协调泳道统一 commit，本报告单文件隔离提交。

## 八、证据索引

- 本报告：`docs/ipd-系统说明/验收/生产就绪差距盘点-20260905.md`
- 前轮关联：`inreview-8卡只读回归-20260905.md`（§七 15:19–15:24 复核对齐）、`ABC依次执行-devTag全量验证+缺陷复评-20260905.md`、`SEC-02-QA04-真库HTTP复核-20260905.md`
- 关键提交：`b74f46bf`（DEF-1 修复+4单测）、`7b13a409`（DEF-1 真库闭环 v6 62/62）、`cfdbca20`（P0-5.4）、`84fac6e8`（QA-03 矩阵）
- 复跑记录：GateElementAuditJsonTest 4/4 @15:24:09；GateEngineTest 12/12 + StageActionServiceTest 13/13 @15:29:57 批次（BUILD SUCCESS）
- 活体探针：15:24:22（16043 stale jar `p041`）与 15:29:57（16039 fresh 15:23:55）两次同判 500——排除「探针打旧 jar」假红后坐实

## 九、收口进展更新（15:42–15:52 · 用户指令「按照建议执行」）

> §一–§八为 15:26–15:40 时点快照，保留不改写。本节为关键路径第一刀落地后的最新真值。

### 9.1 §二 U0 安全线 → **SEC-02 已 CLOSED（done）**
- 缺陷 B：兄弟 `6628ab3b`（15:34:49）advice 全局覆盖非白名单控制器；16039 活体（15:37:53 启动，非 stale）no-token 三端点 401/20001 @15:42:36。
- 缺陷 A-audit：本会话修复 `8fa62686`——Catalog ADMIN_WRITE 补 `ipd:audit-log:list/verify/export`（超管专属），一并入库兄弟在途 A-system-config 脏树码；契约锁 `Sec02AuditCatalogAcceptanceTest` 5 测（反射比对控制器注解字面量与目录授予集）。
- 真 HTTP 全绿 @16045（`ruoyi-admin-sec02a.jar` 自有实例）：**v7 管理端点矩阵 43/43**（15:47:39）＋ **v6 业务矩阵 62/62 重跑**（15:47:51）＋ no-token 401（15:45:22）；单测 17/17（15:44:25）。
- 证据：`QA-03-matrix-result-v7-SEC02收口-{管理端点,业务回归}.json`、脚本 `QA-03-matrix-v7-SEC02收口.py`。

### 9.2 看板推进
| 卡 | 快照时状态 | 现状态 | 说明 |
|---|---|---|---|
| SEC-02 | inreview | **done ✅** | 双缺陷修复＋105 真 HTTP 格全绿，证据 note 入卡 |
| QA-03 | todo | **inreview ◇** | 矩阵实测收口（62+43 格＋审计覆盖核查全过）；残留 SEC-04 集成验收依赖 |
| DEF-4 | （不存在） | **todo ⬜ 新建** | 见 9.3 |

### 9.3 新发现 DEF-4：审计哈希链全量 BROKEN（U1）
verify 端点返回 chain=BROKEN、断裂 368/381。定性：**链接层仅 2 断点**（seq=1 缺失＋seq=150 竞态失配@03:18 多实例共库 append 无锁）；**366 断为 curr_hash 重算失配**——主因毫秒不对称（`create_time=datetime(0)` 截毫秒 vs append 用 `currentTimeMillis()` 哈希，且 append 内两次取 now）。311 行无 JSON 仍断 → 排除 JSON 列规范化主因。影响：AC-AUD-03 防篡改失效，**阻塞 P0-9.1**。修复要点已写入 DEF-4 卡验收项（时间戳秒级对称＋同一 Date 对象＋seq 竞态防护＋verify 锚定库内首行＋历史链重建＋契约测试）。

### 9.4 关键路径刷新（原 §六 序 1 已完成）
1. ~~SEC-02 收口~~ ✅（15:51）
2. **DEF-4 修复**（新增，阻塞 P0-9.1 审计链验收）
3. SEC-04 集成验收（附件下载/审计导出/需求池）→ QA-03 收 done
4. P0-6.1 删除线两卡 → P0-9.1 综合验收
5. 其余原序不变（参数线、前端、OPS-05/06、QA-05/06/08）

生产就绪判定：**仍 NOT READY**（U0 安全线已闭环，但 DEF-4/SEC-04/P0-6.1/P0-9.1/前端 49 页缺口未动）。

---

## 十、DEF-4 闭环 + DEF-5 新缺陷（16:04–16:26 · 关键路径第 2 刀）

> §九为 15:52 时点，保留不改写。本节为 DEF-4 修复闭环后的最新真值。

### 10.1 DEF-4 → **CLOSED（done）**：四层根因全修
§9.3 仅识别到三层根因，本轮补出**第四层（最关键、隐蔽）**：

| # | 根因 | 表现 | 修复 | commit |
|---|---|---|---|---|
| ① | verifyChain 误用 `orderByDesc` + 硬编码 GENESIS/seq=1 | 结构性全行假断（368=当时全量） | 升序遍历 + 锚定库内实际首行 | `7cae2138` |
| ② | 写读毫秒不对称（`datetime(0)` vs `currentTimeMillis`，且两次取 now） | curr_hash 重算失配 | 同一 Date 对象 + `secondMillis` 截秒 | `7cae2138` |
| ③ | `selectLast → insert` 无锁竞态 | seq=150 prev 失配 | `uk_audit_seq` 冲突重试（`FOR UPDATE` 被 DB 权限禁，见 10.2） | `7cae2138`+`c23fd1f2` |
| ④ | **MySQL `datetime(0)` 对毫秒四舍五入（≥.500 进位），而 `secondMillis` 是截断** | 约半数**新写行**读回 +1s → 失配；rebuild 后立即再断在同 seq（实测 406/407） | append 写库前 `createTime` 毫秒归零 | `5b95a9d0` |

第④层定位方法（可复用）：rebuild 后 verify 仍剩 1 行断裂，且**第 2 次 rebuild 修完又断在同一 seq** → 排除并发污染，锁定「写入侧自污染」；再用 `tz_probe` 临时表实测 `.400/.500/.700/.999` → 存储为 `.000/.001/.001/.001`，证实四舍五入语义。

### 10.2 架构约束：DB 层最小权限与 FOR UPDATE 不可用
`ipd_app` 表级对 `audit_logs` 仅 `SELECT, INSERT`（G-02 只追加意图）→ `SELECT ... FOR UPDATE` 报 `SELECT with locking clause command denied` → **login 500/90001**（审计写在登录路径上）。
- 适配：竞态防护改**纯 `DuplicateKeyException` 重试**（权限内可跑）；长期方案 = QA-04 泳道 `audit_log_chain_heads` 原子递增（兄弟归属，`AuditLogService` 不引用，避免撞车）。
- `rebuildChain` 走临时授权：GRANT UPDATE @16:13:48 → 重建 → REVOKE @16:19:44（持权 ~6min）。链自洽时 rebuild 为 no-op（零 UPDATE），不需授权即可 200。

### 10.3 验收证据（16045 `ruoyi-admin-def4.jar`，PID 64280）
- 单测 `AuditChainSymmetryTest` **8/8 绿** @16:18:05（新增 ≥.500 进位契约测；`dbTruncated`→`dbRounded` 校正库语义）。
- 真 HTTP `DEF-4-审计链自洽验证-20260905.py` **24/24 ALL PASS**：rebuild fixed=404→`chain=OK`/0 断裂；**6 次连续写入后仍 OK**（第④层回归锁）；rebuild 幂等 fixed=0；seq 零跳号零重复；`rebuild-chain` 越权矩阵 NOAUTH 401/20001、MARKET/LEADER/RD 403/30001、ADMIN 200。
- SEC-02 无回归：v7 矩阵复跑 **43/43 PASS** @16:24:41，`verify链状态=OK 断裂数=0`（§9.1 时为 BROKEN/368）。
- 库态：397 行/395 断裂 → **424 行/0 断裂**（含兄弟实例并发写入行）。

### 10.4 新发现 DEF-5（U1，已建卡，**未自行修复**）
- **实证** @16:22:04：以 `ipd_app` 身份 `UPDATE audit_logs ... WHERE 1=0` 与 `DELETE FROM audit_logs WHERE 1=0` 均 exit 0 **放行**。
- **根因**：MySQL 权限**累加**（全局→库→表→列，任一上层授予即生效）。库级 `GRANT SELECT,INSERT,UPDATE,DELETE ON ipd_dev.*` 与表级 `GRANT SELECT,INSERT ON ipd_dev.audit_logs` 并存 → 库级 DML 覆盖表级收紧，**G-02「只追加」的 DB 层强制实际未生效**（§二/§三对"DB 层强制"的判断需据此下修）。
- **连带解释**：DEF-4 期间对表级 UPDATE 的 GRANT/REVOKE 对运行时无实效（REVOKE 后 rebuild 仍 fixed=5）；FOR UPDATE 被拒属另一路径（锁定读检查表级权限）。
- **修复方向**：REVOKE 库级 DML 改全逐表授权（表级列表已近乎完备，须先比对 `information_schema.tables` 与 `mysql.tables_priv` 差集确保零功能回归），或触发器/只写视图隔离。
- **未自行执行**：直接 REVOKE 立即冲击全部在跑实例（16039/16044/16045/81711），需停写窗口 + 全量回归；建议归 QA-04 DB 结构泳道或 SEC 线（与 P1-4.2 迁移权限设计同源）。探测项已固化为脚本 M8b（当前 0/2 PASS）。

### 10.5 看板与关键路径刷新
| 卡 | §9.2 状态 | 现状态 |
|---|---|---|
| DEF-4 | todo ⬜ 新建 | **done ✅**（board_total 244，drift=false） |
| DEF-5 | （不存在） | **todo ⬜ 新建**（U1，待认领） |

关键路径（原 §9.4 序 2 已完成）：
1. ~~SEC-02 收口~~ ✅（15:51）
2. ~~DEF-4 修复~~ ✅（16:24）
3. **SEC-04 集成验收**（附件下载/审计导出/需求池）→ QA-03 收 done
4. **DEF-5** 与 P0-6.1 删除线并行（同属"数据不可篡改/不可误删"证据链）→ P0-9.1 综合验收
5. 其余原序不变（参数线、前端 49 页、OPS-05/06、QA-05/06/08）

生产就绪判定：**仍 NOT READY**——U0 安全线（SEC-02）与审计链完整性（DEF-4/AC-AUD-03）已闭环，但 DEF-5（只追加未真强制）、SEC-04、P0-6.1、P0-9.1、前端缺口未动。

### 10.6 遗留（非阻塞）
- 兄弟旧 jar 实例（16039 @15:37:53 / 16044 @15:25:58 / 81711 @15:51:03）不含 `5b95a9d0`，继续写审计会再产毫秒污染行 → **全实例升级后需复跑一次 rebuild 终验**（`AuditLogController.rebuildChain` javadoc 已预警运维顺序）。
- 登录限流：同 IP 同账号 60 秒最多 5 次 → 批量脚本须控频次，或改用 `export/scope` 等非登录写入源产生审计行。
