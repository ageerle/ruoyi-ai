# P1 项5 `idx_sa_project_code` 真实库 apply 状态核验

- 执行会话：主协调（owner 2026-09-05 指令项5）
- 取证时间：2026-09-05 20:47:11（默认 2 库）→ 21:07:34（`--all` 全库），全程**只读 SELECT `information_schema`**，未执行任何 ALTER
- 工具：`docs/ipd-系统说明/验收/p1-ddl-apply-check.py`
- 证据件：`ddl-apply-check-result-20260905.json`（2 库）、`ddl-apply-check-all-20260905.json`（17 库）
- 凭证纪律：沿用 QA-06，从 `.codex/ipd-dev/config/mysql-client.cnf` 解析，不出现在命令行与输出

## 1. 判定：SQL 已 commit，但**任何库里都还没生效**

| 对象 | 出处 commit | 真实库状态 |
|---|---|---|
| `stage_actions.idx_sa_project_code` | `8e861719`（19:52）→ `docs/script/sql/update/2026-09-05-ipd-stage-action-project-code-index.sql` | **17 个含 `stage_actions` 的库全部 MISSING**（21:07:34） |
| `launch_date_change_requests.uk_ldcr_pending_project`（项1a） | `83559abd` → `2026-09-05-ipd-launch-date-pending-unique.sql` | 有该表的 4 库（`ipd_dev` / `ipd_restore` / `ipd_perf` / `ipd_qa04`）**NOT_APPLIED**；其余 13 库连表都不存在 |
| `launch_date_change_requests.pending_project_id` 生成列（项1a 等价物） | 同上 | NOT_APPLIED |
| `launch_date_change_requests.version` 列（项1b 乐观锁） | 同上 | NOT_APPLIED |

⇒ 项1a/1b/项5 目前都是「**Java 侧代码已就位、DB 侧约束未生效**」的半套状态。尤其项1a：并发防护的兜底那一层（唯一索引）在活库里不存在，应用层查重仍是裸 TOCTOU。

仓库无 Flyway/Liquibase（`*.xml` / `*.yml` 全文 0 命中）→ `docs/script/sql/update/` 下的迁移**只能人工/DBA apply**，这就是它会长期漂在「已 commit」的结构性原因。

## 2. 可以安全 ADD 的证据

- `duplicate_pending_rows = []`（17 库全空，21:07:34）：`status='PENDING_SECOND' AND del_flag='0'` 下没有同项目多行 → 加部分唯一索引等价物不会因存量冲突失败。
- 索引 SQL 本身用 `information_schema` + `PREPARE/EXECUTE` 幂等封装，重复执行安全。
- 应用真实数据源经核验：`ipd_dev` @ `127.0.0.1:13306`（MySQL 8.0.46，125 表），socket `.codex/ipd-dev/run/mysql.sock` 与 TCP 13306 是**同一实例**（`SELECT @@port, @@socket`）。`application-dev.yml` 写的 `127.0.0.1:3306/ruoyi-ai` 在本机不存在（3306 连接被拒），真实运行走 gitignored 的 `.codex/ipd-dev/config/application-ipd-local.yml`。

## 3. 顺带发现（不在项5 指令范围内，只登记不擅动）

1. **36 个非系统 schema**（全实例 40 − 4 系统库），其中 `ipd_ops03_restore_*` ×8、`ipd_ops03_fixture_*` ×2、`ipd_p053_test_*` ×2、`ipd_p142_test_*` ×1 等是集成测试留下的孤儿库。清理属破坏性操作，**需 owner 裁决**，本批一律未动。
2. **`person_roles` 表在所有库都不存在**，但兄弟 `2481a92a`（20:36）已把它登记进 `application.yml` 的 `tenant.excludes`。核对结论：`batch_missing_tables.sql:180-181` 里该表的 `CREATE TABLE` 是**注释状态**（「待 owner 裁决；若需多角色支持则取消注释」），且 Java 全仓无引用 → 属「超前登记」，当前无害（对不存在的表加 excludes 是 no-op）。同批 9 张表里其余 8 张在 `ipd_dev` 均已 EXISTS，只此一张缺。
3. `ruoyi-ai`（dev yml 里的库名）在本机不存在 —— 与 §2 末条同源，已作为过期事实同批回写 `AGENTS.md`（见本文末「回写」节）。

## 4. 待办（DBA / owner）

| 步 | 动作 | 影响 |
|---|---|---|
| D1 | 在 `ipd_dev`（应用真实库）apply `2026-09-05-ipd-stage-action-project-code-index.sql` | R8-AUTO-8 的 `loadByCode` `.in()` 优化才真能吃到索引 |
| D2 | apply `2026-09-05-ipd-launch-date-pending-unique.sql`（含 `uk_ldcr_pending_project` / `pending_project_id` / `version`） | 项1a/1b 从「半套」升到「约束生效」 |
| D3 | 决定 `ipd_restore` / `ipd_perf` / `ipd_qa04` 三个仍在使用中的镜像库是否同步 | 不同步则验收/压测环境的并发语义与 dev 不一致 |
| D4 | 裁决 36 个孤儿测试库去留 | 影响备份体积与 `--all` 类扫描的信噪比 |
| D5 | 裁决 `person_roles` 是否真要建表（决定 excludes 那条登记是保留还是删） | 与 RBAC 多角色设计绑定 |

**本项状态：PARTIAL** —— 核验已闭环（结论可复现、有 JSON 证据件），但「apply」本身按 owner 指令属 DBA 动作，未完成即不得标 done。

## 5. 同批回写的失效事实（因本次核验才发现）

| 文件 | 原断言 | 现事实 |
|---|---|---|
| `AGENTS.md` | 「`demo.enabled=true`（application.yml:293）…真实开发先关掉」 | 默认已是 `false`（R8-P0-2）；改为「一旦被翻回 true 才拦截」并去掉行号引用 |
| `AGENTS.md` / `CLAUDE.md` | 「`tenant.excludes`（application.yml:148）」 | 实为 :198；两条均改为按键名引用，并新增「本仓 yml 行号分钟级漂」的成因说明（兄弟在途未提交编辑即推号） |
| `CLAUDE.md` | 「Sa-Token 默认 JWT 密钥是 `abcdefghijklmnopqrstuvwxyz`（application.yml:127）」 | 现值 `${SA_TOKEN_JWT_SECRET_KEY:}` 无内联默认；dev 兜底值以 `devOnly-` 前缀显式标记 |
| 新增事实（两处） | 无 | 本机真实数据源 = `.codex/ipd-dev/config/application-ipd-local.yml` → `127.0.0.1:13306/ipd_dev`；无 Flyway/Liquibase ⇒ 「SQL 已 commit ≠ 约束已生效」 |
