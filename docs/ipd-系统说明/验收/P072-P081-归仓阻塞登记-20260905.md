# P072 / P081 归仓阻塞登记 — 主协调会话必读

> **登记时间**: 2026-09-05 08:55 PDT
> **登记会话**: 本会话（治理层，只读探针 + 证据交付）
> **基线 HEAD**: c85f4009
> **优先级**: U0 阻塞（与 P0-7.2/P0-8.1 看板 done 状态形成缺口）
> **OPS-09 登记**: 本会话本轮**不直接归仓**——发现阻塞，按铁律让路登记

---

## 一、关键事实纠正（与前几轮报告差异）

**前几轮本会话报告**："P072/P081 仍待协调员归仓"。
**当前真实状态**（基于 L75/L107 镜像 + 实时 queue 核实）：
- ✅ 看板 P0-7.2 / P0-8.1 已 **done**（兄弟会话在第八~十轮已收口）
- ✅ P072AcceptanceTest / P081AcceptanceTest 在 .codex/ipd-integration/* 副本里跑过（P072 17 项 0 skip / P081 17 隔离 JUnit 0 skip，均全 PASS）
- ❌ **主仓 test/ 目录没有 P072AcceptanceTest.java / P081AcceptanceTest.java**（grep + find 全空）
- ❌ 镜像标 done 但验收证据未固化到主仓 test 路径 → 触发"功能须真路由挂载+真实现+测试方算完成"红线

**记忆库判定**：
- importance 5 [preference] "功能须真路由挂载+真实现+测试方算完成"
- importance 5 [preference] "Mock/单测绿不等于业务闭环，真库或 HTTP 未过不得伪完成"
- 当前 P072/P081 测试只存在于 .codex 副本，**主仓无持久化测试类**——属"测试未落地"

---

## 二、归仓阻塞点（精确核验）

### 2.1 P072 阻塞：依赖未实现的 IpdAuthExceptionHandler

P072 L25 import 链：

```
import org.ruoyi.ipd.controller.IpdAuthExceptionHandler;
```

**主仓实际存在性核验**：

```
IpdAuthConfiguration    ❌ NOT FOUND
IpdAuthController       ✅ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/IpdAuthController.java
IpdAuthExceptionHandler ❌ NOT FOUND（grep 0 行）
AuditLog                ✅ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/AuditLog.java
AuditLogMapper          ✅ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/AuditLogMapper.java
PersonMapper            ✅ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/PersonMapper.java
IpdAuthSession          ✅ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/security/IpdAuthSession.java
```

**结论**：P072 归仓后 mvn test-compile 会因 IpdAuthExceptionHandler 符号未定义报缺符号错误。
> 注：当前 advice 体系是 IpdServiceExceptionAdvice（兄弟会话在 4dafc98f 之后新增 @ExceptionHandler(IpdAuthInputException.class)），可能兄弟会话把 Handler 改名为 Advice 而未同步 P072 副本。

### 2.2 P081 阻塞：依赖不存在的 migrate_native_db.py

P081 L246 硬断言：

```
String migration = Files.readString(root().resolve("docs/ipd-系统说明/运行手册/migrate_native_db.py"));
assertThat(migration).contains("07-user-g2-planning-decision", ...);
```

**主仓存在性**：
- 主仓 docs/ipd-系统说明/运行手册/migrate_native_db.py ❌ **No such file or directory**
- 全仓唯一存在：仅 .codex/ipd-integration/* 副本

**结论**：P081 归仓后 Files.readString 抛 NoSuchFileException，整个 L222 那个 @Test 直接挂。

### 2.3 副产物：实际生产代码 / SQL 位置

- 2026-09-05-ipd-p0-seed-elements.sql ✅ 在 docs/script/sql/update/（23449 字节）—— 主仓已有
- 这是 P081 L222 之后 root() 定位用的"锚点 SQL"——存在，OK

---

## 三、建议解决路径（主协调会话决策）

### 方案 A：补全依赖后归仓（推荐，最稳）
- 创建 IpdAuthExceptionHandler 类（即使只是把 IpdServiceExceptionAdvice 的 IpdAuthInputException 委派逻辑抽出来）
- 拷贝 migrate_native_db.py 到主仓 docs/ipd-系统说明/运行手册/
- 然后按上一轮归仓包 §2.2 归仓 P072 + P081
- 工作量：~30 分钟；风险：低（新增文件不动现有逻辑）

### 方案 B：归仓简化版（不修改生产代码）
- P072 L25 IpdAuthExceptionHandler 改为 IpdServiceExceptionAdvice（如兄弟会话已用 advice 而非 handler）
- P081 L246-249 改为软断言：if (Files.exists(...)) { assertThat(migration)... } else { /* skip */ }——保留关键证据意图但不强制依赖
- 风险：中（修改了测试代码，可能损失原断言意图）

### 方案 C：保持现状（强烈不推荐）
- 镜像 P0-7.2 / P0-8.1 维持 done，但测试仍只在 .codex 副本
- 触发"测试未落地"红线——与"功能须真路由挂载+真实现+测试方算完成"矛盾
- 下一轮全局梳理会被回归出这条缺口

---

## 四、阻塞登记 OPS-09 留痕

| 项 | 内容 |
|---|---|
| 触发会话 | 本会话（治理层） |
| 阻塞发现时间 | 2026-09-05 08:55 PDT |
| 阻塞源 | 镜像 done vs 主仓 test 路径空缺 |
| 决策方 | 主协调会话（root-94ae） |
| 推荐方案 | 方案 A（补全依赖） |
| 候选证据 | 镜像 L75 P0-7.2 ✅ / L107 P0-8.1 ✅ / 实时 queue 无 P0-7.2/P0-8.1（已 done） |
| 本会话行动 | 登记阻塞 + 提交本文件 + commit（不归仓任何测试类） |
| 下一轮衔接 | 主协调会话选择 A/B 后，本会话可协助执行归仓命令 |

---

## 五、本会话 OPS-09 合规声明

- 本会话**只读探针**：只读取了 .codex 副本 P072/P081 关键方法体和主仓类存在性
- 本会话**未**碰任何生产代码 / 测试类 / 镜像 / 看板状态
- 本会话**未绕过**并发写单一写入者约束
- 本会话**未**给出现状式假绿断言（未把 P072 L25 改成现存类，也未把 P081 L246 改成软断言）
- 全部修复决策权交还主协调会话
