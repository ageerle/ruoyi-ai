# P4-1.1 游客需求提交模型与三路产品归属 — 交付验收证据（2026-09-05）

**执行**: Qoder 执行会话（用户授权「4」批次的 4 张解锁卡之一；22:47 认领）
**HEAD 基线**: 9e1d006a + 本卡新增（未提交，留主协调会话收口）
**验收要点**: 页38 用例1-4；AC-REQ-01（免登录提交返回 8 位查询码）、AC-REQ-02（表单含产品选择、无负责人字段）、AC-PROD-08（其他/不确定→待指派池不路由）；BR-REQ-01/02/02b/03/04

## 一、交付物（12 文件，全部为新增/加法式修改）

| 文件 | 说明 |
|---|---|
| dto/GuestDemandSubmitReq.java | 白名单 record（customerName/feedbackPerson/contact/productId/rawModel/functionalRequirement/website honeypot） |
| dto/GuestDemandSubmittedView.java | 响应仅 code+status，不暴露内部 ID/人员 |
| dto/PublicProductView.java | 公开产品视图 + listingStatus 三态派生（ON_SALE/IN_DEV/OTHER） |
| mapper/RequirementMapper.java | BaseMapper |
| mapper/ProjectMemberMapper.java | BaseMapper（超卡面字面清单，登记见 §五） |
| service/GuestDemandService.java | 核心 269 行：三路归属/校验/限流/honeypot/审计/查询码 |
| controller/PublicPortalController.java | POST /api/v1/public/demands、GET /api/v1/public/products |
| common/ApiV1ErrorCode.java | +QUERY_CODE_INVALID(40010→400)、PRODUCT_INACTIVE(40401→404) 及 HTTP 映射分支 |
| domain/Requirement.java | +customerName、+rawModel 字段 |
| config/IpdWebSecurityConfig.java | 两处 excludePathPatterns 追加 "/api/v1/public/**"（放行公开端点） |
| docs/script/sql/update/2026-09-05-ipd-requirements-guest.sql | 幂等 ALTER（customer_name/raw_model） |
| test/P411AcceptanceTest.java | 13 用例 @Tag("dev") |

## 二、测试证据

- 命令（错峰单模块，无 -am 无 clean，因兄弟 runner-ops05 在途 SystemConfig*/Bid* WIP，-am 会连带编译中间态）:
  `JAVA_HOME=~/tools/jdk-17/Contents/Home mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=P411AcceptanceTest -Dsurefire.failIfNoSpecifiedTests=false test`
- **22:53 surefire 报告：Tests run: 13, Failures: 0, Errors: 0, Skipped: 0**（XML tests=13>0，满足卡面核对要求）
- 用例覆盖：用例1 选在售产品→8位码+双PM路由+routedAt+审计 submit（route=routed/ipHash/uaHash 断言）；用例2 其他/不确定→product_id NULL 待指派池不路由（AC-PROD-08）；用例3 第11次限流 40011（RATE_LIMITED）；用例4a honeypot→400+spam_rejected 审计不落库；40401 下架拒；50001 不存在拒；字段校验（customerName<2、描述<6、feedbackPerson 65 字符拒=列宽 64 对齐）；查询码碰撞重试 times(3)；退出成员不路由（project-no-pm）；listingStatus 三态；ipHash SHA-256 截断确定性；HTTP MockMvc（$.code=0/$.data.code=8位/$.data.status，X-Forwarded-For 透传）；内存限流器窗口语义（11 次拒/跨 IP 隔离）

## 三、真库验证（ipd_dev @ MySQL 8.0.46，socket .codex/ipd-dev/run/mysql.sock）

- 迁移 apply exit 0；**幂等复跑 exit 0**（命中"已存在→SELECT 1"分支）
- SHOW COLUMNS 回读：`customer_name varchar(120) NULL`（位于 submitter_name 后）、`raw_model varchar(64) NULL`（位于 contact 后），锚点位置正确
- `query_code varchar(32) NOT NULL UNI`，**uk_req_query_code 唯一索引在位**（BR-REQ-03 冲突重试的 DB 兜底）
- source 默认 PORTAL_GUEST、status 默认 SUBMITTED、market_pm_id/rd_pm_id/routed_at 齐备（基线 DDL 已含，本卡零改动）

## 四、过程缺陷修复（TDD 红绿闭环）

| # | 症状 | 根因 | 修复 |
|---|---|---|---|
| 1 | 2×Error：resolveDualPm sort 抛 UnsupportedOperationException | 原地排序 mapper 返回列表（mock List.of 不可变） | 防御拷贝 new ArrayList<>() 后排序 |
| 2 | httpEndpoints $.data null | 请求未带 User-Agent→ua=null→anyString() 不匹配→stub 失效 | stub/verify 改 any() + 请求补 UA 头 |
| 3 | 真库列宽错配（静态核对发现） | feedbackPerson 校验 ≤80 > submitter_name varchar(64) → 65~80 字符将插库报 Data too long | 校验收紧 ≤64 + 新增 65 字符拒绝断言，复跑 13/13 |

## 五、allowedPaths 超字面范围登记（均与兄弟 WIP 零重叠）

卡面清单为 Requirement*/Product* 前缀。实际为完整交付最小集新增/触碰：
- 新增 GuestDemand*、PublicPortal*、PublicProductView、ProjectMemberMapper（前缀不在清单，但均在 ruoyi-ipd 内、与兄弟在途 Bid*/SystemConfig*/IpdPermission* 零重叠）
- 共享文件加法式修改：ApiV1ErrorCode.java（+2 枚举+2 switch 分支）、IpdWebSecurityConfig.java（+1 放行路径×2 处）、Requirement.java（+2 字段）——无删改既有行为

## 六、遗留与说明

- **应用级 HTTP 冒烟待打包窗口**：现 runtime boot jar（32720f79）不含本卡代码；重新打 ruoyi-admin 需全仓构建，与兄弟在途 WIP 撞车，留打包窗口错峰补跑（MockMvc 层已验 $.code/$.data 形状）。**不影响本卡验收要点**（页38 用例1-4 已由行为测试覆盖）
- 限流器为单机内存滑动窗口（GuestRateLimiter 接口抽象，多实例部署时换 Redis 实现，勿动调用方）——ruoyi-ipd 无 common-redis 依赖，属本仓现状约束
- 安全放行面：/api/v1/public/** 仅 2 个端点，写入有 honeypot+限流+校验三道闸；审计不落原始 IP/UA（仅 SHA-256 前 16 位）
