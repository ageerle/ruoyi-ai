# inreview 8 卡只读错峰回归 + API-03 收口独立复核（第二方 · 只读边界）

> 执行者：第二方独立复核会话（Qoder，root-94ae 主协调器之外）
> 日期：2026-09-05　时间窗：12:32–12:41（本机 PDT）
> 基线 HEAD：起 `c85f4009` → 复核期间兄弟 owner 泳道推进至 `30a98c39`（随动）
> 边界：**未修改任何 Java 源码**（遵 SSOT L311 用户本轮边界 + r2 报告 §七 + OPS-09）；仅只读验证 + 证据/日志归档。
> 用户本轮裁决：① API-03 守只读边界不代写，交付 handoff 规格；② inreview 8 卡只读错峰回归 + 证据归档，依赖阻塞/真链未过者标 PARTIAL，**不翻 done**。

---

## 一、环境与并发态（12:32–12:33）

- 运行环境**全部在线**：后端 `127.0.0.1:16039` HTTP 200；MySQL `13306/ipd_dev` OPEN；Vibe Kanban `62250` HTTP 200。
- 运行 jar = **兄弟会话的** `ruoyi-admin-p151p181.jar`（PID 61407，12:31PM 启动，config `application-ipd-local.yml`）——为 P1-5.1/P1-8.1 集成而起的运行时。
- 错峰条件：build proc=0 时才跑构建/测试；单模块 + 离线 `-o` + 无 `-am` 无 `clean` + 复用产物。
- **热并发实测**：复核 7 分钟内（12:32→12:39）主协调器完成一次大 burst——`done 52→57`，脏文件 `49→15`，HEAD 前进 3 提交（`056640ca` 蜂群归仓 252 测试全绿 → `30a98c39` API-03 补 MockMvc 反例）。看板 drift 一度 `True`（12:32:25 正在 sync SEC-API-01→done）后归 `False`（12:39:18）。

## 二、独立真实验证（亲自执行，全部带时间戳）

| 时间 | 命令/动作 | 结果 | 判定 |
|---|---|---|---|
| 12:32:46 | `mvn -o -pl ruoyi-ipd test-compile` | BUILD SUCCESS（Nothing to compile，产物与源同步） | green gate 当前 OK，第九轮 P064 构造器冲突已消解 |
| 12:34:49 | `surefire:test` 认证/权限/证书包 | **35/35 GREEN**（Sec02 7·SecApi02 4·Api01 4·IpdAuthChangePassword 6·CertTemplate 4·IpdAuthService 10） | 服务/白盒层绿，XML tests>0 skipped=0 |
| 12:35:36 | `surefire:test P131*` + 真库 flag | **64/64 GREEN**（P131AcceptanceTest 46 + P131DatabaseIntegrationTest **18 真库 MySQL 4.19s**） | P1-3.1 真库层绿，XML skipped=0（guard 已开） |
| 12:36:35 | 真 HTTP `POST /auth/change-password` 错原密码 | **HTTP 400 `code=10001` "原密码错误"** | ✅ 非 500/90001，advice handler 活体生效 |
| 12:36:35 | 真 HTTP `change-password` 新密码过短 | **HTTP 400 `code=10001` "个数必须在8和72之间"** | ✅ Bean 校验路径 400/10001 |
| 12:36:35 | 真 HTTP `POST /auth/refresh` 后复用旧 token `GET /me` | refresh 200 发新 token；**旧 token → HTTP 401 `code=20001`** | ✅ P0-7.3 会话轮换：旧 token 立即失效 |
| 12:37:23 | 真 HTTP `PUT/GET /system-configs`（MARKET_PM + 无 token） | **全部 HTTP 500 `code=90001`** | ⚠️ **缺陷**：应 401/403，见 §四 |
| 12:37:53 | 真 HTTP `GET /projects`（无 token / MARKET_PM）、`/products`（无 token） | 无 token→**401 `20001`**；MARKET_PM→**403 `20003`**（首登强制改密）；/products 无 token→**401 `20001`** | ✅ 已提交控制器安全链健康，缺陷非系统性 |
| 12:38:58 | jshell JDBC 读回 `audit_logs`（真库 13306） | **seq=170 \| 陈市场 \| MARKET_PM \| act=FAILURE \| persons:2096266884189016065 \| reason=原密码错误 \| 12:36:35** | ✅ P0-7.2 真链：拒绝审计经 REQUIRES_NEW 独立落库未被回滚 |
| 12:40:28 | `surefire:test Api03AcceptanceTest` 等（新 HEAD） | **Api03 5/5 GREEN**（XML tests=5 failures=0 skipped=0）+ 批次 **40/40 GREEN** | ✅ API-03 done 独立复核**合法**（非假绿） |

**审计 hash 链完整**：`audit_logs` seq 单调 168→173（LOGIN/FAILURE 交替），只追加表未见断链。DB 读回经 JDK17 `jshell` + `mysql-connector-j-9.6.0` 只读 SELECT（无 mysql CLI / 无 python 驱动；凭据取自 `.codex/ipd-dev/config/mysql-app.cnf`，**未打印明文**）。

## 三、逐卡判定（第二方 · 不翻状态）

| 卡 | 现看板态(12:39) | 第二方判定 | 依据 |
|---|---|---|---|
| **API-03** | done | **✅ done 合法（独立复核确认）** | owner 提交 `Api03AcceptanceTest`(30a98c39) 5/5 GREEN + 本会话活体 HTTP 400/10001（错原密码 & 短密码）+ DB 审计 seq170。handoff 规格（`验收/API-03-handoff-spec-20260905.md`）与 owner 实现同向收敛。 |
| **P0-7.2** | done | **✅ done 有据（真链齐）** | `IpdAuthChangePasswordExceptionTest` 6/6 + 活体 HTTP 400/10001 + DB 读回 `PASSWORD_CHANGE_REJECTED`(act=FAILURE reason=原密码错误, REQUIRES_NEW 未回滚)。 |
| **P0-7.3** | inreview | **◐ 真链证据已产，建议 owner 附证收口** | 活体 HTTP：refresh 发新 token 且旧 token 立即 401/20001。**仓内无 P073 AcceptanceTest**（缺 @Tag dev 回归守护）；行为已真链验证。 |
| **SEC-02** | inreview | **◇ KEEP_PARTIAL（缺陷已定位）** | 已提交控制器 401/403 正确；但 `SystemConfigController` 活体 500/90001（§四）。"全部现存 IPD 入口隔离"未满足。 |
| **API-02** | done | **◐ done 但缺 HTTP 层验收测试** | `CertTemplateServiceTest` 4/4（DTO 白名单+权威字段覆写）；**仓内无 `Api02AcceptanceTest`**，HTTP 层反例缺回归守护。轻度 flag。 |
| **P1-3.1** | done | **✅ done 有据（真库齐）** | `P131AcceptanceTest` 46/46 + `P131DatabaseIntegrationTest` 18/18 真库；dep `P1-2.1` 已 done（阻塞解除）。 |
| **P1-5.1** | done | **⚠️ 假绿风险 flag（owner 复核）** | "Gate当前阶段和缺失动作阻断"。**仓内无 `P151AcceptanceTest`**（卡验收要求 @Tag dev + XML tests>0）。运行 jar 名 `ruoyi-admin-p151p181.jar` 暗示 owner 走运行时验证，但仓内无可复跑验收测试证据。 |
| **P1-8.1** | done | **⚠️ 假绿风险 flag（owner 复核）** | "BioCV C12补挂调用接入及非空阶段修复"。**仓内无 `P181AcceptanceTest`**，同上。 |
| **P1-6.1** | inreview | **◇ blocked / KEEP_PARTIAL** | 仓内无 `P161AcceptanceTest`（untracked sibling candidate）；dep `IpdBusinessException`/advice/ApiV1ErrorCode。收口权归 sibling。 |

> 说明：本轮 8 卡中 API-02/P0-7.2/P1-3.1/P1-5.1/P1-8.1 已由主协调器在复核窗内翻 done；本会话**未翻任何状态**（遵单写协调器纪律），仅就其 done 合法性做第二方核验并标注 flag。

## 四、缺陷精确定位：SystemConfigController 未纳入 IPD 安全链（活体 500/90001）

- **现象**（12:37:23，运行 jar `ruoyi-admin-p151p181.jar`）：
  - `PUT /api/v1/system-configs/{key}`（MARKET_PM 已认证）→ **HTTP 500 `code=90001`**（期望 403 `30001`）
  - `GET /api/v1/system-configs`（MARKET_PM）→ **500 `90001`**（期望 403）
  - `GET /api/v1/system-configs`（**无 token**）→ **500 `90001`**（期望 401 `20001`）
- **隔离对照**（12:37:53，同一 jar）：已提交的 `ProjectController`/`ProductController` 无 token→401/20001、MARKET_PM→403/20003 **全部正确**。故缺陷**非系统性**，仅限 `SystemConfigController`。
- **根因假设**：`SystemConfigController`（提交前为 untracked stub，`056640ca` 才归仓）在**运行 jar 构建时点（12:31）**可能未被 `IpdWebSecurityConfig` 的 SaInterceptor 路径覆盖 / 或 `@SaCheckPermission(type=ipd)` + `ipdPermission.requireAdmin()` 抛出的异常未被 advice 正确映射，落入兜底 `Exception → INTERNAL_ERROR(500/90001)`。无 token 也 500（而非 401）→ 强烈指向**拦截器未覆盖该路径**，请求直达方法体后 `session.currentPerson()`/`requireAdmin()` 抛未映射异常。
- **归属**：`SystemConfigController.java` + `IpdWebSecurityConfig` 安全路径配置，owner = 蜂群归仓泳道（`056640ca`）。**本会话不抢改**。
- **建议**：owner 重建 + 重部署 jar 后，对 `/api/v1/system-configs` 复验：无 token→401/20001、MARKET_PM→403/30001（`requireAdmin` 应抛 `IpdPermissionException`→FORBIDDEN）、SUPER_ADMIN→200。**在真链复验通过前，SEC-02 "全部现存入口隔离" 不得整卡 done。**
- **注**：该 500 尝试未写任何审计行（拦截前失败），与 §二 seq170 拒绝审计对照可证审计仅在业务方法内触发。

## 五、看板写入策略（本轮为何不写板）

- 依用户裁决 + OPS-09「drift=True ⇒ 证据优先于写板」：12:32:25 探测 `has_drift=True`（协调器正 sync SEC-API-01），且 12:32→12:39 处于协调器大 burst（done 52→57、drift True↔False 摆动）。
- 本会话**全程未做 `set`/`sync` 看板写入、未手改脏镜像 `.md`**，改用本报告 + `log.md` 锚点追加 + `.codex/ruflo/…/second-party-own-20260905.md` 归属声明三通道归档，避免与协调器写窗碰撞。
- flag 项（P1-5.1/P1-8.1 假绿风险、SEC-02 SystemConfigController 缺陷、API-02/P0-7.3 缺 HTTP 验收测试）交主协调器在 inreview/done 复核流程内处置。

## 六、OPS-09 自律核验

- 未创建新卡、未重复执行同指令、未绕过并发写单一写入者约束、未翻任何状态。
- 未修改任何 Java 源码 / 未手改镜像 / 未 `git commit`/`push`。
- own 卡集与边界见 `.codex/ruflo/global-20260905/second-party-own-20260905.md`。
- 关联交付物：`验收/API-03-handoff-spec-20260905.md`（handoff 规格 + 独立复核佐证）。

---

## 七、剩余待办执行复核对齐（15:19–15:24 · 第二方 · 仍守只读边界）

> 触发：用户「立即完整执行剩余待办事项」。本轮把上文遗留的可核验项跑到**当前真值**并补独立证据；**仍未改 Java、未翻状态、未写板**（边界不变）。

### 7.1 并发态变化（12:42 → 15:19，兄弟泳道大幅推进）

- `log.md` 571 → **777 行**（兄弟持续写，本会话条目仍完整：`grep -c 第二方独立复核会话=4`，首行未截断）。
- HEAD 前进：P0-5.4 `cfdbca2`（审计角色范围，14:46 set done）→ DEF-1 修复 `b74f46bf` → QA-03 `84fac6e8` → 兄弟 7367cfd0 缺陷 ABC 等。
- 看板 **drift 仍 True**，且**新增未纳管卡 DEF-1**（见 7.2）；`counts unchanged 240`。→ 写板窗口未开，本轮**继续不写板**（OPS-09）。
- 运行态：12:41 时我用的 16039 后端已下线；15:19 探测 16039/16040/16041/16042 health=000；兄弟活体后端在 **16043**（pid 79970，jar `ruoyi-admin-p041.jar`）。15:20 新构建 `ruoyi-admin/target/ruoyi-admin.jar`（282MB）。

### 7.2 DEF-1（U0 阻断）独立复核 → 判**已闭环**

- **现象**（兄弟 QA-03 发现）：`SUPER_ADMIN POST /api/v1/gate-elements` 合法 body → 500/90001，`gate_elements` 无新行、`audit_logs` 无 `GATE_ELEMENT` 行，上线以来 100% 失败。
- **根因**：`GateElementService.audit` 曾把纯文本直写 MySQL JSON 列 `audit_logs.after_data` → `MysqlDataTruncation` → 审计与业务同事务回滚。**正是本会话 §四/§五 flag 的「Mock 绿 ≠ HTTP 闭环」同一类缺陷**（单测 Mock 掉 audit 探测不到）。
- **修复已在仓**：commit `b74f46bf`（`+4 回归单测`），`audit()` 改走 `AuditEventData.json("detail", detail)`。静态核验：`AuditEventData.json` 用 `ObjectMapper.writeValueAsString(LinkedHashMap)` 产出合法 JSON（如 `{"detail":"G1/…"}`）→ 满足 JSON 列约束，根因消除。
- **回归测试质量**：`GateElementAuditJsonTest`（@Tag dev，Mockito）捕获传给 `auditLogService.append` 的 `AuditLog` 并 `readTree` 断言 `afterData` 为合法 JSON——锁死被违反的契约本身（非盲目 Mock），真机 MySQL 行为交由 QA-03 HTTP 证据。
- **第二方 green-gate（亲跑，2026-09-05 15:23:57–15:24:09）**：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=GateElementAuditJsonTest test` → **Tests run: 4, Failures: 0, Errors: 0, BUILD SUCCESS**（surefire XML `tests>0`，dev tag 生效）。
- **判定**：DEF-1 owner 泳道（QA-03）已闭环 + 本会话独立复核确认；**建议协调器补 SSOT 纳管后关卡**（现为未纳管 todo）。

### 7.3 SEC-02（U0 紧急）独立活体复验 → 判**仍开（真链未过）**

- **亲测**（2026-09-05 15:24:22，活体后端 16043，只读零写入 no-token GET）：

  | 端点（无 token） | HTTP | code | 期望 | 判定 |
  |---|---|---|---|---|
  | `GET /api/v1/system-configs` | **500** | 90001 | 401/20001 | ❌ |
  | `GET /api/v1/audit-logs/scope` | **500** | 90001 | 401/20001 | ❌ |
  | `GET /api/v1/projects`（对照） | 401 | 20001 | 401/20001 | ✅ |

- **三方交叉印证**：本会话 §四（SystemConfigController 500）+ 兄弟 P0-5.4 记录（`audit-logs/scope` 无 token→500）+ 本轮 15:24 活体复验同结果 → **全局缺口坐实**：拦截器（`IpdWebSecurityConfig`/Sa-Token preHandle）抛出的鉴权异常未进入 `@RestControllerAdvice`（`IpdPermissionExceptionHandler` 仅覆盖 controller 方法级），落到兜底 `Exception`→INTERNAL_ERROR(90001/500)；controller 方法路径（projects）映射正确，故非系统性。
- **注**：16043 跑的是 `ruoyi-admin-p041.jar`（未必最新 15:20 jar）；SystemConfigController 虽已 tracked，但该部署实例仍复现 500——修复须重部署后复验，**SEC-02 整卡不得 done**（与 §四 建议一致）。

### 7.4 其余 flag 当前真值复核

- **P1-5.1 / P1-8.1 假绿 flag**：仍成立——`ls` 确认仓内**仍无 `P151/P181AcceptanceTest`**（兄弟以 `ruoyi-admin-p151p181.jar` 走运行时验证，log.md:576/580，无可复跑 @Tag dev 验收测试留仓）。已翻 done 属「运行时绿但仓内无回归守护」，交 owner/协调器决定是否补测。
- **API-03**：done，仍成立（`Api03AcceptanceTest` 已在仓，12:xx 复跑 5/5）。
- **P0-7.3 / P1-6.1**：仍 inreview；**API-02/P0-7.2/P1-3.1**：done（兄弟推进）。

### 7.5 本轮为何仍不写板（结论）

- 15:19/15:24 两次 `manage.py check` 均 **drift=True + 存在未纳管卡 DEF-1** → 写板会撞协调器 sync 窗并触发 reconcile 护栏（OPS-09 明令：让路，不绕过）。
- 剩余「看板 note 登记」这一遗留待办，**客观上被持续 drift 阻塞**，非本会话回避；证据改落本 §七（自有交付物，零碰撞）。
- 交协调器在 writable window 处置的三项：① DEF-1 补 SSOT 纳管 + 关卡；② SEC-02 全局 401/403 映射修复（建议单列缺陷，勿混入各业务卡）；③ P1-5.1/P1-8.1 是否补 `P151/P181AcceptanceTest` 以消除运行时-only 假绿。
