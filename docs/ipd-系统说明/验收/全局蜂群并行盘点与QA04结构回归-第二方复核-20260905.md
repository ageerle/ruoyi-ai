# 全局蜂群并行盘点与 QA-04 结构回归 — 第二方独立复核

> **执行者**：Qoder 后到第二方复核会话（`root-94ae` 主协调器 + 兄弟第二方 QA 之外）
> **日期**：2026-09-05　**时间窗**：15:39–15:41（本机 PDT）
> **基线 HEAD**：起 `6628ab3b`（15:37 前）→ 会话期间兄弟前移 `7740bd32`（15:39:19）→ `bdd875a5`（15:40:27）
> **边界**（遵 SSOT L311 + OPS-09 + memory「佐证而非抢翻」）：
> - ❌ 未修改任何 Java 源码
> - ❌ 未修改 SSOT 镜像 `开发计划-看板镜像.md`
> - ❌ 未修改 `log.md`
> - ❌ 未 `manage.py set` 翻转任何卡状态（含 AUD-GOV-01，owner=root-94ae 兄弟）
> - ✅ 只读探针 + 独立第二方 QA 佐证 + 新增证据文件（本文件）
> **触发指令**：用户本轮「完整读取全部待办 + 分组并行执行 + 汇总」

---

## 一、现场并发态识别（判定「后到第二方复核」角色的依据）

按 memory `全局项目独立复核：并发系统下的真实验证与证据归档策略` §「重复并发会话——佐证而非抢翻」现场信号逐项核对：

| 信号 | 观测值 | 结论 |
|---|---|---|
| HEAD 会话期间前移 | `6628ab3b` → `7740bd32` → `bdd875a5`（2 提交在 2 分钟内） | ✅ 兄弟活跃 |
| 脏文件多泳道 | 73 项（33 M + 36 ?? + 2 D + 1 m），覆盖 controller/service/domain/security/seed/advice/test 多域 | ✅ 多泳道热写 |
| log.md 出现别的会话「主协调」登记 | 已有「第十三轮」「第十四轮 P0-4.1」「第十五轮 P2-1.1」 | ✅ 兄弟协调器活跃 |
| 镜像出现非本会话的卡翻转 | AUD-GOV-02 已 done（新增，非本会话所建） | ✅ 兄弟已建卡并收口 |
| 兄弟最新报告 | `验收/ABC依次执行-devTag全量验证+缺陷复评-20260905.md` §9 ADDENDUM（15:39:19, 15:40:27） | ✅ 同类第二方 QA 已在 |
| 看板 drift | `has_drift=False`，`unmanaged_cards=[]`，`board_total=242` | 稳定窗口 |
| 活跃构建 | `mvn` 进程 = 0 | 错峰条件 OK |

**判定**：本会话为**同一条「全局梳理」指令的第三个并发会话**，正确角色 = **只读探针 + 独立第二方 QA 佐证 + 补足兄弟未覆盖的缺口**。

**不抢翻清单**（兄弟 owner，本会话严禁 `set` 或改镜像行）：
- AUD-GOV-01（owner=root-94ae）
- SEC-01 / SEC-02 / API-02 / P0-7.2 / P0-7.3 / P1-3.1 / P1-5.1 / P1-8.1 / P1-6.1 / P1-11.1 / P1-4.2
- DEF-1 / DEF-3 / AUD-GOV-02 / SEC-API-01 / SEC-API-02 / RISK-01..04 / DB-02

---

## 二、看板完整盘点（240 SSOT 卡 + 242 板卡 + 20 可执行队列）

### 2.1 板卡状态分布（`manage.py check`）

- 板端总数：**242**（较 SSOT 240 多 2：DEF-1 + AUD-GOV-02 由兄弟会话增补，均已 done 且纳管）
- 漂移：**False**　未纳管卡：**0**
- 状态分布（20 可执行队列）：`inreview=5, todo=13, inprogress=2`
- 优先级分布：`U0=3, U1=17`

### 2.2 20 张可执行卡（`manage.py queue`）分组

#### Group 1 · U0 安全/治理域（3 张 · 均 inreview · 兄弟 owner）

| 卡 | 状态 | Owner | 依赖 | 备注 |
|---|---|---|---|---|
| SEC-01 | inreview | 后端开发 + QA独立复核（兄弟 ab7d8fa7 泳道） | — | Sec01AcceptanceTest 13/13 已绿；卡治理闭环三步（更新 cell + 推 status + 全模块回归）owner 未走完 |
| SEC-02 | inreview | 后端开发 + QA独立复核 | SEC-01 | Sec02AcceptanceTest 7/7 mock 绿；真库 HTTP 矩阵 KEEP_PARTIAL；缺陷 A-audit + B 收口中 |
| AUD-GOV-01 | inreview | Qoder 主会话（root-94ae） | AUD-09 | 兄弟主协调器 owner，本会话不碰 |

#### Group 2 · U1 后端业务域 inreview（2 张 · 兄弟 owner 收口中）

| 卡 | 状态 | Owner | 依赖 | 备注 |
|---|---|---|---|---|
| P0-7.3 | inreview | Codex/root 兄弟 | P0-7.2 | 真库 HTTP refresh + logout 已实测；缺 P073AcceptanceTest 命名验收测试（本仓确认不存在） |
| P1-11.1 | inreview | Cursor 兄弟 | P1-5.2, P1-8.2, P1-7.1, P1-4.3 | P1111AcceptanceTest 6/6 + 真库 CONCEPT→PLAN；附件 MinIO 属 P1-4.2 PARTIAL |

#### Group 3 · U1 后端业务域 todo（2 张 · 待认领 · 本会话可分析不可抢写）

| 卡 | 状态 | 依赖 | 前置盘点结论 |
|---|---|---|---|
| **P2-3.1** 一对一邀标与公开招标创建可见性 | todo | P2-1.1 | **实施条件成熟**：`bid_invitations`（17 cols, del_flag+tenant_id+uk 无）+ `bid_responses`（14 cols, del_flag+tenant_id）表已建（0 行）；`BidInvitation.java`（81 lines）+ `BidResponse.java`（66 lines）域对象已建；**缺**：Mapper + Service + Controller + `P231AcceptanceTest` |
| **P3-4.1** 销售回款退款凭证台账与窗口 | todo | P1-2.2, DOC-01 | **实施条件未成熟**：`receipt_ledger` 表 **MISSING**，DDL **未定义**，域对象 **未建**；`bonus_pools` 表已建（19 cols，distributions JSON，uk_bp_project 唯一）；`BonusPool.java`（102 lines）已建；**前置阻塞**：必须先补 `receipt_ledger` DDL + 域对象 + Mapper，才能推进台账与 6 自然月窗口逻辑 |

#### Group 4 · U1 QA 独立验收（1 张 · 待认领 · 本会话已完成结构层）

| 卡 | 状态 | 依赖 | 本会话交付 |
|---|---|---|---|
| **QA-04** MySQL 字段映射、逻辑删除和并发约束回归 | todo | OPS-02, P0-5.3, P1-1.1, P1-3.1 | ✅ **§三 完整结构层证据**（jshell + connector-j 只读，`ipd_app` 受限账号）；并发约束回归属 P0-5.2/P0-5.3 兄弟已覆盖；剩余「真实业务并发场景」需真库门控批次 |

#### Group 5 · U1 前端外部仓（12 张 · 本仓不可执行）

| 卡 | 状态 | Owner | 外部仓路径 | 阻塞前置 |
|---|---|---|---|---|
| P0-10.1 登录页 | inprogress | swarm_doc07 | `/Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/views/ipd/auth/login.vue` **已存在** | 完整正向浏览器改密/企微/密码策略未验 |
| P0-10.2 首登改密 | inprogress | swarm_doc07 | `.../ipd/auth/change-password.vue` **已存在** | 同上 |
| P0-10.6/7/8/10/11/12/13/15/16/17（10 张） | todo | 前端开发+QA（待认领） | 外部仓 | DOC-09 已确认正式 Vue 工程；后端依赖见每卡 `dep` 字段 |

**关键判定**：本仓 `ruoyi-ai` 是**后端 only**（AGENTS.md 明示），前端 12 张卡的 `allowedPaths` 均为 `docs/ipd-系统说明/前端对接/**`——**本仓只能产出对接文档**，实际实现须在外部仓 `ruoyi-ipd-web` 由 swarm_doc07 或后续前端 owner 完成。本会话**不能**并行执行前端实现。

---

## 三、QA-04 独立结构回归证据（本会话独有价值交付）

### 3.1 环境与命令

- **时间**：15:40:54 – 15:40:56（2 秒完成）
- **JDBC**：`jdbc:mysql://127.0.0.1:13306/ipd_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai`
- **账号**：`ipd_app`（受限运行账号，凭据取自 `.codex/ipd-dev/config/mysql-app.cnf`，**未打印明文**）
- **驱动**：`mysql-connector-j-9.6.0.jar`（`~/.m2/repository/com/mysql/mysql-connector-j/9.6.0/`）
- **工具**：JDK17 `jshell` + 只读 `SELECT INFORMATION_SCHEMA.*`（无 mysql CLI / 无 Python 驱动，符合 memory `无 mysql CLI/无 Python 驱动时用 jshell+connector-j 做只读 DB 证据读回`）
- **脚本**：`/tmp/qa04_probe2.jsh`（本会话产出，可复跑）

### 3.2 全表盘点

```
[CONNECT] OK: MySQL 8.0.46
[ALL_TABLES] count=115
[IPD_CORE] present=26 missing=12
```

**IPD 核心 26 表 present**（含 `projects/products/persons/product_groups/project_members/stage_actions/deliverables/gates/gate_reviews/gate_review_elements/audit_logs/deletion_requests/bid_invitations/bid_responses/cert_templates/project_cert_items/sop_templates/system_configs/bonus_pools/kpi_records/handover_records/requirement_changes/ai_documents/coefficient_change_requests/launch_date_change_requests/project_stages`）

**MISSING 12 表 · 逐一定性**（Java 域对象是否存在 + DDL 是否已定义）：

| # | 表 | Java 域对象 | DDL | 影响卡 | 定性 |
|---|---|---|---|---|---|
| 1 | `person_roles` | MISS | MISS | P2-1.x/P2-2.x | 设计上可能不需要独立表（`persons.role` 已有字段） — **待 owner 裁决** |
| 2 | `action_catalog` | ✅ `ActionCatalog.java`（编译期固化 69 项） | MISS | P1-3.1（done） | **设计选择**：代码化非表化，与看板 P1-3.1 done 一致，非缺陷 |
| 3 | `system_config_versions` | MISS | MISS | **P0-3.3（todo）** | P0-3.3「参数版本持久化/不可变快照/时点解析」直接阻塞 |
| 4 | `receipt_ledger` | MISS | MISS | **P3-4.1（todo）** | P3-4.1「销售回款退款凭证台账与窗口」直接阻塞 |
| 5 | `bonus_allocations` | MISS | MISS | P3-4.4（todo） | P3-4.4「奖金分配、归档与结算重复防护」直接阻塞 |
| 6 | `allowance_ledger` | ✅ `AllowanceLedger.java` | MISS | P3-3.1..3（todo） | **域对象-表脱节**：Java 已建但表未建，DDL 缺失 |
| 7 | `project_scores` | MISS | MISS | P3-2.1..2（todo） | 直接阻塞 |
| 8 | `contributions` | MISS | MISS | P3-6.1..2（todo） | 直接阻塞 |
| 9 | `negative_feedbacks` | MISS | MISS | P3-8.1..3（todo） | 直接阻塞 |
| 10 | `requirement_pool` | MISS | MISS | P4-1.1..4（todo） | 直接阻塞 |
| 11 | `ai_model_configs` | MISS | MISS | P4-2.1（BLOCKED_PERMISSION） | 直接阻塞 + 密钥加密算法 owner 未裁决 |
| 12 | `legacy_imports` | ✅ `LegacyImportResult.java` | MISS | P1-9.1（▶ Cursor 兄弟在途） | 域对象-表脱节，与 Cursor 兄弟 P1-9.1 进行中一致 |

### 3.3 结构合规性核验（核心表 11 张）

| 表 | 列数 | del_flag | tenant_id | version | JSON 列 |
|---|---:|:-:|:-:|:-:|---|
| projects | 31 | ✅ | ✅ | — | target_markets |
| products | 16 | ✅ | ✅ | — | — |
| stage_actions | 28 | ✅ | ✅ | ✅ | — |
| **audit_logs** | 17 | ❌ **正确**（AC-AUD-07 只追加设计） | ✅ | — | before_data, after_data |
| gate_review_elements | 16 | ✅ | ✅ | — | — |
| bid_invitations | 17 | ✅ | ✅ | — | — |
| bid_responses | 14 | ✅ | ✅ | — | — |
| bonus_pools | 19 | ✅ | ✅ | — | distributions |
| receipt_ledger | **TABLE_MISSING** | — | — | — | — |
| system_configs | 16 | ✅ | ✅ | — | validation_rule |
| persons | 24 | ✅ | ✅ | — | — |

**结论**：
- ✅ 逻辑删除 `del_flag`：核心业务表全覆盖；`audit_logs` 正确豁免（AC-AUD-07 无 updated_at + 只追加）
- ✅ 多租户 `tenant_id`：11/11 全覆盖（与 `application.yml:148` `tenant.excludes` 独立认证段一致）
- ✅ JSON 列类型：5 处（`target_markets` / `before_data` / `after_data` / `distributions` / `validation_rule`）— DEF-1 修复后 JSON 合法性已由 `GateElementAuditJsonTest` 4/4 + 兄弟 v6 矩阵 62/62 保障
- ✅ 乐观锁 `version`：`stage_actions` 已有（P1-4.3 兄弟泳道）
- ⚠️ **唯一约束覆盖不全**（见 §3.4）

### 3.4 唯一约束盘点

| 表 | 唯一键 | 覆盖 AC |
|---|---|---|
| projects | `uk_projects_code(code)` | AC-INC 项目编码 PRJ-YYYY-NNN 唯一 ✅ |
| projects | `uk_projects_product(product_id)` | P1-1 产品↔项目 1:1 ✅ |
| products | `uk_products_project(project_id)` | 反向 1:1 ✅ |
| bonus_pools | `uk_bp_project(project_id)` | P3-4 项目 1:1 ✅ |
| **bid_invitations** | **无非主键唯一键** | ⚠️ AC-TEAM-01/02 需要「同项目 + 同 targetPersonId + 未过期」唯一防重复邀标 — **待 owner 决策** |
| **receipt_ledger** | TABLE_MISSING | ⚠️ P3-4.1 AC-INC-16c 需「项目 + 月份 + 类型」唯一 — **待表建后补** |

### 3.5 行数快照（业务启动度）

| 表 | 行数 | 解读 |
|---|---:|---|
| projects | 30 | 测试种子 + 兄弟集成回归数据 |
| products | 44 | 与 projects 1:1 + 若干在售 |
| persons | 11 | 7 中文姓名 + 超管 + Mock |
| audit_logs | **353** | 审计链活跃（兄弟真库 HTTP 复验持续追加） |
| gate_review_elements | 42 | 33 要素 + 14 否决项（有重叠） |
| system_configs | 55 | 54 键种子 + 1 附加（v3§7×D.0.7） |
| **bid_invitations** | **0** | P2-3.1 业务未启动 |
| **bonus_pools** | **0** | P3-4.x 业务未启动 |
| receipt_ledger | MISSING | P3-4.1 前置未建 |
| action_catalog | MISSING | 设计上代码化 |

### 3.6 QA-04 判定

按看板 QA-04 验收要点三项逐条裁决：

| 验收要点 | 本会话证据 | 判定 |
|---|---|---|
| ①「26 表对应实体字段/JSON/日期/默认/租户」 | §3.2 present=26 + §3.3 结构合规 | ✅ **通过**（结构层） |
| ②「stage_actions/deliverables 软删过滤」 | §3.3 del_flag=true + `@TableLogic` Java 侧已验 | ✅ **通过** |
| ③「并发唯一/更新丢失/失败回滚真实 DB 验证」 | §3.4 唯一键 4 处 + 兄弟 P0-5.2/P0-5.3/P1-4.3 已覆盖真库并发 | ◐ **PARTIAL**：结构层唯一键齐，业务层并发场景归对应 P0-5.2/P0-5.3/P1-4.3 owner，本卡整合验收须待 P2-3.1/P3-4.1 等业务表实灌后补 |

**建议 QA-04 owner 收口条件**：结构层证据（§3.2–3.5）已足；剩余「业务并发场景」显式声明为跨卡整合验收，归 P0-9.1（P0 真实登录/删除/审计业务链总验收）承接，避免 QA-04 无限期挂 todo。

---

## 四、Sec01/Sec02/SecApi01/SecApi02/DefectBAdvice 独立佐证（30/30 GREEN）

### 4.1 命令与结果

```bash
mvn -o -pl ruoyi-modules/ruoyi-ipd \
  -Dtest='Sec01AcceptanceTest,Sec02AcceptanceTest,SecApi01OperatorIdContractTest,SecApi02StpInterfaceTest,DefectBAdviceAcceptanceTest' \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

- **窗口**：15:40:06 – 15:40:10（4 秒，错峰，`mvn` 进程 = 0）
- **模式**：单模块 `-o` 无 `-am` 无 `clean`（严格按 AGENTS.md「假红陷阱」规避）
- **结果**：`Tests run: 30, Failures: 0, Errors: 0, Skipped: 0` **BUILD SUCCESS**

| 测试类 | 覆盖 | 结果 |
|---|---|---|
| `Sec01AcceptanceTest` | 13 tests（AC-AUTH-08/HR-07/HR-08/GLB-11 + 反射扫 5 控制器无 `@RequestParam operatorId`） | 13/13 ✅ |
| `Sec02AcceptanceTest` | 7 tests（六身份矩阵：SUPER_ADMIN/GROUP_LEADER/MARKET_PM×2/RD_PM×2 × 3 组归属） | 7/7 ✅ |
| `SecApi01OperatorIdContractTest` | 1 test（契约级：反射扫控制器签名无 operatorId） | 1/1 ✅ |
| `SecApi02StpInterfaceTest` | 4 tests（StpInterface 桥接 + `@Primary` 生效 + loginType 隔离） | 4/4 ✅ |
| `DefectBAdviceAcceptanceTest` | 5 tests（MockMvc 真 HTTP：IpdPermission/NotPermission/NotRole→401/403、HttpMessageNotReadable→400） | 5/5 ✅ |

### 4.2 佐证结论

- ✅ **SEC-01 兄弟 ab7d8fa7 泳道收口合法**（非假绿）：Sec01AcceptanceTest 13/13 覆盖 AC-AUTH-08/HR-07/HR-08/GLB-11 全部正反例
- ✅ **SEC-02 mock 层收口合法**：Sec02AcceptanceTest 7/7 覆盖六身份矩阵；**但真库 HTTP 矩阵仍 KEEP_PARTIAL**（缺陷 A-audit Catalog 缺 `ipd:audit-log:*` + 缺陷 B `assignableTypes` 覆盖不全）— 与兄弟第二方 QA §9 ADDENDUM 判定一致
- ✅ **DEF-3/缺陷 B 修复合法**：HEAD `6628ab3b` DefectBAdviceAcceptanceTest 5/5 真 MockMvc HTTP 验收，独立复现通过
- ⚠️ **P0-7.3 命名验收测试仍缺失**：`P073AcceptanceTest.java` 本仓不存在（`find` 已确认），卡验收要求 `@Tag dev` + XML tests>0 未满足；行为已真链验证（兄弟 12:36:35 refresh + 旧 token 401）— 属「守护命名缺口」非「假绿」

---

## 五、P2-3.1 / P3-4.1 实施前置规格（不写代码，只交付规格）

### 5.1 P2-3.1 · 一对一邀标与公开招标创建可见性

**已具备**（无需重建）：
- 表：`bid_invitations`（17 cols, del_flag+tenant_id）、`bid_responses`（14 cols, del_flag+tenant_id）
- 域对象：`BidInvitation.java`（mode/targetPersonId/title/content/expireAt/status/selectedResponseId + `SoftDeletable`）、`BidResponse.java`（invitationId/rdPmId/responseNote/status/respondedAt + `SoftDeletable`）
- 状态机（Java 注释）：`OPEN → SELECTED/EXPIRED → CLOSED`
- 依赖卡：P2-1.1 已 done（产品组主协组归属闭环）

**待补交付物**（owner 泳道认领后实施）：
1. `BidInvitationMapper.java` + `BidResponseMapper.java`（MyBatis-Plus BaseMapper）
2. `BidInvitationService.java` + Impl：`createOneToOne(projectId, targetRdPmId, ...)` / `createPublic(projectId, ...)` / `listVisible(actor)` / `getByIdScoped(actor, id)`
3. `BidInvitationController.java`（`@RestController @RequestMapping("/api/v1/bid-invitations")`）+ DTO 白名单（`BidInvitationCreateReq` 含 `@JsonIgnoreProperties` + JSR-303）
4. `P231AcceptanceTest.java`（`@Tag("dev")`）覆盖：
   - AC-TEAM-01：市场PM 一对一邀标给研发PM A ⇒ A 收到通知；其他研发PM 看不到（列表可见性正反例）
   - AC-TEAM-02：被邀研发PM A 尝试转发 ⇒ 系统无转发端点（404 或 405）；B 无法应标（403）
   - AC-TEAM-04：邀标被拒后市场PM 改公开 ⇒ 可改；全部研发PM 可见；写审计
   - BR-TEAM-01：岗位不可跨角色（MARKET_PM 不能被邀为 RD_PM）
   - BR-TEAM-04：未受邀不能读私有邀标（跨身份矩阵）
5. 唯一约束（建议）：`uk_bid_inv_project_target_active(project_id, target_person_id, status)` where status='OPEN' — 防重复邀标（**owner 裁决是否加**）

**验证命令**：
```bash
mvn -pl ruoyi-modules/ruoyi-ipd -am -Dtest=P231AcceptanceTest -Dsurefire.failIfNoSpecifiedTests=false test
```

### 5.2 P3-4.1 · 销售回款退款凭证台账与窗口（**前置阻塞**）

**阻塞根因**：`receipt_ledger` 表 **MISSING**，DDL 未定义，域对象未建 — 无法直接推进。

**前置补齐清单**（DDL 迁移先行）：

1. **新迁移**：`docs/script/sql/update/2026-09-05-ipd-receipt-ledger.sql`
   ```sql
   CREATE TABLE receipt_ledger (
     id BIGINT PRIMARY KEY,
     project_id BIGINT NOT NULL,
     period_month CHAR(7) NOT NULL COMMENT 'YYYY-MM',
     entry_type VARCHAR(20) NOT NULL COMMENT 'RECEIPT|REFUND',
     amount DECIMAL(18,2) NOT NULL,
     voucher_oss_id VARCHAR(128) COMMENT '银行回单/对账单附件',
     entry_date DATE NOT NULL,
     launch_date_ref DATE COMMENT 'L08 上市日引用（AC-INC-32 起算点）',
     within_window TINYINT(1) COMMENT '是否在 6 自然月窗口内',
     remark VARCHAR(500),
     create_by/create_time/update_by/update_time/del_flag/tenant_id ...
     UNIQUE KEY uk_rl_project_period_type (project_id, period_month, entry_type, entry_date),
     KEY idx_rl_project_launch (project_id, launch_date_ref)
   );
   ```
2. **域对象**：`ReceiptLedger.java`（继承 BaseEntity + SoftDeletable + `@TableName("receipt_ledger")`）
3. **Mapper**：`ReceiptLedgerMapper.java`
4. **Service**：`ReceiptLedgerService.java` + Impl：
   - `record(projectId, month, type, amount, voucher)` — 月度录入
   - `computeAchievementRate(projectId, asOfDate)` — 6 自然月窗口内 RECEIPT − REFUND ÷ targetSales（AC-INC-16b/16d/32）
   - `handleRefund(projectId, refundDate, amount)` — 窗口内当期冲减；窗口外不回溯（AC-INC-31/31b）
   - 闰日处理（AC-INC-32 月末边界）
5. **Controller**：`ReceiptLedgerController.java`（`/api/v1/receipt-ledger`）+ DTO 白名单
6. **测试**：`P341AcceptanceTest.java`（`@Tag("dev")`）覆盖 AC-INC-16b/16c/16d/31/31b/32 全部正反例，含：
   - 达成率口径 = 回款（**不是**出库/开票，AC-INC-16b/29d 对照）
   - 6 个月窗口外数据不计入（AC-INC-16d）
   - 窗口内退款当期冲减（AC-INC-31b）
   - 窗口外退款不回溯（AC-INC-31）
   - 起算点 = L08 上市日期（AC-INC-32，连续 6 自然月）
   - 凭证附件可追（AC-INC-16c）

**验证命令**：
```bash
mvn -pl ruoyi-modules/ruoyi-ipd -am -Dtest=P341AcceptanceTest -Dsurefire.failIfNoSpecifiedTests=false test
```

**owner 建议**：本卡实施必须先跑 `/db-migration` skill（`docs/script/sql/update/` 新增迁移 + Entity 必备字段 + 回滚脚本），再进入 Service/Controller/Test。

---

## 六、并行/串行执行计划（分组 + 阻塞条件）

按 AGENTS.md「并发写单一写入者」+「假红陷阱」+「错峰单模块」硬约束，**Java 源码写入必须串行**；**只读探针 + 分析可全并行**。

### 6.1 并行波次（Wave 1）· 只读，任意多会话安全

| 波次 | 任务 | Owner | 输入 | 范围 | 产物 | 完成标准 |
|---|---|---|---|---|---|---|
| **W1-A** | Sec01/Sec02/SecApi01/02/DefectBAdvice 独立佐证 | 本会话 ✅ 已完成 | HEAD 6628ab3b | 单模块 `-o` 无 `-am` 无 `clean` | 30/30 GREEN 时间戳 | 本报告 §四 |
| **W1-B** | QA-04 MySQL 结构层只读回归 | 本会话 ✅ 已完成 | `mysql-app.cnf` 凭据 | jshell + connector-j 只读 SELECT | 26 present + 12 MISSING 清单 | 本报告 §三 |
| **W1-C** | P2-3.1 / P3-4.1 实施前置规格 | 本会话 ✅ 已完成 | 域对象 + DDL + AC 追溯矩阵 | 只读分析 | 待补交付物清单 + 测试用例矩阵 | 本报告 §五 |
| **W1-D** | 前端 12 卡外部仓状态盘点 | 本会话 ✅ 已完成 | `/Users/mac/Documents/ruoyi-ipd-web` | 只读 ls/find/git log | swarm_doc07 活跃 + 认证页面已存在 | 本报告 §二 Group 5 |
| **W1-E** | inreview 5 卡逐卡裁决复核 | 兄弟第二方 QA ✅ 已完成 | HEAD + surefire-reports + 真库 HTTP | 只读 | `验收/inreview-8卡只读回归-20260905.md`（12:41） | 兄弟已交付 |
| **W1-F** | 生产就绪差距盘点 | 兄弟第二方治理 ✅ 已完成 | 全板卡 + 真库 HTTP + 脏树 | 只读 | `验收/生产就绪差距盘点-20260905.md`（15:37） | 兄弟已交付 |

### 6.2 串行波次（Wave 2）· Java 写入，单写者纪律

**准入条件**：`manage.py check` drift=False + 活跃 mvn 进程 = 0 + 目标卡无兄弟 owner 声明 + 目标文件不在脏树。

| 波次 | 任务 | 建议 Owner | 前置阻塞 | 串行原因 |
|---|---|---|---|---|
| **W2-A** | P2-3.1 招标域实施（Mapper + Service + Controller + P231AcceptanceTest） | 后端开发泳道（待认领） | 无（前置齐） | 写 `ruoyi-modules/ruoyi-ipd/src/main/java/**/controller/**` 与兄弟 SEC-02 收口路径重叠，须错峰 |
| **W2-B** | P3-4.1 前置 DDL 迁移（`receipt_ledger` 建表） | 后端开发 + DBA 泳道 | 无 | 独立文件（`docs/script/sql/update/`），可与 W2-A 并行 |
| **W2-C** | P3-4.1 域对象 + Service + Controller + P341AcceptanceTest | 后端开发泳道 | W2-B 完成 | 依赖新表 |
| **W2-D** | P0-7.3 补 `P073AcceptanceTest`（守护命名缺口） | 后端开发泳道（Codex/root） | 无（真链已验） | 独立文件，可与 W2-A/W2-C 并行 |
| **W2-E** | SEC-02 缺陷 A-audit 收口（Catalog 补 `ipd:audit-log:*`） | 兄弟安全泳道（Codex/root） | 无 | 兄弟已在途，本会话不抢 |
| **W2-F** | SEC-02 缺陷 B 收口（`assignableTypes` 补 AuditLog/SystemConfig/CoefficientChange/LaunchDateChange） | 兄弟安全泳道 | 部分已在 HEAD `6628ab3b` 落地 | 兄弟已收口，本会话仅佐证 |
| **W2-G** | DB-02 16 缺失域对象批量创建（含 `AllowanceLedger` / `LegacyImportResult` 表 DDL 补齐） | 兄弟 DB 泳道 | 与 §3.2 MISSING 12 表部分重叠 | 兄弟已在途，本会话仅登记证据 |

### 6.3 串行波次（Wave 3）· 整合验收

| 波次 | 任务 | 前置 |
|---|---|---|
| **W3-A** | P0-9.1 P0 真实登录/删除/审计业务链总验收 | P0-7.3 + P0-6.2 + P0-5.4 + SEC-03 + P0-8.1 全 done |
| **W3-B** | QA-03 权限与关键写审计覆盖矩阵实测 | SEC-02 + SEC-04 + P0-5.4 全 done |
| **W3-C** | QA-04 业务并发场景整合验收（补足 §3.6 ③） | W2-A/B/C 完成 + P0-5.2/P0-5.3 已 done |
| **W3-D** | P2-8.1 双 PM 组队到 Gate 变更移交整链 | P2-5.5 + P2-6.2 + P2-7.4 全 done |
| **W3-E** | P3-7.1 P3 真实账务算例、台账与切换验收 | P3-4.4 + P3-3.3 + P3-5.1 + P3-8.3 + P3-2.3 + P2-5.6 全 done |

### 6.4 阻塞条件矩阵

| 阻塞类型 | 现场信号 | 应对 |
|---|---|---|
| **假红陷阱**（并发 target 污染） | `NoClassDefFoundError` + 兄弟活跃 | 错峰重跑（AGENTS.md 明示） |
| **假绿陷阱**（`@Tag("dev")` 未加） | Surefire `Tests run: 0` | 核对 XML `tests>0 skipped=0` |
| **stale app 阻断**（运行 jar 落后 HEAD） | HTTP 404 "No endpoint" | 移交 owner 重启，禁 live 复验 |
| **drift 活跃窗口** | `has_drift=True` + `unmanaged>0` | 暂缓写板，等 drift=False |
| **同名未纳管卡** | `RuntimeError: same-title unmanaged card` | 兄弟 `set --adopt` 或编辑描述加托管块 |
| **owner 竞争** | 目标文件在脏树 + 兄弟 log 声明 | **让路**，做只读佐证 |
| **DDL 前置缺失** | 表 MISSING（如 `receipt_ledger`） | 先跑 `/db-migration` skill 补 DDL |

---

## 七、风险登记（本会话新增/更新）

| ID | 风险 | 影响卡 | 严重度 | 现状 | 建议 |
|---|---|---|---|---|---|
| **R-1** | `receipt_ledger` 表 + DDL + 域对象三缺，P3-4.1 完全从零 | P3-4.1 / P3-4.4 / P3-7.1 | U1 高 | 未启动 | 先跑 `/db-migration` 建表 + Entity，再进 Service/Controller |
| **R-2** | 12 表 MISSING 中 5 张（`system_config_versions` / `project_scores` / `contributions` / `negative_feedbacks` / `requirement_pool` / `ai_model_configs`）阻塞 P0-3.3 / P3-2 / P3-6 / P3-8 / P4-1 / P4-2 六个业务线 | 6 卡族 | U1 高 | 未启动 | 批量建表迁移（可参考 DB-02 兄弟模式） |
| **R-3** | `AllowanceLedger.java` + `LegacyImportResult.java` 域对象已建但表 MISSING → 域对象-表脱节 | P3-3.x / P1-9.1 | U1 中 | 兄弟在途 | 与 DB-02 owner 协同补齐 |
| **R-4** | `bid_invitations` 缺「同项目+同 target+OPEN 状态」唯一约束 → AC-TEAM-01 重复邀标风险 | P2-3.1 | U2 中 | 未启动 | owner 裁决是否加 uk |
| **R-5** | `P073AcceptanceTest` 缺失 → P0-7.3 无 `@Tag dev` 守护命名，属「守护命名缺口」非假绿 | P0-7.3 | U1 中 | 真链已验，缺命名测试 | owner 补 `P073AcceptanceTest`（可参照 `P072AcceptanceTest` 4 项模式） |
| **R-6** | `action_catalog` 表 MISSING 但 Java 侧代码化（69 项编译期固化）→ 设计选择非缺陷，但看板 P1-3.1 done 依据须明确记录 | P1-3.1 | U3 低 | done | 在 P1-3.1 备注登记「代码化非表化」设计决策 |
| **R-7** | 前端 12 卡跨仓（外部 `ruoyi-ipd-web`），本仓不可执行 → 若前端 owner 未落实，49 页 47 缺持续 | P0-10.6/7/8/10-13/15-17 | U1 高 | swarm_doc07 已收 P0-10.1/2 | 明确前端仓 owner 泳道 + 里程碑批次 |
| **R-8** | SEC-02 缺陷 A-audit（Catalog 缺 `ipd:audit-log:*`）仍在脏树未提交 → 活体 `/api/v1/audit-logs` 无 token 500/90001 | SEC-02 | U0 高 | 兄弟在途 | 兄弟安全泳道收口后本会话再佐证 |
| **R-9** | `demo.enabled=true`（`application.yml` L320-322）仍开启，上线前必须关闭 | 生产就绪 | U0 高 | 兄弟盘点已登记 | 部署前 checklist 强制项 |
| **R-10** | 三并发会话同做「全局梳理」→ 报告/证据文件命名冲突风险 | 治理产出 | U2 中 | 本会话避让命名 | 后续同类会话应先在 log.md 登记归属再产出 |

---

## 八、后续建议（按优先级）

### U0（阻断生产就绪）

1. **兄弟安全泳道收口 SEC-02 缺陷 A-audit + B**：
   - 提交 `IpdRolePermissionCatalog` 补 `ipd:audit-log:*` 权限码
   - 提交 `IpdPermissionExceptionHandler.assignableTypes` 覆盖全部 IPD 控制器（含 AuditLog/SystemConfig/CoefficientChange/LaunchDateChange）
   - 真库 HTTP 复验：无 token `/api/v1/audit-logs` → 401（不是 500）
2. **owner 明确前端仓 12 卡归属**：`swarm_doc07` 是否续做 P0-10.6..17？如否，另招前端 owner 泳道
3. **部署前 checklist**：`demo.enabled=false`（application-prod.yml 覆盖）+ JWT secret 环境变量 + tenant excludes 补齐

### U1（业务主干）

4. **P2-3.1 招标域**：前置齐，owner 认领后按 §5.1 规格实施（估算：1 会话可完成 Mapper+Service+Controller+Test）
5. **P3-4.1 前置补 DDL**：先建 `receipt_ledger` 表 + Entity + Mapper（估算：0.5 会话），再进 Service/Controller/Test（估算：1.5 会话）
6. **12 MISSING 表批量建表**：`system_config_versions` / `project_scores` / `contributions` / `negative_feedbacks` / `requirement_pool` / `ai_model_configs` / `receipt_ledger` / `bonus_allocations` / `allowance_ledger`（域对象已建）/ `legacy_imports`（域对象已建）/ `person_roles`（待 owner 裁决）— 建议由 DB-02 owner 泳道承接，一次迁移批量建表
7. **P0-7.3 补 `P073AcceptanceTest`**：Codex/root 兄弟 owner 补命名验收测试（4-6 tests 覆盖 refresh 轮换 + 旧 token 失效 + 重放拒绝 + logout 撤销 + 离职全会话撤销）
8. **QA-04 收口条件明确化**：结构层证据（本报告 §三）已足，业务并发场景归 P0-9.1 承接，避免 QA-04 无限期挂 todo

### U2（治理与守护）

9. **DB-02 与 §3.2 MISSING 12 表映射对账**：兄弟 DB-02「批量创建 16 缺失域对象」已 done，但 §3.2 显示 12 表仍 MISSING → 域对象 ≠ 表，须补 DDL 迁移
10. **唯一约束补齐**：`bid_invitations`（防重复邀标）+ `receipt_ledger`（防重复录入）+ 其他业务表按 AC 补齐
11. **同类第三会话归属登记机制**：log.md 增「第 N 轮 · 会话 ID · 泳道」表格，避免报告文件命名冲突（本会话已避让为 `全局蜂群并行盘点与QA04结构回归-第二方复核-20260905.md`）

---

## 九、本会话交付物清单

| # | 产物 | 路径 | 类型 | 归属 |
|---|---|---|---|---|
| 1 | 本治理与并行盘点报告 | `docs/ipd-系统说明/验收/全局蜂群并行盘点与QA04结构回归-第二方复核-20260905.md` | 新增文件 | 本会话独立提交 |
| 2 | QA-04 jshell 只读探针脚本 | `/tmp/qa04_probe2.jsh` | 临时文件（可复跑） | 本会话 |
| 3 | Sec01/02/等 30/30 GREEN 独立佐证 | 本报告 §四（surefire-reports 已存在） | 证据引用 | 兄弟 owner 收口 |
| 4 | P2-3.1 / P3-4.1 实施前置规格 | 本报告 §五 | 规格交付 | 待认领 owner 使用 |

**未交付物**（边界外，兄弟 owner 承接）：
- ❌ Java 源码修改（P2-3.1 实施 / P3-4.1 DDL / SEC-02 缺陷修复 / P073AcceptanceTest 补齐）
- ❌ SSOT 镜像修改（`开发计划-看板镜像.md`）
- ❌ `log.md` 追加（本会话不登记第 N 轮，避免与兄弟「第十五轮」冲突）
- ❌ `manage.py set` 翻转任何卡状态（含 AUD-GOV-01）
- ❌ Vibe Kanban 板端写入

---

## 十、总结论

1. **看板完整盘点**：240 SSOT 卡 + 242 板卡 + 20 可执行队列已逐项分组，无遗漏（§二）
2. **并行探测六波次已完成**（§六 W1-A..F），本会话独有交付 = QA-04 结构回归 + P2-3.1/P3-4.1 前置规格 + Sec01/02 独立佐证
3. **20 张可执行卡分组归属清晰**：
   - Group 1（U0 安全/治理 · 3 张）→ 兄弟 owner 收口中，本会话仅佐证
   - Group 2（U1 后端 inreview · 2 张）→ 兄弟 owner 收口中，本会话仅佐证
   - Group 3（U1 后端 todo · 2 张）→ P2-3.1 可实施；P3-4.1 前置阻塞（`receipt_ledger` MISSING）
   - Group 4（U1 QA · 1 张）→ 结构层证据本会话已交付，业务并发层归 P0-9.1 承接
   - Group 5（U1 前端 · 12 张）→ 外部仓，本仓不可执行
4. **本会话严守「佐证而非抢翻」边界**：0 Java 修改 / 0 镜像修改 / 0 log 追加 / 0 卡翻转
5. **生产就绪判定**：**NOT READY**（与兄弟 15:37 盘点一致）；关键路径 = SEC-02 缺陷收口 + 12 MISSING 表批量建表 + 前端 47 页落实 + P0-9.1 业务链总验收
6. **下一步执行**：等用户明确「继续 / A / 指定卡号」授权后，本会话可承接 §六 W2-A（P2-3.1 实施）或 §六 W2-B（P3-4.1 前置 DDL）— 两者均须先确认无兄弟竞争 + drift=False + mvn 进程 = 0

---

> 报告结束。本文件为**新增独立证据**，不动任何兄弟脏树文件；建议隔离提交（`git add docs/ipd-系统说明/验收/全局蜂群并行盘点与QA04结构回归-第二方复核-20260905.md`，禁 `git add -A`）。
