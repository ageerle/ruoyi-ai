# U0/审计卡蜂群核验报告（2026-09-05 23:45）

执行方：Qoder 主会话（root-94ae），1 个代码审查子智能体 + 主会话两组直查（另 2 个子智能体因账户速率限制改为串行执行）。
范围：看板上 12 张 U0/审计问题卡，逐张核对"卡面描述的问题"与"磁盘代码/配置/真库现状"。

## 结论一览

| 卡号 | 问题 | 核验结论 | 看板处置 |
|---|---|---|---|
| SEC-NEW-MED-1 | actuator 全暴露 | 已修复：include 收窄为 health,info,metrics；show-details=WHEN_AUTHORIZED | todo → inreview |
| SEC-NEW-MED-2 | prod 池配置雪崩 | 已修复：p6spy=false、maxPoolSize=80、connectionTimeout=5000 | todo → inreview |
| SEC-NEW-MED-4 | RSA 密钥字面量 | 已修复：改 `${API_DECRYPT_*}` env 注入；git 历史清理待 owner 决策 | todo → inreview |
| QA-05-P1 | dev 无池参数 | 已修复：dev hikari maxPoolSize=80、connectionTimeout=5000 | todo → inreview |
| QA-05-P2 | 审计链全表取尾 | 已修复：chain_heads 锚行锁原子分配；测试 35/35 绿；HTTP 闭环待复跑 | todo → inreview |
| DEF-5 | 库级 grant 架空只追加 | 已修复：全局层四项 N + 库级仅 Select/Insert + 表级 Select/Insert；ipd_app 已无 UPDATE/DELETE 路径 | todo → inreview（manage.py set） |
| SEC-NEW-MED-3 | prod root/root 写死 | 仍在：application-prod.yml master 数据源仍为字面量 | 维持 todo（需用户授权改 env 注入） |
| PERF-P0-1 | 超期审批 N+1 | 仍在：逐条 updateById + 缺 (status, leader_due_at) 索引 | 维持 todo |
| PERF-P0-2 | 招标过期全表扫 | 部分修复：已改单 SQL 条件 UPDATE；缺 (status, expire_at) 索引 | 维持 todo |
| QA-05-P3 | 审计分页慢 | 部分修复：BCrypt 池预算已做；游标分页与 slow log 未动 | 维持 todo |
| QA-04-D1 | gate_element_results 死表 | 大半已解：真库表已存在（126 表）、GateElementService 已读写 | 维持 todo（与 P2-5.1 波次绑定验收） |
| DB-02 | 16 域对象缺失 | 已落地：domain/ 35 类含全部 16 对象；工作树仍有未 commit 文件 | 维持 todo + note（manage.py set） |

## 关键证据

- 配置侧（键名 + 现值）：application.yml `management.endpoints.web.exposure.include=health,info,metrics`；application-prod.yml master 数据源 `username: root`/`password: root` 仍为字面量、`maxPoolSize: 80`；application-dev.yml hikari `maxPoolSize: 80`。
- 代码侧：AuditLogService.append 已删 ORDER BY seq DESC 取尾；DeletionRequestService.escalateOverdueLeaderReview 仍为循环逐条写；BidInvitationService.expireOverdue 已改单 SQL UPDATE。
- 真库侧（root@localhost 只读探针 @23:40-23:45）：ipd_dev 126 表含 gate_element_results；mysql.user 中 ipd_app 四项 DML 全 N；mysql.db 中 ipd_dev 仅 Select=Y/Insert=Y；mysql.tables_priv 中 audit_logs 仅 Select,Insert。

## 处置与同步

- 看板 API 回写 11 张（5 张转 inreview、6 张保持 todo 并附核验记录；FIX-PACK-3 追加勘误：49 张 P0-10.* 前端卡现态为 46 cancelled / 1 todo / 2 inprogress，卡面"维持 todo"表述过期）。
- manage.py set：DEF-5 → inreview、DB-02 → todo（附 note），均已写入开发计划-看板镜像.md。
- manage.py check 终态：PLAN 内 246 张全部 unchanged，零冲突；unmanaged 33 张为历史遗留（含本批治理卡），非本轮造成。

## 遗留给 owner 的决策项

1. SEC-NEW-MED-3：prod 数据源账号改 env 注入（涉部署环境重配，需授权）。
2. SEC-NEW-MED-4：git 历史中旧 RSA 密钥是否轮换/清除。

---

# 追记：最佳实践执行轮（23:46-23:56）

上一节遗留的可直接执行项已全部落地：

1. **PERF-P0-1**（转待审核）：兄弟会话在本报告发布后一小时内完成代码批量化改造（单 SQL 条件 UPDATE + adminDueAt 回捞补审计）与 idx_del_status_leader 索引；本会话 23:53 错峰单模块独立复跑 DeletionRequestServiceTest 9/9 绿（Skipped=0）。
2. **PERF-P0-2**（转待审核）：本会话补齐最后缺口——新增幂等 DDL docs/script/sql/update/2026-09-06-ipd-perf02-bid-expire-index.sql，真库 idx_bi_status_expire(status, expire_at) 已建，二次执行幂等验证通过；新增 BidInvitationServiceTest 契约锁（单 SQL UPDATE、零 selectCount）2/2 绿。
3. **QA-05-P3**（转待审核）：兄弟会话落地游标分页（listByOperatorIds 增 beforeSeq，null 兼容旧 OFFSET）；mysql.cnf 已开 slow_query_log；AuditLogCursorPagingTest 5/5 绿。
4. **DEF-5**（转已完成）：收口判据达成——以 ipd_app 身份（mysql-app.cnf）执行 UPDATE/DELETE audit_logs WHERE 1=0 均 ERROR 1142 command denied（exit=1，与 DEF-4 脚本 M8b L137-138 判定逐字等价），叠加三层授权表证据。

测试汇总：错峰单模块 mvn -o -pl ruoyi-modules/ruoyi-ipd 三测试类 16/16 绿、Skipped=0（23:53:57）。
看板终态：PLAN 内 246 张 manage.py check 全一致；本轮共 10 张卡状态流转（todo→inreview 9、→done 1）。
教训：多会话同树工作下，核验结论分钟级过期——本会话子智能体 23:40 报“PERF-P0-1 仍在”，23:50 磁盘已是修复态；结论必须带时间戳并即时落卡。

---

# 追记二：两张 owner 决策卡收口（23:57-00:01，用户拍板后执行）

1. **SEC-NEW-MED-3 prod root/root**（转待审核）：发现兄弟会话已备好待命补丁与守卫契约（ProdConfigDeltaGuardTest 第5/6条，占位符名 SPRING_DATASOURCE_USERNAME/PASSWORD）——按契约 git apply 验收目录 application-prod-med3-delta.patch 后归档为 .applied-20260905。prod 现态账密均为空默认 env 占位（未注入即连接失败 fail-fast）。守卫三件套 14/14 绿（@23:59:56）。部署时须注入 SPRING_DATASOURCE_USERNAME/PASSWORD。
2. **SEC-NEW-MED-4 RSA 历史清理**（转已完成）：按 O4 裁决先例关闭——密钥串出自 v3.0.0 init（7b8cfe02，fork 自上游 ageerle/ruoyi-ai），上游公开仓库永久可见，重写本仓历史无安全收益；同构先例（JWT 密钥）已裁“不重写历史”；现配置 env 注入 + fail-fast + RsaPemLiteralAbsentTest 4/4 在岗；api-decrypt.enabled 当前 false 无现存暴露。轮换=生产部署时注入新钥（部署事项）。

累计看板流转（三轮）：todo 111 → 99；inreview +12；done +3（DEF-5、SEC-NEW-MED-4、另见 PLAN 内计数）。
