# P1-6.1 QA 独立复核报告

**复核日期**: 2026-09-05
**复核员**: QA 独立复核员（只读模式）
**工作区**: /Users/mac/Documents/ruoyi-ai
**看板卡**: P1-6.1 — Gate 要素定义管理、校验与版本生命周期
**卡片状态**: PARTIAL（本仓复核结论）

---

## 一、复核范围与方法

- **只读复核**：仅使用 grep/read/ls/git log/show 等只读手段，不跑 mvn，不修改任何生产代码或文档。
- **本仓核验范围**：ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/ 内 GateElement*.java 对应 controller/service/domain/mapper 及同名单测。
- **跨仓不可核验声明**：镜像注记引用的验收报告（docs/ipd-系统说明/验收/**）、.codex/ruflo/global-*/**、中央 1230 树等证据，本仓 docs/ipd-系统说明/验收/ 目录当前为空——凡此类声明均标注「**本仓不可核验**」。

---

## 二、实现代码核实

### 2.1 已实现且可核实的组件

| 组件 | 路径 | 状态 |
|---|---|---|
| GateElement 领域实体 | domain/GateElement.java | ✅ 存在，字段完整 |
| GateElementController | controller/GateElementController.java | ✅ 存在，含 @SaCheckPermission |
| GateElementService | service/GateElementService.java | ✅ 存在（**版本二**：含长度校验/双写审计/错误码） |
| GateElementMapper | mapper/GateElementMapper.java | ✅ 存在 |
| GateElementCreateReq DTO | dto/GateElementCreateReq.java | ✅ 存在，白名单 record |
| GateElementUpdateReq DTO | dto/GateElementUpdateReq.java | ✅ 存在，白名单 record |
| P161AcceptanceTest | test/.../P161AcceptanceTest.java | ✅ **已存在**（375 行，@Tag dev） |
| GateElementServiceTest | test/.../GateElementServiceTest.java | ✅ 存在（旧版 5 测） |
| DDL 迁移 | docs/script/sql/update/2026-09-04-ipd-p0-tables.sql | ✅ gate_review_elements 表定义存在 |
| 种子 SQL | docs/script/sql/update/2026-09-05-ipd-p0-seed-elements.sql | ✅ 33 要素，is_veto=1 共 14 项 |
| 对应 commit | 25396d08 feat(ipd): P1-6 缺口补齐 | ✅ 存在 |

### 2.2 关键核实发现

#### P161AcceptanceTest 实际存在

本仓复核初始阶段误判「P161AcceptanceTest 不存在」——后经完整文件路径确认，**该测试文件已存在**（375 行，@Tag("dev")），是本轮 bee-shooter 蜂群任务新增，早于本次复核启动。测试包含：
- 无效 code 拒绝（null/空/越界长度 16）
- 无效 gateCode 拒绝（G0/G6/g1/带空格）
- 无效 flag 拒绝（isVeto/enabled 只允许 "0"/"1"）
- 无效 name 拒绝（null/空/≤2 汉字）
- 存储长度边界（elementName ≤128 汉字，passStandard ≤21845 汉字）
- 默认值注入（isVeto="0", enabled="1", sortOrder=0）
- 重复编码冲突 → ApiV1ErrorCode.STATE_CONFLICT
- listByGate 查询过滤 + 排序（gate_code ASC, sort_order ASC, id ASC）
- 重新启用审计快照（beforeData/afterData JSON）

#### GateElementService 版本二（含增强校验）

服务层已升级为版本二，包含：
- 长度校验：elementCode >16、elementName <2 汉字或 >128 汉字、passStandard >21845 汉字 直接拒绝
- 双写审计日志：CREATE/UPDATE/DISABLE 均写 beforeData+afterData JSON
- STATE_CONFLICT 错误码用于重复编码冲突
- listByGate 移除 enabled 过滤（列表含停用项）+ 加 id ASC 稳定排序

#### 种子数据 33 要素 + 14 否决项

SQL 中 INSERT 语句共 33 条，is_veto='1' 共 14 项（与 DOC-05 裁决一致）。

### 2.3 未实现/缺失组件

| 缺失项 | 说明 |
|---|---|
| **版本生命周期（draft/publish/archive）** | 页 47 定义的 draft/publish/archive 状态机未在代码中发现 |
| **复制/历史恢复（copy/revert）** | 代码中无 copy/revert 实现 |
| **已发布编辑 409 保护** | 代码中无 published 状态检查及 409 返回逻辑 |
| **vetoDualRequired 校验** | grep 零命中，未实现 |
| **thresholdJson 校验** | grep 零命中，未实现 |
| **DB 并发唯一约束** | DDL 中 gate_review_elements 表无 element_code UNIQUE 索引 |
| **Controller @Valid 注解** | GateElementController 的 create/update 方法未加 @Valid（依赖 Service 层校验） |

---

## 三、验收要点逐条判定

### AC-GATE-14：33 项 seed 入库（G1 七项/G2 六项/G3 五项/G4 八项/G5 七项）

**判定：✅ 满足**

- ✅ 种子 SQL 33 条 INSERT，按 element_code 幂等（WHERE NOT EXISTS）
- ✅ 各 Gate 计数：G1=7, G2=6, G3=5, G4=8, G5=7，合计 33
- ✅ is_veto='1' 共 14 项（注意：SQL 注释称 15 但裁决按 14）
- ✅ G2-6 is_veto='1'，pass_standard 含「缺失或周期冲突=否决」语义，与 DOC-05 一致
- ❓ 实际入库执行：需真 MySQL + Flyway 执行迁移后方可核实——**本仓不可核验**

### AC-GATE-18：超管可增删改名称/通过标准/是否否决项

**判定：✅ 满足（删=停用）**

- ✅ create：GateElementController@PostMapping 接受白名单 DTO，Service 含完整校验
- ✅ update：支持部分字段 patch（elementName/passStandard/isVeto/sortOrder/enabled）
- ✅ disable（软停用=禁删策略）：enabled=0，审计留痕，符合 G-02 证据链约束
- ✅ 权限控制：@SaCheckPermission("ipd:gate-element:add|edit:remove")
- ✅ 白名单 DTO：@JsonIgnoreProperties(ignoreUnknown=true)

### 页 47 版本生命周期

**判定：❌ 未实现**

- grep publish/archive/draft/revert/copy 在 gate 代码中零命中
- GateElement 实体无 version/publishedAt/status 字段
- 本仓不可核验：页 47 原文未在本仓提供

### vetoDualRequired 与 thresholdJson 校验

**判定：❌ 未实现**

- grep vetoDual/thresholdJson/threshold_json 零命中
- GateElement 实体无对应字段

### DB 并发唯一约束

**判定：❌ 缺失**

- DDL 仅 idx_gre_gate(gate_code)，无 element_code UNIQUE
- 应用层 selectCount 查重存在 TOCTOU 竞态窗口

---

## 四、既有声明核验

| 声明 | 本仓核验结果 |
|---|---|
| 「test 52 项全过」 | **本仓不可核验**——52 项分布在各工作树；本仓 P161=约 13 测 + GateElementServiceTest=5 测 |
| 「P16144」 | **本仓不可核验**——疑似测试命名缩写，本仓无对应文件 |
| 「3 文件 manifest 已交中央统一真 P053/HTTP/正式回写」 | **本仓不可核验**——Central 1230 树不在本仓 |
| commit 25396d08 存在 | ✅ 核实存在 |

---

## 五、复核结论

### 综合判定：**KEEP_PARTIAL**

**理由**：

1. **核心 CRUD + 校验已实现**：GateElementController + Service（版本二，含长度/枚举/唯一查重校验）+ Domain + Mapper + DTO 白名单全链路完整；P161AcceptanceTest（375 行，~13 测）覆盖主要分支。
2. **种子数据 SQL 已编写**：33 要素 + 14 否决标记落库脚本存在，幂等安全。
3. **关键缺失未交付**：
   - 版本生命周期（draft/publish/archive）、复制、历史恢复、409 保护
   - vetoDualRequired / thresholdJson 校验
   - DB 级 element_code 唯一索引
4. **跨仓声明不可采信**：manifest 中的「52 项全过」「P16144」等因证据不在本仓，标注不可核验。

### 风险等级：**中高**

版本生命周期和否决校验是 Gate 评审正确性的核心保障，缺失将导致：
- 已发布要素可被随意编辑而无版本追溯
- 否决项语义校验缺失可能导致错误判定

---

## 六、遗留缺口（Residual Gaps）

1. **[BLOCKER] 版本生命周期未实现**——draft/publish/archive 状态机、复制、历史恢复、已发布 409 保护
2. **[BLOCKER] vetoDualRequired / thresholdJson 校验未实现**
3. **[HIGH] DB element_code 唯一索引缺失**——应用层查重存在竞态窗口
4. **[MEDIUM] 种子数据真实入库待验**——需真 MySQL + Flyway 执行
5. **[INFO] 跨仓 manifest 声明不可核验**——52 项测试、P16144 等需 Central 1230 树证据

---

## 七、复核纪律遵守声明

- ✅ 仅使用只读手段（grep/read/ls/git log/show）
- ✅ 未修改任何生产代码
- ✅ 未修改 docs/开发说明/** 目录
- ✅ 未跑 mvn 编译测试
- ✅ 未做 git commit/push
- ✅ 未改看板状态
- ✅ 跨仓声明明确标注「本仓不可核验」

---

**报告路径**: docs/ipd-系统说明/验收/P1-6.1-QA独立复核-20260905.md
