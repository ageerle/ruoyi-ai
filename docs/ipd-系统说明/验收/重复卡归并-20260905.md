# 重复卡归并记录（2026-09-05 第十轮）

同一编号在本机看板出现两张卡时，`manage.py reconcile` 会抛 `Duplicate board cards` 并硬失败，导致**所有会话**的 `check` / `set` / `sync` 全部中止。按既有实践（AUD-03/04「重复建议保留 ID 全文并归并原卡」）先归档全文，再归并回 SSOT 托管卡，最后删除重复卡。

## 被归并的重复卡（全文留存）

```json
{
  "id": "8a858a23-802d-43b6-b403-d1432600c748",
  "title": "[P1-2.1] [U1] 项目编码并发唯一与立项四基准值",
  "status": "inprogress",
  "description": "## 执行中（2026-09-05）\n依次执行：①P1-2.1 立项四基准+系数默认/区间+模板市场主组必填；②P0-6.2 Gate执行器+证书去直删。\n负责人：Cursor\n验收：P121AcceptanceTest + P062/DeleteAudit 回归",
  "created_at": "2026-09-05T15:30:47.197Z",
  "updated_at": "2026-09-05T15:30:47.197Z",
  "parent_workspace_id": null
}
```

- 该卡由 **Cursor 会话** 绕过 SSOT 直接建卡认领（描述里的负责人字段为「Cursor」），状态 inprogress。
- 托管卡为 `84d194ab-e471-41f5-a387-1bd5fba97c43`（`[P1-2.1] [U1 高] 项目编码并发唯一与立项四基准值`，带 `<!-- ruoyi-plan:P1-2.1 -->` 托管块），归并前状态 todo。
- 归并动作：把 Cursor 的认领与验收要求写入 SSOT P1-2.1 行的源状态（`manage.py set P1-2.1 inprogress`，负责人登记为 Cursor），删除本重复卡。
- 冲突披露：root-94ae 在同轮 log 中记「P1-2.1 仍 todo，阻塞 P1-3.1/5.1/8.1 翻 done」，与 Cursor 的 inprogress 认领并存；以卡内负责人为准，翻 done 仍需 P121AcceptanceTest 与真库证据。

## 同时修复：三张 U0 卡的托管块被直改覆盖（不是状态争议）

归并过程中 `manage.py set` 先报 `P0-6.2: same-title unmanaged card; refusing to overwrite`，排查发现三张 U0 卡的描述被会话直接改写，`<!-- ruoyi-plan:KEY:begin/end -->` 托管块丢失：

| 卡 | 板卡 id | 板状态 | SSOT 状态 | 是否一致 |
|---|---|---|---|---|
| P1-1.1 | 2a5a2287-a083-47ba-baf3-5455ed573f52 | done | done | 一致 |
| P1-4.3 | 782610d1-0d83-4fe3-9511-283f551c2d7c | done | done | 一致 |
| P0-6.2 | 84bc6ccc-815e-42d3-bb02-0cc33441292d | done | done | 一致 |

三卡的 done 声明**有真库 HTTP+DB 证据**，已核实：`.codex/ipd-dev/runtime/evidence-p111-p062-p143.json`（ts 2026-09-05T08:31:48）—— P1-1.1 返回业务码 10001「一个产品仅对应一个项目」拒绝第二绑；P0-6.2 真库回读 `DELETED` 且审计链 `DELETE_REQUEST_SUBMIT → DELETE_LEADER_APPROVE → DELETE_EXECUTE` 完整；P1-4.3 69 动作实例且深管未完成回回 10001（BR-IPD-03）。因此本次只恢复托管块（原直改描述已全文留存于该卡末尾与 `.codex/vibe-kanban/marker-overwrite-archive-20260905.json`），未降级任何状态。修复后 `sync --apply` 得 `{unchanged: 236, update: 4}`，`check` 恢复 **exit 0**（240/240，未纳管 0）。

启示：直接 MCP/页面改卡即便状态正确也会打断 SSOT 对账（托管块丢失后 reconcile 硬失败，全体会话的 set/sync 均被阻），改卡后必须同轮回写镜像并跑 `check`。

## 本轮另一并发事实

`log.md` 曾于第九轮被并发写入从 215 行 / 17590 字节截断至 12 行 / 1308 字节（首行残留字面量 `undefined`），一至八轮历史由本会话从 `git show HEAD:` 恢复，第八轮（仅存在于未提交工作树）从现场读取逐字回插，被截断原文另存 `.codex/vibe-kanban/log-sibling-round9-20260905.md`。参见 OPS-09。
