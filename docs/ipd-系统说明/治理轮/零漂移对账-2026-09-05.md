# 零漂移对账 · 2026-09-05（第三方治理会话）

用户指令：**「确保整个项目 0 飘移」**。承接上一轮清理台账 `全局废弃冗余清理-2026-09-05.md` 的 O1–O4。

## 一、口径（先定义，再对账）

「漂移」= 系统内某一处的**声明**与其**可执行真值**不一致。因此每一维都必须给出：检测命令 + 时间戳 + 数值；不接受"看起来一致"。反向同样成立：**没有规则被违反的，不算漂移**（见 §五 负结果），否则"零漂移"会退化成按个人口味的批量改写。

本轮检测面覆盖 12 维，其中 8 维可在本会话权限内归零，4 维受写权归属 / 破坏性 / 授权约束，只出补丁与决策材料，不代决。

## 二、全维度真值表

对账基准时刻：19:47–19:52 PDT，HEAD 从 `2266fd09` 前移至 `e660f7c1`（会话期间兄弟继续提交，属常态）。

| Δ | 维度 | 检测命令 | 对账前 | 现在 | 归零方 |
|---|---|---|---|---|---|
| Δ1 | 已追踪却被忽略（索引↔ignore 自相矛盾） | `git ls-files -i -c --exclude-standard \| wc -l` | 22 | **0** | 上轮 `62ef2c34`，本轮复验未复吸 |
| Δ2 | `git submodule status` fatal（孤儿 gitlink） | `git submodule status; echo $?` | rc=1 fatal | **rc=0** | 上轮，本轮复验 |
| Δ3 | `.claude-flow` 运行时机器态误追踪 | `git ls-files .claude-flow/metrics .claude-flow/security …` | 13 | **0**（磁盘保留） | **本轮 `017d159b`** |
| Δ4 | 磁盘异常件（嵌套空仓 / 前导空格目录 / 根目录违规 spec / `.pyc`） | `ls -d` + `git ls-files` | 4 类 | **0** | 上轮 `62ef2c34`，本轮复验在位 |
| Δ5 | 文档 → 代码 的失效路径引用 | 自研审计脚本（480 处引用，去噪后 90 条候选） | 10 条真失效 | **2 条已修，8 条属他人文件** | 本轮 + §四 P2 |
| Δ6 | 文档 → 文档 的失效引用 | 同上 | 10 条（镜像 5 / 提案 4 / 兄弟清单 1） | **待主协调器应用** | §四 P1、P3 |
| Δ7 | 提交 SHA 引用的可解析性 | 167 候选逐个 `git cat-file -t` | 21 无法解析 | **真失效 2 条**，其余为假阳性 | §四 P4 |
| Δ8 | 本会话自己的记录与现实不符 | 复核自己写入的每个断言 | 3 处（O1 错误结论 + 2 处歧义路径 + 1 处件数笔误） | **全部订正** | 本轮 |
| Δ9 | 看板 SSOT ↔ 在线看板 | `manage.py check` → `has_drift` / `unmanaged_cards` | True / **32 张** | **True / 32（未归零）** | §四 P0 |
| Δ10 | 本地 ↔ 远端 | `git status -sb`、`git rev-list --count` | ahead **135** / behind 0 | **未变（需授权）** | §四 P5 |
| Δ11 | 工作树 ↔ HEAD | `git status --porcelain \| wc -l` | 脏 14（兄弟在途） | **未变** | 兄弟泳道自决 |
| Δ12 | 对象库卫生 | `du -sh .git`、`git count-objects -vH`、`git fsck --unreachable --no-reflogs` | .git 624M，loose 540M，15 个 `tmp_obj_*` 垃圾，1371 unreachable blob + 215 unreachable commit | **未变（需窗口）** | §四 P6 |

## 三、本轮已归零项（逐条可复验）

1. **Δ3 / 原 O2**：`.claude-flow` 下 13 个 Ruflo 运行时派生态件（`metrics/` 10 + `security/audit-status.json` + `harness-active-policy.json` + `memory-package.json`）脱离追踪，`.gitignore` 补 4 条声明（`:90-95`）。判据：与 `.claude-flow/.gitignore` 自己声明的 `data/ logs/ sessions/ neural/` 同一意图，此前缺声明致其长期挂库、每次 swarm 运行都可能变脏。保留入库的只有 `config.yaml` / `CAPABILITIES.md` / `.claude-flow/.gitignore` 三个声明件（`CLAUDE.md:260` 明确把前两者作为项目能力文档引用，不可脱库）。磁盘零删除。提交 `017d159b`（14 files changed, +6/−253）。
2. **Δ5**：`naming-convention.md` §8.1 / §8.2 两处代码位置引用已失效——文档写"在 `ruoyi-admin` 新建 `ApiV1Response` / `ApiErrorCode`"，实际落在 `ruoyi-modules/ruoyi-ipd/…/ipd/common/ApiV1Response.java` 与 `ApiV1ErrorCode.java`（类名亦变）。已按勘误级改指现存路径，**原始决策语义以引用块保留、不删**，并加变更日志行。
3. **Δ8**：本会话上一轮的错误结论已在原台账 §六 就地订正（见下条）。
4. **Δ9 的可动部分**：32 张未纳管卡的纳管材料改为**机器生成**，标题列逐字取自在线看板快照 → `零漂移-看板纳管材料-32卡-2026-09-05.md`（42 行 / 32 行表格，含 task id 便于回溯）。

## 四、不能由本会话归零项 · 精确补丁

### P0 · 看板 32 张卡未纳管（Δ9，`has_drift: True` 的唯一成因）

- 真值：镜像 246 张卡 **全部 `unchanged`**（镜像↔看板对已纳管卡零漂移），但 `board_total: 278`，`unmanaged_cards: 32` → `manage.py check` rc=1。
- 32 张卡的来源可辨：`R8-P0-1…10`（10）、`AUD-GOV-*`（8，镜像现仅有 `AUD-GOV-01/02`）、`SEC-NEW-MED-1…4`、`SEC-HIGH-1/3`、`QA-05-P1…P3`、`PERF-P0-1/2`、`AUDIT-CHAIN-IMPL-C`、`QA-04-D1/D2`、`U2 backlog 包` —— 全是兄弟 R8/AUD-GOV 泳道直建看板未回写镜像，与记忆中「17:03 撞车期兄弟直建 5 张卡未回写 → unmanaged=5」同型，现已增长到 32。
- 为什么本会话不代写：镜像是 SSOT 且 AGENTS.md 明定唯一写入者为主协调会话；当前该文件还是 ` M` 脏态（兄弟 11 行在途未提交），任何第三方写入都会与其读改写竞争。`manage.py` 的 `reconcile()` 源码 `:175-176` 亦显式拒绝隐式纳管：*"Do not delete or adopt them implicitly: retain their identity for review."* —— 工具作者与本仓纪律在此完全一致。
- 归零路径（一条命令即可验证）：由镜像写入者在净窗口把 §三.4 的表格整段追加至表尾 → `python3 docs/ipd-系统说明/vibe-kanban/manage.py check` 应报 `has_drift: False`、`total: 278`。若 owner 判定其中部分卡应归并（`验收/重复卡归并-20260905.md` 已确立归并先例），则删冗余卡同样能使 Δ9 归零——**两条路都能到 0，区别只在卡的去留，属 owner 判断面**。

### P1 · 镜像头部 5 条失效文档引用（Δ6，最高优先级，因镜像是读者入口）

`开发计划-看板镜像.md:9` 声明的 4 个配套文档 + `:307` 的 1 个，**全库 `find` 无任何近名文件**（即非改名漂移，而是从未落盘或已被删）：

| 镜像行 | 引用 | 实测 |
|---|---|---|
| :9 | `全局实现审计-20260905.md` | 不存在 |
| :9 | `全局需求完整性审计-文档分册-20260905.md` | 不存在 |
| :9 | `验收追溯矩阵-20260905.md` | 不存在 |
| :9 | `vibe-kanban/接入说明.md` | 不存在 |
| :307 | `验收/蜂群并发覆盖原文-20260905.md` | 不存在，且原文是「已[全文另存]」——**声明了动作而产物不在**，需 owner 判定是文件丢了还是引用写错；若属前者，涉及 41 行旧摘要的可恢复性 |

### P2 · 他人文档里的 8 条失效代码引用（Δ5 余量）

| 文件:行 | 失效引用 | 现值 / 定性 |
|---|---|---|
| `治理轮/兄弟流-不可触碰清单-2026-09-05-19-55.md:82` | `…/ipd/common/InstantIso8601Serializer.java` | 已被兄弟删除（会话开始时的 2 个 ` D` 之一）→ 该清单声称保护的文件已不存在，**清单自身与现实脱节**；按清单自订「只追加不删除」规则，应由其作者追加订正 |
| 同上 `:83` | `…/ApiV1ResponseBareMapperCompatTest.java` | 同上 |
| 同上 `:75` | `docs/ipd-系统说明/开发计划-跨线路径.md` | 全库无此名 |
| `验收/2026-09-05-治理轮总账.md:333` | jar 命名 `32720f79` | 本仓对象库与前端仓均无此对象（同行 `84725511`/`bd783076` 待另验）→ 不可解析引用 |
| `验收/PERF-AUD-…-跨commit性能审查.md:146`（2 处） | `scripts/start.sh` | 仓库无 `scripts/` 目录 → 臆造路径 |
| `验收/全局蜂群并行盘点与QA04结构回归-第二方复核-20260905.md:255` | `docs/script/sql/update/2026-09-05-ipd-receipt-ledger.sql` | 实际为 `docs/script/sql/update/receipt_ledger.sql`（不同命名体系） |
| `开发计划-看板镜像-23卡细分提案-20260905.md:24/:42` | `LegacyImportServiceTest.java` / `AiDocumentVersion.java` | 二者均为提案**要求新建**的产物 → 属计划性引用，**不计为漂移**（判据：上下文是"须有/新建"） |
| 同上 `:59/:174/:266/:332` | `验收/P1…P4-阶段验收-20260905.md` | 仅 `P0-阶段验收记录.md` 存在，P1–P4 尚未产出 → 计划性引用，不计漂移 |

### P3 · 提交引用审计的定量结论（Δ7）

167 个候选中 21 个无法解析，逐条定性后：**真失效 commit 引用仅 2 条**（`log.md:1353` 的 `06f1c1aa`、总账 `:333` 的 `32720f79`），其余 19 个为假阳性——构成：看板 task / project UUID 前缀 4 个（`01dcf15c` `0f328488` `2a5a2287` `331921e8`）、记忆 id 2 个（`0fee5448` `0b9120df`）、Codex 任务 id 1 个（`01a07119`）、更长 SHA256 的子串 10 个（`295eca2f37` `2d508ffd8d` `2ddf1f7534` `320bc4baf8` `43b90fd8b2` 等）、**跨仓引用 1 个**（`04bb27d`：`git -C /Users/mac/Documents/ruoyi-ipd-web cat-file -t` 实测 = commit，属前端仓，非本仓漂移）。
结论：本仓文档的 commit 引用完整率 **165/167 ≈ 98.8%**，不构成系统性漂移；两条真失效属"当时可解析、后被对象库淘汰"，建议以追加脚注方式标注「该对象已不在仓库对象库（疑被 gc / reset 淘汰）」，**不改写原文**。

### P4 · 原 O1 的真实缺陷（订正后，需能跑测试的窗口）

见 §五.1 的实测反转。建议补丁（本会话未执行，因 `ruoyi-ipd` 测试源此刻不可编译）：
1. `IpdServiceExceptionAdvice`：删 `handleIpdPermission`(:77) / `handleNotPermission`(:85) / `handleNotRole`(:93) 三个生产不可达方法，并把 `:79-80` 那段以 `assignableTypes` 为前提的注释改写为「权限三型由 `IpdPermissionExceptionHandler`（`@Order(HIGHEST)` + `basePackages` 全覆盖）独占」。
2. `DefectBAdviceAcceptanceTest`：`setControllerAdvice(new IpdPermissionExceptionHandler(), new IpdServiceExceptionAdvice())`，使缺陷B① ② ③ ④ 四条断言走**生产真实链**（当前只注册一个 advice，绿的是被隔离出来的死代码）；同时删其 Javadoc `:37`、`:57` 两处关于 `assignableTypes` 白名单与"生产链无 handler"的过时表述。
3. 保留 `handleNotReadable`（DEF-2 那条确实只有 advice 有）—— 这也是"不能整类删除"的边界。
4. 验证门：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=DefectBAdviceAcceptanceTest+Sec01AcceptanceTest+Sec02AcceptanceTest+P064AcceptanceTest test` 必须绿，且**删代码前后各跑一次**（先证基线，再证不回归）。前置条件：兄弟修好 `P032HttpAcceptanceTest.java` 语法或将其纳入库。

### P5 / P6 · 需 owner 明示授权的动作

- **Δ10 未推送 135 个提交**：`## main...origin/main [ahead 135]`，behind 0（无冲突风险）。`git push` 被 `.claude/hooks/block-dangerous-git.sh` 阻断且按纪律需明确授权；热并发下推送会把 6 个会话的在途提交一并推到远端，建议由 owner 选定"里程碑净窗口"执行，先 `git fetch && git log origin/main..HEAD --oneline` 复核归属。
- **Δ12 对象库**：见 §六 O4。

## 五、经核查"不算漂移"的负结果（防过度清理）

1. **双 advice 的"顺序不确定"不成立**（本会话上轮误判）：`IpdPermissionExceptionHandler.java:21` = `@Order(Ordered.HIGHEST_PRECEDENCE)`，`IpdServiceExceptionAdvice.java:29` = `@Order(HIGHEST_PRECEDENCE + 1)`，顺序确定、返回体逐字相同 → 无行为漂移，只有 §四 P4 的死代码与过时注释。**错因留档**：上轮用 `grep -A3 '@RestControllerAdvice'` 取证，`@Order` 恰在其上一行，从未进入窗口——"只 grep 不读文件"的教科书式误判，已写入记忆。
2. **`docs/wiki/**` 无漂移**：`node docs/wiki/wiki-lint.cjs` → **通过 121 / 失败 0 / 孤立 raw 0**。我的通用审计曾把 46 条 `../raw/...` 报为失效，属**审计工具的解析基准错**（wiki 链接以 `docs/wiki/` 为根，非以文件所在目录为根）；以仓内权威 linter 为准。
3. **`验收/` 日期命名双轨（47 个 `20260905` vs 7 个 `2026-09-05`）不列为漂移**：`naming-convention.md` 全文无文件名日期格式规定（其 §十 变更表只约束自身），没有规则被违反 → 至多是风格不统一，批量改名会断掉卡面 `evidence` 引用链，风险大于收益。
4. **`.gitignore` 与索引不自相矛盾**（Δ1=0）、**上轮 117 项摘除未被兄弟 `git add -A` 复吸**（`.agents/skills` `.swarm` `ruvector.db` `daemon-state.json` `policy/state.json` `*.DS_Store` 实测全 0）→ 上轮修复自维持。

## 六、O3 / O4 结论（本轮按证据定案）

**O3 · 分支与 stash 收尾 → 判定：均须保留，非废弃件。**
- `feat/perf-01-nextcode-unique`：`git rev-list --count main..` = **2**（未并入），`git diff --stat main...` = 11 files / **+1422 / −40**，含 `P131DatabaseIntegrationTest.java` 673 行、`P143AcceptanceTest.java` 203 行、`PERF-01` 并发安全 DDL；最后提交 `6cfb23e1`（05:38）。→ 是**未合并的待评审成果**，删除即销毁 1400+ 行工作；`git branch -d` 亦会因未并入而拒绝（只有 `-D` 能强删，而 hook 阻断 `-D`）。
- 2 条 stash（均名 `ipd-p111-stash-*`）：逐 blob 比对 HEAD，11 个路径**全部"不同"** → 未被吸收；且兄弟自述"收口后 pop 回来再提交"。→ 保留。
- **交互风险（本轮新发现，必须广播）**：这两条 stash 的索引态包含 `ruvector.db`、`.swarm/memory.db`、`.claude-flow/daemon-state.json`、`.claude-flow/policy/state.json` —— 正是上轮刚脱离追踪的路径。**一旦 pop，它们会重新回到索引并被下一次 `git add -A` 固化进 HEAD**（ignore 规则不作用于已在索引中的文件），上轮清理将被局部复吸。处置：pop 后须立即重跑 `git rm -r --cached --ignore-unmatch <这些路径>` 并同秒提交（模式已验证）。已在原台账与本文件双处登记。

**O4 · 历史体积 → 判定：不做 `filter-repo`，改为分级 gc。**
- 实测：`.git` = 624M，但 `pack` 仅 **69M**、`loose` = **540.6M**（6899 个）；≥30MB 的 8 个大对象（83.0/43.7/40.9/39.8/38/38/35.8/35.4 MB）经 `git log --all --find-object` 逐个验证**无任何 ref 引用**；`git fsck --unreachable --no-reflogs` = 1371 unreachable blob + 1199 tree + 215 unreachable commit；另有 15 个 `tmp_obj_*` 垃圾文件（`count-objects` 已告警）。
- `video/task01.mp4`(16.8M) + `task02.mp4`(85M) 属**上游 `ageerle/ruoyi-ai` 继承件**（`01bffcfa` 加入、`240df6e3` 删除），且 `git ls-files video` = 0（今日不追踪）。`.zvec-grep/`（磁盘 114M）**从未入库**（`--diff-filter=A` 零命中）。
- 三条否决 `filter-repo` 的理由：① 收益小——活跃 pack 才 69M，大 blob 均不在 HEAD；② **本仓特有**：`log.md`（1700+ 行）、SSOT 镜像、各验收报告、台账里以百计的 commit SHA 是**审计证据本身**，重写历史会使全部引用失效，等于销毁审计轨迹（与兄弟清单 L5「删除视为销毁审计轨迹」直接冲突）；③ 6+ 并发会话 + `/private/tmp/p131-worktree` 链接工作树 + `origin`(fork) + `upstream` 三方在重写后需全员重 clone，成本远超 600M 磁盘。
- 建议动作（分级，均需 owner 选窗）：
  - L1 安全：`git gc`（默认，保留 reflog 90 天）→ 把 540M loose delta 压缩打包，纯文本提交历史预期显著缩小；不改任何 SHA；代价是运行期持锁，兄弟 `git` 命令可能瞬时 `cannot lock` 失败，须净窗口执行。
  - L2 半安全：`git prune --expire=now && git gc --prune=now` → 真正回收那 500M 不可达垃圾，但**永久失去 215 个不可达提交的恢复路径**（可能含兄弟 reset 掉的工作）。仅在 owner 确认无人需要恢复时执行。
  - L3 不做：`filter-repo`。

## 七、复验矩阵（全部为本会话实测输出）

| # | 断言 | 命令 | 结果 |
|---|---|---|---|
| Z1 | 索引↔ignore 无矛盾 | `git ls-files -i -c --exclude-standard \| wc -l` | **0** |
| Z2 | 上轮 117 项未复吸 | `git ls-files .agents/skills .swarm ruvector.db .claude-flow/daemon-state.json .claude-flow/policy/state.json` | **0 行** |
| Z3 | 本轮 13 机器态件已脱库、磁盘在 | `git ls-files .claude-flow/metrics …` + `ls .claude-flow/metrics` | 追踪 **0**，磁盘 **10 文件** |
| Z4 | 保留声明件仍在库 | `git ls-files .claude-flow` | `.gitignore` `CAPABILITIES.md` `config.yaml` |
| Z5 | 子模块无 fatal | `git submodule status; echo $?` | rc=**0** |
| Z6 | 磁盘异常件归零态保持 | `ls -d .codex/cleanup-quarantine-*/stray-empty-repo-ruoyi-ai docs/docker/minio docs/docker/neo4j; ls ruoyi-ai` | 隔离区/重命名在位；根目录空仓**已不存在** |
| Z7 | wiki 层无漂移（权威） | `node docs/wiki/wiki-lint.cjs` | **121 通过 / 0 失败 / 0 孤立** |
| Z8 | 勘误后可解析 | `ls` 两个订正后的 java 路径 | 均存在 |
| Z9 | 看板漂移定性与量化 | `manage.py check` | 246 全 unchanged，`board_total 278`，`unmanaged 32`，`has_drift True`（**未归零，已出材料**） |
| Z10 | 远端漂移量化 | `git rev-list --count origin/main..HEAD` | ahead **135** / behind 0（**未归零，需授权**） |
| Z11 | 大对象可达性 | `git log --all --find-object=<8 个大 blob>` | 全部空 = 不可达垃圾 |
| Z12 | HEAD 未被我的提交破坏 | `git log -1 --format=%h`、工作树兄弟脏 14 项原状 | `017d159b` 之上继续正常前移，未吞他人改动 |

诚实边界：本轮**未**跑通任何 `@Tag("dev")` 测试（模块 test 源被兄弟在途文件阻断，非我造成，已给前置条件）；未连真库/真 HTTP；因此涉及 Java 行为的 O1 只给补丁规格不宣称完成。

## 八、回滚

```bash
# 回滚 Δ3（13 机器态件重新入库 + 撤 ignore 声明）
git revert --no-commit 017d159b && git commit -m "revert: 回滚 .claude-flow 机器态脱库 017d159b"
# 回滚 Δ5 勘误
git revert --no-commit <本轮 naming-convention 提交>
```
本轮全部改动均为**索引/文档层**，无磁盘删除、无历史重写、无分支或 stash 变更，回滚零数据损失。

---

## 九、后继轮追记（2026-09-05 21:19，主协调会话，修正上文四处已被超越的陈述）

上文多处「本会话/本轮」限定语所描述的状态，已被同一 owner 指令链的后续批次超越。逐条对账（均为实测，非推断）：

| # | 上文陈述 | 现状（证据 + 时间戳） |
|---|---|---|
| R1 | 「本会话未执行，因 `ruoyi-ipd` 测试源此刻不可编译」 | 已可编译且多轮绿：`83559abd`（项1，20:37:40）后 31/31（20:33:21–22 GateEngineTest 12/12 等）与全模块对照（带改 468/18F/18E vs 纯 HEAD 452/20F/19E，零新增红）；`55597b03`（项2/3/5，21:18:10）后 13/13（20:59:04–10、21:16:26–30 两轮 `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='ProdConfigDeltaGuardTest,CredentialLiteralGuardTest,IpdMockDataInitializerTest' test` BUILD SUCCESS） |
| R2 | 「本轮未跑通任何 `@Tag("dev")` 测试」 | 同上：所有新增/受影响测试类均带 `@Tag("dev")` 且实跑通过；红证侧 1d 4/5 失败、项3 守卫三发破坏注入必红（20:59:24 / 20:59:37 / 20:59:39），红绿双证齐 |
| R3 | 「本轮全部改动均为索引/文档层」 | 对**该轮**仍真；但其后的 `83559abd`/`55597b03` 已含 Java 源码与 yml 变更（GateEngine 豁免收窄、LaunchDate 双签、删 INITIAL_PWD 常量、dev yml 注释勘误），回滚方式应改用对应 commit 的 revert，而非上文仅列的两个 revert 命令 |
| R4 | 「当前该文件还是 ` M` 脏态（兄弟 11 行在途未提交）」 | 该脏态已被后续提交消化；截至 21:19:35 工作树剩余脏件全部归属兄弟会话（log.md / application.yml / RedisConfig / IpdAuthSession / P112/P131/P132/ProductService 测试 / R9 三稿 / AUDIT-CHAIN / session-delivery），本会话零残留 |
| R5 | Δ10 ahead=135 | 现 **169**（21:19:35，`git rev-list --count origin/main..HEAD`），其中本链贡献 `83559abd`、`55597b03` 等；归零仍需 owner 授权 push |
| R6 | Δ12 size-pack 540.6M→354.11M | 现 **129.94 MiB / loose 3107**（21:19:35），但出现 14 个 `tmp_obj_*` 垃圾件（多会话并发 gc 产物，无害，下次 gc 自动清）；不建议在本仓手动 `git gc --prunable` 类干预 |
| R7 | Z12「`017d159b` 之上继续正常前移」 | HEAD 已至 `c0bc8934`（21:19:27，兄弟 FAIL-CLASS-A）；本会话提交均用显式 `--only` 路径，未吞任何他人改动 |
| R8 | Z1=0 | 复测仍 **0**（21:19:35） |

仍未归零、需 owner 一句话或人工动作的清单（截至 21:19:35）：

1. **Z9 看板漂移**：`has_drift` 未归零（材料已交，卡面属主线程）。
2. **Z10 远端**：ahead 169，push 被 `.claude/hooks/block-dangerous-git.sh` 按设计拦截，需 owner 显式授权。
3. **凭证真轮换 R1–R3**（`凭证轮换处置-20260905.md` §3）：11/11 活账号仍共用种子口令（21:06:40 离线 BCrypt 实证），`a8a70ad9` 旧 JWT 密钥仍在本地历史且**任何一次 push 都会外泄**（21:08:38 `git branch -r --contains` 空输出）。
4. **DDL apply D1–D5**（`P1-项5-DDL-apply核验-20260905.md` §4）：`idx_sa_project_code` 17 库全 MISSING、双签唯一索引/版本列 NOT_APPLIED，代码侧已就位但约束未生效。
5. **36 个孤儿测试库**、36 个既有红测试归属：均需 owner 裁决，本链未动。
