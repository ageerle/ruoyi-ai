# R9c · D4 孤儿测试库裁决提案（32 个，实测清单）

> 执行会话：R9c（owner「立即执行剩余待办」授权轮，2026-09-05 21:5x）
> 取证：`SHOW DATABASES` 全量 + 与 `ddl-apply-check-all-20260905.json`（21:07:34）交叉对账
> 现状：全实例 40 库 = 4 系统库 + 4 在用库（ipd_dev / ipd_restore / ipd_perf / ipd_qa04）+ **32 孤儿**
> 协调会话 20:47 记录为 36 孤儿 → 期间已被某会话清掉 4 个，本清单为当前真实存量。

## 一、分类清单（全部按命名规约可机判，无疑难项）

| 类别 | 数量 | 库名样本 | 来源判断 |
|------|------|----------|----------|
| hrtest | 7 | ipd_hrtest_320bc4baf8a2 等 | HR 模块集成测试残留 |
| ops03_restore | 6 | ipd_ops03_restore_159f65e25dcc 等 | ops03 恢复演练快照 |
| ops04_test | 5 | ipd_ops04_test_43b90fd8b225 等 | ops04 轮测试库 |
| p142_migrate | 4 | …8f401deb31_{before,after,fresh,partial} | P142 迁移验收四联快照（成组） |
| ops03_fixture | 2 | ipd_ops03_fixture_{533ee28e999e,d812d9c987e3} | ops03 夹具 |
| p053_test | 2 | ipd_p053_test_{38989d37d5fe,ff21a9002ed2} | P053 集成测试 |
| restore 快照 | 2 | ipd_restore_{49153a49d3b1,68423e608051} | 恢复实验快照 |
| 单例 | 4 | ipd_audit_test_56c781a76ea4 / ipd_auth_test_64f11d269510 / ipd_env_probe_85f3d2da / ipd_p142_test_992f8fce1eb2 | 各专项验收残留 |

## 二、建议

1. **32 个全部可 DROP**：均为带哈希后缀的一次性测试/演练产物；在用 4 库与之零交集（前缀完全不同）。
2. 执行方式：用下方已生成的脚本（**本轮未执行**——DROP DATABASE 属破坏性操作，按 P1-项5 §3 纪律留 owner 点头）：
   `.codex/ipd-dev/run/drop-orphans-r9c.sh`（内容仅 32 条 `DROP DATABASE IF EXISTS`，逐条 echo）
3. 保留基线：若 P142 迁移四联快照还需复看 before/after 差异，可先备份再 drop；否则直接清。

## 三、连带影响

- 清理后全实例 = 8 库，`--all` 类扫描信噪比显著改善；
- 备份体积下降；
- `p1-ddl-apply-check.py --all` 的「17 库 MISSING」噪声同步归零（孤儿库不在 D1/D2 apply 范围是有意为之）。

## 四、执行登记（owner 点头「该清理的要清理掉」后，21:5x）

- `drop-orphans-r9c.sh` 首跑因 shell 反引号命令替换全部报错、**零库被删**（安全失败模式）；
- 改经 pymysql 直执：前置断言（32 个、库名全 `[A-Za-z0-9_]+`、keep 集排除）→ **DROP 32/32 成功**；
- 终态断言：全实例恰为 8 库（4 系统 + ipd_dev/ipd_restore/ipd_perf/ipd_qa04）✅；
- 执行前 processlist 复核孤儿库零活跃连接（安全窗口）✅。
