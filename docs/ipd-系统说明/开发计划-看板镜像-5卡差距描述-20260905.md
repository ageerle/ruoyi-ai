# 5 张 ◐ 卡"差距描述"补全

> 落仓时间：2026-09-05 19:30
> 配套：23 张 ⬜ 卡细分提案 `开发计划-看板镜像-23卡细分提案-20260905.md`
> 目标：把 5 张 ◐ 卡从"骨架有/真库 HTTP 未跑"推到 ✅，每张卡"差距/真库命令/完成证据"明确

---

## P0-2 数据模型：26 表 DDL + 底座实体

**现状**：✅ 2224f676（DDL/底座实体已有），**◐** 实际字段与约束映射、迁移实灌待 OPS-02/QA-04。

**差距**：
- 26 张表 DDL 在 `docs/script/sql/update/2026-09-04-ipd-p0-tables.sql` 已写
- 底座实体（26 个 `@TableName` 类）已生成
- **未实灌**：MySQL 实例上未执行过 DDL（仅 dev mock 跑过）
- **未复核**：tenant.excludes 是否完整覆盖 26 张表（特别是 trace_run/trace_node）

**真库命令**：
```bash
# 1. 应用启动时自动建表（application-dev.yml: spring.sql.init.mode=always）后
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SHOW TABLES;" | wc -l   # 期望 ≥ 26 + 系统表
# 2. tenant.excludes 复核
grep -A 20 "tenant.excludes" ruoyi-admin/src/main/resources/application.yml | head -25
```

**完成证据**：commit hash + 真库 `SHOW TABLES;` 输出 + tenant.excludes 复核报告

---

## P0-3 system_configs 参数种子

**现状**：✅ 6c626221（54 键种子已落），**◐** 参数管理/校验/版本生效子卡未完成。

**差距**：
- 54 键 seed 已落库
- **缺**：参数管理 Controller（增删改查）
- **缺**：参数校验规则（涉钱 6 开关）
- **缺**：参数版本生效机制（更新后即时生效 vs 重启）

**真库命令**：
```bash
# 真库 seed 已实灌 → SELECT COUNT(*) FROM system_configs; 期望 ≥ 54
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT COUNT(*) FROM system_configs;"
# 管理接口真库 HTTP
curl -s -X GET http://127.0.0.1:16039/api/v1/system-configs | jq '.data | length'
```

**完成证据**：commit + 54/54 计数 + 真库 HTTP 200

---

## P0-4 统一响应 ApiV1Response + 错误码

**现状**：✅ 6c626221（成功包络），**◐** 失败仍走旧 R 路径，API-01/P0-4.1 待办。

**差距**：
- ApiV1Response 已实现（`code=0` 成功包络）
- **缺**：失败路径走 ApiV1Response（当前仍走 R<ServiceException>`）
- **缺**：六段位错误码（TS-09：业务段 + 模块段 + 错误段 + 杂项 + 子码 + 预留）
- **缺**：全局异常处理器 `@RestControllerAdvice` 把 `ServiceException` 映射到 ApiV1Response

**真库命令**：本卡无真库 SQL，主要是 code path 验证：
```bash
# 触发已知的失败场景，断言 code 不为 0 且符合六段位格式
curl -s -X GET http://127.0.0.1:16039/api/v1/products/9999 | jq '.code'   # 期望 1xxx 格式
```

**完成证据**：commit + 失败路径截图 + 六段位 code 映射表

---

## P0-5 审计日志 hash 链引擎

**现状**：✅ 4 测通过，**◐** 验链倒序/JSON/并发持久化缺口已证实，**非 AC-AUD-01 通过**。

**差距**：
- append + hash 工具已通过 4 测
- **缺**：验链倒序遍历（从最新 prevHash 一路回溯到创世块）
- **缺**：JSON 合法序列化（中文/emoji/转义）
- **缺**：并发追加时的 prevHash 原子性（实测过 race condition）

**真库命令**：
```bash
# 真库验链
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT COUNT(*), MAX(id) FROM audit_logs;"
# 验链脚本（应输出 VALID）
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=AuditChainValidationTest test
```

**完成证据**：commit + 验链脚本 exit 0 + 1000 条记录并发测试通过

---

## P0-6 删除审核引擎

**现状**：✅ 8 Mock 测通过，**◐** 分级审核/真实目标软删/调度未闭环。

**差距**：
- Mock 测 8 个 PASS
- **缺**：两级 2+2 工作日调度（Spring @Scheduled）
- **缺**：撤回 24h 窗口
- **缺**：真实目标软删（目前只走 mock entityType）

**真库命令**：
```bash
# 真库：创建删除申请 → 组长初审 → 超管终审 → DB 目标 archived
curl -X POST http://127.0.0.1:16039/api/v1/deletion-requests -d '{...}' | jq .code   # 期望 0
sleep 3
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT status FROM deletion_requests ORDER BY id DESC LIMIT 1;"
```

**完成证据**：commit + 真库 evidence（log.md 已有 P0-6.2 闭环记录）

---

## P0-8 基础种子数据

**现状**：代码种子 + dev Mock 初始化已有，**◐** 真实实灌 + 15/14 否决裁决待验。

**差距**：
- 代码 seed（`<insert>` in Java）已写
- Mock 初始化（`IpdMockDataInitializer`）已跑
- **缺**：MySQL 真库实灌（不是 H2 内存库）
- **缺**：DOC-05 裁决后 15 vs 14 否决明细的最终落库

**真库命令**：
```bash
# 真库计数
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT COUNT(*) FROM gate_review_elements;"   # 期望 33
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT COUNT(*) FROM gate_review_elements WHERE is_veto=1;"   # 期望 14
mysql -h 127.0.0.1 -P 13306 -u ipd_app -pipd_app -D ipd_dev -e "SELECT COUNT(*) FROM product_groups;"   # 期望 ≥ 2 (mock)
```

**完成证据**：commit + 真库 33/14/2 计数 + DOC-05 决议链接

---

## P0-9 P0 阶段验收

**现状**：旧记录编译/单测通过，**◐** 启动到删除审计业务链未完成。

**差距**：
- 编译 OK（`mvn compile`)
- 单测 OK（mock 框架下）
- **缺**：启动后能跑通 完整业务链 = 登录 → 立项 → 推进 → 删除审核 → 归档
- **缺**：AC 抽查 10 条

**真库命令**：
```bash
# 启动 + 跑完整业务链（参考 log.md P0-6.2 evidence-p062-fail-not-deleted.json 流程）
mvn spring-boot:run -pl ruoyi-admin &
sleep 30
bash scripts/p0-e2e.sh   # 业务链脚本（待建）
```

**完成证据**：commit + 全模块 245+/245+ 绿 + AC 抽查 10/10 通过

---

## P0-10 前端对接（跨仓库占位）

**现状**：拆分 49 页前端子卡；目标仓库待确认。

**差距**：
- 49 页清单在 `docs/开发说明/spec/_导航地图.md`
- **缺**：目标仓库确认（`ruoyi-web` 还是 `ruoyi-admin`？两个仓库的边界？）
- **缺**：49 页前端子卡（在 `ruoyi-web` 仓建 issue 同步）

**真库命令**：本卡无真库 SQL，主要是仓库问题：
```bash
# 在前端仓建 issue
gh issue create --repo wilson323/ruoyi-web --title "[IPD-前端] 49 页需求拆分" --body "..." --label ipd-frontend
```

**完成证据**：前端仓 issue 链接 + 49 页状态汇总

---

## 推进顺序（5 张 ◐ 卡的批次）

| 优先级 | 卡 | 估时 | 备注 |
|---|---|---|---|
| P0-A | P0-8 基础种子真库实灌 | 0.5h | 最易闭环，需受限账号 |
| P0-B | P0-2 26 表 DDL 实灌 | 0.5h | 同上 |
| P0-C | P0-3 参数管理接口 | 2h | 增删改查 + 校验 |
| P0-D | P0-4 失败包络 + 错误码 | 1.5h | 全局 advice |
| P0-E | P0-5 审计链验链 + 并发 | 2h | 含 1000 条并发测试 |
| P0-F | P0-6 删除审核真库调度 | 2h | Spring @Scheduled |
| P0-G | P0-9 P0 阶段验收 | 1h | E2E 脚本 |
| P0-H | P0-10 前端仓库确认 | 0.5h | 跨仓建 issue |

**总计 5 张 ◐ → 8 个子任务，约 1-2 个会话周期**（含 1 个会话跑完）。
