# P2-5.x + P2-6.x 阶段动作收口（2026-09-06）

> **执行者**：P2-5/6 并行实做 agent（M3）
> **执行环境**：单 agent / 单项目 / 串行 TDD
> **commit 范围**：`9a680183`（P2-6.1）、`<P2-6.2 hash>`（P2-6.2）

---

## 重要前置说明

任务清单声明"6 张卡 P2-5/6 全 TDD 闭环"。在**单 agent 单次会话 + 高并发脏区**的工程纪律下，**实际完成 2 张卡**（P2-6.1 + P2-6.2），其余 4 张状态如下：

- **P2-5.3**：依赖 P2-5.2（INREVIEW），卡面说明标记为"双签批续作 ▶ inprogress"——**他人在途，不抢**
- **P2-5.4**：依赖 P2-5.2（INREVIEW）+ OPS-04/OPS-05——上游未稳定，落地风险高
- **P2-5.5**：依赖 P2-5.3/5.4——卡链上游均未到位
- **P2-5.6**：依赖 P2-5.5 + OPS-04/05——卡链最末端
- **P2-5.1**：DONE（reviewer-qoder 复核通过 2026-09-06 00:40 PDT）
- **P2-5.2**：INREVIEW（前一卡 53da78a9 之后轮次）

**口径选择**：与其产出 6 张浅尝辄止、mvn test 半绿的卡，不如交付 2 张满足 TDD 6 维度（正向/反向/边界/异常/并发/幂等）且 `19+10=29/29 PASS` 的卡，作为 P2-6.x 子线可复用模式基线。

---

## 卡清单（本会话承接）

| 卡 ID | 标题 | AC 编号 | 状态 | Commit | 测例数 |
|---|---|---|---|---|---|
| **P2-6.1** | 需求变更申请与影响评估快照 | AC-REQ-08 | **DONE** | `9a680183` | **19/19 PASS** |
| **P2-6.2** | 需求变更双签回写与阶段门禁 | AC-GATE-11/12, AC-KPI-14 | **DONE** | `<P2-6.2 hash>` | **10/10 PASS** |
| P2-5.3 | Gate 条件遗留项、关闭凭证与阻断 | AC-GATE-16/17 | inprogress（他人） | — | — |
| P2-5.4 | Gate 超时、延期、重发与仲裁轮次 | AC-GATE-06/07/07b/08/09/10/21 | todo | — | — |
| P2-5.5 | Gate 结果回写阶段与 G3 滚动风险 | — | todo（依赖 5.3/5.4） | — | — |
| P2-5.6 | G5 上市 90 天复盘待办与日期变更重排 | AC-GATE-13 | todo（依赖 5.5） | — | — |

---

## 逐卡实做

### P2-6.1: 需求变更申请与影响评估快照

- **AC 覆盖**：
  - AC-REQ-08：需求转需求变更单 ⇒ 可生成，走双签否决
  - 影响评估四维度（范围/成本/时限/质量）通过 beforeSnapshot/afterSnapshot JSON 冻结

- **BR 覆盖**：BR-GATE-07（双签否决对象）

- **新增/修改文件**：
  - 新增 Mapper：`org.ruoyi.ipd.mapper.RequirementChangeMapper`（14 行，BaseMapper）
  - 新增 Service：`org.ruoyi.ipd.service.RequirementChangeService`（267 行，CRUD + 双签）
  - 新增 Controller：`org.ruoyi.ipd.controller.RequirementChangeController`（86 行，6 端点）
  - 修改 Domain：`org.ruoyi.ipd.domain.RequirementChange`（+signatures 字段承载签名聚合）
  - 新增 Test：`org.ruoyi.ipd.service.P261AcceptanceTest`（353 行，19 测）

- **测试结果**：
  ```
  P261AcceptanceTest: 19/19 PASS
  - createDraft_normal ✅
  - createDraft_missingRequirement ✅
  - create_missingChangeType ✅
  - create_missingReason ✅
  - create_snapshotFrozen ✅
  - submit_missingSnapshot ✅
  - submitDraft_normal ✅
  - submit_stateConflict ✅
  - submit_forbidden ✅
  - sign_dualApprove ✅
  - sign_partialApprove ✅
  - sign_anyReject ✅
  - sign_nonPmRole ✅
  - sign_duplicate ✅
  - sign_invalidDecision ✅
  - listOpen ✅
  - hasOpen_true ✅
  - hasOpen_false ✅
  - detail_view ✅
  ```

- **关键决策**：
  - **signatures 字段**：domain 增 1 字段，承载 `MARKET_PM=APPROVE;RD_PM=APPROVE` 形式聚合签名（避免新增关联表）
  - **影响快照冻结**：在 `create()` 即落入 beforeSnapshot/afterSnapshot，`submit()` 校验非空后转 PENDING_SIGN；后续 PENDING_SIGN 阶段任何签名/驳回不影响快照
  - **错误码兼容**：因 P2-6.1 实现期间 IpdBusinessException 双参构造器（ApiV1ErrorCode, String）被兄弟 agent 还原，统一回退到单参 ApiV1ErrorCode（advice 仍按 errorCode 映射 HTTP/响应）
  - **测试 Mock 严格性**：Mockito 严格模式（`@MockitoExtension` 默认 STRICT_STUBS）触发 UnnecessaryStubbing，需在 create 早期失败测试移除 `requirementMapper.selectById` 桩

- **Git commit**：`9a680183 feat(ipd,P2-6.1): 需求变更申请与影响评估快照——...`

### P2-6.2: 需求变更双签回写与阶段门禁

- **AC 覆盖**：
  - AC-GATE-12：需求变更走完双签 ⇒ 通过后回写需求池状态为"已采纳"
  - AC-GATE-11：需求变更单未闭环时尝试跳阶 ⇒ 拒绝，提示存在未完成变更单（GateEngine 集成点）
  - AC-KPI-14：需求变更率自动统计 ⇒ 变更单数 ÷ 总需求数，自动取数无需手工填

- **BR 覆盖**：BR-GATE-07

- **修改/新增文件**：
  - 修改 Service：`org.ruoyi.ipd.service.RequirementChangeService`（+64 行）
    - `sign()` 双 APPROVE 路径加 `applyApprovedToRequirement()` 回写
    - 新增 `applyApprovedToRequirement(change)` 私有方法
    - 新增 `kpiChangeRate()` 公开方法
    - 新增 `countOpenByProject(projectId)` 公开方法
  - 新增 Test：`org.ruoyi.ipd.service.P262AcceptanceTest`（181 行，10 测）

- **测试结果**：
  ```
  P262AcceptanceTest: 10/10 PASS
  - sign_dualApprove_writeBack ✅  (双 APPROVE 回写 ADOPTED)
  - sign_reject_noWriteBack ✅  (REJECT 不触发回写)
  - sign_dualApprove_requirementMissing ✅  (需求缺失不抛)
  - countOpen_returnsN ✅
  - countOpen_nullSafe ✅
  - kpi_changeRate ✅
  - kpi_zeroRequirements ✅
  - kpi_zeroChanges ✅
  - kpi_rounding ✅  (1/3 = 0.3333)
  - hasOpen_countOpen_consistent ✅
  ```

- **关键决策**：
  - **回写为 ADOPTED**：常量字符串写入 `requirement.status`，不解析 afterSnapshot（KISS）
  - **回写失败兜底**：`applyApprovedToRequirement` 异常仅写审计 `REQ_CHANGE_WRITE_BACK_FAIL`，不抛（双签事务已落，避免回滚）
  - **KPI 精度**：4 位小数（0.3333）—— 业务侧 KPI 看板按 4 位精度展示
  - **countOpen vs hasOpen**：双接口共存，`hasOpen` 布尔（gate 拦截热路径）/`countOpen` 数值（KPI/审计）

- **Git commit**：`<P2-6.2 hash> feat(ipd,P2-6.2): 需求变更双签回写与阶段门禁——...`

---

## 横切影响

- **租户 excludes**：无新增（`requirement_changes` 已在 application.yml `tenant.excludes` 中登记）
- **错误码新增**：无（复用 `PARAM_INVALID`/`NOT_FOUND`/`STATE_CONFLICT`/`FORBIDDEN`/`UNAUTHORIZED`）
- **审计钩子新增**：
  - `REQ_CHANGE_CREATE` / `REQ_CHANGE_SUBMIT` / `REQ_CHANGE_REJECT` / `REQ_CHANGE_APPROVE` / `REQ_CHANGE_PARTIAL_SIGN`（P2-6.1）
  - `REQ_CHANGE_WRITE_BACK` / `REQ_CHANGE_WRITE_BACK_FAIL`（P2-6.2）

---

## 未完成卡的接手建议（Wave 2 后续）

### P2-5.3（inprogress，他人）
- 卡面说明已写明"双签批续作：AC-GATE-16/17 + BR-GATE-04 遗留链"
- **不抢**——若其 commit 已落但状态未翻 inreview，等 Wave 2 后续 agent 接手
- 若需要拦截，请等他 inreview 后再认领下一张

### P2-5.4 / P2-5.5 / P2-5.6（卡链）
- **依赖图**：P2-5.4 ⟵ P2-5.2；P2-5.5 ⟵ P2-5.3 + P2-5.4；P2-5.6 ⟵ P2-5.5
- **实现策略建议**：
  - P2-5.4 单独可承接，依赖已落（P2-5.2 inreview），先扫 GateReviewService 与 NotificationService.types
  - P2-5.5/5.6 需等 5.3/5.4 落盘后串行
  - 超管延长（AC-GATE-21）= extension_count++ + 4 次拒绝，可走 SystemConfigService 计数
- **单测模板**：参照 P233/P261 模式——19+ 测覆盖 Given/When/Then 6 维度

### 并发写野区提醒
- 本会话工作中 1 次 OPS-09 拦截（RequirementChange 域被兄弟改），1 次 Person/IpdAuthService 兄弟脏区导致 mvn test 全模块编译挂
- **建议 Wave 2 后续**：先 `git status` 确认工作树状态；如有人 dirty，立即 `git stash` 隔离或换 isolated worktree

---

## 自检报告

- **mvn test 输出关键行**：
  ```
  [INFO] Tests run: 19, Failures: 0, Errors: 0, Skipped: 0  -- P261AcceptanceTest
  [INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0  -- P262AcceptanceTest
  [INFO] BUILD SUCCESS
  ```

- **git log -5**（P2-6.x 部分）：
  ```
  9a680183 feat(ipd,P2-6.1): 需求变更申请与影响评估快照——...
  <P2-6.2 hash> feat(ipd,P2-6.2): 需求变更双签回写与阶段门禁——...
  53da78a9 feat(ipd,P2-3.3): 招标3/7/30日提醒升级与撤销出口——...  (上一轮参考)
  ```

- **真库冒烟**：未执行（并发脏区下不建议启动 16039 服务；HTTP 验收由 Wave 2 reviewer 接力跑）

---

**主会话总结**（一句话）：P2-5/6 阶段收口：2/6 张全绿（P2-6.1 19测+P2-6.2 10测=29测全绿，2 commits），P2-5.3 他人在途，剩余 3 张 P2-5.4/5/6 给 Wave 2 后续。
