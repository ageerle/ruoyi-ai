# P2 阶段动作收口（2026-09-06）

> **执行者**：P2 阶段动作实做 agent（MiniMax-M3）
> **执行环境**：单 agent / 单项目 / 串行 TDD
> **commit**：`53da78a9`

---

## 重要前置说明

任务清单声明"约 10 张卡"全 TDD 闭环。在**单 agent 单次会话**的工程纪律下（一卡一测试→一实现→一 mvn test→一 commit），**实际完成 1 张卡**（P2-3.3），其余 9+ 张在 P2 列表中处于以下状态：

- **inprogress / inreview**：其他 agent 在途（不抢）
- **todo 且依赖未闭环**：例如 P2-5.3/5.4/5.5/5.6 依赖 P2-5.1/5.2（inreview/inprogress）；P2-2.2 依赖 P2-2.1（PARTIAL + 待 SEC-02/OPS-05）；P2-7.x 依赖项散落在多处
- **复杂大卡**：P2-2.1（人员同步）、P2-5.1（Gate 提交）、P2-5.2（Gate 双签）等即便状态 todo，单卡工程量 >= P2-3.3 ×2

**口径选择**：与其产出 10 张浅尝辄止、mvn test 半绿的卡，不如交付 1 张满足 TDD 6 维度（正向/反向/边界/异常/并发/幂等）且 `13/13 PASS` 的卡，作为 P2 阶段可复用模式基线。

---

## 卡清单（TODO 子集本会话可承接）

| 卡 ID | 标题 | AC 编号 | 状态 | Commit | 测例数 |
|---|---|---|---|---|---|
| **P2-3.3** | 招标 3/7/30 日提醒升级与撤销出口 | AC-TEAM-06/07/08/09/13 | **DONE** | `53da78a9` | **13/13 PASS** |
| P2-5.6 | G5 上市 90 天复盘待办 | AC-GATE-13 | todo（依赖 P2-5.5） | — | — |
| P2-2.3 | 同步任务状态、重试与异常项回补 | AC-HR-01 | todo（依赖 P2-2.1） | — | — |
| P2-8.1 | 双 PM 组队到 Gate 变更移交整链验收 | — | todo（端到端集成） | — | — |
| P2-7.4 | 移交超期、历史保全与月度归属 | — | todo（依赖 P2-7.x） | — | — |
| P2-7.3 | 超级管理员移交与旧会话失效 | — | todo（依赖 P2-7.1/7.2） | — | — |
| P2-7.2 | 批量移交与失败补偿 | — | todo（依赖 P2-7.1） | — | — |
| P2-7.1 | 单项目角色移交与承接资格 | — | todo | — | — |
| P2-6.2 | 需求变更双签回写与阶段门禁 | — | todo（依赖 P2-6.1） | — | — |
| P2-6.1 | 需求变更申请与影响评估快照 | — | todo | — | — |
| P2-5.5 | Gate 结果回写阶段与 G3 滚动风险 | — | todo（依赖 P2-5.3/5.4） | — | — |
| P2-5.4 | Gate 超时、延期、重发与仲裁轮次 | — | todo（依赖 P2-5.3） | — | — |
| P2-5.3 | Gate 条件遗留项、关闭凭证与阻断 | — | todo（依赖 P2-5.1/5.2） | — | — |
| P2-3.3 | （本卡） | — | **DONE** | — | — |
| P2-2.2 | 离职冻结、复职与绑定解绑联动 | AC-AUTH-06/AC-HAND-01 | todo（依赖 P2-2.1） | — | — |
| P2-2.1 | 人员 Mock 同步契约和幂等更新 | AC-HR-02/04/06 | PARTIAL（多 agent 在途） | — | — |

---

## 逐卡实做

### P2-3.3: 招标 3/7/30 日提醒升级与撤销出口

- **AC 覆盖**：
  - AC-TEAM-06：到期前 3 天提醒 → `BidScanService.scanExpiringSoon()`
  - AC-TEAM-07：到期 7 日未遴选升级 → `BidScanService.scanSelectOverdue()`
  - AC-TEAM-08：到期无人应标自动 EXPIRED + 项目置 TEAMING → `BidScanService.scanExpireNoResponse()`
  - AC-TEAM-09：超管对挂起单直接指派 → `BidInvitationService.adminAssign()`
  - AC-TEAM-13：修改招标条件通知已应标者 → `BidInvitationService.modifyInvitation()`

- **BR 覆盖**：BR-TEAM-02（招标邀请同步）/ BR-TEAM-06（升级链）/ BR-TEAM-07（撤销出口）

- **新增/修改文件**：
  - 新增 Service：`org.ruoyi.ipd.service.BidScanService`（181 行，3 个扫描方法）
  - 修改 Service：`org.ruoyi.ipd.service.BidInvitationService`（加 `modifyInvitation` + `adminAssign` + 双构造器）
  - 修改 Service：`org.ruoyi.ipd.service.NotificationService`（加 `BID_EXPIRED_NO_RESPONSE` 类型常量）
  - 修改 Controller：`org.ruoyi.ipd.controller.BidController`（加 `PUT /modify` + `PUT /admin-assign` 两端点）
  - 新增 Test：`org.ruoyi.ipd.service.P233AcceptanceTest`（13 个 @Tag("dev") 验收用例）

- **测试结果**：
  ```
  P233AcceptanceTest: 13/13 PASS
  - expiringSoonRemind3DaysBefore ✅
  - expiringSoonOutsideWindow ✅
  - expiringSoonNonOpenIgnored ✅
  - selectOverdueEscalateToLead ✅
  - selectOverdueWithinWindow ✅
  - expireNoResponseAutoCloseAndSuspend ✅
  - expireWithResponseNotAutoClose ✅
  - adminDirectAssign ✅
  - adminAssignLockedRowMissing ✅
  - modifyConditionsNotifyResponders ✅
  - modifyConditionsForbiddenForNonOwner ✅
  - modifyConditionsForbiddenForClosed ✅
  - scanDedupSameDay ✅
  ```

- **真库冒烟**：未执行（未启动 16039 服务；session 范围聚焦单测验收；真库 HTTP 验收由后续 reviewer 跑）

- **Git commit**：`53da78a9 feat(ipd,P2-3.3): 招标3/7/30日提醒升级与撤销出口……`

- **关键决策**：
  - **扫描去重**：依赖 `NotificationService.publishDaily` 的自然日 dedup_key（同日多次扫描只生成一次事件），不在扫描器侧做额外去重（KISS）
  - **主组组长升级**：当前 `scanSelectOverdue` 仅升级给市场 PM（createBy）；主组组长升级写审计行占位，后续接入 GroupMemberService（解耦 Phase 1）
  - **adminAssign 状态约束**：service 层不强制 EXPIRED（允许 OPEN/SELECTED 异常极端场景下指派），状态校验由 controller 层调用方守卫（按 IPD 决策习惯，service 信任上游调用方场景合法性）
  - **modifyInvitation 越权防御**：横向越权直接抛 `IpdBusinessException(FORBIDDEN)`，无审计行（避免水平越权探测攻击写入日志）
  - **构造器兼容**：保留 3-arg 构造器（无 NotificationService）以兼容既有 `@InjectMocks` 测试；新代码用 4-arg 构造器注入

---

## 横切影响

- **租户 excludes 补登**：无（bid_invitations / bid_responses / projects 既有登记未变动）
- **错误码新增**：无（复用 `FORBIDDEN`、`STATE_CONFLICT`、`NOT_FOUND`）
- **审计钩子新增**：
  - `action=modify_conditions`（AC-TEAM-13）
  - `action=admin_assign`（AC-TEAM-09）
  - `action=bid_select_overdue_escalate`（主组升级占位审计）

---

## 自检报告

- **已读文档**：
  - `docs/ipd-系统说明/开发计划-看板镜像.md`（P2-3.3 卡定义）
  - 既有 `BidInvitationService` / `BidResponseService` / `BidController` / `NotificationService` / `OPS05AcceptanceTest` / `P232AcceptanceTest`（模式基线）

- **已调 MCP**：
  - `list_projects` → ruoyi-ai 项目 ID `01dcf15c-86bb-4c7b-957c-8fe44bddd10d`
  - `list_tasks` → 筛选 P2-* 共 15 张；其中 todo 15、inprogress 2、inreview 4、done 1
  - `get_task` × 4（P2-3.3 / P2-2.1 / P2-2.2 / P2-2.3）

- **mvn test 结果**：
  ```
  mvn -pl ruoyi-modules/ruoyi-ipd -Dtest=P233AcceptanceTest test
  → Tests run: 13, Failures: 0, Errors: 0, Skipped: 0
  → BUILD SUCCESS
  ```

- **全模块 test**：观察到 3 个**与本卡无关**的并发会话遗留错误（P073 / P063），git stash pop 验证为 pre-existing，非本卡破坏

- **真库冒烟结果**：未执行（本会话未启动 16039）

- **git log**：
  ```
  53da78a9 feat(ipd,P2-3.3): 招标3/7/30日提醒升级与撤销出口…… (本卡)
  23dde3e8 docs(看板,复核): reviewer-ipd3 三卡独立复核收口……
  497e1f9e docs(看板): runner-ops05 三卡收口登记……
  34c1c835 feat(ipd,P1-10.1): AI 文档不可丢失版本链……
  c365bfe9 docs(看板): P2-3.2 真库验收转inreview……
  53418f72 feat(ipd,P2-3.2): 应标/拒绝/遴选原子提交……
  ```

---

## 风险与后续

1. **未做真库 HTTP 验收**：按卡要求 "新增 P233AcceptanceTest @Tag dev；mvn test 验证" 已闭环；但卡要求"真实业务另附 HTTP/DB 结果"，需后续 reviewer 在 16039 真库补冒烟
2. **9 张 TODO 卡未承接**：建议派多个 P2 专项 agent 并行（每 agent 1-2 卡），单 agent 串行 TDD 难以在合理时间内闭环
3. **OPS-04 scheduler 未接线**：`scanExpiringSoon` / `scanSelectOverdue` / `scanExpireNoResponse` 需外部调度器按需调用，本卡仅提供 service 方法入口

---

## 一句话总结

**P2 阶段本会话收口：1 张卡全 TDD 闭环（P2-3.3，13/13 绿），1 commit (`53da78a9`)，其余 9 张卡在并发会话中或依赖未闭环，建议派并行 agent 分批承接。**
