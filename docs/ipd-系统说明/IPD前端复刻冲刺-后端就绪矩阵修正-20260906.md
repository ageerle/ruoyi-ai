# IPD 前端原型复刻冲刺 · 后端就绪矩阵修正与占位登记（2026-09-06）

> 结论先行：以 `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/` 全量 40 个 Controller 的
> 类级/方法级 Mapping 为磁盘证据，修正此前「需求变更仅 domain 类」「P3 域未开始」等过时判断；
> ruoyi-ipd-web 侧 `router/routes/modules/ipd.ts` 全部 pending 占位的 ipdBackend 文案已按本矩阵改写。

## 一、本轮复刻落地的页面（ruoyi-ipd-web）

| 原型页面 | 本仓路由 | 数据来源 | 登记 |
| --- | --- | --- | --- |
| StageConfirmPage（/reviews） | /ipd/reviews | GET /projects/{id}/gate-checklist + POST /projects/{id}/advance-stage 真实推进 | 双PM确认链/双周评审/五大关键 Gate/豁免为页内如实空态（后端未交付） |
| ChangesPage（/changes） | /ipd/changes | RequirementChangeController P2-6.1 全 6 端点（list/create/submit/sign）真实 | 五节点协作链页内如实空态；字段适配见页头注 |
| FirstLoginPage | core /change-password | 既有 auth.changePassword 行为保留，视觉/文案一比一原型 | 移除原型没有的强度条；has-error 字段级红框依赖 per-field 校验器，暂以 form-error 呈现 |
| RequirementsPage（/requirements） | /ipd/requirements | DemandController | 前轮完成 |
| ProductWorkspacePage（/product-space） | /ipd/products | ProductWorkspaceController | 前轮完成 |
| ProductCatalogPage（超管 /products） | /ipd/product-catalog | GET /products；导入三步流后端未交付已禁用登记 | 前轮完成 |

## 二、就绪矩阵修正（重要，旧记录作废）

1. **需求变更 P2-6.1 已整类交付**：RequirementChangeController 提供
   POST /requirement-changes、PUT /{id}/submit、PUT /{id}/sign、GET /{id}、
   GET /requirement-changes（分页）、GET /requirement-changes/open。
   状态机 DRAFT→PENDING_SIGN→APPROVED/REJECTED，signatures 聚合「ROLE:actorId=DECISION」，
   双 APPROVE 回写需求池 ADOPTED（SEC-REV-REQ-CHANGE-01 回滚保护在位）。
   → change.ts 旧头注「仅 domain 类」已改写。
2. **Gate 评审域已交付**：GateReviewController（/api/v1/gates/{gateId}/sign|review|reopen|
   extend-deadline|arbitrate|final-ruling）+ GateSignScanController（scan-timeout/scan-remind）
   + GateElementController/GateElementResultController。
3. **绩效域大量交付**：KpiRecordController（GET /kpi/functional|performance|trend）、
   BonusPoolController（compute/freeze/distribute/{id}/list）、
   ContributionController（preview/save/market-share/confirm）、
   NegativeFeedbackController（submit/decide/lift/{id}/by-project）、
   ProjectScoreController（{projectId}/{personId}[+/settle]）+ ProjectScoreTaskController（scan）、
   SharedKpiController（仅 POST deadline-scan）。
4. **报表/导出已交付**：IpdReportController（GET /report/project-summary、
   /report/export/allowance|bonus|project）；无 analytics 统计端点。
5. **移交已交付**：HandoverController（POST /、POST /batch、POST /{id}/accept、
   GET /inbox、POST /super-admin）；与原型 /api/handoff-batches 五节点预检链不同构。
6. 其他可用：PmDirectoryController（GET /pm-directory）、ProjectCircleController、
   NotificationController、ComplianceController、SwitchingAcceptanceController（月度切换验收 run/lock/unlock）、
   LegacyScanController、SopTemplateController、CertTemplateController、AiDocument/AiModelConfig、
   DeletionRequestController、AuditLogController、WorkbenchController、PublicPortalController。

## 三、真缺口（全仓无对应 Controller，前端一律占位/页内如实空态，不做假数据）

| 缺口域 | 原型对应 | 影响页面 |
| --- | --- | --- |
| documents / attachments | /documents 资料库、变更/动作证据附件 | ⑦ 资料库占位；变更单证据附件暂无承接 |
| collaboration（五节点决策链） | DecisionChainPanel | /ipd/reviews 与 /ipd/changes 页内空态外壳 |
| biweekly-reviews | BiweeklyPanel（开发双周评审） | /ipd/reviews DEV 阶段空态 |
| key-gates | KeyGatePanel（五大关键联合 Gate） | /ipd/reviews 空态说明 |
| waivers | 豁免双审批 | /ipd/reviews 豁免按钮禁用登记 |
| V3.1 receipts / settlement | PerformanceV31Panel 回款台账 | ⑨ 协同绩效占位（部分交付文案） |
| 津贴台账读端点（仅导出） | 津贴台账页33 | IpdAllowance 占位 |
| timeline 聚合 | /timeline | ⑩ 占位（audit-logs 已交付为现状入口） |
| analytics | ProcessAnalyticsPage cycle-time/insight | ⑪ 占位（project-summary/导出已交付） |
| identity-source | /identity-sync 人员同步 | ⑭ 超管占位（P2-2.3） |
| recruitments | /recruitments 招募条件版本 | ⑤ 现挂 fe-bid（BidController 在位），原型招募生命周期面板无承接 |
| requirements 列表读端点 | 发起变更的关联需求下拉 | /ipd/changes 创建弹窗暂以需求 ID 录入 |

## 四、待复刻候选（后端已就绪，前端尚缺页面）

- 项目详情-Gate 评审页签（P0-10.23）：gate-checklist + GateReviewController。
- 协同绩效（⑨/ZK-D2）：KPI/奖金池/贡献度/负反馈 + project-summary 导出。
- 报表分析（⑪）：/report/project-summary + 三个 export（原型 analytics 视图部分降级适配）。
- 项目移交（⑫/P0-10.27）：/handovers 单体+批量模型适配原型 HandoffWorkbench。
- 项目绩效评定（P0-10.31）：ProjectScoreController。

## 五、验证记录

- `pnpm run check:type`（vue-tsc）全绿（2026-09-06，含 review/change/change-password 新页）。
- `pnpm exec vitest run --config vitest.ipd.config.mts` 285/286 通过；
  唯一改动：auth.test.ts 首登页用例按一比一原型 DOM 断言重写（强度条移除）。
- 占位文案改动仅 meta 描述，卡号（P0-10.x/ZK-D2）一律未动，看板镜像未改（等待看板串行写入窗口）。
