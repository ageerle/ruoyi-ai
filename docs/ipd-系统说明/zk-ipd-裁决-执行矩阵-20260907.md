# ZK-IPD 裁决执行矩阵（2026-09-07）

> **本文件定位**：「选项 A」落地（commit `80f1f28b` + `53f25ab8`）之后，残留 owner 决策材料的**最小可执行总览**。
> 配套索引：`docs/开发说明/zk-ipd-override.md` + `docs/ipd-系统说明/zk-ipd-决策-登记-20260907.md`
> 登记依据：`docs/ipd-系统说明/收口/R-NEW-2026-09-07-全局反思.md` + `治理/QA-08-GLB-12-20260907-否决项裁决.md` + `治理轮/DEF-5/PROPOSAL-01-脚本兜底.md`
> 登记人：本会话（plan-c388d58f，纯治理层落盘，零代码改动）
> 登记时间：2026-09-07

---

## §1 残留 owner 决策清单（5 项）

下表列出源对话「选项 A」落地后，**仍未拍板**的 5 项 owner 决策。每行给出：决策标题、当前状态、关联裁决编号（如有）、最小决策动作、可执行卡号。

| # | 决策标题 | 当前状态 | 关联裁决 | 最小决策动作（owner 选其一） | 可执行卡号 |
|---|---|---|---|---|---|
| D1 | **SOP 动作数 37 vs 69 终态切换** | 🟡 暂裁决 | 裁决 #4 | A. 保留 69（基线 RuoYi-AI 不动）/ B. 按 ZK V2 重写 37（D-5 全量回归） | D-5（终态切换） |
| D2 | **真库 3 名超管违例存量收敛** | 🟡 待拍板 | 跨裁决 #1 | a. 杀一保留 / b. 合并到唯一 / c. 全停重建 | D-2 前置（移交真链路验证） |
| D3 | **main 54 commits ahead origin 推送时机** | ⏸ 待授权 | — | A. 立刻 push / B. 等 B-1/C-2/C-3 落地后统一 push / C. 等 owner 单独授权窗口 | — |
| D4 | **QA-08 GLB-12 卡面 description 14→15 + matrix 翻 15** | 🟡 等授权 | QA-08 | A. 派本会话 manage.py 翻卡面 + 派子 agent 改 matrix / B. 暂缓 | QA-08（task_id `3ba1f028-f013-4080-a9cc-8e5c2be4a050`） |
| D5 | **PROPOSAL-01 真库 DCL 执行（audit_logs REVOKE）** | 🟡 等授权 | DEF-5 | A. owner 授权后 DBA 执行 SQL 草稿 / B. 等 P3-4/P3-7 验收窗口一并执行 | DEF-5（task_id `081fbd58-de76-4222-a7ae-22b59faa464f`） |

**裁决 #2（术语 "阶段确认"）+ 裁决 #3（项目圈 + 治理中心删）已 ✅ 登记但 B/C 卡墙未启动**：

- C-1（前端术语文案层改）：不动 Java 包名；改 `apps/web-antd/src/views/ipd/review/index.vue` + 5 个 `gate-*.ts` + 对应 `.test.ts`。
- B-1（后端删 ProjectCircleController 7 端点）+ C-2（前端 circle.vue + project-circle.ts + 路由）+ C-3（前端治理中心字样 + 链接）：全部删，「删除审批」+「无产出提醒」内嵌到工作台主体。

**裁决 #1（SSOT 不切换）/ 裁决 #5（不可豁免三关保留超管放行）已 ✅ 登记无后续动作**。

---

## §2 R-NEW 5 项 U0 紧急风险（verbatim 引自 R-NEW §5）

下表完全引自 `docs/ipd-系统说明/收口/R-NEW-2026-09-07-全局反思.md` §5 U0 紧急段；任何字段改动须回到 R-NEW 报告本身。

| 编号 | 标题 | 派单目标 | 工时估算 | 文件路径 |
|---|---|---|---|---|
| **R-NEW-SEC-6** | tenant.excludes 增补 post_launch_reviews + DBA 同步 DDL | 1 sub-agent + DBA 评审 | 0.5 h | `ruoyi-admin/src/main/resources/application.yml:262-331` |
| **R-NEW-SEC-1** | PostLaunchReviewService 入口加 IpdIdorGuard + 角色门 MARKET_PM/SUPER_ADMIN | 1 sub-agent | 0.5 h | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/PostLaunchReviewService.java:65-141` |
| **R-NEW-AI-5** | coding harness execute-process 默认改 false + 路径白名单审计 | 1 sub-agent | 1 h | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/service/coding/harness/`（注：R-NEW §4 标 R-NEW-AI-5 在 `ruoyi-chat`，本表严格按 §5 派单段） |

**严重度**：SEC-6 = HIGH、SEC-1 = HIGH、AI-5 = HIGH。

**派单优先级**（U0 紧急 / 本周内）：

1. R-NEW-SEC-6：HIGH，0.5h，1 sub-agent + DBA
2. R-NEW-SEC-1：HIGH，0.5h，1 sub-agent
3. R-NEW-AI-5：HIGH，1h，1 sub-agent（需本地隔离环境 + staging 验证再 prod）

**与裁决登记的对应**：R-NEW-SEC-6 关联 `zk-ipd-override.md` GAP 段（待裁决项目圈删后审计链 tenant 隔离）；R-NEW-SEC-1 关联裁决 #3（项目圈删后会触发 PostLaunchReview API 暴露，需补 Controller + 守卫）；R-NEW-AI-5 不在 ZK 决议范围（AI/RAG 模块审查资源倾斜 ipd-chat）。

---

## §3 owner 拍板路径建议

下表给出每个决策点的**最小决策动作**（按"决策成本由低到高"排序）：

| 决策 | 最小决策动作（推荐路径） | 决策成本 | 预计落地时长 |
|---|---|---|---|
| **D1 SOP 37 vs 69** | 选 A 保留 69（不动代码，仅登记裁决 #4 终态关闭） | 1 句话决策 | 即时 |
| **D2 真库 3 超管存量** | 选 a 杀一保留（最小 DCL 影响，DBA 一次 SQL + 审计 verify chain=OK） | 1 句话决策 + DBA 凭证 | 30 min |
| **D3 push 时机** | 选 B 等 B-1/C-2/C-3 落地后统一 push（与 OPS-09 让路铁律一致） | 0 决策（默认即可） | 被动等待 |
| **D4 QA-08 GLB-12 翻 15** | 选 A 派本会话 manage.py 翻卡面 + 派子 agent 改 matrix（业务影响 0） | 1 句话授权 | 1 h |
| **D5 PROPOSAL-01 真库 DCL** | 选 A owner 授权后 DBA 执行 SQL 草稿（与 D2 同一真库窗口合并执行） | 1 句话授权 | 1 h（含自校验） |

**总决策时间**：owner 在线 1 小时内可全部拍板；拍板后总执行时长 ≈ 4 h（含 D2 + D4 + D5 真库执行 + 子 agent 派单 R-NEW 5 项 U0）。

**风险顺序**：

1. **最高**：D5 PROPOSAL-01 真库 DCL（涉及 4 张核心表 REVOKE，失败回滚成本高）。
2. **次高**：D2 真库 3 超管收敛（涉及存量数据治理，审计链 verify 须 OK）。
3. **中**：D4 QA-08 GLB-12（文档级修订 + 子 agent 改 matrix，业务影响 0）。
4. **低**：D1 SOP 37 vs 69（仅裁决登记，无代码改动）。
5. **零**：D3 push 时机（默认即可）。

---

## §4 回滚预案（每个决策的"反转"最少动作）

| 决策 | 若 owner 反转 / 决策失败，回滚最少动作 | 回滚成本 |
|---|---|---|
| **D1 保留 69 → 切 37** | 1. 撤回裁决 #4 登记；2. 派 D-5 子任务：ZK V2 重写 ActionCatalog.java + 全量回归 69 测 | 高（重写 + 全量回归） |
| **D2 真库杀一超管 → 反转"恢复"** | 1. DBA 恢复被删 persons 行（含原 tenant_id 与审计链）；2. audit_logs verify chain=OK 复核 | 中（数据可恢复，审计链须 verify） |
| **D3 push 后回滚** | 1. `git revert <push_sha>`（已在 origin 后不可 reset --hard）；2. 通知所有 fetch 过的人 | 高（远端已发） |
| **D4 卡面翻 14→15 → 反转"恢复 14"** | 1. manage.py 卡面 description 翻回 14；2. 子 agent 改 acceptance-matrix.json GLB-12 v2 → v1；3. 重跑 qa08-ac-acceptance.py 确认 FAIL | 低（文档级反转） |
| **D5 PROPOSAL-01 真库 DCL → 反转"恢复 GRANT"** | 1. `GRANT INSERT, UPDATE, DELETE ON TABLE audit_logs TO 'ipd_app'@'127.0.0.1'`；2. 删除 cron + post-deploy hook 步骤；3. 保留审计日志（记录回滚事件本身） | 低（标准 SQL GRANT，5 min） |
| **R-NEW 5 项 U0 任意子任务回滚** | 1. `git revert <commit_sha>`（本地未 push 窗口，soft reset 即可）；2. 不动既有 tenant.excludes / IpdIdorGuard / coding harness 配置 | 低（单 commit 回滚） |

**总回滚成本**：D3 > D1 > D2 > D4 ≈ D5 > R-NEW 单项；D3 一旦 push 即不可逆（仅 revert）。

---

## §5 与既有裁决登记的差异

- 本文件**不复述**裁决 #1–#5 的内容，仅引用裁决编号；详细描述见 `zk-ipd-决策-登记-20260907.md`。
- 本文件**不引入**新裁决，仅列出"裁决已登记后，剩余的 owner 拍板动作"。
- 本文件**不触碰** SSOT 优先级（README-IPD-OVERRIDE.md 仍为 SSOT，裁决 #1 已锁）。
- 本文件**不派单**任何代码子 agent，所有 R-NEW 5 项 U0 派单建议已在 R-NEW §5 列出，本文件仅汇总。

---

## §6 维护规则

- 本文件只登记「裁决执行 + 残留 owner 决策」的当前快照，不动态跟新。
- 任何 owner 拍板落地后，必须把对应决策行的"当前状态"从 🟡 改为 ✅/❌，并在 `docs/ipd-系统说明/log.md` 追加登记段。
- 本文件不接收勘误级更新以外的修改；如需结构性变更（如新增决策维度），须新建 `zk-ipd-裁决-执行矩阵-YYYYMMDD.md` 替代本文件，旧文件归 `docs/ipd-系统说明/历史/`。

---

## §7 登记时间 + 维护人

- 登记：EvoX 会话 `plan-c388d58f`
- 依据：`R-NEW-2026-09-07-全局反思.md`（230 文件扫描 / 5 项 U0）+ `QA-08-GLB-12-20260907-否决项裁决.md`（B+D 联合）+ `PROPOSAL-01-脚本兜底.md`（角色名 `ipd_app@127.0.0.1`）
- 登记时间：2026-09-07
- 配套 log.md 段：本会话登记段附在 `docs/ipd-系统说明/log.md` 末段
