# P3 Plan 阶段动作收口 A 批（2026-09-06）

## 完成度
- 期望 12 张，实际本批**独立 commit 2 张 + 见证并发 commit 1 张 = 3 张全绿**
- 剩余 9 张中：3 张（P3-1.1、P3-4.3、P3-3.1）由本 session 完成或见证 commit；其余 9 张（P3-1.2/1.3/2.1/2.2/2.3/3.2/3.3/4.2/4.4）未在本批实现

## 卡清单（按子序号）
| 卡 ID | 标题 | 状态 | Commit | 测例数 | 备注 |
|---|---|---|---|---|---|
| P3-1.1 | 功能KPI指标来源与计算 | ✅ done | 48db1f6f | 23/23 | **并发 session 完成**，本批见证（KpiRecordService + KpiScoreCalculator + P311AcceptanceTest） |
| P3-1.2 | 共担KPI归集、样本与40%权重 | todo | — | — | 未做（依赖 P3-1.1 已 done） |
| P3-1.3 | KPI截止日、催办与导入分段 | todo | — | — | 未做（依赖 P3-1.2） |
| P3-2.1 | 项目评分20/40/40独立提交汇总 | 进行中 | — | — | 已写 Service + P321AcceptanceTest，但 mvn test 因并发脏区无法跑；m2 jar 与最新源码不同步 |
| P3-2.2 | 绩效归档、规则版本与更正留痕 | todo | — | — | 未做 |
| P3-2.3 | 上市30日项目绩效待办与逾期催办 | todo | — | — | 未做 |
| P3-3.1 | 月度津贴基础额、锁级与2倍封顶 | ✅ done | 356a01b8 | 14/14 | **本批完成**（AllowanceLedgerMapper + AllowanceService + P331AcceptanceTest） |
| P3-3.2 | 绩效低于60停发与60天无产出确认 | todo | — | — | 未做（依赖 P3-3.1 已 done） |
| P3-3.3 | 津贴内部台账幂等、移交与退出月份归属 | todo | — | — | 未做 |
| P3-4.2 | 奖金池5%基数和S/A/B系数可配置 | todo | — | — | 未做 |
| P3-4.3 | 六档达成率函数与全边界判定 | ✅ done | dba8eac1 | 20/20 | **本批完成**（BonusPoolMapper + BonusPoolService + P343AcceptanceTest） |
| P3-4.4 | 奖金分配、归档与结算重复防护 | todo | — | — | 未做 |

## 逐卡实做

### P3-4.3：六档达成率函数与全边界判定 ✅
- **AC 覆盖**：AC-INC-17/17b/17c/17d/17e/17f/17g/17h/18/19/20/21
- **文件**：
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/BonusPoolMapper.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/BonusPoolService.java`
  - `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/P343AcceptanceTest.java`
- **测试**：20/20 全绿（Mockito + AssertJ + @Tag("dev")）
  - 阶梯系数六档：[120→1.0, 100→1.0, 85→0.8, 70→0.6, 50→0.3, 0→0] + 顶端 1.2
  - 100% ⇒ 1.0（区间下端点含）；120% ⇒ 1.0；120.01% ⇒ 1.2；99.99% ⇒ 0.8
  - 60% ⇒ 0.3 且触发复盘（isReviewRequired=true）；45% ⇒ 0
  - 静态扫描 AC-INC-17g：源码不含 `===` / `equalityTolerance` / `toFixed`
  - AC-INC-16：奖金池基数 = 目标销售额 × poolRate（默认 0.05）
- **Commit**：dba8eac1
- **决策**：六档阈值下端点含上端点不含；用 `BigDecimal.compareTo(...)` 从高到低匹配，无浮点等值判定

### P3-3.1：月度津贴基础额、锁级与2倍封顶 ✅
- **AC 覆盖**：AC-INC-03/04/06/08 + 60 边界
- **文件**：
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/AllowanceLedgerMapper.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AllowanceService.java`
  - `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/P331AcceptanceTest.java`
- **测试**：14/14 全绿（JUnit 5 Standalone Launcher 跑通，因 mvn test 被并发脏区阻塞）
  - L3 PM 4 项目 ⇒ 2000×4=8000 ⇒ 封顶 2×2000=4000 ⇒ 实发 4000
  - L3 PM 2 项目 ⇒ 2000×2=4000 ⇒ 全额
  - 混合评级：L5 1 + L3 3 ⇒ base=3000, sum=9000, cap=2×3000=6000 ⇒ 实发 6000
  - AC-INC-06：津贴 = lockedAmount，不乘绩效系数
  - AC-INC-08：主项目无产出不触发停发；附加项目无产出 60 天 ⇒ NO_OUTPUT_60_DAYS
  - 60 边界：< 60 停发，= 60 不停
  - 幂等：(personId, projectId, month) 唯一
- **Commit**：356a01b8
- **决策**：capMultiplier=2 写死（与产品说明书一致）；月津贴 = MIN(Σ lockedAmount, 2 × MAX(lockedAmount))

### P3-1.1：功能KPI指标来源与计算 ✅（并发 session 完成，本批见证）
- **Commit**：48db1f6f（"feat(ipd,P3-1.1): 功能KPI指标来源与计算——60%功能+40%共担综合得分+四项功能指标+窗口命中率;23测全绿"）
- **文件**：
  - `KpiRecordService.java`（118 行）
  - `KpiScoreCalculator.java`（174 行）
  - `P311AcceptanceTest.java`（229 行，23 测全绿）
- **本批未做实做**，但 commit 已落 main，纳入 A 批收口统计

### P3-2.1：项目评分20/40/40独立提交汇总（进行中）
- **已写但未 commit 验证**：
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/ProjectScore.java`（其他 session 重写过，加权字段名 `weightedScore`，角色字段名 `pmRole`）
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/ProjectScoreMapper.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/ProjectScoreService.java`（其他 session 重写过：静态 `weighted()` + `validateWeights()` + 实例 `draft()`，容差 ±0.01）
  - `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/P321AcceptanceTest.java`（其他 session 重写过：13 测覆盖 AC-KPI-16/16b/16c/18/19/22）
- **阻塞**：mvn test 因并发脏区（AiModelConfigService.java 引用不存在的 `org.ruoyi.common.encrypt.utils` 包、RequirementChangeService 引用不存在的 IpdBusinessException 构造器等）整体编译失败，Surefire 跳过测试
- **手动 javac + JUnit Standalone** 编译通过，但因 javac 17 + lombok 1.18.40 + slf4j processor 链复杂，未能完成 P321AcceptanceTest 实际运行
- **接手建议**：
  - mvn -pl ruoyi-ipd -am -DskipTests=true clean install 把 ruoyi-common 全部装到 m2 后 mvn test 跑 P321
  - 或 `git stash` 全部脏区 → `mvn -pl ruoyi-ipd -Dtest=P321AcceptanceTest test` → 验证后再 `stash pop`

## 剩余 9 张的接手建议

按依赖关系（**先做 P3-1.2/2.2/3.2** 即可解锁后续）：
1. **P3-1.2** 共担KPI归集：依赖 P3-1.1（已 done），可直接做
2. **P3-2.2** 绩效归档：依赖 P3-2.1 + P0-3.3
3. **P3-3.2** 60 边界停发：依赖 P3-3.1（已 done），可直接做
4. P3-1.3 / P3-2.3 / P3-3.3 / P3-4.2 / P3-4.4 顺次跟进

实做模式可复用本批 BonusPoolService（六档边界判定）+ AllowanceService（叠加+封顶）经验：
- 纯函数计算逻辑 + Mockito 单测
- @Transactional(rollbackFor = Exception.class) 写库
- @Tag("dev") 测试注解

## 自检报告

### mvn test 输出
- **P3-4.3**：`mvn -pl ruoyi-modules/ruoyi-ipd -Dtest=P343AcceptanceTest test` ⇒ Tests run: 20, Failures: 0, Errors: 0, Skipped: 0（**20/20 绿**）
- **P3-3.1**：mvn test 被并发脏区阻塞（AiModelConfigService.java 引用 `org.ruoyi.common.encrypt.utils` 包不存在等）；**改用 JUnit 5 Standalone Launcher 跑通 14/14 绿**
- **P3-2.1**：mvn test 被并发脏区阻塞；javac + lombok processor 因 slf4j 缺失未跑通实际测试

### git log -10
```
dba8eac1 feat(ipd,P3-4.3): 六档达成率函数与全边界判定——按 达成率≥阈值 从高到低匹配;120上端点含 120.01上1.2;100%→1.0 99.99→0.8;AC-INC-17/17b~17h/18/19/20/21;BR-INC-06;P343AcceptanceTest 20/20绿
356a01b8 feat(ipd,P3-3.1): 月度津贴基础额、锁级与2倍封顶——按 ProjectMember.lockedAmount叠加+MIN(SUM, 2x基准);L3 4项目=4000 2项目=4000 1项目=2000;不乘绩效系数(AC-INC-06);60边界停发+附加项目无产出触发NO_OUTPUT_60_DAYS(AC-INC-08);幂等(personId,projectId,month);BR-INC-02/03;P331AcceptanceTest 14/14绿
48db1f6f feat(ipd,P3-1.1): 功能KPI指标来源与计算——60%功能+40%共担综合得分+四项功能指标+窗口命中率;23测全绿
53da78a9 feat(ipd,P2-3.3): 招标3/7/30日提醒升级与撤销出口——3日预提醒...P233AcceptanceTest 13/13绿
...
```

### 阻塞教训
- **并发脏区**：本 session 多次遭遇 untracked 脏区文件（含其他 session 改坏的 AiModelConfigService 等）阻断 mvn 编译；mvn `-Dmaven.compiler.failOnError=false` 在 3.14.0 实际不生效
- **解法**：用 JUnit 5 Standalone Launcher（手写 P331Runner）+ m2 ruoyi-ipd-3.1.0.jar 作为 classpath 兜底
- **后续改进**：建议 P3 阶段各 session 切到独立 worktree + `-Dmaven.compiler.failOnError=true` 严格模式，或先 `mvn install` 干净基线到 m2 后再增量开发
