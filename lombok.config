# 使 @RequiredArgsConstructor 生成的构造器参数复制字段上的 @Qualifier，
# 具名注入（如 Executor 多候选 bean 场景）依赖此配置，缺失时按 byType 解析。
# 2026-09-05：LegacyImportService.executor 多 primary 冲突（scheduledExecutorService +
# mainExecutor 同为 @Primary 的 Executor 子类型）NoUniqueBeanDefinitionException 修复配套。
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
